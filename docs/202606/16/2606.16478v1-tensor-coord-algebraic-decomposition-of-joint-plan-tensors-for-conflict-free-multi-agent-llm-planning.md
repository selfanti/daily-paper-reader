---
title: "Tensor-Coord: Algebraic Decomposition of Joint Plan Tensors for Conflict-Free Multi-Agent LLM Planning"
title_zh: Tensor-Coord：面向无冲突多智能体大模型规划的联合计划张量代数分解
authors: Mudit Rastogi
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16478v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 通过张量分解实现多智能体强化学习协作
tldr: "多智能体LLM规划存在冲突（碰撞、资源竞争等）。提出Tensor-Coord，将联合计划表示为三阶张量，利用CP和Tucker分解识别协调结构。CP秩定义协调复杂度，残差定位冲突，Tucker因子生成语言约束供LLM重规划。实验显示2、3、4智能体冲突解决成功率100%、80%、60%，迭代次数少。该框架无需领域规则即可检测并修正协调失败。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16478v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1686, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 1455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1565, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16478v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1702, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16478v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1755, \"height\": 318, \"label\": \"Table\"}]"
motivation: LLM在多智能体规划中因独立生成计划导致空间碰撞、资源争用等协调失败，缺乏通用冲突检测与修正机制。
method: 将N个智能体的联合计划构建为三阶张量，使用CP/Tucker分解提取潜在协调结构，通过残差定位冲突，将分解因子转为自然语言约束引导LLM重规划。
result: "2/3/4智能体任务100%/80%/60%收敛至无冲突计划，平均迭代1.4/3.2/4.0次，CP秩与智能体数近似线性。"
conclusion: 张量分解可有效量化多智能体协调复杂度并定位冲突，为LLM规划提供可解释的冲突修复途径。
---

## 摘要
大语言模型在多智能体规划中仍存在局限性，因为独立生成的计划可能导致协调失败，如空间碰撞、资源争用和时间死锁。我们提出Tensor-Coord，一个多重线性代数框架，将N个智能体的联合计划表示为一个三阶张量 \(T \in R^{N \times H \times A}\)，维度分别为智能体、时间步和动作。利用典型多路分解和Tucker分解来识别潜在协调结构。最小ε近似CP秩R*定义了一个可计算的协调复杂度度量，即 \(CC(Pi)=(R*-N)/N\)。我们证明R*=N是计划独立性的充分必要条件。残差 \(E=T-T_{R*}\) 定义了智能体对、时间步和动作上的冲突分数，无需特定领域规则即可定位失败。Tucker因子提供了可解释的智能体角色、时间阶段和动作聚类，这些被转化为自然语言约束，用于迭代的LLM重新规划。在多机器人配送任务中，针对简单（2个智能体，5×5网格）、中等（3个智能体，5×5网格）和困难（4个智能体，5×5网格）设置的实验显示，在2个智能体情况下100%收敛到无冲突计划，平均迭代次数为1.4次；3个智能体情况下80%收敛，平均迭代3.2次；4个智能体情况下60%收敛，平均迭代4.0次。CP秩近似线性增长，即 \(R*(N) = 3.9N + 0.5\)，支持其作为协调复杂度预测因子的用途。

## Abstract
Large language models (LLMs) remain limited in multi-agent planning because independently generated plans can create coordination failures such as spatial collisions, resource contention, and temporal deadlocks. We introduce Tensor-Coord, a multilinear algebra framework that represents the joint plan of N agents as a third-order tensor \(T \in R^{N \times H \times A}\) over agents, timesteps, and actions. Canonical Polyadic (CP) and Tucker decompositions are used to identify latent coordination structure. The minimal epsilon-approximate CP rank R* defines a computable coordination complexity measure, with \(CC(Pi)=(R*-N)/N\). We prove that R*=N is necessary and sufficient for plan independence. The residual \(E=T-T_{R*}\) defines a conflict score over agent pairs, timesteps, and actions, localizing failures without domain-specific rules. Tucker factors provide interpretable agent roles, temporal phases, and action clusters that are converted into natural language constraints for iterative LLM replanning. Experiments on multi-robot delivery tasks across Easy (2 agents, 5x5 grid), Medium (3 agents, 5x5 grid), and Hard (4 agents, 5x5 grid) settings show convergence to conflict-free plans in 100% of 2-agent cases within 1.4 iterations on average, 80% of 3-agent cases within 3.2 iterations, and 60% of 4-agent cases within 4.0 iterations. CP rank scaled approximately linearly as \(R*(N) = 3.9N + 0.5\), supporting its use as a predictor of coordination complexity.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）在**多智能体规划**中，当各智能体独立生成计划时，容易产生**协调失败**，包括空间碰撞、资源争用和时间死锁。这种失效源于LLM固有的概率生成特性，缺乏结构化规划所需的显式状态跟踪和约束满足能力。
- **背景与动机**：现有方法（联合提示、顺序规划、经典多智能体规划）各有缺陷：联合提示受限于上下文窗口；顺序规划有顺序偏置；经典方法（如Conflict-Based Search）需要严格的领域规格，与LLM的自然语言优势不兼容。因此，需要一种**不依赖领域规则、能自动检测并引导修复协调失败**的方法。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将多智能体的**联合计划**编码为一个**三阶张量** \( \mathcal{T} \in \mathbb{R}^{N \times T \times K} \)（N个智能体，T个时间步，K种动作），利用**张量分解**（CP分解和Tucker分解）自动发现其中隐藏的协调结构，并用分解的秩和残差来**量化协调复杂度**和**定位冲突**，最后将分解得到的因子转化为**自然语言约束**，引导LLM进行迭代重规划。
- **关键技术细节**：
  - **软计划张量**：为避免硬编码张量的稀疏和秩退化，采用软编码：所选动作赋予 \( \gamma = 0.8 \)，其他动作均分剩余 \( (1-\gamma)/(K-1) \)），使张量满秩且分解稳定。
  - **CP分解与协调复杂度**：CP分解将张量近似为R个秩1分量的和： \( \mathcal{T} \approx \sum_{r=1}^R \lambda_r \cdot u_r \otimes v_r \otimes w_r \)。**最小ε-近似CP秩 \( R^* \)** 用于定义**协调复杂度度量** \( CC(\Pi) = \frac{R^* - N}{N} \)。理论证明 \( R^* = N \) 是计划独立性的充要条件；\( R^* > N \) 表示智能体间存在耦合，需要协调。
  - **残差冲突检测**：计算CP残差 \( E = \mathcal{T} - \mathcal{T}_{R^*} \)，定义每个智能体每时间步的残差幅度 \( \rho(i,t) = \|E(i,t,:)\|_2 \)，再定义**冲突分数** \( CS(i,j,t) = \rho(i,t) \cdot \rho(j,t) \)。当 \( CS > \tau = 0.15 \) 时判定为冲突。该方法无需领域特定规则即可代数定位冲突。
  - **Tucker分解与可解释结构**：采用Tucker分解 \( \mathcal{T} \approx \mathcal{G} \times_1 U \times_2 V \times_3 W \)，得到因子矩阵：U（智能体角色）、V（时间阶段）、W（动作类型）、G（交互核心）。这些因子可自然翻译为**三种自然语言约束**：空间约束、阶段边界约束、角色约束。
  - **迭代重规划算法（Tensor-Coord算法）**：循环进行各智能体LLM独立规划 → 构建软张量 → CP分解求 \( R^* \) → 计算残差定位冲突 → Tucker分解提取结构 → 生成自然语言约束 → 更新各智能体提示 → 重复直至冲突数为0或达最大迭代次数。

### 3. 实验设计：数据集/场景、基准、对比方法

- **实验场景**：**多机器人配送任务**，在网格世界中，机器人需从起点移动到目标并携带包裹，协调约束为**空间互斥**（同一时间不能在同一单元格）。
- **五组配置**：
  - Easy: 2 agents, 5×5 grid, 2 packages, T=10
  - Medium: 3 agents, 5×5 grid, 3 packages, T=15
  - Hard: 4 agents, 5×5 grid, 4 packages, T=20
  - Very Hard: 5 agents, 6×6 grid, 5 packages, T=30
  - Complex: 3 agents, 6×6 grid, 6 packages, T=35
- **基准方法**：
  - **B1: Independent Planning** – 各智能体独立规划，无协调。
  - **B2: Round-Robin** – 智能体顺序规划，后面智能体可看到前面计划。
  - **B3: Single-LLM Joint** – 单个LLM调用同时为所有智能体生成计划。
- **LLM后端**：Azure OpenAI GPT-5, temperature=0.2, max tokens=500。

### 4. 资源与算力

- 论文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。仅提及LLM后端调用Azure OpenAI GPT-5。张量分解的计算复杂度为 \( \mathcal{O}(n_{iter} \cdot R \cdot N \cdot T \cdot K) \)，属于轻量级运算，实验可能仅依赖CPU进行张量分解，LLM调用通过API完成。因此**算力消耗未具体报告**。

### 5. 实验数量与充分性

- **实验数量**：每组配置使用**5个随机种子**（seed），最多迭代**5次**，共计 5×5 = 25组独立实验（加上baseline重复）。
- **充分性评估**：
  - **覆盖范围**：覆盖了从2到5个智能体、不同网格大小和包裹数的多种难度级别，基本合理。
  - **消融实验**：未做单独的消融（如去掉Tucker约束、去掉残差检测等），但通过对比三个基线间接说明了各组件的贡献。
  - **统计显著性**：只进行了5个种子，数量偏少，未提供置信区间或方差指标，难以判断结果稳定性。
  - **公平性**：所有方法使用同样LLM后端和最大迭代数，对比客观。但基线的迭代次数是否相同未明确（独立规划仅一次？），可能存在比较不对等。
- **总体评价**：实验设计有代表性，但数量较少，统计显著性不足，缺少消融研究。

### 6. 论文的主要结论与发现

- **Tensor-Coord** 在2智能体任务上达到**100%收敛到无冲突计划**（平均1.4次迭代），3智能体**80%**（平均3.2次），4智能体**60%**（平均4.0次），5智能体**20%**（剩余10.4个冲突）。
- **CP秩**与智能体数呈近似线性关系：\( R^*(N) \approx 3.9N + 0.5 \) (\(R^2 = 0.94\))，支持CC指标作为协调复杂度预测因子。
- **与基线对比**：Tensor-Coord在所有配置上的**冲突减少率（CRR）** 均最高（Easy 100%, Medium 87%, Hard 75%, Very Hard 27%, Complex 58%），显著优于独立规划、轮询规划和单LLM联合规划。
- **LLM Fallback Paradox**：当LLM因过多约束返回空计划（等待动作）时，反而降低了张量残差复杂度，有时加速收敛，提示**显式等待协调**可能有价值。

### 7. 优点：方法或实验设计上的亮点

- **理论创新**：首次将**CP秩**与**协调复杂度**建立代数联系，给出了可计算、可证明的度量。
- **无领域特异性**：冲突检测完全基于张量残差，无需定义碰撞规则等，泛化性强。
- **可解释性**：Tucker分解得到的角色、阶段、动作类型因子可直接生成自然语言约束，人类可理解。
- **迭代闭环**：代数分析与LLM生成形成反馈循环，可逐步优化计划。
- **实验覆盖多难度**：5种配置+3种基线，结果清晰显示了方法的优势和局限。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **收敛率随智能体数增加急剧下降**：5智能体仅20%收敛，说明当前约束生成策略在密集场景下不足，存在非单调约束交互问题。
- **实验量有限**：仅5个种子，缺少方差统计，结论的稳健性存疑。
- **缺乏消融实验**：未单独验证CP残差检测、Tucker约束、软编码等各组件的贡献。
- **应用限制**：实验局限于网格世界、静态环境、确定性动作；未考虑部分可观察性、随机性、动态障碍物等真实场景。
- **LLM依赖**：框架依赖外部LLM的质量，如果LLM返回无效或无意义计划（如空白），可能导致失败；文中提到LLM fallback时有发生。
- **计算与资源**：未报告算力需求，大规模场景下张量分解的ALS迭代可能成为瓶颈。
- **未与经典规划方法（如CBS）对比**：虽然论文指出CBS需领域规格，但可以讨论性能或结合可能性，缺少此类比较。

（完）
