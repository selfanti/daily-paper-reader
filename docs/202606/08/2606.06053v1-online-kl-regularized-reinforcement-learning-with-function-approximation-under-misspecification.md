---
title: Online KL-Regularized Reinforcement Learning with Function Approximation under Misspecification
title_zh: 在错误设定下使用函数逼近的在线KL正则化强化学习
authors: "Haoyang Hong, Zichen Wang, Quanquan Gu, Huazheng Wang"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06053v1"
tags: ["query:agentic-rl"]
score: 8.0
evidence: 带有KL正则化和函数近似的理论强化学习
tldr: 现有KL正则化强化学习理论依赖可实现性假设，无法处理模型错误指定。本文针对上下文赌博机和episodic RL提出KL错误指定公式，采用基于回归的算法与Gibbs策略更新。建立了高概率KL遗憾界，包含显式错误指定项，在可实现时退化为标准界。首次将KL正则化RL推广到错误指定场景，为实际应用提供理论保障。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06053v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 985, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06053v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1430, \"height\": 1374, \"label\": \"Table\"}]"
motivation: 现有KL正则化RL理论依赖可实现性假设，无法应对模型错误指定，亟需更鲁棒的保证。
method: 引入KL错误指定度量，提出基于回归的Gibbs策略更新算法。
result: 得到高概率KL遗憾界，明确包含错误指定项，在可实现时恢复已有结果。
conclusion: 首次在函数近似错误指定下为KL正则化RL提供理论保证，拓展了应用范围。
---

## 摘要
我们研究在一般函数逼近与模型错误设定下的KL正则化上下文赌博机与情节强化学习。现有保障依赖于可实现性，因此无法推广至错误设定模型，在此类模型中经典遗憾界可能失效。本文针对上下文赌博机与情节强化学习提出了KL错误设定公式，并分析了基于回归的吉布斯策略更新算法。建立了带有显式错误设定项的高概率KL遗憾界，将标准的可实现KL正则化设定作为特例恢复。

## Abstract
We study KL-regularized contextual bandits and episodic reinforcement learning (RL) under general function approximation with model misspecification. Existing guarantees rely on realizability and therefore do not extend to misspecified models, where classical regret bounds may fail. This work introduces KL misspecification formulations for contextual bandits and episodic RL and analyzes regression-based algorithms with Gibbs policy updates. High-probability KL-regret guarantees with explicit misspecification terms are established, recovering the standard realizable KL-regularized setting as a special case.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：KL正则化在RLHF和LLM微调中被广泛使用，现有理论分析（如Zhao等人）依赖“可实现性”假设，即真实奖励或价值函数必须位于学习器所使用的函数类中。然而，实际应用中模型往往存在错误设定（misspecification），例如使用线性模型拟合非线性奖励，此时可实现性失败，已有遗憾界不再成立。
- **核心问题**：如何在一般函数逼近且允许模型错误设定的条件下，为KL正则化上下文赌博机和episodic RL提供可证明的KL遗憾界？
- **整体含义**：本文首次将KL正则化RL的理论保证推广到错误设定场景，给出了显式依赖于错误设定水平（ζ）的遗憾界，当ζ=0时退化为已有可实现结果，从而弥合了理论与实际部署之间的差距。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：
  - 引入适用于KL正则化目标的“点态错误设定”度量（对bandits）和“分阶段点态错误设定”度量（对RL），使用已知错误设定水平ζ直接构造乐观奖励/价值估计。
  - 采用岭回归（ERM）结合基于Eluder维度的不确定性额定量（bonus），通过Gibbs策略更新实现策略优化。
- **关键技术细节与公式**：
  - **KL正则化上下文赌博机**：定义点态错误设定  ζ_Bandit = inf_{R∈R} sup_{x,a} |R(x,a)-R⋆(x,a)|。算法MR-KL-UCB（Algorithm 1）在每个回合执行：ERM回归 → 计算不确定性bonus → 构造乐观奖励估计 → 根据Gibbs策略采样（πt(a|x) ∝ π_ref(a|x) exp(η·乐观奖励)）。
  - **KL正则化episodic RL**：定义分阶段点态错误设定 ζ_RL（沿Bellman备份路径）。算法MR-KL-LSVI（Algorithm 2）采用反向规划：每个episode从H到1依次回归、添加bonus、裁剪、计算乐观Q值，然后根据Gibbs策略更新 π_{t,h}，最后执行一次完整的episode收集数据。
  - **技术工具**：Gibbs二次自界不等式（Lemma B.13）、将平方Q间隙转化为平方Bellman残差（Lemma B.17）、Eluder维度下的bonus平方和界（Lemma B.16）、Freedman不等式实现predictable-to-realized对齐。
- **理论结果**：
  - Bandit：Reg(T) = O( η [ log(NR T/δ) d(R,λ,T) + T ζ²_Bandit ] )
  - RL：Reg(T) = Õ( η H² β² d_RL(λ,T) + η H³ T ζ²_RL )，其中β包含对数项。
  - 当ζ=0时恢复已有可实现结果。

### 3. 实验设计

- **本文为纯理论论文，未进行任何实验**。因此不涉及数据集、benchmark或方法对比。
- 论文在引言和结论中提到了潜在的实践动机（RLHF、LLM后训练），但未提供实证验证。

### 4. 资源与算力

- **未提及**。由于没有实验，文中未涉及GPU型号、数量或训练时长等信息。

### 5. 实验数量与充分性

- **不适用**。论文的贡献完全体现在理论证明上，不包含实验。因此无法评估实验充分性或公平性。

### 6. 论文的主要结论与发现

- 针对KL正则化上下文赌博机和episodic RL，在一般函数逼近且存在模型错误设定的条件下，首次给出了高概率KL遗憾界，且界中包含显式的错误设定项（T ζ² 或 H³ T ζ²）。
- 所提出的算法（MR-KL-UCB和MR-KL-LSVI）在错误设定存在时仍能保持乐观性，并通过ELuder维度控制统计复杂度。
- 当错误设定水平ζ=0时，遗憾界退化为已有的可实现KL正则化界，说明结果是可达的且不弱于现有最优结果。
- 技术贡献包括：Gibbs二次自界不等式的泛化、分阶段Bellman残差与Q间隙之间的转换、以及针对KL正则化RL的Freedman对齐分析。

### 7. 优点

- **理论创新性**：首次将KL正则化RL的错误设定问题形式化，并给出通用的高概率遗憾界，弥补了该领域关键理论空白。
- **方法上的严谨性**：采用模块化证明（先建立置信度和乐观性条件，再通过不确定性求和与对齐得到最终界），便于后续扩展。
- **兼容性**：所提算法和理论框架可自然退化到可实现情形，且无限函数类可通过覆盖数参数化扩展。
- **技术贡献**：提出了新的Gibbs自界不等式和Bellman残差转化引理，这些工具可能独立于本文有价值。

### 8. 不足与局限

- **无实验验证**：作为纯理论工作，缺乏仿真或实际任务上的实证检验，无法评估算法在实际RLHF场景中的表现与界限的紧度。
- **理论假设较强**：
  - 要求错误设定水平ζ已知（虽然提及可通过模型选择包装移除，但未给出具体分析）。
  - 假设噪声为次高斯（light-tailed），而实际中RLHF可能遇到重尾噪声或对抗性干扰。
  - 点态错误设定度量要求sup-norm界，可能在实际中过于保守。
- **遗憾界中的因子**：RL界中H³因子较大，且作者承认能否改进以及匹配下界仍是开放问题。
- **仅针对奖励错误设定**：未处理偏好或交互反馈等更丰富的信号形式，也未涵盖Nash学习等变体。
- **依赖函数类有限性**：虽然可扩展到无限类，但文中主要结果基于有限类，覆盖数参数化部分仅作为注释提及，细节未完全展开。

（完）
