---
title: "Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning"
title_zh: 连接点：通过强化学习训练具有跨域泛化能力的长生命周期AI智能体的大语言模型
authors: "Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20002v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 强化学习驱动长生命周期智能体自我进化
tldr: 长期生命周期代理需持续学习与自我更新。提出CoD框架，通过端到端强化学习训练LLM，在长序列中交替执行任务与更新环境上下文。实验证明该框架能激发元能力，实现域内、跨域及到Ralph-loop的分布外泛化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20002v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1608, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1523, \"height\": 1466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 1463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 1272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20002v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 1453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 545, \"label\": \"Table\"}]"
motivation: 现有LLM代理无法在长期部署中持续学习并自适应环境，缺乏跨域泛化的元能力。
method: 设计GRPO风格RL算法与细粒度信用分配，构建交替任务求解与上下文更新的长回滚序列，并定制任务环境。
result: 端到端RL训练有效提升元能力，验证域内、跨域及向Ralph-loop设置的分布外泛化能力。
conclusion: CoD框架连接了多项先前工作，为训练长期生命周期AI代理开辟新路径，并开源实现。
---

## 摘要
本工作提出了一个通用框架，用于训练大语言模型（LLMs）具备“连接点”（CoD）能力，这是长生命周期智能体所需的元能力：当基于LLM的AI智能体部署在环境中时，它需要解决一长串任务，同时持续探索环境、从自身经验中学习，并迭代更新关于环境的上下文，从而基于更新后的上下文在未来任务中实现渐进式改进。CoD框架的主要组成部分包括：（1）用于端到端强化学习（RL）的算法设计和基础设施，其中包含长序列的展开，交替执行任务解决和上下文更新；（2）任务和环境，用于在训练过程中激励和引发LLM中的目标元能力，以及在评估中忠实衡量进展。我们展示了CoD框架的概念验证实现，包括具有细粒度信用分配的GRPO风格RL算法，以及针对目标元能力（而非特定领域LLM能力或标准逐任务RL）定制的任务和环境。实证结果验证了端到端RL训练在CoD设置中的有效性，并展示了所引发的元能力在分布外泛化（训练域内、跨不同域以及从CoD到Ralph-loop设置）的潜力。我们对CoD的研究连接了多项先前工作，并为推进LLMs和AI智能体开辟了新的机会。为促进进一步研究和应用，我们在\url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}发布了实现代码。

## Abstract
This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前基于大语言模型（LLM）的AI智能体在长期部署中缺乏持续学习和自适应能力。当面对一组序列化、相关但不同的任务时，智能体往往需要人工设计的复杂脚手架（agent scaffold）才能稳定运行，无法自动从自身经验中学习并改善未来表现。这被称为“连接点”（Connect the Dots, CoD）元能力的缺失。
- **研究背景**：LLM已从对话模型发展到推理模型，并作为AI智能体的骨干应用于现实。然而，现有LLM在环境中容易迷失（尤其是在欠规范环境下），且标准逐任务强化学习（RL）训练模式要求每个任务从头开始解决，与长期部署中跨任务转移知识的场景不匹配。因此，需要专门设计RL流程来训练LLM具备CoD元能力，使其能在部署中通过自我上下文更新实现渐进式改进。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出CoD框架，将长期部署抽象为交替执行“任务解决片段”（solve-task episode）和“上下文更新片段”（update-context episode）的序列。训练时，通过端到端RL让LLM学会在解决新任务的同时主动探索环境并更新关于环境的上下文（如提示、技能等），从而在后续任务中受益。
- **关键技术细节**：
  - **CoD-Deploy**：部署时的抽象模式，每个环境M中，智能体按顺序解决任务x_i，并更新其上下文z_i。上下文作为系统提示的一部分，在后续任务中使用。
  - **CoD-Train**：对应的RL训练流程，回滚模式与部署一致，但覆盖多个环境以实现泛化。每条轨迹包含多个片段（任务解决和上下文更新）。
  - **信用分配**：采用动态规划原则，每个片段的回报定义为当前片段及未来所有任务解决片段的平均奖励。然后按轨迹组内相同位置计算基线，得到优势值。具体公式：
    - 对于任务解决片段：$R_{x_{i,j}} = \frac{1}{S-j} \sum_{\ell \ge j} r_{x_i,\ell}$，基线为组内平均，优势 $A = R - \text{baseline}$。
    - 对于上下文更新片段：类似地，包括当前更新奖励和未来任务奖励。
  - **RL算法**：基于GRPO（Group Relative Policy Optimization），但移除了重要性采样权重，并引入自适应权重（RED-Weight）平衡正负梯度。损失函数为带裁剪的REINFORCE变体，公式为：
    $$g = \frac{1}{N} \sum_i \nabla_\theta \log \pi_\theta(i) \cdot A_i \cdot \exp\left(\frac{A_i}{T}\right) \cdot M_i$$
    其中T通过二分法使得加权后平均优势接近0，$M_i$为裁剪掩码。
  - **环境设计**：为激励元能力，设计了专门的环境：
    - **FrozenLake-Obscure**：经典冰湖游戏的变体，动作符号（A/B/C/D）与实际方向（上下左右）的映射随机且隐藏，每个任务必须通过试错推理映射，上下文应记录此映射。
    - **Alchemy-Random**：炼金术游戏，元素配方随机初始化，智能体需探索并记录有效配方，上下文应包含配方知识。
    - **TerminalSimulator**：模拟终端环境（Linux/Windows/macOS），智能体需完成文件操作，上下文可包含高效命令序列等。

## 3. 实验设计
- **数据集/场景**：
  - 训练时使用FrozenLake-Obscure（设置A）或混合FrozenLake-Obscure和Alchemy-Random（设置B）。每个环境实例随机生成，任务序列长度为4。
  - 评估时使用域内困难版本（更大地图/更多元素，序列长度8）、跨域环境（Alchemy-Random、TerminalSimulator）以及Ralph-loop设置（重复解决同一任务）。
- **基准（Benchmark）**：没有引入标准LLM基准，而是自行设计的环境。与标准逐任务RL的区别是主要对比。
- **对比方法**：
  - 主要展示CoD训练前后性能提升（基线为初始Qwen3-8B-Instruct）。
  - 在RL算法消融中，对比了三种方法：
    1. 标准GRPO（带重要性采样权重）
    2. REC-OneSide-NoIS（无重要性采样）
    3. 最终算法（REC-OneSide-NoIS + 自适应RED-Weight）
- **评估指标**：任务解决成功率（reward），分位置汇报（如pos 0、pos 1等）。

## 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量或训练时长。仅提到基于Qwen3-8B-Instruct模型，使用Trinity-RFT框架。实验规模较小（任务序列长度4，回滚批次大小32，组大小8），可推断算力需求相对中等，但具体数据缺失。

## 5. 实验数量与充分性
- **数量**：
  - 主要展示了两个训练设置（A和B）的奖励曲线，包括训练奖励和多项评估（域内困难、跨域、Ralph-loop），共约10张图。
  - RL算法消融实验（图5）对比三种方法，但仅在Alchemy-Random上展示。
  - 报告了FrozenLake-Obscure上从位置0成功率18%→45%，位置3成功率28%→76%的数值。
- **充分性**：
  - 实验覆盖了域内、跨域泛化以及Ralph-loop，角度全面。
  - 但环境较为简单（游戏和模拟），未在真实复杂场景（如代码仓库维护、个人助手）中验证。
  - 仅使用了8B参数模型，未扩展到更大模型（如70B、130B）。
  - 未与现有元RL方法（如LaMer、MAGE、Orbit）进行定量比较。
  - 训练稳定性仅做了有限消融，且终算法仍有启发式成分。

## 6. 论文的主要结论与发现
- 端到端RL训练能有效激发LLM的CoD元能力：在FrozenLake-Obscure上，第四任务的成功率从28%显著提升至76%，而首任务成功率仅从18%升至45%（受信息论极限约束），说明智能体学会了利用上下文而非单纯提升单任务性能。
- 该元能力展现出域内泛化（更难环境、更长序列）、跨域泛化（从FrozenLake-Obscure到Alchemy-Random和TerminalSimulator）以及向Ralph-loop设置的泛化能力。
- 提出的细粒度信用分配（基于位置平均回报）优于粗粒度方法（如Orbit的求和回报），使得有效训练可行。
- 自适应RED-Weight机制对训练稳定性至关重要，尤其在Alchemy-Random域上。

## 7. 优点
- **框架新颖**：将长期生命周期智能体的元能力训练形式化为端到端RL问题，并设计了匹配的部署和训练模式，连接了元RL、推理扩展和终身智能体等多条研究线。
- **信用分配设计合理**：基于动态规划原理，将回报定义为当前加未来任务的平均奖励，与标准GRPO风格结合，实现细粒度、位置感知的优势计算。
- **环境针对性**：专门设计了FrozenLake-Obscure和Alchemy-Random，确保任务不能从零解决，从而激励上下文更新，避免了标准RL任务的局限性。
- **泛化评估全面**：不仅评估域内，还评估跨域和Ralph-loop，显示了元能力的广泛适用性。
- **开源实现**：代码已发布，便于复现和后续研究。

## 8. 不足与局限
- **实验规模有限**：环境仍属于游戏/模拟级别，与真实世界长期部署（如个人助手、代码仓库维护）差距较大。任务序列长度仅为4（训练）或8（评估），较短。
- **RL算法存在启发式成分**：自适应RED-Weight通过二分法调整温度，缺乏严格理论保证；论文也承认基线计算中不同轨迹的片段并非完全可比。
- **上下文更新机制简单**：仅通过重写文本“提示”来实现，未涉及更复杂的记忆系统（如持久记忆库、技能文件等），这在更挑战场景中可能不足。
- **训练不稳定性**：在混合域设置中，Alchemy-Random的奖励曲线出现波动，说明算法仍有改进空间。
- **未与其他先进方法对比**：没有与LaMer、MAGE、Orbit等近期RL²工作进行比较，无法直接评价CoD的相对优势。
- **缺少真实场景验证**：未在人类用户或实际软件工程环境中测试，泛化到现实的能力存疑。
- **算力资源未报告**：无法评估训练成本或可扩展性。

（完）
