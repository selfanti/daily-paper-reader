---
title: "Bias-Controlled Primal-Dual Natural Actor-Critic: Optimal Rates for Constrained Multi-Objective Average-Reward RL"
title_zh: 偏差控制的原始对偶自然演员-评论家：约束多目标平均回报强化学习的最优速率
authors: "Ankur Naskar, Swetha Ganesh, Vaneet Aggarwal"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25012v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 策略梯度演员-评论家方法用于约束多目标强化学习
tldr: 针对无限时域平均奖励强化学习中多目标优化与安全约束问题，凹标量化方法因非线性导致策略梯度和actor-critic估计产生偏差。本文提出基于多层蒙特卡罗的原始-对偶Natural Actor-Critic算法，通过控制标量化目标、约束评估和actor-critic估计中的偏差，无需混合时间先验知识。理论证明该算法实现了全局收敛率和约束违反率的最优Õ(1/√T)界。这是首个在平均奖励设定下为凹标量化多目标强化学习（含约束和无约束情形）建立最优收敛且不依赖混合时间信息的工作。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25012v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1185, \"height\": 425, \"label\": \"Table\"}]"
motivation: 平均奖励强化学习常需优化多个冲突目标并满足约束，凹标量化引入偏差，现有方法缺乏最优收敛保证或依赖混合时间信息。
method: 提出基于多层蒙特卡罗的原始-对偶Natural Actor-Critic算法，在平均奖励MDP中控制标量化目标、约束及actor-critic偏差。
result: 算法达到全局收敛和约束违反率的最优Õ(1/√T)界，无需混合时间知识。
conclusion: 首次为平均奖励设定下凹标量化多目标强化学习（含约束）建立最优收敛，且无需混合时间信息。
---

## 摘要
在无限时域平均回报设置中，许多强化学习问题需要优化多个相互冲突的目标，同时满足多个安全约束。一种常见的方法是凹标量化，即智能体最大化效用 $ f(J^π_{r_1}, \ldots, J^π_{r_M}) $，并满足标量化约束 $ g(J^π_{c_1}, \ldots, J^π_{c_N}) \ge 0 $，其中 $J^π_{r_m}$ 和 $J^π_{c_n}$ 分别表示策略 $π$ 下的平均回报和成本。然而，$f$ 和 $g$ 的非线性会在策略梯度和演员-评论家方法中引入偏差，因为梯度必须使用 $J^π$ 的有噪估计来评估，且 $ \mathbb{E}[\partial f(J^π)] \neq \partial f(\mathbb{E}[J^π]),$ 这种偏差会通过原始更新和对偶更新传播。我们提出了一种基于MLMC的原始对偶自然演员-评论家算法，用于平均回报MDP，该算法在不需要混合时间知识的情况下，控制标量化目标、约束评估和演员-评论家估计中的偏差。我们证明该算法实现了最优的全局收敛速度和约束违反率，均为 $ \tilde{O}(1/\sqrt{T}) $。据我们所知，这是首个在平均回报设置下，无论有无约束，为凹标量化多目标强化学习建立最优收敛的结果，并且是首个在无混合时间信息的情况下做到这一点的结果，即使在没有标量化的情况下也是如此。

## Abstract
Many reinforcement learning (RL) problems in the infinite-horizon average-reward setting require optimizing multiple conflicting objectives while satisfying multiple safety constraints. A common approach is concave scalarization, where the agent maximizes a utility $ f(J^π_{r_1}, \ldots, J^π_{r_M}) $ subject to a scalarized constraint $ g(J^π_{c_1}, \ldots, J^π_{c_N}) \ge 0 $, where $J^π_{r_m}$ and $J^π_{c_n}$ denote the average-reward and cost under policy $π$. However, the nonlinearity of $f$ and $g$ introduces bias in policy-gradient and actor-critic methods, since gradients must be evaluated using noisy estimates of $J^π,$ and $ \mathbb{E}[\partial f(J^π)] \neq \partial f(\mathbb{E}[J^π]),$ and this bias propagates through both primal and dual updates. We propose an MLMC-based primal-dual Natural Actor-Critic algorithm for average-reward MDPs that controls bias in scalarized objectives, constraint evaluation, and actor-critic estimation without requiring mixing-time knowledge. We show that the algorithm achieves optimal global convergence and constraint-violation rates of $ \tilde{O}(1/\sqrt{T}) $. To our knowledge, this is the first result establishing optimal convergence for concave scalarized multi-objective RL in the average-reward setting, both with and without constraints, and the first to do so without mixing-time information even in the absence of scalarization.

---

## 论文详细总结（自动生成）

# 论文总结：Bias-Controlled Primal-Dual Natural Actor-Critic: Optimal Rates for Constrained Multi-Objective Average-Reward RL

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：许多实际强化学习系统（如网络资源分配、推荐系统、自动驾驶）需要在长期平均回报框架下同时优化多个冲突目标并满足安全约束。目标通常通过凹标量化函数（如最大化效用函数`f(J^π_r)`并满足约束`g(J^π_c)≥0`）来建模。
- **核心挑战**：非线性标量化函数`f`和`g`在策略梯度和演员-评论家（Actor-Critic）方法中引入偏差：`E[∂f(估计值)] ≠ ∂f(E[估计值])`，这种偏差在平均回报设置下会通过原始-对偶更新放大。现有方法要么只处理标量化但依赖表格/模型（不适用于大状态空间），要么只处理参数化策略但仅限于单目标或无约束，且大多需要混合时间（mixing time）知识。
- **研究意义**：首次在平均回报设置下，针对**凹标量化多目标约束强化学习**，在参数化策略条件下建立了全局最优收敛率`Õ(1/√T)`，且无需混合时间知识。这填补了表1中所示的关键空白。

## 2. 方法论：核心思想、关键技术细节、算法流程

- **核心思想**：采用原始-对偶自然演员-评论家（Primal-Dual Natural Actor-Critic）框架，利用多层蒙特卡洛（Multi-Level Monte Carlo, MLMC）技术联合控制三个来源的偏差：凹标量化梯度偏差、演员-评论家近似偏差、原始-对偶动态中误差传播，同时避免依赖混合时间知识。
- **关键技术细节**：
  - **问题形式化**：优化问题为 `maxθ f(J^θ_r) s.t. g(J^θ_c) ≥ 0`，通过拉格朗日函数 `L(θ,λ)=f(J^θ_r)+λ g(J^θ_c)` 转化为鞍点问题，λ为对偶变量。
  - **NPG更新**：参数θ通过自然策略梯度更新：`θ_{k+1}=θ_k + α w_k`，其中`w_k`是Fisher信息矩阵伪逆乘以梯度`∇θ L(θ_k,λ_k)`的估计；对偶变量λ通过投影更新：`λ_{k+1}=Π_{[0,2/δ]}(λ_k - β g_k)`。
  - **MLMC估计**：对于批评家（critic）估计（值函数和平均回报）、NPG方向估计、标量化梯度估计，均采用MLMC技巧：生成几何分布长度的轨迹（Geom(1/2)），构造无偏或低偏估计，保证偏差阶数为`O(τ_mix / T_max)`，方差为`O(τ_mix log T_max)`，而期望样本数仅为`O(log T_max)`。具体公式见(17)(18)。
  - **批评家线性递归**：使用线性随机递归（11）更新批评家参数ξ，利用MLMC估计量（ˆq, ˆP）。该递归是引理2（通用线性随机迭代）的特例。
  - **NPG线性递归**：使用SGD更新NPG方向w（13），梯度由(14)给出，也用MLMC估计量。
- **算法流程**（Algorithm 1）：
  1. 外循环K次：对每个外迭代k，先通过MLMC估计标量化梯度`∂f, ∂g`和约束值`g_k`。
  2. 内循环H次：运行批评家递归更新ξ（对每个目标/成本），再运行NPG递归更新w。
  3. 用(9)更新θ和λ。

## 3. 实验设计

- **本文为纯理论论文，没有进行实验验证**。所有结论均为理论分析（收敛率和约束违反率），未涉及任何数据集、仿真环境或对比方法。文中提供了与现有工作的理论比较表（Table 1），但没有数值实验。

## 4. 资源与算力

- 论文未提及任何GPU型号、数量、训练时长等计算资源信息。由于是理论分析，不需要实验算力。

## 5. 实验数量与充分性

- 不适用。无实验。因此无法评估实验的充分性、客观性、公平性。

## 6. 主要结论与发现

- **主要定理**（Theorem 1）：在假设1-8下，选择适当步长和内/外循环次数（`α=β=1/√T`，`T_max=K=T`，`H=Θ(ln T)`），所提算法MO-PDNAC的期望目标误差和约束违反率均达到`Õ(1/√T + √ε_bias + √ε_app)`，其中`ε_bias`和`ε_app`分别是参数化误差和函数逼近误差。
- **关键贡献**：
  - 这是首个在平均回报设置下，为凹标量化多目标约束强化学习（含参数化策略）建立最优收敛率的工作。
  - 无需混合时间知识，改进了先前需要混合时间的方法（如[33]）。
  - 即使在无标量化、单目标情形，也改进了[33]：达到`Õ(1/√T)`而非`Õ(1/T^{1/2-ε})`。
- **创新点**：MLMC技术联合控制三类偏差（标量化、批评家近似、原始-对偶动态），并避免了混合时间依赖。

## 7. 优点

- **理论贡献突出**：填补了凹标量化多目标平均回报强化学习的一个理论空白，首次实现最优收敛率。
- **技术优雅**：采用MLMC同时解决偏差和样本效率问题，无需混合时间知识，设计简洁。
- **鲁棒性**：在多个假设（光滑性、Slater条件、特征条件等）下都得到保证，且涵盖了有/无约束、有/无标量化的一般情况。
- **分析深入**：将复杂的三类偏差解耦，分别用引理控制，最后通过原始-对偶框架合并，证明结构清晰。

## 8. 不足与局限

- **缺乏实验验证**：作为纯理论论文，没有提供任何数值实验或仿真来验证理论结果的实用性。实际算法的行为可能与理论界有差距（例如常数项可能较大）。
- **假设较强**：
  - 假设MDP是遍历的（mixing time有限），且特征矩阵满足正定性（Assumption 5, 7）。
  - 要求标量化函数`f,g`光滑且有界梯度（Assumption 3）。
  - 要求Slater条件（Assumption 2）和参数化误差有界（Assumption 6）。
  - 在现实大规模问题中这些假设可能难以严格满足。
- **收敛率仅期望意义**：定理给出的是期望界，未提供高概率界或尾部风险分析。
- **算法复杂度**：虽然MLMC降低了样本复杂度，但内循环H = Θ(ln T)次，每次内循环需生成几何分布长度的轨迹，实际实现中调参可能较敏感。
- **应用限制**：本文聚焦平均回报设定，可能不适用于折扣回报或有限时域问题。

（完）
