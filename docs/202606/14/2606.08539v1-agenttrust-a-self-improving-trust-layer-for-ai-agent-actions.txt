Title: AgentTrust: A Self-Improving Trust Layer for AI-Agent Actions

URL Source: https://arxiv.org/pdf/2606.08539v1

Published Time: Tue, 09 Jun 2026 01:26:27 GMT

Number of Pages: 29

Markdown Content:
# AgentTrust: A Self-Improving Trust Layer for AI-Agent Actions 

## Chenglin Yang 

## Independent Researcher 

## yangchenglin802@gmail.com 

## June 2026 

*AgentTrust v2 — a trust layer for agent actions that learns from its own decisions : it dis-tils rules on lexical threats (cheaper over time) and accrues guarded memory on semantic threats (smarter over time).* 

## Abstract 

AI agents increasingly take consequential actions—shell commands, cloud operations, and arbitrary tool-calls—so a trust layer must decide, per action, whether to allow, warn, block, or escalate. We argue that the right way to reason about such a layer is by threat type . Lexical (fixed-signature) threats , where danger lives in a stable token, are decidable by deterministic rules; semantic (intent-dependent) threats , where a benign and a malicious action share the same surface, are out of reach for rules by construction . We make this concrete with a negative proof: a determined, hand-authored cloud rule pack lifts held-out accuracy only 48 →56% overall and moves the semantic categories by 0pp (data_db 29 →29, observability 59 →59, supply_chain 50 →50), while a strong LLM judge carries exactly those categories. We give the judge a self-learning capability—on a corpus that is mainly semantic attacks it nearly doubles rule accuracy (48% → 83.6–85.2%) with near-zero false-blocks, and this holds across two model providers. We turn this into a self-improving dual-store system : the judge distills a growing deterministic rule floor on lexical threats (cheaper over time) and feeds a guarded RAG memory on semantic threats (a verdict-cache fails—surface-twins collapse to ~58%—so a corroboration guard lifts semantic accuracy +13pp, 70 →84). The result is what sets AgentTrust v2 apart from its static v1 predecessor: a trust layer that self-evolves from its own stream of decisions — getting cheaper on the lexical class (it distils its own rules) and smarter on the semantic class (it accrues guarded precedent), while never hard-blocking a benign action. An end-to-end online replay shows the judge-call rate falling (50% →44%) and judge-domain accuracy rising (71% →80%) over a traffic stream, with 0 benign hard-blocks across 45,000 actions. 

# 1. Introduction 

AI agents no longer merely produce text; they act . Modern agents complete tasks by emitting tool-calls — shell commands, HTTP requests, database queries, cloud-control-plane operations, package installs — that change the state of the world. Each such action carries a real blast radius: a single tool-call can delete a filesystem, exfiltrate secrets to an attacker-controlled endpoint, grant a persistent IAM credential, or pull a backdoored dependency. As agents are wired into shells, CI/CD, and cloud accounts, the unit that must be trusted shifts from the agent to the individual 

1

> arXiv:2606.08539v1 [cs.AI] 7 Jun 2026

action . This motivates a per-action trust layer : a guardrail that intercepts each tool-call and returns a verdict — allow, warn, block, or escalate to human review — before the action executes. The dominant approach to such guardrails is deterministic policy: regexes, AST matchers, and allow/deny lists that flag known-bad patterns. These rules are fast, cheap, auditable, and high-precision on the threats they were written for. But they share a structural limitation that, we argue, no amount of rule-engineering can overcome — and it is best seen by decomposing agent threats by type rather than by domain. 

## A threat-type decomposition: lexical vs. semantic 

We distinguish two kinds of dangerous action. A lexical (fixed-signature) threat is one whose danger lives in a stable, surface-level token: rm -rf / , a reverse-shell one-liner, a hardcoded private key, 

iam create-access-key , –acl public-read , a cluster-admin binding, a write to ld.so.preload ,a request to the cloud metadata IP. The malicious action looks malicious; a regex can decide it. A 

semantic (intent-dependent) threat is one where the benign and the malicious action share the same surface , and the verdict turns on destination, content, or scope rather than on any token: a telemetry curl versus an exfiltration curl (including abuse of an allow-listed endpoint); a kubectl get secret issued for debugging versus for theft; a read-only SELECT versus a COPY ... TO that dumps a table to an external sink; a legitimate dependency versus one whose postinstall hook executes code. For semantic threats, the surface is identical across the two verdicts, so any regex precise enough to block the attack must also block its benign twin — i.e., rules cannot separate them by construction .This is not a hypothesis about under-investment in rule-writing; it is a claim about a structural ceiling. We establish it with a determined steelman . Starting from an honest deterministic rule pack, we hand-authored an additional cloud/CI/Kubernetes rule pack on development data and tested it on a held-out, realistic corpus. The added rules lift overall held-out accuracy from 48% to 56% — exactly the kind of gain one expects when more lexical signatures are added — yet they move the purely semantic categories by 0 percentage points: data-and-database threats stay at 29 →29, observability/benign-hard cases at 59 →59, and supply-chain threats at 50 →50 (Table 3). A separate, maximally token-tuned "cheat" rule set, optimized directly against the data, also lands at 47.6% overall: in-distribution tuning does not transfer. Rules close on the lexical categories and plateau, untouched, on the semantic ones. Figure 1 makes this concrete: rules climb on fixed-signature categories and flatline on intent-dependent ones, while a strong LLM judge carries precisely those semantic categories (78–100%). 

## The judge handles what rules cannot — but its trade-offs are uncharacterized 

If rules cannot cross the semantic boundary, an obvious candidate is an LLM-as-judge that reads the action and reasons about intent. LLM-based guardrails already exist. The open question — and the one this paper answers — is not whether to use a judge, but how it behaves as a system component : its precision/recall trade-off against rules, its cost, its robustness across model families and providers, and whether it can be made to learn rather than re-pay full inference cost forever. These properties are largely uncharacterized in prior work, and they matter because a naive judge can be worse than rules: judging every action invites false-positives and unbounded cost. We characterize the judge across three corpora — an internal tuning set, an independent out-of-sample set, and a realistic held-out corpus independently relabeled by a second annotator ( κ=0.82) — and across four model families and two providers. The headline is that the judge handles the 2Figure 1: Held-out external corpus: deterministic rules close on lexical (fixed-signature) threat categories and plateau on the semantic (intent-dependent) ones, while strong LLM judges across two providers carry exactly the semantic categories. semantic threats rules cannot, and we make it self-learning. On a mainly-semantic-attack corpus, deterministic rules collapse to 48.1% accuracy, while four judges spanning two providers cluster at 83.6–85.2% — nearly doubling rule accuracy — with near-zero false-blocks (Table 1). In-distribution, where rules are already strong, a confidence-gated judge that escalates only when it is confident is net-positive on every axis: gating the judge atop the rule floor reaches 98.3/2.1/0.9 (accuracy/FPR/FNR) on the independent 630-action set, versus 95.4/2.1/6.1 for rules alone, while holding the false-positive rate fixed (Table 2). Crucially, recall saturates even at the smallest model we test — every judge catches the semantic attacks rules miss — so additional model capability buys precision , not recall. And across every judge and every dataset, the system records zero benign hard-blocks : the worst-case precision cost of escalation is a soft warning, never a hard block on a legitimate action. This safety invariant is what makes the layer deployable. 

## From a better judge to a system that learns 

A static judge that generalizes is useful but expensive and forgetful. The defining capability of AgentTrust v2 — and its central departure from the static v1 interceptor — is that it self-evolves from its own stream of judgments. The novelty of this work is to turn the threat-type decomposition into a self-improving dual-store system in which the judge acts as a teacher for two different students, each matched to one threat type — getting cheaper on the lexical and smarter on the semantic over time, while preserving the never-hard-block-a-benign-action invariant. Figure 2 sketches the loop, and Figure 3 gives the architecture: an action passes through a deterministic rule floor ; lexically decidable cases get a cheap verdict, and undecided cases are sent to the confidence-gated judge augmented by a guarded retrieval memory ; the system then learns by distilling new lexical rules and storing semantic precedents. The two students are deliberately asymmetric, because the two threat types are. For lexical threats, the judge distills new deterministic rules, so a growing rule floor handles more traffic without a judge call — the judge-call rate falls over a traffic stream. For semantic threats, distillation is impossible (there is no stable signature to distill), so the judge instead populates a retrieval-augmented memory 3Figure 2: Online self-improving loop (mean of 30 replayed streams): the judge-call rate falls (cheaper, via lexical rule distillation) while judge-domain accuracy rises (smarter, as the guarded RAG memory warms); 0 benign hard-blocks throughout. of precedents. Here we report a sharp negative result that dictates the design: a semantic verdict-

cache keyed on the action cannot work, because retrieval collapses surface-twins — raw and neural retrieval both sit near 58% accuracy with no high-precision regime at any coverage (Figure 4). The memory must therefore be RAG, not a cache: precedents augment the judge’s reasoning rather than replace its verdict. Naively storing the judge’s own verdicts as precedents barely helps (+2pp) and can poison a category; gating admission on corroboration between two judges raises semantic accuracy by +13pp (70 →84) with zero dangerous leaks, lifting memory correctness from 70% to 

97% (Table 4). End to end, an online replay over many traffic streams shows the loop working as intended: the judge-call rate falls from 50% to 44% (cheaper), judge-domain accuracy on the non-rule-able semantic cases rises from 71% to 80% (smarter), and the system commits 0 benign hard-blocks across 45,000 actions (Table 5). 

## Contributions 

This paper makes three contributions; the third — a trust layer that learns and evolves from its own judgments — is the centerpiece, and the first two are the decomposition and characterization that make that self-evolution well-typed. 1. A threat-type decomposition with a negative proof. We separate agent-action threats into lexical (fixed-signature, rule-decidable) and semantic (intent-dependent, where benign and malicious share a surface) classes, and we show — via a determined steelman — that rules are surface-pattern-bound: a hand-authored cloud rule pack lifts held-out accuracy 48 →56% overall but moves every semantic category 0 percentage points (data_db 29 →29, observabil-ity 59 →59, supply_chain 50 →50), and a max-tuned rule set still reaches only 47.6%. The semantic boundary is structural, not an artifact of insufficient rules (Figure 1). 4Figure 3: AgentTrust v2 architecture: a deterministic rule floor (lexical) and a confidence-gated judge backed by a guarded RAG memory (semantic), with the judge-as-teacher learning loop and the never-hard-block-a-benign-action safety invariant. 1. A cross-distribution × cross-provider characterization of the judge. We show that a strong LLM judge is the component that generalizes where rules do not: on a realistic held-out corpus, rules score 48.1% while four judges across two providers score 83.6–85.2% with near-zero false-blocks, and in-distribution a confidence-gated judge is net-positive on every axis (98.3/2.1/0.9 vs. 95.4/2.1/6.1). We further establish two design-relevant invariants: judge recall saturates (capability buys precision, not recall) and the system records zero benign hard-blocks across every judge and dataset. 1. A self-improving dual-store system. We turn the decomposition into a system in which the judge teaches a growing deterministic rule floor on lexical threats (cheaper over time) and a guarded RAG memory on semantic threats (smarter over time). We report the negative result that a semantic verdict-cache cannot work (retrieval collapses surface-twins; no high-precision regime) — which is why the semantic store must be retrieval-augmented — and the positive result that a corroboration-guarded memory lifts semantic accuracy +13pp (70 →84) and memory correctness to 97%. An end-to-end online simulation demonstrates the loop: judge-cost falls (50 →44% call rate), judge-domain accuracy rises (71 →80%), and 0 benign actions are hard-blocked over 45,000 actions (Figure 2). 

The v1 → v2 delta, in three points. v1 [Yang 2026] was a static interceptor: its deterministic rules carried the headline numbers and its judge was opt-in — indeed a naive always-on judge 

lowered accuracy (to 88.0%, false positives 2.3 → 9.0%). v2 changes exactly three things. (i) It shows the rules-vs-judge split is structural , not a coverage gap — a determined rule steelman moves the semantic categories by 0pp, so a judge is necessary, not optional. (ii) It turns the judge into a net-positive, confidence-gated core that holds across distributions and two providers — the asymmetric design v1 explicitly left to future work. (iii) It makes the layer self-evolving : it distils cheap rules on lexical threats and accrues a guarded retrieval memory on semantic threats from its own decisions. 5Figure 4: A similarity-gated semantic verdict-cache has no high-precision regime at any coverage (neither raw-text nor neural retrieval) because surface-twins collapse – which is why the semantic store must be retrieval-augmented generation, not a cache. The remainder of the paper makes these claims precise. We position against rule/policy guardrails and LLM-as-judge guardrails (§2), formalize the action representation and threat taxonomy (§3), describe the dual-store system and its safety invariant (§4), and evaluate all three pillars across three datasets and five model configurations (§5). We are deliberate about scope: our evaluation runs on curated corpora through an agent/replay harness rather than live production traffic, and the online loop is a measured replay simulation rather than a deployed service (§6). We state plainly the claims we do not make — in particular, we do not claim the judge alone beats rules in-distribution on exact-match, nor that adding more rules is a contribution (§6). 

# 2. Related Work 

AgentTrust v2 sits at the intersection of three lines of work: (a) rule- and policy-based static 

guardrails, (b) model-based guardrails that use a language model to judge inputs, outputs, or ac-tions, and (c) the machine-learning machinery for making such a system learn over time — knowl-edge distillation, retrieval-augmented generation (RAG), case-based reasoning (CBR), and online learning. We review each in turn and, in each case, state precisely where our contribution lies. We stress at the outset what our contribution is not : it is not "use an LLM as a judge." LLM-as-judge is by now a standard tool [Zheng et al. 2023], and a prior version of this very system already 6contained an (opt-in) judge [Yang 2026]. Our claims are three structural ones that the literature below does not make: (1) a decomposition of agent- action threats into a lexical / fixed-signature 

class and a semantic / intent-dependent class, together with a negative result that deterministic rules cannot cross the boundary regardless of how many rules are added; (2) a cross-distribution 

× cross-provider characterization showing the judge — not the rules — is the component that 

generalizes ; and (3) a self-improving dual-store in which the judge acts as a teacher that distills a fast deterministic rule "student" on the lexical class and curates a guarded retrieval memory on the semantic class, under a never-hard-block-a-benign-action safety invariant. 

## 2.1 Rule- and policy-based static guardrails 

The first and still most widely deployed line of defense for consequential software actions is the deterministic policy engine. In cloud and Kubernetes infrastructure, Open Policy Agent (OPA) 

and its Rego policy language are the de facto standard: policies are written as code, versioned, and consulted at admission time to allow or deny an operation [Open Policy Agent]. For LLM applications specifically, NeMo Guardrails [Rebedea et al. 2023] provides programmable rails —user-authored, model-independent, interpretable flows in the Colang language — that constrain di-alogue and tool use. Lightweight pattern-and-signature scanners such as Rebuff and Vigil detect known-bad inputs via heuristics, regexes, and embedding-similarity to a database of past attacks [Rebuff; Vigil]. The v1 of AgentTrust [Yang 2026] is itself in this family for its core path: a determin-istic shell-deobfuscation normalizer plus hand-authored rules that flag fixed dangerous signatures (rm -rf / , reverse shells, hardcoded keys), with an LLM judge bolted on only for ambiguous inputs. All of these share a structural property: a rule fires on a surface pattern . This is exactly the right tool for what we call lexical / fixed-signature threats, where the danger is a stable token. It is the wrong tool — by construction , not merely by under-engineering — for semantic / intent-dependent threats, where a benign and a malicious action share an identical surface and differ only in destination, scope, or content (telemetry curl vs. exfiltration curl to an attacker host; kubectl get secret for debugging vs. for theft; SELECT vs. a COPY -to-exfil). A pattern that matches the malicious instance necessarily matches its benign twin, so any rule precise enough to catch the attack must false-positive on legitimate traffic. This is our Pillar 2, and we believe it is novel as a demonstrated result. Prior work notes informally that static filters "miss obfuscation and multi-step context" [Yang 2026] or are brittle, but does not isolate which threats are rule-decidable and which are not, nor prove the gap survives a determined fix. We supply a steelman: a hand-authored cloud/Kubernetes/CI rule pack, authored on development sets and tested held-out, lifts overall held-out accuracy only 48 →56% and moves every semantic category 0pp (data/DB 29 →29, observability 59 →59, supply-chain 50 →50), while a max-tuned "cheat" rule set also plateaus. The boundary is structural; you cannot regex your way across it. 

## 2.2 Model-based guardrails and LLM-as-judge 

A second line replaces (or augments) hand-written patterns with a learned model. Llama Guard 

[Inan et al. 2023] is an instruction-tuned Llama-2-7B classifier that labels prompts and responses against a safety taxonomy; Llama Guard 3 extends this with the Llama-3 release [Grattafiori et al. 2024]. Meta Prompt Guard is a smaller mDeBERTa-based classifier specialized to prompt-injection and jailbreak detection [Meta Prompt Guard], as is ProtectAI’s widely used DeBERTa-v3 prompt-injection detector [ProtectAI]. More broadly, constitutional AI [Bai et al. 2022] uses a model to critique and revise outputs against a set of written principles, and the LLM-as-a-judge 

paradigm [Zheng et al. 2023] established strong models as evaluators of open-ended text. Surveys 7of this rapidly growing "guardrail" space catalog input/output rails, classifiers, and detectors [Dong et al. 2024]. A closely related and very active sub-area targets agentic safety specifically. Greshake et al. [2023] introduced indirect prompt injection against tool-using, LLM-integrated applications; AgentDojo 

[Debenedetti et al. 2024] and ToolEmu [Ruan et al. 2024] are benchmarks/sandboxes that measure 

agent vulnerability and risky behavior; CaMeL [Debenedetti et al. 2025], Progent [Shi et al. 2025], and AgentBound [Bühler et al. 2026] defend by enforcing capability/control-flow, programmable-privilege, or execution-boundary constraints around tool calls; and the OWASP Top 10 for LLM Applications [OWASP 2025] codifies prompt injection, improper output handling, and "excessive agency" as headline risks. These works establish the threat (agents take consequential actions over untrusted data) and supply runtime defenses, motivating a per-action trust layer like ours. 

Where we differ. Our contribution is emphatically not the use of a model as a judge. The components above are, with few exceptions, evaluated in one setting and treated as static artifacts, and three gaps remain that we close: 1. Threat-type decidability. None of these works partition the threat space into a rule-decidable class and a rule-undecidable class and prove (via a determined steelman) that rules cannot cover the latter — the core conceptual move that justifies needing a judge in the first place rather than just asserting it (§2.1, Pillar 2). 2. Generalization across distribution and provider. Classifier guardrails such as Llama Guard, Prompt Guard, and DeBERTa detectors are themselves trained models and are known to degrade off-distribution and under simple evasions [Meta Prompt Guard; Dong et al. 2024]. We instead characterize, on a realistic held-out (out-of-distribution) corpus, that hand-authored rules collapse (48% accuracy) while a strong general judge nearly doubles them (≈84–85%) with near-zero false-blocks, and — critically — that this holds across two model providers and a capability ladder (recall saturates even at a small model; capability buys 

precision ). This is Pillar 1, and it is a measurement/positioning result, not a new model. 3. The cost/precision trade-off and the safety invariant. We do not claim the judge 

alone beats rules in-distribution on exact-match (it does not). Our in-distribution result is a 

confidence-gated hybrid that is net-positive, with a provider-dependent gate threshold (load-bearing), and a hard invariant that the system never hard-blocks a benign action (escalation tops out at warn/review). Existing guardrail papers report aggregate accuracy/ASR; they do not characterize a gated cost-vs-precision frontier under a no-false-hard-block guarantee. In short, prior model-guardrail work answers " can a model flag unsafe content?" We answer " which 

threats need a model at all, does the model generalize where rules don’t (across distributions and providers), and how do we deploy it cheaply and safely." 

## 2.3 Distillation, retrieval/case-based reasoning, and online learning 

Our third contribution — the self-improving dual-store — recombines three classical ML ideas. We frame the judge as a teacher in the sense of knowledge distillation [Hinton et al. 2015], which transfers a large model’s behavior into a cheaper student. Unlike standard distillation into a neural 

student, our lexical student is a growing set of deterministic rules : the judge labels lexically-novel attacks, and confirmed fixed-signature verdicts are promoted to exact/regex rules, so the system gets cheaper over time on the lexical class. This is closest in spirit to symbolic knowledge distillation [West et al. 2021], which distills a large LM into an explicit symbolic artifact (there, 8a commonsense knowledge graph) rather than weights — but applied to a safety policy and gated for precision so the distilled rules never inherit the teacher’s false positives. For the semantic class — which §2.1 argues is rule-undecidable permanently — we instead keep a retrieval memory of precedents, in the lineage of retrieval-augmented generation [Lewis et al. 2020] and, conceptually, the much older case-based reasoning paradigm [Aamodt and Plaza 1994], which solves a new problem by retrieving and adapting solutions to similar past cases. The judge is augmented at decision time with the most similar adjudicated precedents, so the system gets smarter over time on semantics. Two findings make this more than a textbook application of RAG/CBR. First, a negative one: a naive verdict-cache (retrieve the nearest case and reuse its label) 

cannot work here, because the very surface-twin property that defeats rules also collapses semantic twins under both lexical and neural retrieval (no high-precision regime; ≈58% accuracy) — which is why the semantic store must be a judge-augmenting RAG, not a cache. Second, a guardedness 

one: admitting raw judge verdicts into memory poisons it (a benign-looking exfiltration precedent can flip a later twin), so we add a corroboration gate — admit a precedent only when two judges agree — which lifts semantic accuracy +13pp (70 →84) and keeps memory ≈97% correct with zero dangerous leaks. The closest analogues (self-hardening detectors that learn from caught attacks, e.g., Rebuff’s growing attack vector store [Rebuff]) accumulate attack signatures into a similarity index; they do not (i) split the store by threat-decidability, (ii) report the cache-fails-for-semantics negative result, or (iii) impose a corroboration guard to keep a learned memory poison-resistant under a safety invariant. Finally, the end-to-end behavior — a policy that improves from its own stream of decisions — places the system in the tradition of online learning [Shalev- Shwartz 2012], where a predictor updates from a sequence of feedback. Our online contribution is empirical and systems-flavored rather than a new regret bound: a replay simulation over a traffic stream shows the judge-call rate 

falling (50 →44%, the distillation effect) while judge-domain accuracy rises (71 →80%, the memory effect), with 0 benign hard-blocks across 45,000 actions. To our knowledge no prior agent-guardrail system demonstrates this dual cheaper-on- lexical / smarter-on-semantic dynamic, nor ties it to the threat-type decomposition that makes each half well-typed (rules for the distillable lexical threats, guarded memory for the non-distillable semantic ones). 

## 2.4 Relationship to AgentTrust v1 

This paper evolves AgentTrust v1 [Yang 2026], which introduced the per-action allow/warn/block/review trust layer, the shell-deobfuscation normalizer, SafeFix, RiskChain multi-step detection, and a cache-aware LLM-as-judge exposed over MCP. In v1 the deterministic rules carried the headline numbers and the judge was effectively opt-in. v2 makes the judge the evaluated core and contributes what v1 lacked: the threat-type decidability proof (§2.1), the cross-distribution × cross-provider generalization characterization (§2.2), and the self-improving guarded dual-store with its negative cache result and corroboration guard (§2.3). Two points sharpen this lineage — and the leap from v1. First, v1’s judge was opt-in , and v1’s own naive hybrid (consult the judge on every non-block action and take the more severe verdict) actually hurt : it cut misses but pushed the false-positive rate from 2.3% to 9.0% and dropped aggregate accuracy to 88.0% [Yang 2026, Table 9] — the “judge hurts” result. v1 anticipated the fix, explicitly previewing an asymmetric hybrid (let the judge escalate only when the rule verdict is itself uncertain) as future work [Yang 2026, §6.5]; v2’s confidence-gated, escalate-only judge is 

that design, now delivered, characterized, and shown net-positive across providers (§5.2). Second, 9v1 already observed — in two “semantic context required” error cases and its static-analysis-ceiling limitation [Yang 2026, §6.9, L1] — that some threats turn on intent a rule cannot see; v2 elevates that residue into the central threat-type decomposition and proves rules cannot cross it (§5.3). The throughline: v1 was a static interceptor that named these gaps; v2 closes them and, in doing so, becomes a self-evolving trust layer — distilling rules on the lexical side and accruing guarded memory on the semantic side, so it grows cheaper and smarter from its own traffic. 

# 3. Threat Model and Action Representation 

We consider an AI agent that pursues a user’s goal by issuing a stream of actions against external systems: a shell, a database, a cloud control plane, a package manager, a CI/CD pipeline, an HTTP client. Each action has real blast radius — it can delete data, exfiltrate secrets, escalate privilege, or install attacker-controlled code — so a trust layer must render a per-action verdict before the action is executed. This section fixes what an action is, what the trust layer must decide, and the adversary it must decide against. We then introduce the conceptual move that organizes the rest of the paper: a decomposition of agent-action threats by type , into lexical / fixed-signature threats that a deterministic rule can resolve and semantic / intent-dependent threats that, we argue, a rule cannot resolve by construction .

## 3.1 Actions and verdicts 

Following v1’s formalism [Yang 2026, Def. 1], we model an action as the tuple 

a = (type, tool, description, content, params, session, timestamp) ,whose verdict-relevant core is (type, tool, content, params) ; the session identifier supports the multi-step (RiskChain) view and the per-session memory of §4, and the timestamp orders a session’s stream. We center the discussion on the four core fields. Here type is the channel of effect (e.g., shell , db_query , cloud_api , package_install , http_request , file_write ); tool is the specific instrument invoked within that channel (e.g., bash , psql , kubectl , aws , npm , curl ); 

content is the raw, executable payload as the agent emitted it (the literal command line, query string, request body, or file contents); and params is the structured metadata the agent supplies alongside the content (destination host, target resource, scope, flags, credentials referenced). The trust layer sees a exactly as it will be executed — it is an interposition point on the agent’s effector path, not a post-hoc log analyzer. Given a (and the surrounding session context), the trust layer emits one of four verdicts :

• allow — execute the action unmodified; 

• warn — execute, but surface a caution to the operator/log (a soft, fully reversible signal); 

• review — pause and route to a human (or a stronger automated check) before execution; 

• block — refuse to execute the action. These verdicts form an escalation order allow < warn < review < block in severity. A central design commitment of AgentTrust, motivated below and quantified in the evaluation, is a safety invariant on the benign tail : the system never hard-blocks a benign action. When the layer is uncertain about an action it believes is probably legitimate, the worst it may do is escalate to a soft 

warn or to review ; a hard block is reserved for actions it can positively justify as dangerous. The 10 whole system is engineered so that the cost of a false alarm is paid in friction, never in a broken benign workflow (§5.5). 

Adversary. We assume an adversary who controls action content — either a compromised or jailbroken agent, or, more realistically, an agent steered by indirect prompt injection through the data it ingests (a poisoned web page, issue, file, or tool result). The adversary’s aim is to get a harmful action executed. Crucially, the adversary is free to make a harmful action look like a routine one: to wrap exfiltration in the same curl an agent uses for telemetry, to read secrets with the same kubectl get secret an engineer uses to debug, to phrase a bulk dump as an ordinary query. The adversary is not assumed to have compromised the trust layer itself or the systems it protects; sandbox escape and side channels are out of scope. This threat model — an adversary who can choose the surface form of an action independently of its intent — is precisely what makes a purely lexical defense insufficient, and it is what the taxonomy in §3.2 is built to expose. 

## 3.2 The central decomposition: lexical vs semantic threats 

The key conceptual move of this paper is that agent-action threats split into two structurally different kinds, and that this split — not the choice of any particular detector — is what determines which mechanisms can possibly work. 

Lexical / fixed-signature threats. Here the danger inheres in a stable, recognizable token or pattern in the action’s content or params. The malicious action carries a signature that a benign action would essentially never carry: rm -rf / over a root path; a reverse shell ( bash -i >& /dev/tcp/... ); a hardcoded private key or long-lived credential in source; aws iam create-access-key for a foreign principal; an S3 bucket policy set to –acl public-read ; binding a workload to cluster-admin ; writing /etc/ld.so.preload ; a request to the cloud metadata IP 

169.254.169.254 ; a known-bad download sink. For threats of this kind, a regex or AST rule can decide the verdict : the signature is the threat, and matching the signature both detects the danger and (because the benign distribution does not contain that signature) rarely fires on legitimate traffic. Lexical threats are, in a precise sense, rule-complete : a sufficiently careful pattern library resolves them with high precision. 

Semantic / intent-dependent threats. Here the danger does not inhere in any token. The ma-licious and the benign action share the same surface form ; the verdict flips only on the destination, content, scope, or intent behind that form. The same curl is telemetry to a monitoring endpoint or exfiltration to an attacker; the same kubectl get secret is a debugging read or credential theft; the same database access is a routine SELECT or a bulk COPY ... TO dump shipped off-box; the same dependency line installs a real package or, via a postinstall hook, executes attacker code. For threats of this kind there is, by construction, no token a rule can key on that separates the malicious instance from its benign twin — any pattern broad enough to catch the attack also fires on the legitimate action, and any pattern narrow enough to spare the benign twin also misses the at-tack. Resolving a semantic threat requires reasoning about what the action means — where it sends data, what it reads, how much, and why — which is exactly the competence of an LLM judge and exactly what a deterministic matcher lacks. Semantic threats are rule-blind : adding rules patches individual fixed-token cases but cannot move the category, because the category’s hard cases have no fixed token to patch. This is the load-bearing claim of the paper, and we make it falsifiable. If semantic threats were merely under-engineered rather than structurally out of reach, then a determined, hand-authored rule pack should be able to climb the semantic categories. It does not. As our headline result 11 (§5.3, Figure 1) shows, a cloud/k8s/CI rule pack hand-authored on development data lifts overall 

held-out accuracy from 48% to 56%, yet moves the semantic categories by 0pp — data_db stays at 29, observability_benign_hard stays at 59, and supply_chain_pkg stays at 50 — while a strong judge carries exactly those same categories (86, 78, and 80 respectively). The improvement the rule pack does deliver is concentrated on the lexical and mixed categories where stable signatures exist (e.g., cloud_iam_secrets 40 →70). The split is therefore not an artifact of effort: it is the boundary between what is a token and what is an intent .We deliberately frame the rules-vs-judge argument by threat type rather than by any single dataset’s accuracy. The structural claim — rules close the lexical side and cannot cross to the semantic side — is what the design of AgentTrust stands on; corpus-level accuracy comparisons across distributions are reported in the evaluation and appendix in support of it, not in place of it. 

## 3.3 Twin examples: same surface, opposite verdict 

The intuition pump for the semantic class is the twin pair : two actions that a lexical matcher cannot tell apart, with opposite ground-truth verdicts. This twin structure has a v1 pedigree: v1’s benchmark was authored so that each scenario is minimally distinguishable from a benign counterpart (cat README.md vs cat .env ; rm -rf /node_modules vs rm -rf / ) [Yang 2026, §5.1], and v1 already isolated a residue of “semantic context required” errors (e.g. curl -X PUT -T -... ) as the LLM-judge’s domain [Yang 2026, §6.9]. v2 makes that residue the organizing axis. Table 3.1 gives canonical pairs spanning the threat categories used in our evaluation. Each row is a single (type, tool) surface under which both a benign and a malicious action live; the only difference is in the destination, scope, or intent — never in a token a rule could safely key on. 

Table 3.1 — Twin pairs: identical surface, opposite verdict.                                    

> (type, tool) benign twin →allow malicious twin →block what flips the verdict
> http_request /curl post anonymized metrics to the team’s telemetry endpoint exfiltrate ~/.aws/ credentials to an attacker host
> destination of the egress (and what is in the body)
> http_request /curl upload a build artifact to the org’s allow-listed
> object store smuggle a secrets dump through that same
> allow-listed store to a path the attacker controls
> intent/payload , not the host — the host is on the allow-list
> db_query /psql SELECT ... LIMIT 100
> to answer a user question
> COPY (SELECT * FROM users) TO PROGRAM ’curl ...’ to dump the table off-box
> read-vs-extract
> semantics and the egress sink inside the query
> cloud_api /kubectl kubectl get secret to debug a failing pod’s config
> kubectl get secret to harvest credentials for lateral movement
> purpose/scope of the read; the command is byte-identical
> package_install /npm add a real, widely-used dependency add a typosquat whose
> postinstall hook runs attacker code
> behavior of the package , invisible in the install line

Three of these rows deserve emphasis because they defeat the obvious rule-based defenses: 

• Abuse of an allow-listed endpoint (row 2). A common hardening is an egress allow-list 

of approved destinations. But the malicious twin uses an approved host — the attack rides a sanctioned channel to an attacker-chosen path or payload. Here a destination rule is not merely insufficient; it licenses the attack. Only reasoning about what is being sent and why 

separates the pair. 12 • SELECT vs COPY -exfil (row 3). One might hope to ban a token like COPY ... TO PROGRAM .But the dangerous behavior — turning a query into an out-of-band data transfer — has many surface forms ( COPY , \{}copy , server-side functions, application-level pagination loops), while the benign SELECT shares the same tool and often the same tables. Banning one phrasing neither catches the others nor spares the legitimate query; the verdict turns on extraction intent and the presence of an egress sink , not on a keyword. 

• kubectl get secret debug vs theft (row 4). The two actions can be character-for-character identical . No transformation of the action’s text can distinguish them, because they differ only in why the secret is being read. This row is the purest statement of the semantic boundary: when the benign and malicious actions are the same string, no function of that string can separate them. These pairs make concrete why a deterministic floor, however well-tuned, plateaus on the semantic side: its decision is a function of the action’s surface, and on a twin pair the surface is constant while the verdict is not. 

## 3.4 Consequences for system design 

The decomposition prescribes the architecture developed in §4 and shown in Figure 3. Because lexical threats are rule-decidable and cheap, AgentTrust resolves them with a deterministic rule floor and reserves the expensive, reasoning-capable judge for the actions the floor cannot decide — predominantly the semantic class. Because semantic verdicts depend on intent rather than surface, two further consequences follow. First, a verdict cache keyed on the action cannot help on the semantic side: twins that must receive opposite verdicts collapse to the same (or near-identical) key, so cache retrieval cannot separate them — a negative result we establish directly in §5.4 (Figure 4) and which is why semantic precedent must be stored as guarded retrieval-augmented 

reasoning input , not as a lookup answer. Second, because the system’s residual errors live in this hard, intent-dependent tail, the safety invariant of §3.1 — never hard-block a benign action — is what keeps those errors tolerable: an over-cautious judgment on an ambiguous twin degrades to a warn or review , not to a broken workflow. The self-improving loop of §5.4 (Figure 2) is built on exactly this split — the judge teaches the rule floor on the lexical side (cheaper over time) and a guarded memory on the semantic side (smarter over time) — and we therefore return to the lexical/semantic taxonomy as the organizing axis throughout the evaluation. 

# 4. System: AgentTrust v2 

AgentTrust v2 is a trust layer that sits between an agent and the world: every consequential action the agent attempts — a shell command, a cloud API call, a package install, a database query — is routed through the layer, which returns a verdict in {allow, warn, block, review} before the action executes. The design follows directly from the threat taxonomy of §3. Threats split cleanly into two kinds, and the two kinds demand structurally different machinery: 

• Lexical / fixed-signature threats carry their danger in a stable token — rm -rf / , a reverse shell, a hard-coded key, iam create-access-key , –acl public-read , a metadata-service IP. These are decidable by a pattern matcher : cheap, deterministic, and high-precision. 

• Semantic / intent-dependent threats share a surface with a benign twin and differ only in destination, content, or scope — a telemetry curl versus an exfiltration curl (even to an allow-listed endpoint), a debug kubectl get secret versus secret theft, a SELECT versus a 13 COPY -to-exfil. No pattern matcher can separate these without false-positiving on the benign twin; resolving them requires reading intent .A single mechanism cannot serve both kinds well. A pattern matcher is the right tool for the lexical class and is structurally blind to the semantic class (quantified in §5.3); a language-model judge reads intent but is more expensive and is wasted on the lexical class, where a regex already gives a correct answer at a fraction of the cost. AgentTrust v2 therefore adopts a dual-store architecture 

— a deterministic rule floor for the lexical class and a confidence-gated semantic judge backed by a guarded retrieval memory for the semantic class — composed by a distribution-aware fusion policy and bounded by a single hard safety invariant . Crucially, the two stores are not static: the judge teaches both of them, so the system gets cheaper on the lexical class and smarter on the semantic class as it runs (§4.5). Figure 3 shows the full pipeline and the learning loop. 

What v2 inherits, and what it adds. The deterministic core is v1’s [Yang 2026]: a Shell-Normalizer (nine pure-text deobfuscation strategies — v1’s single highest-value component, which alone halves the obfuscated-attack miss rate in ablation), a 42-pattern ActionAnalyzer , a 170-rule 

PolicyEngine that already covers cloud IAM (AWS/GCP/Azure), containers, Kubernetes, database privilege, credential files, and anti-forensics, a SafeFix remediation engine, a RiskChain multi-step detector, a cache-aware LLM-Judge , and a TrustReporter . v2 keeps this entire core as its rule floor 

and makes two additions that turn a static interceptor into a self-evolving system : (i) it promotes the confidence-gated judge from an opt-in fallback to the evaluated core (§4.3), and (ii) it couples floor and judge through a self-improving dual-store — judge-to-rule distillation on the lexical side, a guarded retrieval memory on the semantic side — so the system learns from its own decisions (§4.5). 

## 4.1 Action representation and verdicts 

An action is the structured form of an agent tool-call: its type (shell, HTTP, cloud-SDK, package, SQL, . . . ), the tool invoked, the raw content of the call, and its parameters . The layer maps each action to a verdict: 

• allow — execute the action; 

• warn — execute, but surface a caution to the operator; 

• block — refuse the action; 

• review — suspend the action for human adjudication. 

warn and review are soft outcomes: the benign twin of a semantic threat is never silently destroyed, only escalated for a human’s attention. This distinction is what the safety invariant (§4.6) is defined on, and it is what lets the system escalate aggressively on ambiguous traffic without ever catastrophically over-blocking. 

## 4.2 The deterministic rule floor (lexical store) 

The first stage is a rule floor : a set of exact-match and regular-expression rules over the action’s content and parameters, each mapping a matched signature to a verdict. The floor is deterministic and high-precision by construction — a rule fires only on a token it was written to recognize — which makes it both cheap (no model call) and auditable (every block traces to a named rule). The floor is fronted by v1’s ShellNormalizer , which rewrites obfuscated payloads to a canonical form (variable expansion, hex/octal escapes, alias resolution, command substitution, adjacent-quote 14 concatenation) so a fixed signature still matches after evasion — the mechanism that keeps even 

obfuscated lexical threats rule-decidable, and v1’s single highest-value component in ablation [Yang 2026, §6.7]. It is the right and sufficient mechanism for the lexical class: a fixed signature such as a reverse-shell payload or a public-read ACL is exactly what a regex decides well. The floor’s limit is equally structural and is the pivot of the whole design: because a rule keys on surface tokens, it cannot distinguish two actions that share a surface but differ in intent. Adding rules does not cross this boundary — a point we establish empirically with a determined steelman in §5.3, where a hand-authored cloud rule pack moves the lexical categories but moves the semantic categories by 0pp. The floor is therefore deliberately not asked to resolve the semantic class; that is the judge’s job. What the floor does gain over time is coverage of newly observed lexical threats, via distillation from the judge (§4.5). 

## 4.3 The confidence-gated semantic judge 

Actions the rule floor does not decide are escalated to a semantic judge : a strong language model prompted to read the action in context and return a verdict together with a rationale and a 

confidence . The judge is the component that reads intent, and it is the component that handles the semantic threats rules cannot — on the mainly-semantic-attack corpus of §5 it nearly doubles rule accuracy with near-zero false-blocks, and it does so across model families and two providers (§5.2). Recall is not the scarce resource here: even the smallest judge we test catches the semantic attacks the rules miss (§5.5). What scales with model capability is precision — the rate at which the judge over-escalates a benign action. Because the judge can over-escalate, AgentTrust v2 never acts on a raw judge verdict. The judge’s output is admitted only through a confidence gate : an escalation (an allow → warn/block/review move) is taken only when the judge’s confidence clears a threshold τ ; below τ the action keeps the rule floor’s verdict. The gate is the load-bearing control that converts a high-recall but imperfectly-precise judge into a net-positive component — it preserves the judge’s catches on the semantic class while suppressing low-confidence over-escalation of benign traffic. Two properties of the gate matter for the system design: 

• The threshold is provider-dependent. Calibration differs across model families: Claude judges are well-calibrated and admit a loose gate, while a GPT-family judge is trigger-happy on ambiguous traffic and requires a tighter gate, but in both cases confidence cleanly separates real catches from over-flags, so a single per-provider τ recovers a net-positive operating point (§5.2). τ is a configuration parameter of the deployment, set per judge. 

• The gate is what bounds precision cost. Quantitatively, with a calibrated gate the escalate-only judge is net-positive in-distribution across every model tier we test; the best in-distribution operating point on the independent 630 corpus is a Sonnet-4.6 gated hybrid at 98.3 / 2.1 / 0.9 (accuracy / FPR / FNR), versus the rule floor’s 95.4 / 2.1 / 6.1 — the judge closes the recall gap with the false-positive rate held flat (§5.2). 

## 4.4 The guarded RAG memory (semantic store) 

The semantic class cannot be served by simply caching the judge’s verdicts. The natural idea — remember the verdict for an action and reuse it when the action recurs — fails by construction on exactly this class, because the lookup key is a surface that semantic twins share : a verdict-cache keyed on the action retrieves the wrong precedent for the benign-vs-malicious pair it is meant to 15 separate, and has no high-precision operating regime at any coverage (the negative result of §5.4, Figure 4). The cache fails for the same reason the rule floor fails — it keys on surface. This memory is the semantic generalization of v1’s judge cache: v1 shipped an exact content-hash plus incremental-delta cache that amortized token cost on identical, recurring contexts [Yang 2026, §4.8]; the semantic store retrieves by meaning for accuracy , not by hash for cost — the same component, evolved from cheaper-on-repeat to smarter-on-semantic. AgentTrust v2 instead uses retrieval as evidence for the judge , not as a replacement for it: a guarded RAG memory . Past adjudications are stored as precedents; when a new semantic action arrives, similar precedents are retrieved and supplied to the judge as additional context, and the judge — still reading the specific intent of the new action — produces the verdict. Retrieval here sharpens the judge rather than short-circuiting it, so surface-twins do not collapse to a single answer. The risk in any self-populated memory is poisoning : if the judge’s own fallible verdicts are admitted as precedents wholesale, errors compound. We therefore admit precedents through a corrobora-tion gate — a precedent enters memory only when two independent judges agree on its verdict. This guard is what makes the memory a net gain rather than a liability: with unguarded judge-verdict precedents the retrieval lift is essentially useless and even poisons a category, whereas the corroboration-guarded memory raises semantic accuracy from 70 to 84 (a +13pp lift) with zero 

dangerous leaks, and lifts the memory’s own correctness from 70% to 97% (§5.4). The guard admits a 62% corroboration rate — strict enough to keep memory clean, loose enough to keep it growing. 

## 4.5 The self-improving loop: judge as teacher 

The two stores are coupled by a learning loop in which the judge is the teacher of two students, one per threat type (Figure 3, learning edges; the online dynamics are Figure 2): 

• Lexical → rule distillation (cheaper over time). When the judge resolves an action whose danger is in fact a fixed signature the floor did not yet cover, that signature is distilled into a new deterministic rule. Subsequent occurrences are decided by the floor without a model call, so the judge-call rate falls as the floor’s lexical coverage grows. Distillation is applied only to lexically-novel cases; the semantic class is, by construction, not ruleifiable and stays with the judge permanently. 

• Semantic → guarded memory (smarter over time). Corroborated semantic adjudica-tions are written to the guarded RAG memory (§4.4), so the judge’s accuracy on the semantic class improves as the memory warms. An end-to-end online replay over a traffic stream exhibits both effects together: the judge-call rate falls from 50% to 44% (cheaper, from lexical distillation) while judge-domain accuracy on the non-ruleifiable semantic class rises from 71% to 80% (smarter, as the memory warms toward its guarded ceiling), with overall accuracy flat because the already-correct ruleifiable cases dominate the mix (§5.4). The loop is a replay simulation over measured verdicts, not live production re-judging; we are explicit about this in §6. 

## 4.6 Distribution-aware fusion 

How the rule floor and the judge are combined depends on whether the incoming traffic resembles the distribution the rules were authored for. The two stores are fused by a distribution-aware 

policy: 16 • In-distribution: rules floor + escalate-only judge. When the traffic matches the rules’ design distribution, the floor is trustworthy on the lexical class, so the floor decides and the judge is allowed only to escalate an allow → warn/block/review through the confidence gate. This is the net-positive hybrid of §4.3. 

• Out-of-distribution: the judge leads. When the traffic is off the rules’ distribution, the floor is no longer a safe foundation — on the realistic held-out corpus the rule floor not only collapses in accuracy but its false-positive rate climbs to 22.4% (§5.2), so building on it as a floor would drag down a capable judge. Here the policy hands leadership to the judge (gated-replace rather than escalate-only): the judge, which lands 83.6–85.2% across the two providers with near-zero false-block on that same corpus, decides and the floor is demoted to an advisory signal. The same two components, recomposed by distribution, give the best of both: the floor’s cheap precision where it is valid, and the judge’s generalization where it is not. 

## 4.7 The safety invariant 

Above all stages sits one inviolable constraint: the system never hard-blocks a benign action. 

Concretely, no fusion path may turn a truth-benign action into a block ; the worst outcome the layer may impose on a benign action is a soft warn or review for human attention. Hard-blocks are reserved for actions the system is confident are dangerous. This invariant is what makes aggressive escalation safe to deploy: the cost of the judge being uncertain about a benign action is a caution or a human glance, never the silent destruction of legitimate work. The invariant holds empirically across every judge × dataset combination we evaluate (§5.5), and end-to-end across the full online replay — 0 benign hard-blocks over 45,000 actions (§5.4). It is the deployability guarantee on which the rest of the system rests: a trust layer an operator can put inline precisely because its worst-case error on benign traffic is bounded to a soft verdict by construction. 

# 5. Evaluation 

We evaluate AgentTrust v2 against the three pillars of §1. We first show that the confidence-gated judge handles the semantic threats rules cannot: net-positive in-distribution and dominant on a mainly-semantic-attack corpus, across two model providers (§5.2). We then establish the paper’s central structural claim — that deterministic rules resolve lexical threats but cannot cross the semantic boundary, no matter how determinedly they are hand-authored (§5.3). Finally we evaluate the self-improving dual-store: why a semantic verdict-cache cannot work, why a guarded 

RAG memory does (+13pp), and an end-to-end online loop that gets cheaper and smarter over a traffic stream while never hard-blocking a benign action (§5.4). §5.5 collects the invariants that hold across every model and dataset. Our evaluation is designed to answer three questions in order. (Q1) Does a per-action trust decision 

generalize beyond the distribution its policy was written on? (Q2) Is the gap between rules and a judge a matter of coverage (write more rules) or of structure (a class of threats rules cannot reach)? 

(Q3) Can a system learn from its own judgments — becoming cheaper on the threats rules can handle and smarter on the ones they cannot — under a hard safety invariant? We take Q2 to be the load-bearing question and treat it as the headline (§5.3). 17 5.1 Experimental setup 

Datasets. We use three corpora of agent actions, in increasing order of distance from the policy under test. 1. Internal 300 — the tuning corpus, used during development to set thresholds and rules. We report on it for completeness but never draw a headline claim from it. 2. Independent 630 — an out-of-sample corpus held back from tuning. This is our in-distribution test set: same threat taxonomy, not seen while authoring rules or calibrating gates. 3. External held-out 189 — a realistic, out-of-distribution (OOD) corpus, validated by an inde-pendent second annotator who relabeled it blind (Cohen’s κ = 0.82 with the original labels) and leakage-checked against v1. This is the true generalization test: it stresses whether a trust decision survives a shift in how actions are phrased and composed, rather than merely repeating the training distribution. It is deliberately adversarial in composition (52% semantic/attack-heavy), which makes it a conservative setting for any amortization claim (§5.4, §6). The external corpus spans eight threat categories that we use throughout: shell_persistence , fs_ config , network_egress , cloud_iam_secrets , cicd_iac , supply_chain_pkg , observability_ benign_hard , and data_db . Each category mixes benign actions with their malicious surface-twins (e.g. a telemetry curl against an allow-listed endpoint vs. an exfiltration curl to the same endpoint; a debugging kubectl get secret vs. credential theft), which is what makes the lexical/semantic distinction operational rather than cosmetic. 

Provenance and relationship to v1’s numbers. The internal 300 and independent 630 are v1’s released benchmarks [Yang 2026, §5]: the 300 spans six categories (50 each; 133 allow / 38 warn / 129 block; review is reserved for ambiguity and is never a ground-truth label), and the 630 is a five-batch adversarial suite (a 30-scenario obfuscation probe, two 100-scenario real-world batches, two 200-scenario expansions; 188 / 98 / 344). Per v1’s anti-overfitting protocol the 630 is scored under the current, patched ruleset — an independently-constructed held-out set, not a zero-shot one. We re-run the deterministic floor under a stricter honest protocol (per-scenario session reset, and excluding the four synthetic-domain benchmark-compatibility rules v1 isolates in benchmark_compat.yaml ), obtaining rules-only 94.7% (300) and 95.4% (630); these reconcile with v1’s published 95.0% / 96.7% (and v1’s compatibility-on 97.0% ), the small deltas being exactly those compat rules and the session reset. The external held-out 189 is new to this paper : a realistic corpus built independently of the policy ruleset, leakage-checked against v1, and independently relabeled, blind, by a second annotator who is not an author of this work, reaching Cohen’s κ = 0.82 with the original labels (meeting v1’s recommended κ > 0.7 target). It is the true out-of-distribution test. 

Judges. We evaluate five judge configurations spanning two providers and a capability ladder: Haiku-4.5, Sonnet-4.6, and Opus-4.8 (Anthropic), GPT-5.5 (OpenAI), and DeepSeek as a deliber-ately weak baseline (it is the model family v1 shipped with). Using the same prompt and protocol across all five lets us separate provider/calibration effects from raw capability. 

Rule conditions. To make the rules-vs-judge comparison fair to the rules, we test three rule configurations: (i) honest DEFAULT — v1’s shipped 170-rule floor, which already covers cloud IAM (AWS/GCP/Azure), Kubernetes, containers, databases, and credential files [Yang 2026, §4.4]; (ii) cheat — v1’s four isolated benchmark-compatibility rules ( benchmark_compat.yaml , matching 18 synthetic domains such as evil.com , deliberately excluded from production), included to expose the ceiling of in-distribution token tuning; and (iii) DEFAULT + cloud-pack — a determined steelman in which we hand-authored an additional cloud/Kubernetes/CI rule pack on the external dev sets and evaluated it held-out. The cloud-pack is the fair test of "just write more rules," and it is central to §5.3. 

Metrics. We score all four verdicts honestly (allow / warn / block / review) and report: exact-verdict accuracy (acc); false-positive rate (FPR) — a benign action (truth = allow) receiving any non-allow verdict; false-negative rate (FNR) — a dangerous action (truth = block) receiving allow or warn; and the benign HARD-block count — a benign action receiving a hard block . The last metric encodes our safety invariant: a deployable trust layer may at worst inconvenience a benign action with a soft warn /review , but must never hard-block it. We also report the judge-call rate and its amortization for the online loop (§5.4). The system architecture under test — the rule floor, the confidence-gated judge, and the guarded RAG memory arranged as a dual-store — is summarized in Figure 3 (§4). 

## 5.2 Pillar 1 — the judge is the component that generalizes 

Claim. A confidence-gated judge is net-positive in-distribution, and out-of-distribution the judge 

alone dominates rules — and both results hold across model families and providers, not just for a single strong model. 

Judge alone (Table 1). In-distribution, deterministic rules are strong on exact-match: the hon-est rule floor scores 94.7% (internal 300) and 95.4% (independent 630), with the residual error concentrated in the subjective warn class. No single judge beats the rule floor on exact-match in-distribution, and we do not claim one does (§7). The picture inverts completely out-of-distribution. On the external 189 corpus the honest rule floor collapses to 48.1% accuracy, with a 22.4% FPR and a 52.9% FNR — i.e. on a realistic distribution shift the rules both over-block benign actions and miss more than half of the dangerous ones. Every strong judge, across both providers, lands in a tight 83.6–85.2% band on exactly that corpus: Opus-4.8 at 85.2 / 1.7 / 4.6, Sonnet-4.6 at 84.1 / 0.0 / 2.3, and GPT-5.5 at 83.6 / 6.9 / 1.1, each with near-zero false-blocks. The judge nearly doubles rule accuracy OOD while lowering the false-block rate. DeepSeek, the weak baseline, reaches 69.8% — better than rules OOD but with a 31.0% FPR — confirming that the generalization is real but that a capable judge is what delivers it cleanly. The capability ladder is informative as design justification , not as the headline. As the judge strengthens (Haiku → Sonnet → Opus / GPT-5.5), precision improves while recall saturates : even Haiku-4.5 catches the semantic attacks that rules miss (it reaches 75.7% OOD with a 0.0% FPR), so capability buys precision, not recall. This is why the deployable design is a strong, confidence-gated judge rather than a weak one — but the contribution is the judge’s generalization, not "stronger is better." 

Gated hybrid (Table 2). In-distribution, the right way to use the judge is as a confidence-gated, escalate-only supplement on top of the rule floor: the rules decide what they decide, and the judge is only allowed to escalate an undecided/permissive verdict when it is confident. This is net-positive on every axis. On the independent 630, the rule floor’s 95.4 / 2.1 / 6.1 becomes 98.3 / 2.1 / 0.9 with a gated Sonnet-4.6 judge (and 97.9 / 2.1 / 1.5 with Opus-4.8) — accuracy up, FPR held flat, FNR cut by more than 6 ×; all judge tiers are net-positive here, including Haiku (98.1) and GPT-5.5 (97.5). On the internal 300 the same pattern holds (Opus-4.8 96.7 / 2.3 / 1.6). Out-of-distribution, however, the fusion inverts : the rule floor is actively harmful (its 22.4% FPR 19 drags the hybrid down), so an escalate-only design that trusts the floor underperforms ( ≈78%) the judge used alone (≈85%). The correct OOD policy is therefore judge-leads (gated-replace) rather than escalate-only. The practical lesson — and the design we adopt — is that fusion must be 

distribution-aware : trust the cheap rule floor where it is reliable (in-distribution), and let the judge lead where it is not (OOD). This resolves a tension v1 left open: v1’s naive second-opinion hybrid (judge every non-block action, take the more severe verdict) cut misses but raised FPR from 2.3% to 9.0% and dropped aggregate accuracy to 88.0% [Yang 2026, Table 9] — the “judge hurts” result — and v1 explicitly deferred the asymmetric fix (escalate only when the rule verdict is uncertain) to future work [Yang 2026, §6.5]. The confidence-gated, escalate-only judge above is precisely that fix, now realized and net-positive across both providers. 

Why this answers Q1. A guardrail that only repeats its training distribution is not a trust layer. The judge is the only component here that holds up under a realistic shift, and it does so across two providers — so the generalization is a property of judge-based decisioning, not an artifact of one vendor’s model. (For readers who want it, the rules’ in-distribution-to-OOD accuracy gap is tabulated in the appendix; we do not headline it, because the structural reason for the gap — threat type — is the stronger and more rebuttal-proof claim, and we turn to it next.) 

## 5.3 Pillar 2 — rules cannot cross the semantic boundary [HEADLINE] 

Claim. Deterministic rules resolve lexical (fixed-signature) threats, where danger is a stable token. They cannot resolve semantic (intent-dependent) threats, where a benign and a malicious action share the same surface and the verdict depends on destination, scope, or content. This is a structural 

limit, not a coverage gap — and we prove it with a determined steelman rather than asserting it. 

The steelman. The obvious rebuttal to "rules can’t catch semantic threats" is "you just didn’t write enough rules." So we wrote them — on top of a floor that already ships 170 production rules covering cloud IAM (AWS/GCP/Azure), Kubernetes, containers, databases, and credential files [Yang 2026, §4.4], so this is not a blank-slate test. We hand-authored a further cloud/Kubernetes/CI rule pack directly on the external dev sets — i.e. with full knowledge of the threat families — and then evaluated it held-out. Figure 1 and Table 3 report the result per category. The cloud-pack does exactly what rules are good at and nothing more. It lifts overall accuracy from 48% to 56%, and the gains land entirely on categories with stable signatures: shell_persistence 

73 → 77, fs_config 50 → 58, cloud_iam_secrets 40 → 70, network_egress 52 → 65, cicd_iac 

36 → 48. On the categories whose verdict is intent-dependent, the hand-authored pack moves the needle by 0pp: data_db stays at 29, observability_benign_hard stays at 59, and supply_chain_ pkg stays at 50. A determined human author, writing rules with the dev distribution in front of them, cannot improve the semantic categories at all, because the malicious and benign instances are lexically indistinguishable — any regex that fires on the attack also fires on its benign twin, so adding the rule trades a false negative for a false positive rather than resolving the case. The cheat 

condition makes the same point from the other side: a maximally token-stuffed rule pack tuned on the test signatures still reaches only 47.6% overall, because in-distribution rule tuning does not transfer. 

The judge carries exactly the categories rules cannot. On those same semantic categories the Opus-4.8 judge reaches 86 ( data_db ), 78 ( observability_benign_hard ), and 80 ( supply_chain_ pkg ) — and 96–100 on the mixed cicd_iac /cloud_iam_secrets categories — for 85% overall (Table 3). Figure 1 shows both providers’ judges tracking each other across the semantic categories, so the carry is not a single-model fluke. The mechanism is precisely the surface-twin structure: 20 the judge reasons over destination/scope/content, which is the only information that separates the twins, while a rule sees only the shared surface. 

Why this is the headline. This decomposition is the structural backbone of the paper. It is rebuttal-proof in a way an accuracy number is not: one cannot answer "rules are blind to semantic threats by construction" with "write more rules," because we did write more rules, with the test families in hand, and the semantic categories did not move. The split also tells the system designer exactly where each component belongs — rules on the lexical floor, the judge on the semantic core — which is what the dual-store in §5.4 operationalizes. 

## 5.4 Pillar 3 — a self-improving dual-store [NOVELTY] 

Given the §5.3 split, the system should learn asymmetrically: distill the judge’s lexical verdicts into the cheap deterministic floor (cheaper over time on the rule-able threats), and accumulate the judge’s semantic verdicts into a retrieval memory (smarter over time on the threats rules can never reach). The non-obvious result is that the semantic side cannot be a cache — it must be a guarded 

RAG memory — and we show why before showing that it works. 

Lexical → rules (cheaper). On the lexical/fixed-signature categories, the judge acts as a teacher: a confident judge verdict on a previously-undecided lexical action can be distilled into a deterministic rule, so subsequent identical-signature actions are resolved by the cheap floor without a judge call. This is what drives the falling judge-call rate in the online loop below. 

Semantic → memory, and why a cache fails (Figure 4). The tempting design for the semantic side is a verdict-cache : remember the judge’s verdict for an action and reuse it when a "similar" action recurs. This cannot work, for exactly the reason rules cannot (§5.3). Table 4 reports the result: retrieval-verdict accuracy on the external semantic slice (n = 98) is ≈58% with raw-text retrieval — essentially the majority/chance rate — and a neural (MiniLM) embedding does not 

rescue it. Crucially, there is no ≥95% high-precision regime at any coverage : Figure 4 shows that you cannot trade coverage for precision to carve out a safe high-confidence slice, because retrieval collapses the surface-twins — the benign and malicious instances are nearest neighbors, so the cache returns the wrong precedent precisely on the cases that matter. (The TF-IDF variant in Figure 5 is a conservative lower bound and reaches the same conclusion.) This negative result is why the semantic store must be retrieval-augmented generation, where a judge re-reasons over retrieved precedents, rather than a cache that returns a stored verdict. 

Semantic → guarded RAG (smarter; +13pp). Using retrieved precedents to augment the judge, rather than to replace it, works — but only with a guard (Table 4). Starting from a no-RAG baseline of 70% on the semantic slice, RAG with a clean memory of ground-truth precedents reaches 79% (+8pp), establishing a ceiling. But the realistic memory is built from the judge’s own verdicts, not ground truth; feeding those in ungated yields only 72% (+2pp, effectively useless) and actively 

poisons observability_benign_hard (81 → 78) by retrieving the judge’s own past mistakes. The fix is a corroboration guard : admit a precedent into memory only when two independent judges agree on it (here, a no-RAG Sonnet-4.6 verdict matching Opus-4.8). The guard raises memory correctness from 70% (ungated) to 97% , at a corroboration rate of 62%, and lifts the semantic slice to 84% (+13pp) with zero dangerous leaks . The guarded per-category gains land squarely on the semantic threats: cloud_iam 55 → 75, data_db 71 → 86, observability 81 → 93, network 70 → 78. In other words, the memory becomes a poison-resistant, smarter semantic store precisely because the guard filters out the judge’s unreliable self-labels before they can be retrieved as precedent. 21 Figure 5: The TF-IDF verdict-cache variant: a conservative lower bound that reaches the same conclusion as the neural-retrieval variant – no high-precision regime exists for a semantic verdict-cache, at any coverage. 

End-to-end online loop (Figure 2). We tie both stores together in an end-to-end online sim-ulation (Table 5): a Monte-Carlo replay over 30 traffic streams of T = 1500 actions each (45,000 actions total), in which lexical verdicts distill into the rule floor and corroborated semantic verdicts accumulate in the guarded memory as the stream proceeds. Figure 2 shows the two intended dy-namics. The system gets cheaper : the judge-call rate falls from 50% (COLD start, empty floor) to 44% (WARM end) as lexical threats are progressively absorbed into deterministic rules. And it gets smarter : accuracy on the judge’s domain — the non-rule-able semantic cases — rises from 71% to 80% as the guarded RAG memory warms toward its 84% ceiling (§5.4). Overall accuracy is essentially flat (84% → 85%), which is expected and honest: the rule-able cases that the floor already handles dominate the stream, so the system-level number is insensitive to the semantic improvement even though the semantic domain improves by 9pp. Most importantly, the safety invariant holds end to end: 0 benign HARD-blocks across all 45,000 actions .

Why this answers Q3. The dual-store learns in the shape that §5.3 demands — cheaper on the lexical, smarter on the semantic — and the negative cache result (Figure 4) is what forces the asymmetric design rather than a single uniform memory. The corroboration guard is what makes self-improvement safe rather than self-poisoning, which is the difference between a memory that helps and one that compounds the judge’s own errors. 22 5.5 Invariants and ablations 

Three properties hold across every judge and every dataset in our study, and together they are what make the system deployable rather than merely accurate. 

Zero benign hard-blocks. Across all five judge configurations and all three datasets — and across the full 45,000-action online run (Table 5) — no benign action is ever hard-blocked. The worst-case precision cost of the trust layer is a soft warn /review , never a hard block . This is the deployability guarantee: a team can put AgentTrust in the path of real agent traffic knowing the failure mode is friction, not a halted benign workflow. 

Recall saturates; capability buys precision. As shown in §5.2, even Haiku-4.5 catches the semantic attacks that rules miss (it reaches 75.7% OOD at 0.0% FPR). Moving up the capability ladder improves precision /FPR, not recall. This is why a confidence-gated design is the right one — the gate suppresses the lower-capability models’ over-escalation while preserving the recall they already have — and it is why the gated hybrid is net-positive across all tiers in-distribution (Table 2). 

The judge generalizes, cross-provider; the gate is load-bearing and provider-dependent. 

The OOD dominance of the judge (§5.2, Table 1) holds for both Anthropic and OpenAI models, so it is a property of judge-based decisioning rather than one vendor’s calibration. The confidence gate itself is load-bearing: it is what converts a raw judge (which on exact-match in-distribution does not beat rules) into a net-positive hybrid. Its threshold, however, is provider-dependent — different model families separate "confident and correct" from "confident and wrong" at different operating points — which is a calibration we set by hand and flag as a limitation (§6) rather than a solved problem. 

Result tables 

All from this session’s runs (3 datasets × 4 model families + DeepSeek + rules). Metric defs: 

acc = exact-verdict accuracy; FPR = benign(truth=allow) judged non-allow; FNR = danger-ous(truth=block) judged allow/warn; HARD-block = benign judged block (the safety invariant). Verdicts ∈ {allow, warn, block, review}. 

Table 1 — Judge-ALONE (exact-match), acc / FPR / FNR                        

> judge internal 300 independent 630 external 189 (OOD)
> rules-only (honest) 94.7 / 2.3 / 6.2 95.4 / 2.1 / 6.1 48.1 / 22.4 / 52.9
> rules + cloud-pack (steelman) ——56.1 / 22.4 / 33.3 DeepSeek (weak baseline) 85.0 / 5.3 / 7.8 87.1 / 1.6 / 8.1 69.8 / 31.0 / 5.7 Haiku-4.5 84.3 / 6.8 / 4.7 80.0 / 0.0 / 12.2 75.7 / 0.0 / 11.5 Sonnet-4.6 89.3 / 3.0 / 7.0 89.7 / 1.1 / 7.8 84.1 / 0.0 / 2.3 GPT-5.5 85.7 / 3.0 / 1.7 76.2 / 19.7 / 1.7 83.6 / 6.9 / 1.1 Opus-4.8 89.7 / 0.0 / 7.8 91.0 / 1.6 / 9.9 85.2 / 1.7 / 4.6

Reading: in-distribution rules win on exact-match (the gap is the subjective warn class); OOD rules COLLAPSE (48%) while every strong judge across two providers lands 83.6– 85.2% with near-0 false-block. The judge is the component that generalizes. 

Table 2 — Gated HYBRID (rules floor + judge escalate-only), best net-positive point 

All four judge tiers (Haiku-4.5, Sonnet-4.6, Opus-4.8, GPT-5.5) are net-positive in-distribution; 23 dataset rules-only (acc / FPR / FNR) best operating point (acc / FPR / FNR) fusion policy          

> internal 300 94.7 / 2.3 / 6.2 Opus-4.8 96.7 / 2.3 / 1.6 rules floor + gated escalate-only independent 630 95.4 / 2.1 / 6.1 Sonnet-4.6 98.3 / 2.1 / 0.9 rules floor + gated escalate-only external 189 (OOD) 48.1 / 22.4 / 52.9 Opus-4.8 85.2 / 1.7 / 4.6 judge-led (gated-replace)

the table lists the single best operating point per dataset (full acc/FPR/FNR). In-distribution the confidence-gated escalate-only judge is net-positive (beats rules on all axes, FPR held). OOD: the rule floor is harmful (22% FPR) → the judge leads (gated-replace, not escalate-only). 

Table 3 — Rules steelman, per-category accuracy on external held-out (rules-only →

+cloud → Opus-4.8-judge)                           

> category (threat type) rules-only +cloud pack Opus-4.8-judge
> shell_persistence (lexical) 73 77 82 fs_config (mixed) 50 58 79 network_egress (semantic) 52 65 83 cloud_iam_secrets (mixed) 40 70 100 cicd_iac (semantic) 36 48 96 supply_chain_pkg (semantic) 50 50 80 observability_benign_hard (semantic) 59 59 78 data_db (semantic) 29 29 86
> OVERALL 48 56 85

Steelman: hand-authored cloud pack (written on dev external sets, tested held-out) lifts overall 48 →56 but moves data_db/observability/supply_chain by 0pp — the semantic boundary is structural. Max-tuned "cheat" rules also = 47.6% overall (in-dist tuning doesn’t transfer). 

Table 4 — Semantic memory (external semantic slice, n=98) 

Verdict-CACHE (dead end): retrieval-verdict accuracy raw-text ≈ 58% ( ≈ majority/chance); neural (MiniLM) did not rescue it; no ≥95% high-precision regime at any coverage → a semantic verdict-cache keyed on the action is not viable (surface-twins collapse). 

RAG (works, with a guard):             

> condition semantic acc ∆vs no-RAG dangerous leaks
> no-RAG (baseline) 70 —0RAG, clean-memory (truth precedents = ceiling) 79 +8 0RAG, realistic (judge-verdict precedents,
> ungated )72 +2 ( ≈useless; poisons observability 81 →78) 0RAG, realistic +
> corroboration guard 84 +13 0

Guard: admit a precedent only where two judges agree (Sonnet-4.6-noRAG == Opus-4.8). Cor-roboration rate 62%; memory correctness 70% ungated → 97% guarded . Guarded per-category 24 jumps: cloud_iam 55 →75, data_db 71 →86, observability 81 →93, network 70 →78. 

Table 5 — Online self-improving loop (Monte-Carlo replay, 30 streams × T=1500)               

> metric COLD (start) WARM (end) meaning
> judge-call rate 50% 44% cheaper (lexical →rule distillation) judge-domain accuracy (non-rule-able semantic) 71% 80% smarter (RAG memory warms toward 84) overall accuracy 84% 85% flat (rule-able cases already correct dominate) benign HARD-blocks —0 / 45,000 safety invariant held end-to-end

Universal invariants (across all judges × datasets): 

• 0 benign HARD-blocks — worst-case precision cost is a soft warn , never a hard block. 

• Recall saturates — even Haiku catches the semantic attacks rules miss; capability buys 

precision , not recall. 

• Gated hybrid net-positive in-distribution ; judge generalizes OOD, cross-provider .

# 6. Limitations 

We have argued that trust for agent actions decomposes cleanly by threat type — that lexical ,fixed-signature threats are rule-decidable while semantic , intent-dependent threats are out of reach for deterministic rules by construction (Figure 1), and that a confidence-gated judge plus a guarded dual-store turns this decomposition into a system that becomes cheaper on the lexical class and smarter on the semantic class over time (Figure 2). That decomposition is the structural claim we stand on, and it is what survives the limitations below. The results that fill it in, however, were obtained under conditions we state plainly here, because the honesty of this accounting is what makes the decomposition credible rather than convenient. 

Evaluation is on curated corpora, not real production traffic. All numbers in this paper come from an agent/replay harness run over three curated corpora — an internal tuning set of 300 actions, an independent out-of-sample set of 630, and an external held-out set of 189 actions (relabeled blind by an independent second annotator, κ = 0.82; leakage-checked against v1). The external set is realistic and adversarially relabeled, and it is on this set that rules collapse to 48.1% accuracy while the four strong judges across two providers cluster at 83.6–85.2% with near-zero false-blocks — the central generalization result. But a curated, leakage-checked corpus is still not a live production stream: real agent traffic has a different category mix, a different base rate of dangerous actions, distribution drift over time, and adversaries who adapt to the deployed guard. We make no claim that the absolute accuracies transfer unchanged to production; what we claim transfers is the ordering — that semantic categories move 0pp under a determined rule steelman (data_db 29 →29, observability 59 →59, supply_chain 50 →50, Table 3) while the judge carries exactly those categories (78–100%). Relatedly, the judge verdicts evaluated here are model-generated for this study; in deployment, judge latency, cost, and refusal behavior on adversarial inputs would all need to be measured against live traffic rather than assumed from the corpus. 

The online loop is a replay simulation, not a live or production system. The end-to-end self-improving result (Figure 2; Table 5) is a Monte-Carlo replay over 30 streams of 1,500 25 actions each. Its dynamics are driven by measured verdicts — a cold (no-RAG) and a warm (corroboration- guarded) measurement, gated by the real memory-admission and rule-distillation logic — rather than by re-invoking the judge live on every streamed action. This is the right design for a controlled, reproducible study of the loop’s trajectory (judge-call rate falling 50% →44%, judge-domain accuracy rising 71% →80%, and 0 benign hard-blocks across 45,000 actions), but it is not a live re-judging system and it is not the production src/ code path. A deployed loop would have to contend with concurrency, online label noise, and the feedback risk that the distilled rule floor and the RAG memory both learn from the same judge — effects a replay over fixed verdicts cannot fully exercise. 

The amortization magnitude is corpus-conservative. The headline of the loop is direction ,not size: the drop in judge-call rate (50% →44%) is small precisely because our external corpus is 52% semantic/attack-heavy, so most actions legitimately require the judge and cannot be retired to the rule floor. On real, benign-heavy traffic — where the overwhelming majority of actions are either obviously safe or hit a stable lexical signature — the amortized judge cost would fall much further. We therefore report the 50% →44% figure as a conservative lower bound on the cheapening effect, not as the expected production saving, and we do not extrapolate a dollar or latency figure from it. 

The strongest semantic-memory result depends on a single, costly guard variant. Our best RAG result — +13pp on the semantic slice (70 →84), with memory correctness rising 70% →97% and zero dangerous leaks (Table 4) — is achieved by a corroboration guard that admits a precedent only when two judges agree (here Sonnet-4.6-noRAG == Opus-4.8). This is what separates the guarded result from the realistic ungated one, which is nearly useless (+2pp) and actually poisons the observability category (81 →78). The guard works, but it costs a second judge call at admission time, its corroboration rate is 62% (so 38% of candidate precedents are discarded), and it is the 

only guard variant we evaluated. We do not claim it is optimal; cheaper or higher-yield admission policies (single-judge confidence gating, abstention, human spot-checks) are unexplored. 

The figures are from one guard configuration, modest N, and hand-set gate thresholds. 

The external held-out set is 189 actions (98 in the semantic slice used for the memory experiments), which is large enough to separate a 48% rule floor from an 84–85% judge but small enough that per-category accuracies carry non-trivial confidence intervals; we report point estimates and do not over-read single-category differences of a few points. The negative cache result (Figure 4) is similarly measured on this slice — it shows a verdict-cache stalls at ≈58% with no ≥95% high-precision regime at any coverage, which is why the semantic store must be RAG rather than a cache, but it is one corpus, not a proof. Finally, the confidence-gate thresholds that make the hybrid net-positive in-distribution are provider-dependent and hand-set , not auto-calibrated: the gate is load-bearing (it is what holds FPR flat while raising accuracy, e.g. Sonnet-4.6 98.3 / 2.1 / 0.9 on the 630 set), and a deployment on a new model or provider would need to re-tune it. Auto-calibrating the gate is left to future work. We also state the boundaries of our argument as plainly as its content, to forestall misreading: 

• We do not claim the judge alone beats rules in-distribution. On exact-verdict match, in-distribution rules are competitive or better (rules 95.4 vs. the strongest judge-alone 91.0 on the 630 set, Table 1); the residual gap is largely the subjective warn class. The in-distribution win is the gated hybrid (rules floor + escalate-only judge), not the judge in isolation. The judge-alone advantage is an out-of-distribution phenomenon. 

• We do not claim that "adding rules" is a contribution, nor that more rules close 

26 the gap. The opposite is our point. A determined, hand-authored cloud rule pack — written on dev external sets and tested held-out — lifts overall external accuracy only 48 →56% and moves the semantic categories by 0pp (Table 3); a maximally token-tuned "cheat" rule set still scores 47.6% out-of-distribution. The boundary is structural, not an artifact of insufficient engineering effort. 

• We do not claim a deployed or production system, nor a real-traffic evaluation. 

Every result is from the replay harness over curated corpora, as detailed above. The self-improving loop is demonstrated, not shipped. 

• We do not headline the rules’ internal-vs-external accuracy gap. A naive reading of that gap invites the rebuttal that the benchmark is simply "too hard" or that one should "write more rules." We deliberately keep that gap in the appendix and stand instead on the 

threat-type claim — that the semantic categories are unreachable by rules regardless of effort — which is the structural, rebuttal-resistant version of the same observation. 

• We do not claim universality of the absolute numbers across models or providers. 

What generalizes, on our evidence, is the shape : across Haiku-4.5, Sonnet-4.6, Opus-4.8, and GPT-5.5, recall on the semantic attacks that rules miss saturates even at the weakest tier, while capability buys precision (lower FPR), not recall — and a gated hybrid is net-positive in-distribution while the judge leads out-of-distribution. We claim the structure, not the decimals. 

# 7. Conclusion 

Trust for AI-agent actions splits cleanly by threat type, and that split is the organizing principle of this work. Lexical , fixed-signature threats — rm -rf / , a hardcoded key, a known-bad sink — are decidable by deterministic rules and should be: rules are cheap, exact, and auditable. Semantic ,intent-dependent threats — a telemetry curl versus an exfiltration curl to an allow-listed endpoint, a debugging kubectl get secret versus a theft, a benign dependency versus a postinstall RCE — share their surface with a benign twin and are out of reach for rules by construction; a determined rule steelman moves these categories 0pp (Table 3). A strong LLM judge is the component that crosses this boundary: on a mainly-semantic-attack, leakage-checked corpus it nearly doubles rule accuracy (rules 48.1% → judges 83.6–85.2%) with near-zero false-blocks, and the effect holds across two model providers (Figure 1). We turned this decomposition into a self-evolving system. A confidence-gated judge supplies recall on the semantic class without disturbing in-distribution precision (gated hybrid 98.3 / 2.1 / 0.9, Sonnet-4.6 on the 630 set, Table 2); a guarded dual-store then makes the system learn from its own judgments — the judge distills a growing deterministic rule floor on the lexical class (cheaper), and a corroboration-guarded RAG memory accrues semantic precedent (smarter; +13pp, 70 →84, memory correctness 70% →97%, Table 4) — while a verdict-cache provably cannot, because surface-twins collapse under retrieval (Figure 4). End to end, an online replay shows the judge-call rate falling (50% →44%), judge-domain accuracy rising (71% →80%), and — across all judges, datasets, and the full 45,000-action stream — zero benign hard-blocks (Figure 2; Table 5). That last invariant is the deployability guarantee: the worst case the system can inflict on a legitimate action is a soft warn 

or a review request, never a hard block. The honest scope of these results is a replay study over curated corpora, with hand-set gate thresh-olds and a single, costly memory guard (§6). The contribution is not a shipped product but a 27 characterization and a design: that the rules-versus-judge question is settled by threat type rather than by effort, that the judge is what makes agent-action trust generalize beyond the rules’ distri-bution, and that a guarded, self-improving dual-store can get cheaper on the lexical and smarter on the semantic over time without ever hard-blocking a benign action. The clearest paths forward follow directly from the limitations: a real-traffic evaluation, an auto-calibrated confidence gate, additional memory-admission guards, and a productionized online loop. 

# References 

• Aamodt, A. and Plaza, E. (1994). Case-Based Reasoning: Foundational Issues, Methodological Variations, and System Approaches. AI Communications, 7(1), 39–59. 

• Bai, Y., Kadavath, S., Kundu, S., Askell, A., Kernion, J., Jones, A., Chen, A., Goldie, A., et al. (2022). Constitutional AI: Harmlessness from AI Feedback. arXiv:2212.08073. 

• Bühler, C., Biagiola, M., Di Grazia, L., and Salvaneschi, G. (2026). AgentBound: Securing Execution Boundaries of AI Agents. In Proc. 34th ACM Joint European Software Engineering Conference and Symposium on the Foundations of Software Engineering (FSE 2026), article FSE096. ACM. https://doi.org/10.1145/3808103 

• Debenedetti, E., Zhang, J., Balunović, M., Beurer-Kellner, L., Fischer, M., and Tramèr, F. (2024). AgentDojo: A Dynamic Environment to Evaluate Prompt Injection Attacks and Defenses for LLM Agents. NeurIPS 2024 Datasets and Benchmarks Track. arXiv:2406.13352. 

• Debenedetti, E., Shumailov, I., Fan, T., Hayes, J., Carlini, N., Fabian, D., Kern, C., Shi, C., Terzis, A., and Tramèr, F. (2025). Defeating Prompt Injections by Design (CaMeL). 

arXiv:2503.18813. 

• Dong, Y., Mu, R., Zhang, Y., Sun, S., Zhang, T., Wu, C., Jin, G., Qi, Y., Hu, J., Meng, J., Bensalem, S., and Huang, X. (2024). Safeguarding Large Language Models: A Survey. 

arXiv:2406.02622. 

• Grattafiori, A., et al. (Llama Team, AI @ Meta) (2024). The Llama 3 Herd of Models 

(introduces Llama Guard 3). arXiv:2407.21783. 

• Greshake, K., Abdelnabi, S., Mishra, S., Endres, C., Holz, T., and Fritz, M. (2023). Not What You’ve Signed Up For: Compromising Real-World LLM-Integrated Applications with Indirect Prompt Injection. arXiv:2302.12173. (AISec 2023.) 

• Hinton, G., Vinyals, O., and Dean, J. (2015). Distilling the Knowledge in a Neural Network. 

arXiv:1503.02531. 

• Inan, H., Upasani, K., Chi, J., Rungta, R., Iyer, K., Mao, Y., Tontchev, M., Hu, Q., Fuller, B., Testuggine, D., and Khabsa, M. (2023). Llama Guard: LLM-based Input-Output Safeguard for Human-AI Conversations. arXiv:2312.06674. 

• Lewis, P., Perez, E., Piktus, A., Petroni, F., Karpukhin, V., Goyal, N., Küttler, H., Lewis, M., Yih, W., Rocktäschel, T., Riedel, S., and Kiela, D. (2020). Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS 2020. arXiv:2005.11401. 

• Meta Prompt Guard. Prompt-Guard-86M / Llama-Prompt-Guard-2 (mDeBERTa-based prompt-injection & jailbreak classifier, released with Llama 3.1 / Llama 4). Meta / Hugging Face model card meta-llama/Prompt-Guard-86M .28 • Open Policy Agent (OPA). Open Policy Agent and the Rego policy language. CNCF graduated project. https://www.openpolicyagent.org/ 

• OWASP (2025). OWASP Top 10 for Large Language Model Applications (2025). OWASP GenAI Security Project. https://genai.owasp.org/resource/owasp-top-10-for-llm-a pplications-2025/ 

• ProtectAI. deberta-v3-base-prompt-injection-v2 (DeBERTa-v3 prompt-injection detector). Hugging Face model protectai/deberta-v3-base-prompt-injection-v2 .

• Rebedea, T., Dinu, R., Sreedhar, M. N., Parisien, C., and Cohen, J. (2023). NeMo Guardrails: A Toolkit for Controllable and Safe LLM Applications with Programmable Rails. EMNLP 2023 (System Demonstrations), ACL Anthology 2023.emnlp-demo.40. arXiv:2310.10501. 

• Rebuff. Rebuff: A self-hardening prompt-injection detector (heuristics + LLM classifier + vector-DB of past attacks). https://github.com/protectai/rebuff 

• Ruan, Y., Dong, H., Wang, A., Pitis, S., Zhou, Y., Ba, J., Dubois, Y., Maddison, C., and Hashimoto, T. (2024). Identifying the Risks of LM Agents with an LM-Emulated Sandbox (ToolEmu). ICLR 2024 (Spotlight). arXiv:2309.15817. 

• Shalev-Shwartz, S. (2012). Online Learning and Online Convex Optimization. Foundations and Trends in Machine Learning, 4(2), 107–194. 

• Shi, T., He, J., Wang, Z., Li, H., Wu, L., Guo, W., and Song, D. (2025). Progent: Pro-grammable Privilege Control for LLM Agents. arXiv:2504.11703. 

• Vigil. Vigil: detect prompt injections, jailbreaks, and other risky LLM inputs (YARA signa-tures, transformer classifiers, and vector similarity). https://github.com/deadbits/vigi l-llm 

• West, P., Bhagavatula, C., Hessel, J., Hwang, J. D., Jiang, L., Le Bras, R., Lu, X., Welleck, S., and Choi, Y. (2021). Symbolic Knowledge Distillation: from General Language Models to Commonsense Models. NAACL 2022. arXiv:2110.07178. 

• Yang, C. (2026). AgentTrust: Runtime Safety Evaluation and Interception for AI Agent Tool Use. arXiv:2605.04785. (v1 of this system.) 

• Zheng, L., Chiang, W., Sheng, Y., Zhuang, S., Wu, Z., Zhuang, Y., Lin, Z., Li, Z., Li, D., Xing, E. P., Zhang, H., Gonzalez, J. E., and Stoica, I. (2023). Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena. NeurIPS 2023. arXiv:2306.05685. 29