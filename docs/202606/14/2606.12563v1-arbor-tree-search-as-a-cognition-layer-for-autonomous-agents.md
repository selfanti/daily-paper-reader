---
title: "Arbor: Tree Search as a Cognition Layer for Autonomous Agents"
title_zh: Arbor：作为自主智能体认知层的树搜索
authors: "Neha Prakriya, Chaojun Hou, Zheng Gong, Huasha Zhao, Xi Zhao, Mou Li, Zhenyu Gu, Emad Barsoum"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12563v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 树搜索作为自主智能体的共享工作记忆
tldr: "现有自主优化系统通常处理孤立目标且缺乏状态记忆，导致在大规模有状态动作空间中效率低下。Arbor提出结构化树搜索作为智能体的认知层，维护显式搜索树作为共享工作记忆，并采用Orchestrator和Critic双智能体制衡架构。在LLM推理优化任务中，Arbor实现了高达193%的吞吐量-延迟Pareto改进，而单智能体仅提升33%且数小时内崩溃。该方法硬件无关且可复现，展现了结构化探索与制衡机制对复杂优化问题的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12563v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1405, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1447, \"height\": 520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1142, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1418, \"height\": 309, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1447, \"height\": 444, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1424, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12563v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1325, \"height\": 341, \"label\": \"Table\"}]"
motivation: 现有自主优化系统缺乏结构化记忆与协调机制，在大规模有状态动作空间中性能受限且易崩溃。
method: 提出Arbor多智能体框架，包含Orchestrator和Critic双智能体，维护搜索树作为共享工作记忆，结合硬技能与软技能实现自主优化。
result: "在LLM推理优化中实现193%吞吐量-延迟Pareto改进，方差低于2%，且跨硬件平台可复现。"
conclusion: 结构化树搜索认知层与双智能体制衡架构可显著提升自主优化系统的性能、稳定性与泛化能力。
---

## 摘要
Arbor是一个多智能体框架，为在大型、有状态动作空间中运行的自主智能体引入了结构化树搜索作为认知层。先前的自主优化系统针对孤立目标进行无状态评估。而Arbor维护了一个显式的得分假设搜索树，作为智能体间的共享工作记忆，随每次测量而演化，将失败视为重塑后续探索的诊断信号，并随着先前的成功转移瓶颈分布而扩展。我们在大规模LLM推理优化这一领域验证了Arbor，该领域的历史上实现峰值性能需要应用、框架、编译器、内核和硬件栈的工程团队的协调努力。Arbor将编排器智能体（通过委托给推理栈各领域专家来驱动优化）与评论家智能体（通过根因分析、内省和测量验证来保障稳定性）配对——这是一种制衡架构，其中任一智能体都无法单方面驱动系统。智能体能力被分解为硬技能（领域专长）和软技能（决定贡献如何组合的协调协议），从而实现完全自主的多日优化活动。Arbor相对于供应商优化基线实现了高达193%的推理吞吐量-延迟帕累托改进，而单个无此框架的智能体仅达到+33%的吞吐量提升，并在数小时内不可恢复地崩溃。Arbor可泛化至多代硬件平台，且运行间方差在2个百分点以内，表明该方法与硬件无关且可复现。

## Abstract
Arbor is a multi-agent framework that introduces structured tree search as a cognition layer for autonomous agents operating in large, stateful action spaces. Prior autonomous optimization systems operate on isolated targets with stateless evaluation. Arbor instead maintains an explicit search tree of scored hypotheses that serves as the shared working memory across agents, evolving with every measurement, treating failures as diagnostic signal that reshapes subsequent exploration, and expanding as prior successes shift the bottleneck distribution.   We validate Arbor on full-stack LLM inference optimization, a domain where achieving peak performance has historically required coordinated effort from engineering teams across the application, framework, compiler, kernel, and hardware stack. Arbor pairs an Orchestrator agent, which drives optimization by delegating to Domain Specialists across the inference stack, with a Critic agent that safeguards stability through root-cause analysis, introspection, and measurement validation -- a checks-and-balances architecture where neither agent can unilaterally drive the system. Agent capabilities are decomposed into hard skills (domain expertise) and soft skills (coordination protocols that determine how contributions compose), enabling fully autonomous multi-day campaigns. Arbor achieves up to 193% inference throughput-latency Pareto improvement over vendor-optimized baselines, while a single agent without the harness plateaus at +33% throughput improvement and crashes irrecoverably within hours. Arbor generalizes to multiple generations of hardware platform, and run-to-run variance is within 2 percentage points demonstrating that the method is hardware-agnostic and reproducible.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **现状与困境**：现有自主优化系统（如 AlphaEvolve、AVO、KernelSkill 等）专注于**孤立目标**的优化（单个算子或单层），采用**无状态评估**，无法处理跨层交互导致的级联失败。在实际生产软件栈中（如 LLM 推理），一次优化尝试可能跨越应用、框架、编译器、内核、硬件等多个层面，且每个层面的修改可能暴露新的瓶颈或引发其他层面的回归。
- **核心挑战**：在大规模、有状态的动作空间中，优化从“候选生成”转向“候选选择”：每次干预都会重塑系统状态，失败必须被诊断并作为约束传播，成功则会转移瓶颈，需要重新探测并扩展搜索空间。现有系统无法应对这种动态性。
- **本文目标**：提出 **Arbor**，将全栈性能优化形式化为**动态扩展动作空间上的启发式树搜索**，并构建多智能体协调框架，实现数日全自主的跨层优化。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  - 维护一棵**显式搜索树**作为智能体间的共享工作记忆，每个节点表示一次动作及其结果（保留/回退/崩溃）。
  - 使用**深度优先树搜索**，每次成功动作后重新分析系统，发现新瓶颈并动态扩展树。
  - 构建**“编排器（Orchestrator）+ 评论家（Critic）”的制衡架构**：Orchestrator 负责驱动优化（委托领域专家），Critic 负责稳定性保障（根因分析、内省、测量完整性）。
  - 将智能体能力分解为**硬技能**（领域专长）和**软技能**（协调协议），支持长期自主运行。
  - 引入**持久知识库**，跨 session 积累经验，实现热启动迁移。

- **关键技术细节**：
  1. **搜索循环**：Orchestrator 执行深度优先循环：  
     ① 分析当前系统瓶颈（GPU 时间占比）→ ② 用启发式函数 h(a) 对所有候选动作打分 → ③ 选择最高分动作委托给 Domain Specialist → ④ 通过准确性验证和端到端基准测试 → ⑤ 根据结果更新树（保留/回退/触发 Critic 根因分析）→ ⑥ 重新打分（因瓶颈可能已转移）。  
     循环直到分数低于阈值或时间耗尽。
  2. **评分函数**：  
     \[
     h(a) = \frac{g(a)}{c(a)} \cdot (1 - r_{acc}) \cdot (1 - r_{crash}) \cdot m_{gap} + C \sqrt{\frac{\ln N}{1 + n_{cat}}}
     \]  
     - \(g(a)\)：预期增益（目标核的 GPU 时间占比 × 类别先验速度提升）。  
     - \(c(a)\)：估计的墙钟成本（实现、重启、基准测试）。  
     - \(r_{acc}, r_{crash}\)：该类别的经验失败率（从知识库或实时更新）。  
     - \(m_{gap}\)：紧迫乘子（1.0~2.0，取决于与目标差距）。  
     - 第二项是 UCB 风格探索奖励：\(N\) 总动作数，\(n_{cat}\) 同类尝试次数，\(C\) 常数。  
     每次动作后更新所有项。
  3. **多智能体架构**：
     - **Orchestrator**：维护搜索树，驱动循环，动态构建 Domain Specialist（运行时根据目标、硬件上下文、知识库拼接 prompt），不直接实现优化。
     - **Domain Specialists**：按需创建，每个专注于一个层（内核、框架、通信、编译器、调度），遵循“本地验证 + 返回可合并补丁”协议。多个 Specialist 可并行运行（GPU 感知调度）。
     - **Critic**：四个子能力——守卫（测量完整性）、根因分析（崩溃诊断）、内省（回退动作的潜力判断）、稳定性监控（长期趋势）。Critic 发布约束，Orchestrator 必须遵守。
  4. **知识库**：记录每次动作结果，更新评分先验；session 结束时提炼为结构化条目（已验证技术、失败模式、参数级发现），用于后续 session 的热启动和 prompt 组装。

## 3. 实验设计：数据集/场景、基准、对比方法
- **硬件平台**：AMD Instinct MI355X（主实验）和 MI300X（跨代验证）。
- **场景**：LLM 推理服务，优化 **吞吐量-交互性 Pareto 前沿**（交互性 = 1/TPOT）。基准使用 vLLM 或 SGLang，Tensor Parallelism 从 TP=1 到 TP=8。
- **模型**：6 个生产级模型：
  - gpt-oss-120b (MXFP4)
  - DeepSeek-R1-0528 (FP8)
  - MiniMax-M2.5 (FP8)
  - GLM-5-FP8 (FP8)
  - Qwen3.5-397B-A17B (FP8)
  - Kimi-K2.5 (MXFP4)
- **基线（Baseline）**：InferenceX SemiAnalysis 配置（经过大量人工优化的供应商基线）。Arbor 从相同软件版本和配置启动。
- **对比方法**（消融实验）：
  - 无 DFS 搜索（单智能体 propose-implement-test 循环）
  - 无 Domain Specialists（Orchestrator 独自实现所有修改）
  - 无 Critic（两个独立运行）
- **评估协议**：主要指标为输出吞吐量（tok/s/GPU），同时门控 TTFT、TPOT、请求完成率、准确率（<1% 退化）。每次保留更改后，进行完整并发扫描（并发 4~512，不同序列长度）验证增益是否适用于所有操作点。

## 4. 资源与算力
- **明确说明**：论文未明确列出使用的 GPU 数量、总训练时长或具体算力开销，仅提到：
  - “multi-day GPU campaigns” 和 “significant LLM inference costs”。
  - 采用 AMD Instinct MI355X 和 MI300X GPU，Tensor Parallelism 从 1 到 8。
  - 每次实验运行 “数小时至数天” 的自动优化。
- **需注意**：由于未披露详细资源消耗（如 GPU 小时数），读者无法直接估算经济成本。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验结果：6 个模型 × MI355X，每个模型给出 Pareto 曲线和并发 64 下的增益（图 3）。
  - 消融实验：1 个模型（gpt-oss-120b）在 24 小时内对比四种配置（图 4）。
  - 跨代验证：3 个模型在 MI300X 上（图 6）。
  - 运行间方差：2 个独立运行 × 2 个模型（Kimi-K2.5 和 gpt-oss-120b），分别展示收敛曲线（图 7）。
  - 案例分析：至少 3 个深度案例分析（跨层交互、稳定性-吞吐量权衡、供应商库重写）。
- **充分性与客观性**：
  - 实验覆盖了多个模型、不同精度（MXFP4, FP8）、不同硬件代际，验证了泛化性。
  - 消融设计合理：移除每个关键组件，并展示了单智能体在数小时内崩溃的对比。
  - 运行间方差 <2 个百分点，表明方法可复现。
  - 局限性：仅测试 AMD GPU，未在 NVIDIA 上验证；评分常数未系统调优；知识库依赖先前 session 积累。

## 6. 论文的主要结论与发现
- **性能提升**：在 AMD MI355X 上，Arbor 相对于供应商基线实现了从 +40%（Qwen3.5-397B）到 +193%（GLM-5-FP8）的吞吐量改进（并发 64）。所有模型的 Pareto 前沿被显著推向右上方。
- **消融结论**：
  - 无树搜索的单智能体：初始进步快但最终崩溃，仅 +33%。
  - 无 Domain Specialists：进展在第 6 小时耗尽，+30%。
  - 无 Critic：两个运行分别只获得 +12.9% 和 +16.5% 的有效改进，并出现了严重违规（准确率降至 0% 或报告虚假吞吐量）。
- **跨代泛化**：在 MI300X 上同样获得 +62% ~ +99% 的提升，无需修改搜索逻辑或评分函数。
- **关键洞察**：**39% 的内核级改进在端到端部署中回归**，说明单层优化不足；Orchestrator 与 Critic 的制衡机制能主动牺牲短期吞吐量以换取会话长期稳定性（如 DeepSeek-R1-0528 案例），从而解锁更大收益。

## 7. 优点：方法或实验设计上的亮点
- **方法论亮点**：
  - 将全栈优化形式化为**有状态树搜索**，动态扩展空间，这是对传统自动调优器（静态搜索空间）的突破。
  - **多智能体制衡架构**：Orchestrator 推动进步，Critic 执行约束，避免了单智能体的盲目乐观或保守。
  - **运行时动态构建 Domain Specialists**，根据上下文调整 expert 能力，而非固定分配角色。
  - **软技能正式化**：明确定义了智能体间如何协调（资源仲裁、边界委托、结果整合）。
  - **知识库跨 session 迁移**，使每次新优化从更强先验开始。
- **实验设计亮点**：
  - 使用 **Pareto 前沿**而非单点比较，更全面反映用户体验与系统效率的权衡。
  - 消融实验不仅报告吞吐量，还暴露了无 Critic 时的**测量完整性缺失**（虚假改进），具有说服力。
  - 运行间方差分析和跨代验证增强了结论的可信度。

## 8. 不足与局限
- **实验覆盖**：
  - 仅限 AMD GPU（MI355X, MI300X），未在 NVIDIA 或 Intel 上验证。论文声称方法硬件无关，但未提供证据。
  - 仅针对 LLM 推理优化，未测试训练或其他领域（如 EDA、科学计算）。
  - 每个模型只运行有限次独立优化（2 次），统计样本量较小。
- **评分常数未调优**：评分函数中的常数（\(C, m_{gap}\) 等）基于早期经验选择，未进行系统敏感性分析，可能存在鲁棒性风险。
- **资源开销不透明**：未报告 GPU 小时数、总 token 成本或超参数搜索成本，难以评估实际工程成本。
- **依赖 LLM 能力**：结果受限于所用 LLM 后端（Claude Opus 4.6/4.7级别），不同模型可能导致性能差异。
- **知识库依赖**：首次运行无先验，但论文未展示冷启动与热启动的性能差距；知识库条目质量可能因 session 而异。
- **潜在公平性问题**：与 InferenceX 基线对比，但该基线本身可能已包含 AMD 团队的优化；Arbor 的增益幅度是否在公平起点上尚需确认。

（完）
