---
title: Multi-agent rendezvous in fluid flows via reinforcement learning
title_zh: 基于强化学习的多智能体在流体中的会合
authors: "Bocheng Li, Jingran Qiu, Lihao Zhao"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11274v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 使用多智能体强化学习实现流体中会合
tldr: 多智能体在流体环境中的集结点任务面临挑战，现有策略未能有效利用流场特性。本文采用多智能体强化学习（MARL）在涡旋流中开发物理信息策略，相比朴素策略大幅提升汇集率，且策略对涡旋强度、尺度和群体规模具有可迁移性。通过打破对称性，MARL防止智能体被困于分离涡旋，提取的启发式策略同样优于朴素策略。理论分析表明流体变形阻碍汇集，弱变形区域应作为目标规划。工作揭示了智能体-流体交互的关键作用，展示了MARL在复杂流场中探索群体智能的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11274v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1108, \"height\": 1023, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11274v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1635, \"height\": 1152, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11274v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1641, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11274v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1647, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11274v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1822, \"height\": 1590, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11274v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1717, \"height\": 1026, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11274v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1855, \"height\": 319, \"label\": \"Table\"}]"
motivation: 多智能体在流体环境中集结点时，如何利用流体运动促进收敛尚不明确，需开发有效策略。
method: 采用多智能体强化学习（MARL）框架，设计物理信息奖励，在涡旋流中训练智能体协同汇集。
result: MARL策略显著提高汇集率，可迁移至不同涡旋强度、尺度和群体规模，提取的启发式策略也优于朴素策略。
conclusion: 流体变形阻碍汇集，应选择弱变形区域规划目标；MARL能有效利用流场动力学实现群体智能。
---

## 摘要
会合是多智能体系统的一项关键任务，要求智能体协调到未指定位置相遇。然而，在流体环境中实现这一目标具有挑战性，因为目前尚不清楚智能体如何利用底层流体运动学来促进收敛。在本研究中，我们采用多智能体强化学习（MARL）方法，在涡流中开发了物理启发的会合策略。与智能体相互导航的朴素策略相比，MARL策略显著提高了会合率。MARL策略还展示了在不同涡旋强度、涡旋尺度和集群规模下的可迁移性。通过打破状态-动作映射的对称性，MARL策略利用了一种非直观机制，防止智能体被困在单独的涡旋中，从而提高了会合成功率。此外，从学习策略中提取了一种启发式策略，其性能也优于朴素策略。进一步的理论分析表明，流体形变阻碍了会合过程。大的有限时间李雅普诺夫指数识别出流体效应分离相邻智能体的区域，表明目标应规划在弱形变区域。我们的发现揭示了智能体-流体相互作用在多智能体任务中的重要作用，并突出了MARL在复杂流环境中探索群体智能的能力。

## Abstract
Rendezvous is a critical task for multi-agent systems, requiring agents to coordinate to meet at an unspecified location. However, achieving this in fluid environments presents a challenge, as it remains unclear how agents can exploit underlying fluid kinematics to facilitate convergence. In this study, we adopt a multi-agent reinforcement learning (MARL) approach to develop physics-informed rendezvous strategies in vortical flows. Compared to a naive strategy, where agents navigate toward their counterparts, MARL strategies significantly improve the rendezvous rate. MARL strategies also show transferability across varying vortex intensities, vortex scales, and swarm sizes. By breaking the symmetry of the state-action map, MARL strategy leverages a non-intuitive mechanism that prevents agents from becoming trapped in separate vortices, thereby enhancing rendezvous success. Additionally, a heuristic strategy is extracted from the learned strategy and also outperforms the naive strategy. Furthermore, a theoretical analysis demonstrates that fluid deformation impedes the rendezvous process. Large finite-time Lyapunov exponents identify where fluid effects separate adjacent agents, suggesting that targets should be planned in weak-deformation regions. Our findings reveal the important role that agent-fluid interactions play in multi-agent tasks and highlight the MARL capability to explore swarm intelligence in complex flow environments.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多智能体系统在流体环境中执行“会合”（rendezvous）任务时，智能体如何利用底层流体运动学来促进相互收敛，目前尚不清楚。传统控制策略（如朴素地朝向群体中心导航）在复杂涡流场中效果不佳。
- **研究动机**：多智能体会合在自然界（生物交配、信息共享）和工程应用（水下机器人、飞船对接）中至关重要。许多会合过程发生在流体环境中（如海洋、大气），但流体动力学具有非线性、旋转和变形特征，使得传统策略次优。现有研究很少考虑小尺度流场结构对会合的影响，大多将流场视为扰动。因此，作者提出使用多智能体强化学习（MARL）来挖掘流体信号，探索智能体-流体交互机制，设计物理启发的会合策略。
- **整体含义**：该工作揭示了流体变形是会合的根本障碍，证明了MARL能够通过打破对称性（防止智能体被困在分离涡旋中）显著提升会合成功率，并提供了可迁移的智能策略。这为在复杂流场中实现群体智能（如集群、协作搜索）提供了新思路。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：采用多智能体强化学习（MARL）框架，智能体在稳态Taylor-Green涡流（TGV）中学习导航策略，通过最大化基于总距离减小的即时奖励来优化会合行为。策略以深度神经网络表示，输入包括自身局部流场速度分量（\(u_p, u_q\)）以及与其他智能体的相对位置（距离 \(d\) 和角度 \(\theta\)）。使用Deep Set Proximal Policy Optimization (DS-PPO) 算法，保证对智能体顺序的置换不变性，并支持不同群体规模。
- **智能体动力学**：智能体为点质量，具有固定推进速度 \(v_s\) 和受控转向角速度 \(\omega_s\)（受限 \(\omega_m\)）。运动方程：
  \[
  \frac{d\mathbf{x}}{dt} = \mathbf{u} + \mathbf{v}_s\mathbf{p}, \quad \frac{d\mathbf{p}}{dt} = \omega_s \mathbf{e}_z \times \mathbf{p}
  \]
  其中 \(\mathbf{u}\) 为当地流体速度，\(\mathbf{p}\) 为推进方向单位向量。
- **奖励设计**：每个智能体 i 在时间步 \(m\) 的奖励为 \(R^{(i,m)} = D^{(i,m)} - D^{(i,m+1)}\)，\(D^{(i)}\) 是智能体 i 到其他所有智能体的距离之和。即鼓励每一步减少总距离。
- **网络架构**：输入分为局部状态 \(s_l^{(i)} = \{u_p^{(i)}, u_q^{(i)}\}\) 和共享状态 \(s_s^{(i,j)} = \{d^{(i,j)}, \theta^{(i,j)}\}\)。局部状态经 \(\phi_{\text{local}}\) 网络；每个共享状态经相同 \(\phi_{\text{shared}}\) 网络后取平均池化，再与局部特征拼接，输出动作的均值和标准差（服从高斯分布）。策略由PPO更新。
- **训练细节**：每0.025\(\tau\)（\(\tau = \omega_m^{-1}\)）决策一次，每5\(\tau\)更新网络。学习率从 \(10^{-4}\) 线性衰减至0，训练最多1000个episode（每个episode初始化10个群体，最大时长100\(\tau\)）。

### 3. 实验设计：使用的数据集/场景、基准、对比方法
- **场景/数据集**：使用稳态Taylor-Green涡流（TGV）作为基准流场，包含周期性反向旋转涡旋。参数空间包括：涡旋强度 \(\beta_u = u_0/v_s \in \{0,1,2,3,4,5\}\)，涡旋尺度 \(\beta_l = L_0\omega_m/v_s \in \{5,7,10,14,20,40\}\)，群体规模 \(n \in \{2,3,4\}\)。初始位置均匀随机分布在方形区域 \(\Phi\) 内。
- **基准策略（naive）**：每个智能体始终以最大角速度转向群体几何中心，忽略流场效应（公式(4)）。
- **对比方法**：
  - 多个MARL策略（例如N2U4表示训练于 \(n=2, \beta_u=4\) 的策略）。
  - 从N2U4策略中提取的启发式策略（分段三阶多项式拟合状态-动作映射，见附录C）。
  - 消融实验：将流场信号置零（mask flow signals）以测试其重要性。
- **评价指标**：会合率 \(R_r = N_r / N\)，其中 \(N\) 为随机初始化的群体总数，\(N_r\) 为在时间窗 \(T_t=100\tau\) 内成功会合（任意两智能体距离 < \(d_r\)）的群体数。通过Monte Carlo方法统计。

### 4. 资源与算力
- **论文未明确说明**所使用的GPU型号、数量、训练时长等具体算力资源。仅提到训练过程采用DS-PPO算法，每个case训练不超过1000个episode，每个episode包含10个群体，每个群体最多100\(\tau\)步（每0.025\(\tau\)决策一次）。未提及硬件配置。

### 5. 实验数量与充分性
- **实验数量**：广泛涵盖了参数空间：
  - 不同涡旋强度（\(\beta_u=0\)至5）和涡旋尺度（\(\beta_l=5\)至40）下的两智能体会合实验（图3）。
  - 不同群体规模（\(n=2,3,4\)）以及组合实验（图4）。
  - 转移性测试：训练于特定\(\beta_u\)的策略在其他\(\beta_u\)上的表现（图3c）；训练于特定\(\beta_l\)的策略在其他\(\beta_l\)上的表现（图3b）；训练于特定\(n\)的策略在其他\(n\)上的表现（图4b,c）。
  - 消融实验：遮蔽局部流场信号（\(u_p,u_q\)）测试对性能的影响（图5e）。
  - 机制分析：通过轨迹旋转平均（TRA）和有限时间李雅普诺夫指数（FTLE）进行定量验证（图5h-j, 图6）。
- **充分性**：实验设计较为充分。基准（naive策略）和所提MARL策略在多种参数组合下进行了对比，并测试了跨参数转移能力，验证了鲁棒性。统计分析（Monte Carlo、Gaussian kernel密度估计）增强了结论的可靠性。消融实验明确了状态贡献。实验公平性：训练和测试使用相同或不同参数，报告了相对会合率比值。
- **客观性**：结果以会合率随参数变化的图表展示，并分析了统计误差（如Gaussian-weighted gridded statistics）。未发现明显的偏差。

### 6. 论文的主要结论与发现
- **MARL显著优于naive策略**：MARL策略在所有测试的涡旋强度下均提升会合率，最高提升59%（N2U4在\(\beta_u=4\)时）。
- **策略具备可迁移性**：训练于特定\(\beta_u\)或\(\beta_l\)的策略在未训练环境上仍优于naive；训练于特定群体规模\(n\)的策略也可用于其他\(n\)。
- **机制：打破对称性避免涡旋陷阱**：MARL策略（以N2U4为例）的核心机制是当两个智能体距离较远时，不强求转向对方，而是通过非对称动作使其中一个智能体摆脱所在涡旋，从而避免被分别困在不同涡旋中。该机制通过状态-动作图的奇偶性断裂实现，并且可提取为简约启发式策略。
- **流体变形是会合的根本障碍**：通过FTLE分析，高FTLE区域（对应强流体变形）会指数级拉开相邻智能体，导致会合概率低；会合点倾向于分布在涡旋中心附近（低变形区）。这一认识为规划会合目标提供了指导：应选择弱形变区域。
- **局部流场信息并非必须**：遮蔽\(u_p,u_q\)仅导致会合率下降约4%，说明MARL策略主要依赖相对位置信息，但流场信息仍有一定辅助作用。

### 7. 优点：方法或实验设计上的亮点
- **物理启发的奖励设计**：奖励直接对应总距离减少，简单有效，避免了复杂稀疏奖励。
- **DS-PPO架构**利用Deep Sets实现置换不变性，支持可变群体规模，避免维度灾难，且训练后可直接用于不同\(n\)，具有实用价值。
- **全面而系统的参数扫描**：不仅在同一参数下对比，还测试了跨强度、跨尺度、跨群体规模的迁移性，证明了策略的鲁棒性和泛化能力。
- **从黑盒策略中提取可解释的启发式策略**：通过Gaussian加权网格统计和分段多项式拟合，得到显式的状态-动作函数，有助于工程实施和物理理解。
- **结合LCS理论（eLCS、FTLE）进行机制分析**：用轨迹旋转平均（TRA）量化智能体被涡旋捕获的程度，用FTLE证明流体变形是造成会合失败的关键因素，为实验结果提供了坚实的物理基础。
- **实验统计严谨**：采用Monte Carlo方法和核密度估计，并报告了方差，增强了结果的可信度。

### 8. 不足与局限
- **流场过于理想化**：仅使用稳态Taylor-Green涡流，真实流体环境往往是非定常、湍流且具有更复杂结构（如障碍物、边界）。策略在更真实流场（如海洋环流、大气湍流）中的表现未验证。
- **智能体模型简化**：视为点质量，忽略本体尺寸、体积、阻力以及智能体之间的直接相互作用（如流体力影响、碰撞）。亦未考虑传感器噪声、通信延迟或丢失等实际问题。
- **训练与测试参数范围有限**：\(\beta_u\) 最大为5，\(\beta_l\) 最小为5，更大或更极端的参数未探索。并且仅考虑了固定推进速度 \(v_s\) 和最大转向速率 \(\omega_m\)。
- **未提供具体计算资源**：缺少训练所需的GPU型号、数量、时间等，不利于复现和评估计算成本。
- **转移性不完美**：训练于较小\(n\)的策略用于较大\(n\)时性能下降（见图4b,c下三角优于上三角），说明完全泛化仍需更多研究。
- **局部流场信息作用微弱**：实验发现遮蔽流场信号后性能仅下降4%，表明MARL策略未充分挖掘流场信息，可能存在更优利用流场特征（如梯度、涡度）的策略未被发现。
- **方法局限于同构智能体**：所有智能体共享相同网络和参数，无法处理异构智能体或角色分工场景。
- **大规模集群可扩展性未验证**：论文仅测试了\(n \leq 4\)，对于更大规模（如几十个）智能体，DS-PPO的池化层可能面临信息瓶颈，通信和计算成本未讨论。

（完）
