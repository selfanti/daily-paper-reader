---
title: Governed Shared Memory for Multi-Agent LLM Systems
title_zh: 面向多智能体LLM系统的受控共享内存
authors: "Yanki Margalit, Nurit Cohen-Inger, Erni Avram, Ran Taig, Oded Margalit"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24535v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向多智能体LLM系统的受控共享内存及失效模式分析
tldr: "多智能体LLM系统共享内存存在泄露、过时、矛盾、溯源丢失四种故障模式。本文定义作用域检索、时间超驰、溯源追踪和策略传播原语，实现MemClaw服务。实验表明溯源重建100%正确，跨舰队零泄漏，写延迟优化至单次搜索往返。揭示了生产环境中作用域执行不对称和管道排序冲突等架构问题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1400, \"height\": 1030, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 566, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 856, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 860, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 848, \"height\": 560, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 853, \"height\": 566, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24535v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 839, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24535v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 896, \"height\": 421, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24535v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1843, \"height\": 1299, \"label\": \"Table\"}]"
motivation: 多智能体LLM系统需要共享知识管理，但面临泄露、过时、矛盾和溯源丢失等系统级故障。
method: 定义作用域检索、时间超驰、溯源追踪和策略传播四项系统级原语，并在生产多租户内存服务MemClaw中实现。
result: "100%重建深度四溯源链，跨舰队零泄漏，强写模式下写延迟优化至单次搜索往返。"
conclusion: 长上下文检索不足以支撑生产多智能体内存，需受控共享内存的系统级抽象，且生产环境评估能暴露设计阶段遗漏的问题。
---

## 摘要
多智能体LLM环境需要强大的共享知识管理机制。本文形式化了集群内存问题，并识别了四种基础失效模式：未授权泄露、过时传播、矛盾持续和溯源崩溃。为解决这些问题，我们定义了明确的系统级原语：限定范围检索、时间性取代、溯源追踪和策略驱动的内存传播。这些原语在MemClaw（一个生产级多租户内存服务）中实现，并通过ArgusFleet（一个可复现的测试框架，评估四个治理维度）进行评估。本研究并非基线比较，而是测量真实生产服务，强调实际架构洞察和负面结果。关键评估结果：溯源：成功重建了100%的四层推导链，且每跳延迟低于一秒，作者身份正确。传播：展示了高集群内可见性，且零跨集群泄露。在强写入模式下，写入到可见的延迟优化到一次搜索往返。发现的生产架构问题：非对称范围执行：租户隔离有效，但子租户范围最初在通过直接GET-by-id请求获取智能体作用域凭证时被绕过（研究期间披露并修复）。管道排序冲突：虽然矛盾取代适用于已批准的写入，但同步的近似重复门可能会在异步矛盾检测器评估之前过早拒绝矛盾的写入。结论：仅依靠长上下文检索不足以应对生产级多智能体内存。受控共享内存需要明确的系统级抽象，而实时评估对于揭露仅凭设计处理无法发现的执行和管道排序失败至关重要。

## Abstract
Multi-agent LLM environments require robust mechanisms for shared knowledge management. This paper formalizes the fleet-memory problem and identifies four foundational failure modes: unauthorized leakage, stale propagation, contradiction persistence, and provenance collapse. To address these, we define explicit systems-level primitives: scoped retrieval, temporal supersession, provenance tracking, and policy-governed memory propagation. These primitives are implemented in MemClaw, a production multi-tenant memory service, and evaluated via ArgusFleet, a reproducible harness testing four governance dimensions. Rather than a baseline comparison, this study measures a live production service, emphasizing real-world architectural insights and negative results. Key Evaluation Results Provenance: Successfully reconstructed 100% of depth-four derivation chains with correct writer identity at sub-second per-hop latency. Propagation: Demonstrated high intra-fleet visibility with zero cross-fleet leakage. Under strong write mode, write-to-visible latency was optimized to a single search round-trip. Production Architectural Issues Discovered Asymmetric Scope Enforcement: Tenant isolation held, but sub-tenant scope was initially bypassed on direct GET-by-id requests for agent-scoped credentials (disclosed and remediated during the study). Pipeline Ordering Conflict: While contradiction supersession works for admitted writes, a synchronous near-duplicate gate can prematurely reject contradictory writes before the asynchronous contradiction detector can evaluate them. Conclusion: Long-context retrieval alone is insufficient for production multi-agent memory. Governed shared memory demands explicit systems-level abstractions, and live evaluation is vital to expose enforcement and pipeline-ordering failures missed by design-only treatments.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义

**研究动机与背景**  
- 现代 LLM 系统正从单智能体助手发展为多智能体协作集群。当前大多数内存系统（如 MemGPT、Mem0、Zep 等）基于单智能体抽象（一个用户、一个会话、一条检索流），在多代理共享内存场景下无法满足治理需求。
- 多智能体共享内存不仅是一个检索问题，更是一个分布式系统问题，涉及作用域访问、时间一致性、溯源、同步和策略传播。

**整体含义**  
- 论文主张 AI 内存正在从“上下文窗口问题”演变为“受控分布式内存问题”，需要明确的系统级抽象和原语，而仅靠上下文扩展或语义检索是不够的。

### 2. 方法论

**核心思想**  
- 形式化集群内存系统为 \(F = (A, M, G, P, T)\)，分别代表代理集合、共享内存基板、治理策略层、溯源元数据、时间排序与取代语义。
- 定义了四种失效模式：未授权泄露、过时传播、矛盾持续、溯源崩溃。

**关键技术原语**  
- **作用域检索**：代理只能看到其被授权的作用域内的内存。定义四种作用域：Agent-local、Team-shared、Tenant-global、Restricted（策略约束）。
- **时间取代**：内存视为演化状态而非不可变日志。矛盾写入通过时间顺序和策略进行解决（超驰/标记过期）。
- **溯源追踪**：每个内存对象记录作者身份、源系统、推导历史、修改谱系。实现可审计性和调试能力。
- **策略治理传播**：内存传播受信任级别控制（例如 `trust_level ≥ 3` 才能执行重新分发）。

**算法流程（文字描述）**  
- 写入操作：\(w_i = (a_i, c_i, s_i, t_i, p_i)\)，其中包含作者、内容、作用域、时间戳、溯源元数据。
- 检索操作：\(r_j = f(q_j, a_j, g_j, t_j)\)，包含查询、请求代理、治理约束、时间条件。并非仅语义相似度，还需通过授权、新鲜度、矛盾解决、溯源验证等步骤。
- 检索流水线：①语义候选生成 → ②策略过滤 → ③时间解决 → ④溯源增强 → ⑤排序交付。

**实现**  
- **MemClaw**：生产级多租户内存服务，REST API（写入、搜索、按ID获取、实体 upsert、重新分发等）。支持多种写入模式（fast/strong/auto/stm）。
- **ArgusFleet**：开源评估工具，包含四个实验（Leakage, Contradiction, Provenance, Propagation），可针对 MemClaw 或任何兼容服务运行。生成 JSONL 痕迹文件，所有数据可复现。

### 3. 实验设计

**使用场景/数据集**  
- 所有数据由 ArgusFleet 自动生成（如含独特 canary 令牌的秘密、操作声明、RDF 三元组、推导链等），非真实数据集。
- 在单一租户下的 MemClaw 生产服务（memclaw.net）上进行测量。

**基准与对比方法**  
- **无外部基线对比**：论文明确说明这不是与基线方法的比较，而是对自己生产服务的测量。作者认为其贡献在于给出真实系统的测量结果和架构洞察。

**实验细节**  
- **Leakage 实验**：4 个舰队，每舰队 6 个代理。写入 2 个秘密（舰队级、代理级），所有代理进行搜索探测。192 个探针：164 个预期拒绝，28 个预期允许。额外进行聚焦的代理凭证探测（96 个 GET-by-id 探针）。
- **Contradiction 实验**：200 个事实对（每个事实对在一个主题-谓词模板上先后或并发写入矛盾值）。测量检测率和过时读取率。
- **Provenance 实验**：50 条深度为 4 的推导链（共 200 次写入）。测量完整度、准确度、深度保真度。
- **Propagation 实验**：40 个舰队级事实，120 个舰队内探针 + 80 个跨舰队探针（检测泄漏）。另外 8 个事实用于紧密轮询测量时间到可见性窗口。

### 4. 资源与算力

- **文中未明确指出** GPU 型号、数量或训练时长。
- 实验针对的是 MemClaw 的生产 REST API 服务，未提及底层硬件。ArgusFleet 客户端在普通机器上运行（Python 3.12，httpx 库），硬件需求很低。
- 局限性中提及需注意该实验未涉及训练或微调，仅涉及推理和存储操作。

### 5. 实验数量与充分性

**数量**  
- 每个实验的探针/写入总量约 200 次（例：Leakage 192 探针，Provenance 200 写入 × 50 链，Propagation 200 探针，Contradiction 400 写入 → 200 事实对）。
- 矛盾实验额外进行了 4 个独立 N=200 运行以验证稳定性。
- 溯源和传播实验有单次运行，但重复性通过种子保证。

**充分性评估**  
- **充分方面**：覆盖了四种失效模式；使用置信区间（Wilson Score）统计评估；所有超时和失败均有记录；修复后重新探针验证。
- **不足方面**：仅单租户；未测跨租户泄漏；未与替代系统比较（缺乏生态外部基准）；矛盾实验的条件依赖于服务器端谓词的单值性限制；传播时间窗口仅 8 个事实、250ms 粒度，代表性有限。作者在局限性中诚实讨论了这些问题。

**公平性与客观性**  
- 作者自评服务，存在固有偏差，但通过公开负面结果（如 GET 路径缺陷、去重-矛盾管道冲突）和提供完整痕迹文件来缓解。缺少独立第三方验证。

### 6. 主要结论与发现

- **溯源链**：50 条深度为 4 的链完全重建，每跳 p50=291ms，作者身份 100% 正确。
- **传播正确性**：舰队内可见率 117/120=0.975（95% CI [0.929,0.991]），零跨舰队泄漏（0/80，95% CI [0.000,0.046]）。
- **强写模式一致性**：一致性成本在前台写入时支付，写入到可见窗口约为一次搜索往返时间（p50 ≈ 0.83s），远小于早期批量化测量的数十秒（早期测量是调度伪像）。
- **关键架构问题**：
  - **作用域执行不对称（已修复）**：租户隔离有效，但直接 GET-by-id 路径未检查子租户作用域（舰队/代理），导致信任级别 1 的代理能跨舰队读取。已在研究期间修复，修复后探针确认正确执行（跨舰队拒绝 0/36）。
  - **去重与矛盾检测管道排序冲突**：同步近似内容去重（409 拒绝）在异步矛盾检测器运行之前拒绝矛盾写入。当两个写入内容接近（如“X is A” vs “X is B”）时，矛盾检测器无法运行。条件概率下，当两个写入都通过时，取代成功率为 100%（90/90）。
- **总结性主张**：长上下文检索不足以应对生产多智能体内存；受控共享内存需系统级原语，且真实服务测量能暴露设计阶段遗漏的执行和管道排序失败。

### 7. 优点

1. **问题形式化清晰**：给出集群内存系统数学模型 \(F=(A,M,G,P,T)\)，明确定义写入/检索操作及其治理要求。
2. **四类失效模式全面**：覆盖多代理共享内存的典型故障场景，有助于指导系统设计。
3. **生产级实现与开放性**：MemClaw 作为实际服务运行，ArgusFleet 开源发布，痕迹文件已提交，方便他人复现与验证。
4. **诚实报告负面结果**：主动披露自身系统中的安全缺陷和管道问题，并展示修复过程，增加可信度。
5. **分离时间窗口测量与批量化调度伪像**：通过紧密轮询+低负载设计正确得到了强写模式的一致性窗口。
6. **注重可复现性**：所有实验种子、痕迹文件、代码均公开，结果可重新生成。

### 8. 不足与局限

1. **自评价偏差**：作者同时开发了 MemClaw 和 ArgusFleet，缺乏独立第三方审计。虽通过公开负面结果缓解，但仍有风险。
2. **缺少外部基准对比**：未与零基线（如无治理的纯检索）或其他内存系统（如 Letta、Mem0 等）进行比较，因此“长上下文不够”的主张尚缺定量对比。
3. **实验环境局限**：所有测量在单一租户内进行，跨租户隔离未实际测试；矛盾实验依赖服务器端谓词单值性配置；时间窗口测量仅 8 个事实和 250ms 粒度，泛化性有限。
4. **可扩展性测试不足**：写入延迟在高负载下出现长尾（p99 达 19s），但未对不同写入模式（fast、stm）进行横向对比，也未进行大规模代理集群测试。
5. **矛盾检测的条件假设**：仅针对 RDF 三元组和服务器允许单值的谓词有效。如果谓词设计为多值，结构检测器不会触发。
6. **Agent-scoped 探针仅聚焦**：原始泄漏探测主要使用 tenant 级密钥，子租户绕过结论来自小规模探针（虽然后续修复验证规模扩大至 96 个探针），完整性仍可加强。
7. **未讨论成本与性能权衡**：强写模式提供强一致性但写入延迟高，未提供其他写入模式的对比数据来判断实际场景下的选择。

（完）
