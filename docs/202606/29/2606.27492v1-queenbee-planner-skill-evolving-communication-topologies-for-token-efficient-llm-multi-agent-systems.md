---
title: "QueenBee Planner: Skill-Evolving Communication Topologies for Token-Efficient LLM Multi-Agent Systems"
title_zh: QueenBee Planner：面向Token高效LLM多智能体系统的技能演化通信拓扑
authors: "Congjia Tian, Yuhang Yao, Jiaming Cui"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27492v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: LLM多智能体系统的自进化通信拓扑
tldr: LLM多智能体系统的通信拓扑设计通常依赖人工且难以优化。QueenBee Planner将通信拓扑视为可自进化的设计技能，冻结工人代理，由外层LLM planner生成时序通信DAG，并通过执行轨迹提炼证据支持的设计规则，结合多重验证门控防止退化。在Count-Frequency和Silo-Bench任务中，自进化生成的图不仅将RMSE从12.53降至7.87，还减少了消息、模型调用和token成本。该方法表明多智能体系统可通过学习可复用的架构设计知识实现高效协同，而非仅记忆任务答案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27492v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1379, \"height\": 741, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27492v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1353, \"height\": 1197, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27492v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 564, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27492v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27492v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27492v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1339, \"height\": 233, \"label\": \"Table\"}]"
motivation: 现有LLM多智能体系统依赖固定或人工设计的通信拓扑，缺乏自适应性和泛化能力。
method: 冻结工人代理，由外层LLM planner生成时序通信DAG，通过执行轨迹提炼Preserve、Modify、Avoid三类设计规则，并采用接受门控、方差感知等机制防止自我进化退化。
result: 在Count-Frequency任务中，最优生成图将RMSE从12.53降至7.87，同时消息数、模型调用和token成本分别减少。
conclusion: 多智能体系统可通过自进化学习可复用的通信架构设计知识，提升效率与性能。
---

## 摘要
大型语言模型（LLM）多智能体系统不仅依赖于单个智能体的推理能力，还越来越依赖于智能体之间的连接方式。本文介绍了QueenBee Planner框架，该框架将智能体间的通信拓扑视为一种可检索且自我改进的设计技能。其中，工作者智能体池、任务适配器和评分函数被冻结；只有外部LLM规划器学习生成时间通信有向无环图（DAG），指定谁在哪个轮次向谁发送信息、谁合并消息以及谁输出最终答案。执行轨迹被提炼为证据支持的设计规则，包含三个动作：保留、修改和避免。为了防止自我演化将偶然的成功或看似合理但错误的解释固化为策略，QueenBee使用了保留集接受门、方差感知信用、基序级归因、迁移信任、洞察证伪和结构去重。我们在计数-频率聚合任务和类似Silo-Bench的分布式协调任务上评估了该方法。在固定工作者智能体的情况下，自我演化的图生成产生了优于固定拓扑和冷启动生成的通信结构。在CF完整测试设置中，最佳生成图将均方根误差从最强固定拓扑的12.53降至7.87，同时减少了消息数量、模型调用次数和Token成本；类似Silo的结果显示出相对于冷启动和固定拓扑基线的相同改进趋势。这些结果表明，多智能体系统可以学习可重用的架构设计知识，而不仅仅是记忆任务答案。

## Abstract
Large language model (LLM) multi-agent systems increasingly depend not only on how individual agents reason, but also on how agents are connected. This paper introduces QueenBee Planner, a framework that treats inter-agent communication topology as a retrievable and self-improving design skill. A pool of worker agents, the task adapter, and the scoring function are frozen; only an outer LLM planner learns to generate temporal communication DAGs specifying who sends information to whom, in which round, who merges messages, and who emits the final answer. Execution traces are distilled into evidence-backed design rules with three actions: \emph{Preserve}, \emph{Modify}, and \emph{Avoid}. To prevent self-evolution from turning lucky runs or plausible but false explanations into policy, QueenBee uses held-out acceptance gates, variance-aware credit, motif-level attribution, transfer trust, insight falsification, and structural deduplication. We evaluate the method on Count-Frequency aggregation and Silo-Bench-style distributed coordination tasks. With fixed workers, self-evolved graph generation produces communication structures that improve over fixed topologies and cold generation. In the CF fulltest setting, the best generated graph reduces RMSE from 12.53 for the strongest fixed topology to 7.87 while also reducing messages, model calls, and token cost; Silo-style results show the same direction of improvement over cold and fixed-topology baselines. These results suggest that multi-agent systems can learn reusable architectural design knowledge rather than merely memorizing task answers.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLM）多智能体系统的性能不仅取决于单个智能体的推理能力，更关键地取决于智能体之间的通信拓扑结构（谁与谁通信、何时通信）。现有工作要么依赖人工设计的固定拓扑，要么通过优化器为单个任务寻找拓扑，但缺乏跨任务的积累与复用能力。本文的核心问题是：如何让多智能体系统**自主学会设计通信拓扑**，并随着经验积累不断优化，而非仅仅记忆任务答案。

- **整体含义**：作者将“智能体如何连接”这一架构决策视为一种可学习、可演化的设计技能。通过将通信知识压缩为证据支持的设计规则（保留/修改/避免），系统能产生更优通信图，在降低错误率和成本的同时，实现架构设计层面的自我进化。

## 2. 方法论

### 2.1 核心思想

- **冻结工人智能体（Worker Agents）**：所有工人智能体的模型、角色提示、解析规则和评分函数固定不变，不参与学习。
- **外层LLM规划器（QueenBee Planner）**：负责为每个任务生成一个**时序通信有向无环图（Temporal Communication DAG）**，指定每一轮谁向谁发送信息、如何合并消息、谁输出最终答案。
- **技能库（Skill Bank）**：将执行轨迹提炼为三类设计规则——**Preserve**（保留已验证的结构）、**Modify**（修改以适应新条件）、**Avoid**（避免已知失败模式），并作为未来图生成的先验记忆。
- **门控机制**：为防止自我进化产生噪声或过度拟合，采用保留集接受门、方差感知信用、基序级归因、迁移信任、洞察证伪和结构去重等约束，确保仅可靠的改进进入技能库。

### 2.2 关键技术细节

- **时序DAG定义**：\( G = (N, T, E, h, I, R) \)，其中 \(N\) 为工人数，\(T\) 为通信轮数，\(E \subseteq [N]\times[N]\times[T]\) 为有向时序边，\(h\) 为最终答案持有者，\(I\) 为接收方指令，\(R\) 为合并规则。边同步执行：同一轮中消息基于上一轮的状态传递。
- **生成与执行流程**：
  1. 规划器从技能库检索相关规则，生成候选DAG（受可行性约束：最大轮数、最大消息数、单节点扇入上限）。
  2. 工人按DAG执行：初始化→每轮发送消息→接收方用 \(R\) 合并→更新信念→最终从 \(h\) 输出。
  3. 执行后记录损失（RMSE/确匹配）、通信成本（消息数、模型调用、Token）作为证据。
- **技能演化**：
  - 从证据蒸馏出候选技能更新（\(\Delta S\)），必须通过**保留集生成门**：只有使规划器在保留集上的损失降低超过阈值 \(\epsilon\) 的更新才能加入技能库。
  - 使用**基序级信用分配**：将图分解为8种结构基序（如步数、扇入桶、是否存在审计边等），按基序的历史表现打分，避免因单个完整图偶然成功而误导。
  - **方差感知信用**：对高方差、低样本的基序附加惩罚项，防止单次幸运运行主导。
  - **洞察证伪**：LLM提出的自然语言洞察必须先通过保留集证伪检查。
  - **结构去重**：通过拓扑等价哈希合并同构DAG，确保信用不分裂。
- **自进化循环**（算法2）：每轮在训练任务上生成并执行DAG，收集证据，蒸馏技能更新，验证后（通过保留集门）加入技能库，再进入下一轮。规划器的生成策略随技能库演化而改善。

## 3. 实验设计

### 3.1 数据集/场景

1. **Count-Frequency（CF）聚合任务**：一个全局数组（长度1024，值0-9）被分割为8个私有分片，每个工人只能看到自己的分片。系统需输出全局频率向量。答案精确可知，可直接测量RMSE、确切匹配率、消息数、模型调用次数和Token成本。*目的：隔离通信拓扑对性能的影响。*
2. **Silo-Bench风格分布式协调任务**：包含6个保留集案例（来自3个难度桶），每个案例用4种种子评估。*目的：测试技能迁移到更多样任务的能力。*

### 3.2 基准（Benchmark）

- **固定拓扑**：如树、网状星形（mesh-star）、单对指数DAG星形（one-peer exponential DAG star）等人工设计结构。
- **冷启动自由DAG生成**：无长期记忆的规划器直接生成图。
- **逐案例Oracle固定拓扑**：对于每个任务/种子，允许后见之明选择最优固定拓扑（仅Silo任务）。
- **自我演化图生成**：使用演化后的技能库生成图。

### 3.3 对比方法

- 在CF任务中：对比了多种固定拓扑与演化生成的最佳图（表1）。使用多个不同的工人模型（DeepSeek-V3/V4-Flash、Qwen3.5-Flash/Plus、GPT-4o、MiMo-V2-Flash等）进行配对比较（表2）。
- 在Silo任务中：对比冷生成、最佳单一固定拓扑、逐案例Oracle固定拓扑和演化图生成（表3）。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。提到的工人模型包括DeepSeek-V4-Flash、GPT-4o、Qwen等，可能通过API调用或本地推理实现。规划器也是LLM（例如DeepSeek-V4-Pro），同样依赖LLM调用。文中未提及具体的硬件配置或算力预算。

## 5. 实验数量与充分性

- **CF任务**：在多个模型设置、多种阵列大小（1024/2048）、不同种子（101-104）上进行了实验。表2展示了9种模型/设置的配对结果。演化进行了5轮（见图3左）。实验数量较充分，但缺乏消融研究（如基序级信用、各个门控的作用未被单独剥离评估）。
- **Silo任务**：6个保留案例×4种子 = 24个实例，演化3轮（最终报告0.539 EM）。论文指出“当前三轮结果”，最终版本应补充更多独立运行。
- **总体评价**：实验覆盖了核心任务和多个工人模型，验证了方法的有效性。但消融实验不足，尤其缺乏对各个门控机制贡献的定量分析；Silo任务实验规模较小，且论文提到“最终版本将补充均值和方差”，暗示当前结果可能不完整。
- **公平性**：固定拓扑和冷生成基线在同一设置下公平对比；演化改进不考虑参数或工人改变，因果关系清晰。

## 6. 主要结论与发现

- **结论**：LLM多智能体系统的通信拓扑可以被当作一种可学习的、可转移的设计技能。通过自进化，规划器能够生成更稀疏、更可靠、成本更低的通信结构，且改进主要来源于架构知识积累而非工人能力提升。
- **关键数字**：
  - CF任务（DeepSeek-V4-Flash全测试）：最强固定拓扑RMSE=12.53，演化最佳图RMSE=7.87（下降37.2%），同时消息数减少74.2%、模型调用减少54.5%、Token成本减少59.6%。
  - 多个模型上均观察到改进（GPT-4o提升56.1% RMSE）。
  - Silo任务：演化三代后EM=0.539，超过最佳固定拓扑（0.333）和Oracle固定拓扑（0.417）。
  - 演化轨迹显示，虽然单轮可能存在探索波动，但保留的最佳存档持续下降，技能库增长与性能提升一致。

## 7. 优点

- **新颖视角**：将“通信拓扑设计”本身作为学习对象，而非仅优化解答过程。
- **严格的自我进化控制**：多重门控（保留集验证、方差惩罚、基序级信用、洞察证伪）有效防止伪改进污染技能库。
- **Token高效**：演化产生的图比固定拓扑更稀疏，大幅节省通信和模型调用成本。
- **泛化能力**：在Silo任务上表现优于固定拓扑族的上界（Oracle固定拓扑），表明技能不是简单选择拓扑名称，而是学习到了任务条件化的结构偏好。
- **方法论完整**：从定义、算法到执行细节清晰，包含伪代码和附录技能卡片示例。

## 8. 不足与局限

- **消融实验缺失**：未单独评估各个门控机制（如保留集门、方差信用、基序信用）的必要性及贡献。
- **Silo任务实验规模偏小**：仅24个实例、3轮演化，统计显著性可能不足。论文本身指出“当前结果，最终版本将补充均值和方差”。
- **基序设计简单**：仅用8个手工特征表示结构，可能无法捕捉复杂拓扑的动态属性。
- **可扩展性未知**：N=8工人，演化为5轮，未测试更大规模智能体或更长演化周期。
- **仅评估聚合/协调任务**：未涉及开放式对话、代码生成等更复杂场景，结论的普适性有限。
- **资源开销未报告**：规划器自身也需要LLM调用（生成图、蒸馏规则、验证），这部分成本未与直接手工设计或搜索方法对比。
- **潜在偏差**：验证集的选择和任务分桶方式可能影响结果；LLM作为规划器和规则分析师可能存在自我偏差。

（完）
