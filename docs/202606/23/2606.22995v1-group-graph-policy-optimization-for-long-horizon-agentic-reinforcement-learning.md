---
title: Group-Graph Policy Optimization for Long-Horizon Agentic Reinforcement Learning
title_zh: 面向长时程智能体强化学习的组图策略优化
authors: "Yunan Wang, Minghui Song, Zihan Zhang, Shaohan Huang, Haizhen Huang, Furu Wei, Weiwei Deng, Feng Sun, Qi Zhang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22995v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向智能体强化学习的组图策略优化
tldr: "长程智能体强化学习面临奖励稀疏与延迟，现有方法将轨迹视为线性序列，导致状态值估计高方差和信用分配短视。本文提出组图策略优化（G2PO），将交互轨迹转化为全局状态转移图，通过聚合相同观测节点降低方差，并基于边的重要性进行优势估计。在WebShop、ALFWorld和AppWorld基准上，G2PO相比GRPO等基线成功率提升最多22.2%。该方法为长程任务信用分配提供了更精准的图结构视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22995v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 689, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1435, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 393, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1447, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 555, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 699, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 728, \"height\": 322, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22995v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22995v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1442, \"height\": 832, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22995v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 590, \"height\": 323, \"label\": \"Table\"}]"
motivation: 长程智能体强化学习面临严重的奖励稀疏和延迟，现有步级训练方法仍将状态转移视为线性轨迹，导致信用分配粗粒度、高方差。
method: 提出G2PO，将线性轨迹转换为全局状态转移图，通过组聚合状态值估计降低方差，并基于边中心优势估计优先关键转移。
result: "在WebShop、ALFWorld和AppWorld三个长程基准上，G2PO成功率相比最优基线提升最多22.2%。"
conclusion: G2PO通过图结构视角有效缓解了长程任务中信用分配的高方差和短视问题，显著提升了性能。
---

## 摘要
基于组的强化学习显著增强了智能体场景中的大语言模型。为了实现更细粒度的策略更新，最近的智能体强化学习框架已从轨迹级训练转向步骤级训练。然而，长时程智能体强化学习面临严重的奖励稀疏性和延迟问题，因为反馈通常延迟数十个交互步骤。虽然现有的步骤级框架细化了训练粒度，但其信用分配仍然粗粒度，且仍将智能体探索视为孤立的线性轨迹。这种过于简化的视角忽略了状态转移的固有图结构，导致高方差的状态价值估计和短视的局部信用分配。为了克服这些关键瓶颈，我们提出了组图策略优化（G2PO），这是一种专为多轮智能体任务设计的新型基于组的强化学习算法。G2PO将线性交互轨迹显式转换为全局状态转移图。通过聚合不同轨迹中的相同观测，我们引入了组聚合状态价值估计，以减少采样方差和轨迹依赖偏差。此外，我们将智能体动作重新定义为状态节点之间的转移，并提出了以边为中心的优势估计策略。通过全局标准化整个图中的时间差分误差，G2PO显式识别并优先考虑驱动绝对任务进展的关键转移。在代表性的长时程基准测试（WebShop、ALFWorld和AppWorld）上的大量实验表明，G2PO显著优于最先进的基于提示和强化学习的基线方法，成功率比GRPO最高提升22.2%。

## Abstract
Group-based Reinforcement Learning (RL) has significantly enhanced Large Language Models (LLMs) in agentic scenarios. To achieve finer-grained policy updates, recent agentic RL frameworks have shifted from trajectory-level to step-level training. However, long-horizon agentic RL suffers from severe reward sparsity and delay, as feedback is often deferred for dozens of interaction steps. While existing step-level frameworks refine training granularity, their credit assignment remains coarse-grained and still treats agent exploration as isolated, linear trajectories. This oversimplified perspective ignores the inherent graph structure of state transitions, leading to high-variance state-value estimation and myopic, localized credit assignment. To overcome these critical bottlenecks, we propose Group-Graph Policy Optimization (G2PO), a novel group-based RL algorithm tailored for multi-turn agentic tasks. G2PO explicitly transforms linear interaction trajectories into a global state-transition graph. By aggregating identical observations across different trajectories, we introduce group-aggregation state-value estimation that reduces sampling variance and trajectory-dependent bias. Furthermore, we redefine agent actions as transitions between state nodes and propose an edge-centric advantage estimation strategy. By globally standardizing Temporal Difference (TD) errors across the entire graph, G2PO explicitly identifies and prioritizes critical transitions that drive absolute task progress. Extensive experiments on representative long-horizon benchmarks-WebShop, ALFWorld, and AppWorld-demonstrate that G2PO substantially outperforms state-of-the-art prompt-based and RL baselines, achieving remarkable success rate improvements of up to 22.2% over GRPO.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
长时程（Long-Horizon）智能体强化学习（RL）面临严重的**奖励稀疏性与延迟**问题：智能体需与环境交互数十步才能获得最终奖励，中间步骤无即时反馈。现有方法已从轨迹级训练（长序列、高内存）转向步骤级训练（以单步观测-动作为样本），但仍**将智能体探索视为孤立、线性轨迹**，忽略了不同轨迹中常出现相同或相似环境状态（如相同网页或物理配置）这一事实。这种线性视角导致两个关键瓶颈：
- **高方差的状态价值估计**：同一状态在不同轨迹中可能导向不同最终结果，基于单条轨迹的估计极不稳定。
- **局部信用分配短视**：现有方法（如GiGPO）仅在相同本地状态内比较动作，无法衡量动作的**绝对全局重要性**（如关键突破性步骤 vs 无关紧要的改进）。

论文提出**组图策略优化（G2PO）**，将线性轨迹显式构建为**全局状态转移图**，利用图结构实现更精确、低方差的信用分配。

## 2. 提出的方法论
### 核心思想
将采样的多条轨迹重构为一个**状态组图（State Group Graph）**：具有相同或相似观测的步骤被聚为同一“状态组”作为节点，智能体动作作为连接节点的有向边。在该图基础上，引入两种新设计：
- **组聚合状态价值估计**：状态组的值定义为组内所有步骤折扣收益的平均值（而非单条轨迹结果），大幅降低方差。
- **边中心优势估计**：动作的优势由从源节点到目标节点的**值增量（1步TD误差）** 衡量，并在**全局范围内**标准化，从而显式识别并优先奖励驱动绝对任务进展的关键转移。

### 关键技术与公式
1. **状态组构建**：对所有轨迹中的观测`o_ij`进行聚类，相同观测归入同一组`G_k`。状态图节点为`G_k`，边为`(G_s, a, G_t)`。
2. **组聚合状态值**：  
   `v_ij = γ^(T-j+1) * R_i`（折扣收益），  
   `V(G_k) = (1/|G_k|) * Σ v_ij`（组内平均）。
3. **边中心优势**：  
   `δ_ij = V(G_k') - V(G_k)`（值增量），  
   `A_EC(a_ij) = (δ_ij - mean(全体δ))/std(全体δ)`（全局标准化）。
4. **节点中心优势**：在局部组内标准化`A_NC`。
5. **回合级优势**：类似GRPO的全局奖励标准化`A_EP`。
6. **最终优势**：`A_ij = A_EP + w·(A_NC + A_EC)`。
7. **目标函数**：使用裁剪代理目标 + KL散度惩罚（同GRPO）。

**算法流程**：采样轨迹→构成状态组图→计算组聚合状态值→计算多粒度优势→更新策略。

## 3. 实验设计
- **数据集/场景**：三个代表性长时程智能体基准：
  - **WebShop**：电商网站模拟，需搜索、浏览、选择商品属性。
  - **ALFWorld**：具身家庭任务（6类：Pick、Look、Clean、Heat、Cool、Pick2）。
  - **AppWorld**：通过Python API调用操作多个App的软件生态系统。
- **基准方法**：
  - **前沿闭源模型**：Gemini-2.5-Pro、DeepSeek-V3.2、Qwen3.5-397B。
  - **提示方法**：ReAct、Reflexion。
  - **RL训练方法**：PPO（含critic）、RLOO、GRPO、GiGPO。
- **基础模型**：Qwen2.5-1.5B/7B-Instruct（主实验），Qwen2.5-14B-Instruct（AppWorld）。

## 4. 资源与算力
- **硬件**：ALFWorld和WebShop使用**4×H100 GPU**（100次迭代），AppWorld使用**8×H100 GPU**（50次迭代）。
- **环境配置**：所有组RL方法使用128个环境（16组×8个），PPO使用128个独立环境。
- **时间开销**：G2PO在优势计算阶段**仅增加约1秒/步**，占整个训练步的0.4%，且完全运行于CPU，几乎无额外GPU成本。

## 5. 实验数量与充分性
- **主实验**：表1（WebShop和ALFWorld）报告了1.5B和7B模型在6个子任务和总体指标上的成功率/分数，每种条件平均3个随机种子（含标准差）。表2报告AppWorld成功率/分数。
- **消融实验**（图3）：从`A_EP`基线逐步加入`A_NC`、组聚合（GA）、`A_EC`，验证每个组件贡献。
- **超参数分析**（图4a）：分析权重`w`的影响（0.6~1.2），确定最优值0.8。
- **方差分析**（图4b）：展示状态值估计的方差分布，证明组聚合必要性。
- **效率分析**（图4c）：对比交互步数，G2PO比GRPO/GiGPO更高效。
- **计算预算**（图5）：时间消耗分解。
- **组大小分析**（图6-7）：展示训练过程中组大小动态和分布。
- **实验充分性**：覆盖多种模型尺寸、多种任务、多种基线；消融和超参数分析完整；对比基于相同环境和超参数，设置公平。但未报告多次重复实验的统计显著性。

## 6. 主要结论与发现
- G2PO在**所有基准和模型规模上一致优于所有RL基线**，尤其相对GRPO：  
  - WebShop成功率提升**最多22.2%**（1.5B）。  
  - ALFWorld整体成功率提升**14.4%**（1.5B），7B提升12.2%。  
  - AppWorld也取得最佳成绩。
- 组聚合机制有效降低了状态值估计方差（图4b证实大量状态存在非零方差）。
- 边中心优势提供了全局视角，优于仅局部比较的GiGPO。
- 训练后模型**交互步数更少**，推理成本更低。
- 几乎所有步骤（WebShop 88.1%，ALFWorld 91.9%）属于尺寸≥2的组，因此大多数步骤都能受益于方差降低。

## 7. 优点
- **创新性强**：将轨迹提升为图结构，利用状态重复特性进行聚合，是信用分配的新视角。
- **实用高效**：几乎不增加计算开销（CPU操作，<0.5%总时间），适合大规模RL训练。
- **泛化性好**：在三个差异较大的长程任务（电商、具身、软件API）上均有效，且适用于不同模型尺寸。
- **理论支撑**：附录B提供方差降低的严格证明（B.1价值估计方差线性降低；B.2优势估计方差上界优于轨迹级）。
- **分析充分**：除性能外，还分析了方差、效率、组件贡献、超参数敏感性。

## 8. 不足与局限
- **框架依赖性**：论文仅基于verl-agent框架实现，未在其他RL框架（如RL4LMs、TRL等）验证，通用性待确认。
- **rollout设置局限性**：仅采用独立同分布采样，未探索树搜索采样或异步采样等更复杂设置。
- **状态分组条件**：依赖观测完全相同进行分组；对于语义相似但字符串不同（如自然语言变体）的状态，无法自动聚合，可能限制应用。
- **实验覆盖**：虽有三个基准，但均属于交互式任务，未在纯推理类（如数学、代码）任务上验证（但论文聚焦智能体RL，合理）。另外，仅使用相同家族模型（Qwen2.5），未在其他LLM（如Llama）上验证。
- **可解释性**：尽管提供了案例分析，但对图构建的具体聚类方法（字符串精确匹配？）未详细展开，可能影响复现。
- **超参数敏感性**：`w`需要调优（图4a），不同任务可能需调整，增加使用成本。

（完）
