---
title: Distilling Collaborative Dynamics into Latent Space for Implicit Coordination in Decentralized Multi-Agent Manipulation
title_zh: 将协作动态蒸馏到潜在空间以实现分散式多智能体操作中的隐式协调
authors: "Chanyoung Park, Minsung Yoon, Andrew Jeong, Sung-eui Yoon"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22982v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 去中心化多智能体隐式协调方法
tldr: "集中式多臂操作在团队规模增大时扩展性差。本文提出CLS-DP，在集中训练去中心执行（CTDE）范式下，将多智能体协作动态蒸馏到潜空间，各智能体从局部RGB观测和共享任务指令中推断协作潜变量，并以此调节扩散去噪过程，实现无需全局视角或通信的隐式协调。在涉及2-4个智能体的RoboFactory任务上，CLS-DP平均成功率38%，远超最佳集中式基线（20%）和无协作潜变量的消融（9%），且参数效率更优。归因分析表明，协作潜变量使智能体在规划中同时关注自身与队友的关节和夹爪，有效编码了部分可观测下的协作动态。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22982v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1816, \"height\": 771, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22982v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1825, \"height\": 835, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22982v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 895, \"height\": 788, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22982v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1807, \"height\": 722, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22982v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 898, \"height\": 398, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22982v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 366, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22982v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1833, \"height\": 586, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22982v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22982v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 862, \"height\": 701, \"label\": \"Table\"}]"
motivation: 集中式多臂操作随团队规模扩展性差，且部分可观测下缺乏无需通信的隐式协调机制。
method: 在CTDE下将特权多智能体动态蒸馏到潜空间，各智能体从局部RGB和任务指令推断协作潜变量，并调节扩散去噪过程以隐式协调运动。
result: "在2-4智能体RoboFactory任务上，CLS-DP平均成功率38%，优于集中式基线20%和无协作潜变量消融9%，参数效率领先。"
conclusion: 协作潜变量从局部观测编码多智能体动态，使智能体关注队友状态，实现部分可观测下的高效隐式协调。
---

## 摘要
多臂操作需要精确的时空协调，然而许多集中式方法随着团队规模扩大而扩展性变差。为此，我们提出CLS-DP，一种分散式多智能体框架，能够在部分可观测性下实现隐式协调，无需共享全局视图、显式状态信息或智能体间通信。在集中训练与分散执行（CTDE）范式下，CLS-DP将特权多智能体动态蒸馏到潜在空间中。部署时，每个智能体从其局部RGB观测和共享任务指令中推断出协作潜在向量；然后在该潜在向量上调节扩散去噪过程。这种设计实现了隐式协调，每个智能体的成本与团队规模无关。在跨越两到四个智能体的六个RoboFactory基准任务中，CLS-DP达到了38%的平均成功率，优于最佳集中式基线（20%）和没有协作潜在向量的分散式消融实验（9%）。它在所有智能体配置下也保持了优越的参数效率。注意力图显示，基于协作潜在向量调节的智能体在执行过程中，将高注意力放在自身及其队友的关节和夹爪上。这表明学习到的潜在向量有效地从局部观测中编码了协作动态，从而便于在部分可观测性的现实环境中实现隐式协调。

## Abstract
Multi-arm manipulation demands precise spatiotemporal coordination, yet many centralized approaches scale poorly as team size increases. To address this, we propose CLS-DP, a decentralized multi-agent framework that enables implicit coordination under partial observability without shared global views, explicit state information, or inter-agent communication. Under the centralized training and decentralized execution (CTDE) paradigm, CLS-DP distills privileged multi-agent dynamics into a latent space. At deployment, each agent infers a collaborative latent from its local RGB observation and a shared task instruction; it then conditions the diffusion denoising process on this latent. This design enables implicit coordination with a per-agent cost independent of team size. Across six RoboFactory benchmark tasks spanning two to four agents, CLS-DP achieves a 38% mean success rate, outperforming the best centralized baseline (20%) and a decentralized ablation without the collaborative latent (9%). It also maintains superior parameter efficiency across all agent configurations. Attribution maps show that an agent conditioned on the collaborative latent places high attribution on the joints and grippers of both itself and its teammates throughout execution. This suggests that the learned latent efficiently encodes collaborative dynamics from local observation, which facilitates implicit coordination in realistic settings characterized by partial observability.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多臂机器人操作任务（如协同搬运、对齐、按压等）需要精确的时空协调。然而，传统的**集中式方法**将所有智能体的观测和动作聚合到一个统一策略中，导致随着团队规模扩大，联合观测-动作空间急剧增长、同步复杂性提高、对单点故障敏感，并且依赖难以在实际部署中保证的通信和共享全局观测。**分散式方法**虽然避免了这些弊端，但在部分可观测性下，各智能体缺乏队友意图信息，容易导致碰撞、物体掉落或死锁。
- **研究动机**：如何在分散式执行下，仅依靠局部观测（RGB图像）和共享任务指令，实现隐式协调，避免对全局视图、显式状态信息或智能体间通信的依赖。
- **整体含义**：通过集中训练阶段蒸馏特权多智能体动态到一个潜在空间，部署时每个智能体从自身局部观测推断该潜在向量，从而让每个智能体“感知”队友行为，实现隐式协调。这被视为一种**CTDE（Centralized Training with Decentralized Execution）** 范式的有效应用。

## 2. 论文提出的方法论
- **核心思想**：在CTDE框架下，设计一个**上下文器（Contextualizer）**和一个**动作专家（Action-Expert）**。上下文器利用CVAE（条件变分自编码器）将训练时已知的多智能体联合轨迹（特权信息）蒸馏为一个**协调感知的潜在变量 \( z_i^t \)**。部署时，每个智能体仅从自身局部RGB观测和共享文本指令推断该潜变量，并以此调节扩散去噪过程，生成协调的动作序列。
- **关键技术细节**：
  - **上下文器（两阶段训练的第一阶段）**：
    - 先验网络 \( p_{\theta_i}(z_i^t | o_i^t, l) \)：使用冻结的SigLIP编码器提取局部图像和文本指令的特征，经Transformer融合后输出高斯分布的参数（均值、方差）。
    - 后验网络 \( q_{\psi_i}(z_i^t | s_{1:N}^{t+1:t+H}) \)：利用**多智能体运动学编码器**处理所有智能体的未来联合轨迹（特权信息），输出残差参数（均值偏移、方差调整），叠加到先验均值上形成后验。
    - 解码器 \( D_{\psi_i} \)：从当前自身本体感知 \( s_i^t \) 和潜变量 \( z_i^t \) 重构所有智能体的未来联合轨迹。训练目标包含KL散度（对齐后验和先验）和未来轨迹重构误差。训练后丢弃多智能体运动学编码器和解码器。
  - **动作专家（第二阶段）**：
    - 采用 **扩散策略（Diffusion Policy）**，输入包括观测历史 \( O_i^t \)、本体感知历史 \( S_i^t \)、以及冻结的上下文器输出的潜变量 \( z_i^t \)。潜变量通过交叉注意力注入U-Net的降采样和瓶颈层。
    - 训练损失为标准扩散噪声预测损失 \( \mathcal{L}_{AE} = \mathbb{E}[\| \epsilon - \epsilon_{\phi_i}(A_{i,k}^t, k, O_i^t, S_i^t, \mathrm{sg}(z_i^t))\|^2] \)。
  - **部署阶段**：每个智能体独立运行：先通过上下文器先验采样潜变量 \( z_i^t \)，再通过动作专家执行K步去噪得到动作序列。每个智能体的计算成本恒定，不随团队规模增长。
- **公式（文字说明）**：
  - 上下文器损失：\( \mathcal{L}_{CT} = \beta D_{KL}(q_{\psi_i} \| p_{\theta_i}) + \mathbb{E}_{z_i^t \sim q_{\psi_i}}[\| \hat{s}_{1:N}^{t+1:t+H} - s_{1:N}^{t+1:t+H}\|^2] \)
  - 动作专家损失：扩散策略的标准噪声预测损失。
  - 去噪反向过程：\( A_{i,k-1}^t = \lambda_k (A_{i,k}^t - \gamma_k \epsilon_{\phi_i}(A_{i,k}^t, k, O_i^t, S_i^t, z_i^t) + \eta_k \epsilon') \)

## 3. 实验设计
- **数据集/场景**：使用 **RoboFactory** 仿真基准，包含6个多臂操作任务，覆盖**紧同步**、**角色非对称协调**、**严格顺序依赖**等需求：
  - 2个智能体：Lift Barrier、Place Food、Two Robots Stack Cube、Camera Alignment
  - 3个智能体：Three Robots Stack Cube
  - 4个智能体：Take Photo
- **对比方法**：
  - **集中式基线**：DP (Diffusion Policy)、LargeDP（参数扩大的DP）、2D Dense Policy、DP3（基于3D点云）、3D Dense Policy、Global GauDP（基于3D高斯泼溅重建全局场景）。
  - **分散式基线**：Local GauDP（各智能体独立运行GauDP）、Ours w/o CLS（去掉协作潜变量的CLS-DP，即普通分散扩散策略）。
- **评估指标**：每个任务100个未见随机种子的成功率（平均成功率）。还评估了参数效率（成功率/参数量）。

## 4. 资源与算力
- **文中未明确说明使用的GPU型号、数量、训练时长**。只提到所有方法训练100个epochs，使用Adam优化器，学习率\(10^{-4}\)，批量大小32（动作专家阶段），上下文器批量大小512。其他细节如预热80% epoch、余弦衰减等。未提及硬件配置。

## 5. 实验数量与充分性
- **实验数量**：在6个任务上评估，每个任务100个随机种子（共600次测试）。消融实验包含Ours w/o CLS（验证潜变量贡献）、参数效率对比、跨模态注意力权重分析、以及归因图（Integrated Gradients）定性分析。
- **充分性与客观性**：
  - 对比的方法全面（7种集中式+2种分散式），且控制了观测编码器、超参数等一致。
  - 消融实验直接证明了协作潜变量的关键贡献（38% vs 9%）。
  - 参数效率分析展示了模型大小与性能的关系，验证了CLS-DP的轻量高效。
  - 归因图和跨模态注意力揭示了潜变量如何帮助智能体关注队友。
  - **不足**：所有实验仅在仿真环境中进行，未在真实机器人上验证；任务数量有限（6个），且未涵盖更复杂的多智能体场景（如非对称任务分配、动态障碍等）；未进行统计显著性检验（如置信区间）。

## 6. 论文的主要结论与发现
- CLS-DP在6个任务上平均成功率**38%**，是**最佳集中式基线（Global GauDP，20%）的近两倍**，远超无潜变量的分散式基线（9%）。在3智能体堆叠任务（Three Robots Stack Cube）上，只有CLS-DP达到非零成功率（20%）。
- **参数效率**显著优于所有基线：每百万参数成功率高出一个数量级（例如2智能体场景下0.2056 vs 第二好的LargeDP 0.0351）。
- **归因图**显示：有协作潜变量的智能体在任务执行过程中持续将高归因指向自身和其队友的关节、夹爪；而无潜变量的基线智能体仅关注自身（egocentric），导致协调失败（如相机掉落、抓取失败）。
- **跨模态注意力分析**表明：上下文器根据任务需求动态调节文本和图像注意力的权重（如角色非对称任务更依赖文本顺序描述，空间对齐任务更依赖图像反馈）。

## 7. 优点
- **方法创新**：将多智能体联合动态蒸馏到局部可观测潜变量，实现了**无需通信、无需全局视图、无需显式状态共享**的隐式协调。
- **可扩展性**：每个智能体的推理计算成本恒定，团队规模增大时无额外开销。
- **性能优异**：在仿真基准上大幅度超越现有方法，特别是对3-4智能体任务，是唯一实现显著成功的方案。
- **分析全面**：通过消融、参数效率、跨模态注意力、归因图等多种角度验证了协作潜变量的作用机理。
- **训练高效**：使用CTDE，训练阶段利用特权信息，部署阶段完全轻量；上下文器多智能体编码器仅在训练时使用，不增加部署开销。

## 8. 不足与局限
- **仿真环境局限**：所有实验在RoboFactory仿真中完成，未在真实机器人上验证。真实环境中的感知噪声、物理不确定性、通信延迟等挑战未考虑。
- **空间精度瓶颈**：在需要细粒度空间精度的任务（如Take Photo的按下快门）上，CLS-DP（8%）落后于使用3D点云的DP3（XYZ+RGB，11%），说明隐式潜变量在捕捉精确三维几何信息方面存在局限。
- **任务覆盖有限**：仅6个操作任务，且均为非对抗性合作场景。未涵盖更复杂的动态交互、部分可观测性更极端、或含有错误/延迟执行的情况。
- **依赖高质量演示**：方法基于行为克隆（Imitation Learning），需要大量成功演示；收集多智能体协调演示本身是困难的。
- **未考虑安全性**：论文未讨论碰撞避免、容错机制或安全约束。
- **统计严谨性不足**：未报告置信区间或标准差，无法判断性能波动的显著性。

（完）
