---
title: "KV-PRM: Efficient Process Reward Modeling via KV-Cache Transfer for Multi-Agent Test-Time Scaling"
title_zh: KV-PRM：基于KV缓存传输的高效过程奖励建模用于多智能体测试时缩放
authors: "Peng Kuang, Haibo Jin, Xiaoyu Han, Yanli Wang, Xiaopeng Yuan, Ye Yu, Kaidi Xu, Haohan Wang"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09153v1"
tags: ["query:agentic-rl"]
score: 8.0
evidence: KV-PRM为多智能体测试时扩展提供高效过程奖励建模
tldr: 现有PRM需重编码整个轨迹文本，成本随序列长度平方增长，成为长上下文场景的计算瓶颈。KV-PRM直接复用LLM生成阶段的KV缓存，仅需处理单个验证令牌，将评分成本从O(L²)降至O(L)。实验证明接近或超越文本PRM，同时实现高达5000倍FLOPs减少和37倍延迟降低。该工作为过程奖励模型在长序列多智能体系统中的应用提供了高效解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09153v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 718, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09153v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1438, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09153v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1367, \"height\": 518, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09153v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 685, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 740, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09153v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 753, \"height\": 334, \"label\": \"Table\"}]"
motivation: 文本PRM对长轨迹重编码成本二次增长，限制其在长上下文多智能体TTS中的应用。
method: KV-PRM利用LLM生成时的KV缓存，通过单令牌验证直接建模过程奖励，将复杂度降至线性。
result: 在MATH、GSM8K、AIME上匹配或超越文本PRM，评分FLOPs减少5000倍，延迟降低37倍。
conclusion: KV-PRM以更低成本实现高效过程奖励，可扩展至长序列多智能体测试时扩展。
---

## 摘要
过程奖励模型（PRM）已被证明在引导测试时缩放（TTS）方法方面非常有效，这些方法显著提升了基于LLM的多智能体系统的能力。然而，现有的PRM是基于文本的：它们从头开始重新编码整个轨迹文本。在长时间的多智能体展开中，评分成本随序列长度L呈二次方增长，造成了严重的计算瓶颈，严重限制了PRM在长上下文场景中的应用。为了解决这个问题，我们引入了KV-PRM，一种高效的过程奖励模型，它通过直接读取LLM生成阶段自然产生的KV缓存，消除了繁重的文本重新编码。通过针对预先存在的KV缓存处理单个“验证令牌”，KV-PRM将评分成本从O(L^2)降低到O(L)。我们正式证明KV缓存包含比文本严格更大的信息容量，并且对于下游奖励建模更加高效。在MATH、GSM8K和AIME基准测试中，经验上，在各种TTS方法（如束搜索、蒙特卡洛树搜索和加权投票）下，KV-PRM匹配或严格优于文本PRM，与基于文本的PRM相比，评分FLOPs减少了高达5000倍，延迟减少了37倍，每个序列的内存占用减少了34倍。

## Abstract
Process Reward Models (PRMs) have been proven to be highly effective in guiding test-time scaling (TTS) methods, which significantly boost the capabilities of LLM-based multi-agent systems. However, existing PRMs are text-based: they re-encode the entire trajectory text from scratch. In long multi-agent rollouts, the scoring cost, growing quadratically with respect to sequence length L, creates a severe computational bottleneck, severely limiting PRMs' application in long-context scenarios. To resolve this, we introduce KV-PRM, a highly efficient process reward model that eliminates the heavy text re-encoding by directly reading the KV cache produced naturally during the LLM's generation phase. By processing a single "verify token" against the pre-existing KV cache, KV-PRM reduces the scoring cost from O(L^2) to O(L). We formally prove that the KV cache contains strictly greater information capacity than text, and is more efficient for downstream reward modeling. Empirically, across the MATH, GSM8K, and AIME benchmarks, KV-PRM matches or strictly outperforms text-PRMs under various TTS methods such as Beam Search, MCTS, and Weighted Voting, with up to a 5,000x reduction in scoring FLOPs, a 37x reduction in latency, and a 34x reduction in per-sequence memory footprint compared to text-based PRMs.

---

## 论文详细总结（自动生成）

# KV-PRM 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的过程奖励模型（PRM）在引导测试时缩放（TTS）方法中非常有效，用于评估多智能体系统中的中间推理步骤。然而，现有 PRM 是基于文本的（text-based），对于每次候选轨迹的评分，都需要从头重新编码整个序列的文本令牌。由于自注意力机制的计算成本与序列长度 L 呈二次方关系（O(L²)），在多智能体长轨迹展开（通常数千至上万令牌）中，PRM 的评分成本成为严重的计算瓶颈，甚至在 FLOPs 上与生成过程相当，限制了 PRM 在长上下文推理场景中的应用。
- **整体含义**：本研究旨在解决这一瓶颈，通过复用 LLM 生成过程中自然产生的 KV 缓存，将评分成本从 O(L²) 降至 O(L)，实现高效的过程奖励建模，从而支持更经济的测试时缩放。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用 LLM 自回归生成过程中自然产生的键值缓存（KV cache）作为轨迹的丰富、高维表示，而不是将其解码为文本后再重新编码。通过一个轻量级的验证令牌（verify token）直接在此 KV 缓存上进行评分，从而消除文本重编码的二次开销。
- **关键技术细节**：
  - **KV-PRM 架构**：基于基础语言模型（如 Qwen3），在其上附加一个 LoRA 适配器。设计一个特殊的“验证令牌”（例如“?”）和两个判断令牌（“+”和“-”）。
  - **评分过程**：在多智能体系统中，当某个智能体完成生成后，其产生的 KV 缓存已经存在。KV-PRM 仅需将验证令牌作为查询，通过 LoRA 适配器处理该令牌，利用预先存在的 KV 缓存进行单次前向传播，输出正判断的概率作为过程奖励分数。
  - **复杂度分析**：验证令牌前向传播的计算成本为 O(d·L)（d 为隐藏维度，L 为序列长度），而文本 PRM 需要 O(d·L²)，因此速度提升因子为 O(L)。对于典型轨迹（L≈5000），可实现约 5000 倍的 FLOPs 减少。
  - **理论支撑**：作者通过理论框架证明 KV 缓存的信息容量严格大于文本令牌（Proposition 1），且单令牌读取（k=1）即可捕获大部分可提取的奖励信息（Theorem 2），逼近最优性能。
- **训练流程**（Algorithm 1）：
  1. 使用蒙特卡洛树搜索（MCTS）生成训练标签（Q 值 y∈[-1,1]）。
  2. 对于每个轨迹-智能体步对，基础模型（适配器禁用）编码轨迹产生 KV 缓存（不计算梯度）。
  3. 模型（适配器启用）处理验证令牌，得到正概率 P(+)。
  4. 损失函数为 MSE(P(+), (y+1)/2)。
  5. 仅更新 LoRA 参数 Δθ。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：MATH、GSM8K、AIME 2024、AIME 2025。评估指标为精确匹配准确率，包含数值等价性检查。
- **基准（Benchmark）**：在不同搜索算法下评估：步级束搜索（SBS）、蒙特卡洛树搜索（MCTS）、加权投票（Weighted Voting）。不同超参数配置使得实验覆盖多个场景。
- **对比方法**：
  - **No PRM**：仅随机采样或多数投票，无过程奖励引导。
  - **Policy Log-prob**：利用生成器自身的令牌级对数概率对轨迹排序，附加成本可忽略。
  - **Text-PRM**：与 KV-PRM 相同架构、训练数据和损失函数，但通过重新编码完整轨迹进行评分（O(L²) 成本）。
  - **KV-PRM**（本文方法）。
- **模型规模与多智能体拓扑**：
  - 模型：Qwen3 系列（0.6B、4B、8B），关闭思考模式。
  - 拓扑：Sequential（Reader→Planner→Solver→Verifier）和 Hierarchical（Math/Science/Code Agents→Task Summarizer）。

## 4. 资源与算力

- 文中未明确说明训练和推理使用的具体 GPU 数量、时长等详细算力信息，仅提及推理延迟测试在 **NVIDIA GH200 120GB GPU** 上进行。训练部分只给出了超参数（LoRA rank=256、全局 batch size=2048、精度 bf16 等），但未提及训练时长或显卡数量。因此，算力开销细节被省略。

## 5. 实验数量与充分性

- **实验数量**：在 4 个数学推理数据集上，对 3 种模型规模（0.6B/4B/8B）和 2 种多智能体拓扑，在 3 种搜索算法（SBS、MCTS、Weighted Voting）的多组超参数下进行了广泛评估，共计约 60+ 组性能比较（表 1-2）。此外，还额外进行了分析实验：不同验证令牌数量（k）的影响、不同规模 Text-PRM 的效率对比、层次拓扑下的评估以及 KV Steering 概念验证。消融实验包括验证令牌数量的增益分析（图 2）和延迟/内存开销测量（图 3）。
- **充分性与公平性**：
  - 实验覆盖了常见的数学推理基准和流行搜索策略，对比基线包括无 PRM、策略对数概率和文本 PRM，且 Text-PRM 与 KV-PRM 使用相同训练数据和 LoRA 配置，确保公平比较。
  - 延迟和内存测量也在相同硬件条件下进行。
  - 但论文未在更多样化的推理任务（如科学、代码推理）或更大规模模型上评估，也缺乏对非数学推理领域的泛化性测试。此外，所有实验均基于 Qwen3 系列，未使用其他模型族（如 Llama、DeepSeek），可能对一般化结论有一定影响。

## 6. 论文的主要结论与发现

- KV-PRM 在 **MATH、GSM8K、AIME** 等基准上匹配或超越了文本 PRM 的准确率，同时在大部分设置下实现**更优**的性能。
- 评分 FLOPs 减少高达 **5000 倍**，推理延迟降低 **15–37 倍**，每个序列内存占用减少 **34.2 倍**。
- 理论分析验证了 KV 缓存的信息优势以及单令牌读取的近最优性。
- 额外概念验证 **KV Steering** 证明梯度化 KV 缓存优化可进一步提升准确率，揭示了一种超越搜索的新型测试时优化方向。

## 7. 优点

- **高效性**：方法大幅降低了过程奖励模型的计算成本，使得在长轨迹多智能体系统中使用 PRM 变得实际可行。
- **理论扎实**：为 KV 缓存的信息容量优越性及单令牌读出的合理性提供了正式证明，给出了信息论和统计学习理论支撑。
- **易于集成**：KV-PRM 可作为即插即用替换现有文本 PRM，无需改变搜索算法。
- **额外能力**：KV-PRM 的评分可微性启发了梯度操纵 KV 缓存的 KV Steering 方法，这是文本 PRM 无法做到的。
- **实验充分**：在多种模型规模、搜索算法、拓扑结构下验证了稳健性。

## 8. 不足与局限

- **模型架构限制**：生成器和验证器必须共享同一基础模型架构（KV 缓存格式一致），限制了使用不同模型族作为验证器的灵活性。
- **理论基础依赖假设**：线性表示假设（Assumption 1）和低秩奖励结构假设（Assumption 2）在严格理论上可能不完全成立，影响理论结论的普适性。
- **KV Steering 尚处初步**：该部分仅为概念验证，缺乏更系统的实验和超参数研究，实际效益和稳定性有待进一步探索。
- **评估范围有限**：仅测试了数学推理（MATH、GSM8K、AIME），未涉及其他领域（如科学、代码、对话）；模型也仅限于 Qwen3 系列，未在 Llama、DeepSeek 等其他流行模型上验证。
- **训练数据生成成本**：虽然推理高效，但训练标签仍需通过 MCTS 生成，本身可能消耗较多计算资源（尽管文中未强调）。
- **缺失更仔细的公平比较**：未与更先进的 PRM 变体（如 GenPRM、Process Reward Models that Think）比较，仅对比了简单的 LoRA 文本 PRM。

（完）
