Title: Entropy Regularization Improves Policy Robustness in Continuous-Time Reinforcement Learning

URL Source: https://arxiv.org/pdf/2607.03168v1

Published Time: Tue, 07 Jul 2026 01:03:33 GMT

Number of Pages: 45

Markdown Content:
ENTROPY REGULARIZATION IMPROVES POLICY ROBUSTNESS IN CONTINUOUS-TIME REINFORCEMENT LEARNING 

JIALUN CAO 1,∗, FERNANDO ACERO 2,∗, DAVID ŠIŠKA 1, AND YUFEI ZHANG 3

Abstract. Entropy regularization is widely used in continuous-time reinforcement learning (RL) to reduce sensitivity to environmental perturbations, yet its robustness benefits lack a rigorous theoretical foundation. This paper establishes the first robustness guarantees for entropy-regularized continuous-time Markov decision processes. We show that maximizing an entropy-regularized objective yields a lower bound on a worst-case robust RL problem with joint reward and transition perturbations. We analytically characterize the induced robust sets and prove that they expand monotonically with the regularization strength, justifying the empirical observation that stronger entropy improves robustness. In contrast to prior discrete-time analyses, our results remove the intractable state-distribution entropy term and provide guarantees invariant to action frequency. Experiments on queueing network control and market making confirm our theory, showing that entropy-regularized policies outperform greedy and ϵ-greedy baselines under dynamics perturbations. 

1. Introduction 

Policy robustness is a central requirement for deploying reinforcement learning (RL) in real-world systems. Policies trained in simulation or under a nominal model often face environments at deployment that differ from those seen during training, due to model misspecification, unmodeled dynamics, non-stationarity, human biases, or even adversarial disturbances [ 15 ]. Such sim-to-real and train-to-deploy gaps can severely degrade performance if the learned policy is brittle [ 57 , 45 , 1]. Ensuring that policies maintain reliable performance under perturbations of the environment is therefore a key challenge in modern RL. One approach is robust RL, which models uncertainty via a set of plausible transition kernels or reward functions and seeks policies that perform well in the worst case over this set [ 40 , 44 , 37 ,47 , 56 ]. This leads to a two-player game between a controller and an adversary that perturbs the dynamics and rewards. While this framework yields explicit worst-case guarantees, it requires specifying a suitable uncertainty set and solving a more challenging minimax problem, often with specialized algorithms and increased sample and computational cost [41, 61, 8, 36, 6]. In parallel, entropy-regularized RL has emerged as a widely used alternative that often exhibits robust behavior without explicitly modeling an adversary or solving a minimax problem. By augmenting the reward objective with an entropy term, entropy-regularized methods encourage stochastic policies that explore more broadly, improve optimization stability, and empirically tend to be less sensitive to perturbations in the environment. Such methods underlie many state-of-the-art algorithms in large-scale RL [ 53 , 54 ], where entropy regularization is primarily viewed as a tool for numerical stability, yet is widely observed to improve robustness in practice. Recent theoretical work in discrete-time Markov decision processes (MDPs) has begun to formalize this phenomenon. Several authors have shown that entropy-regularized RL can be interpreted as optimizing robust objectives under suitable perturbation models for the reward and transition kernel [ 15 , 5, 12 , 2]. These results suggest that robustness may arise implicitly from entropy regularization, without the algorithmic and modeling overhead of explicit robust RL. 

Key words and phrases. Entropy regularization; policy robustness; robustness in reinforcement learning; continuous-time reinforcement learning; continuous-time Markov decision processes. 

> ∗

Equal contribution. 

> 1

School of Mathematics, University of Edinburgh, United Kingdom. {galen.cao}{d.siska}@ed.ac.uk .

> 2

J.P. Morgan AI Research, United Kingdom. fernando.acero@jpmorgan.com .

> 3

Department of Mathematics, Imperial College London, United Kingdom. yufei.zhang@imperial.ac.uk . 

> 1
> arXiv:2607.03168v1 [math.OC] 3 Jul 2026 ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 2

However, most of this theory is developed in discrete-time settings, whereas many applications of RL, especially in physical systems, robotics, finance, and inventory management, are more naturally modeled in continuous time. In such domains, the environment state can change at any time, control acts continuously, and performance is evaluated via continuous-time functionals. Continuous-time RL provides a natural framework for these problems, avoiding the need to artificially discretize time and approximate the underlying dynamics, as well as the potential performance degradation that can arise when applying algorithms designed for discrete-time MDPs [ 29 , 30 , 31 , 63 , 9]. In these settings, entropy regularization is often used explicitly in algorithm design, yet its robustness benefits have not been rigorously analyzed. This raises the following question: 

To what extent does entropy regularization in continuous-time RL provide robust-ness guarantees, and against which classes of environment perturbations? 

Answering this question requires techniques beyond those used in the discrete-time setting, as the structure of continuous-time dynamics can fundamentally change how entropy regularization interacts with model uncertainty. In fact, as we shall show, existing robust guarantees for discrete-time RL can degenerate as the number of time steps increases (Example 3.7). 

Our Contribution. In this paper, we take an initial step toward addressing this gap by establishing robustness guarantees of entropy-regularized continuous-time MDPs, where the state dynamics is given by a controlled continuous-time Markov chain. Such problems arise naturally in various domains, such as network queuing control [ 10 ], market making for high-frequency trading [ 7], or supply-chain management [ 48 ]. Our main contributions are summarized as follows: 

• We show that the entropy-regularized objective with a log-transformed reward provides a lower bound for a robust RL problem over a set of joint reward and state perturbations (Theorems 3.2 and 3.6). In contrast to existing robustness results for discrete-time MDPs [ 15 ,2], our reward does not involve a difficult-to-compute entropy term of the state distribution, making it applicable to general RL tasks (Remark 3.3). In the case with reward-only perturbations, we show that entropy-regularized RL is exactly equivalent to a robust RL problem (Theorem 3.1). 

• We provide two complementary characterizations of the corresponding robust sets: one in terms of induced state-occupancy measures, and another in terms of the local entropy rate of the state transitions. Both sets expand as the entropy regularization parameter increases, thereby justifying empirical observations that stronger entropy regularization improves policy robustness (Propositions 3.4 and 3.8). We further prove that robust-set constructions proposed in [ 15 ] for discrete-time RL degenerate to an empty set as the number of time steps increases and thus fail in the continuous-time limit (Example 3.7). 

• Numerical experiments on queueing network control and market making confirm our theory: entropy regularization yields more robust policies than both greedy policies for a nominal environment model and ϵ-greedy policies (Section 4 and Appendix D) and validate the tightness of the theoretical certificates (Appendix E). 1.1. Closely Related Works. Given the extensive research literature on RL, we focus on the specific subfield that is most relevant to our work. 

Robust RL. In RL, robustness has traditionally been studied via robust MDPs, where policies optimize worst-case performance over uncertainty sets for rewards or transition kernels [ 41 , 26 , 61 ]. While these methods provide theoretical guarantees against model misspecification, they typically require inner adversarial optimization and are computationally expensive. Recent work proposes more scalable approaches for particular robustness sets [ 20 , 59 , 33 , 64 , 35 ], but results largely remain for discrete-time MDPs. Continuous-time robust RL is far less developed and has primarily been studied through H∞ control and differential-game formulations for nonlinear controlled ODEs [40, 46, 38]. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 3

To the best of our knowledge, there is no prior work on continuous-time robust RL for stochastic dynamics. Moreover, as in discrete time, these formulations are computationally challenging, often requiring time discretization and inner minimax updates [46]. 

Entropy-regularized RL and robustness. Entropy-regularization is widely used to improve exploration, probabilistic inference, and optimization stability [ 58 , 49 , 18 , 23 , 34 , 32 ] (see Appendix A for applications in continuous-time RL). Recent work connects robustness and entropy regularization, but primarily in discrete-time MDPs. Under reward uncertainty, policy regularization admits an exact robust interpretation against worst-case reward perturbations [ 25 ,5]. Under transition uncertainty, however, ordinary policy-entropy regularization typically provides only a lower bound on a robust control objective rather than an exact equivalence [ 15 , 12 ], and robust formulations may require value-dependent regularization (e.g., twice-regularized MDPs [ 11 ]). State-entropy regularization further strengthens this view by yielding a nontrivial, generally unimprovable lower bound under kernel uncertainty [2]. However, these robustness bounds often involve an intractable state-distribution entropy term in the reward, limiting applicability to general RL settings. Moreover, these discrete-time guarantees can degrade as the step size shrinks, preventing direct extension to continuous-time dynamics. 2. Problem Formulation 

Entropy-regularized CTMDP. Consider an infinite-horizon continuous-time Markov decision process (CTMDP) defined by the tuple  S, A, (As)s∈S , λ, R , as in [ 21 ]: S and A are finite state and action spaces, respectively. For each state s ∈ S , As ⊆ A is a nonempty set, representing the available actions in state s. For each state–action pair (s, a ) ∈ S × A s, λass ′ is the transition rate from state s to state s′ under action a, which satisfies λass ′ ≥ 0 for all s′̸ = s, and 

λass = − P  

> s′̸=s

λass ′ . R : S × A → (0 , ∞) is the instantaneous reward function. We consider optimizing a discounted objective over stationary randomized Markov policies. Let P(A) be the space of probability measures on A, and define the admissible policy class by 

Π := π : S → P (A) | X

> a∈As

π(a|s) = 1 , ∀s ∈ S .

Let ρ ∈ P (S) be the initial state distribution. For each π ∈ Π, let Pπ denote the law of the controlled continuous-time Markov chain (CTMC) (St)t≥0 initialized according to ρ, with the generator matrix λπ = ( λπss ′ )s,s ′∈S induced by π, where λπss ′ := P 

> a∈A s

π(a|s)λass ′ for all s, s ′ ∈ S .Let r > 0 be the discount rate and τ ≥ 0 be the regularization parameter. We seek a policy 

π ∈ Π that maximizes the entropy-regularized objective (2.1) Jτ (π) := EPπ

> ρ

 Z ∞

> 0

e−rt 

 X

> a∈A St

π(a|St) R(St, a ) − τ KL( π ∥ μ)( St)



dt



,

where μ : S → P (A) is a reference policy and KL (π∥μ) is the Kullback-Leibler (KL) divergence between π and μ defined by 

KL( π∥μ)( s) := KL  π(·| s)∥μ(·| s) = X

> a∈A s

π(a|s) log π(a|s) 

> μ(a|s)

,

with the convention 0 log (0 /x ) = 0 . For simplicity, we assume supp μ(·| s) = As for all s ∈ S , and for each π ∈ Π, define the normalized discounted state occupancy measure ¯dπρ ∈ P (S) by 

¯dπρ (s) := rEPπ

> ρ

h Z ∞

> 0

e−rt 1{St=s} dt

i

, ∀s ∈ S .

The objective Jτ (π) has a natural event-driven implementation, in which an agent interacts with a controlled CTMC at Poisson-distributed decision epochs and samples actions from π

based on the observed states; see Appendix B.1 and [48, Ch. 11] for details. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 4

Policy Robustness in CTMDP. The main goal of this paper is to show that maximizing the entropy-regularized objective (2.1) yields policies with robustness guarantees. We measure the robustness of a policy π ∈ Π by its performance under perturbed instantaneous reward functions and perturbed CTMCs with modified transition rates. Specifically, we consider a parameterized family of transition rates and reward functions indexed by shared parameters θ ∈ Θ. This allows us to model structured perturbations that simultaneously affect both the dynamics and the rewards, as is common in many CTMDP applications (see Section 3.3). We denote dependence on θ ∈ Θ by a subscript, for example in the transition rates λaθ = ( λass ′,θ )s,s ′∈S , the reward function Rθ = ( Rθ(s, a )) (s,a )∈S×A , and the law of the CTMC Pπθ . Moreover, we require that for any θ ∈ Θ, the parameterized components maintain the standard CTMDP structure defined previously; in particular, the reward function satisfies Rθ(s, a ) > 0. For any given π ∈ Π, we define the robust objective in terms of a robust set C ⊂ Θ:(2.2) inf 

> ˜θ∈C

EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt X

> a∈A St

π(a|St) R˜θ(St, a ) d t



.

Finding an optimal policy that maximizes (2.2) can be computationally expensive, as it requires solving a zero-sum game. In the sequel, we show that the entropy-regularized objective (2.1) for a baseline model provides a lower bound on the robust objective (2.2) , and further characterize the corresponding robust set C analytically in terms of the baseline model, the policy, and the entropy-regularization parameter. This implies that a policy maximizing the entropy-regularized objective (2.1) implicitly guarantees robustness over a family of adversarial models. 3. Main Theoretical Results 

This section presents robustness guarantees for entropy-regularized CTMDPs. Our main results show that the entropy-regularized objective with a simple pessimistic reward provides a lower bound for a robust RL problem with respect to a family of joint reward and state perturbations. Unlike existing robustness results, our pessimistic reward does not involve a difficult-to-compute state-entropy. We provide two characterizations of the robust sets: one in terms of induced state-occupancy measures, and another in terms of the local entropy rate of the state transitions. We further show that robust-set constructions from discrete-time RL degenerate as the number of time steps increases and do not extend to the continuous-time setting. In the sequel, let θ ∈ Θ

be a fixed baseline (nominal) model. 3.1. Robustness under Reward-only Perturbations. We first establish a strong duality between the entropy-regularized objective and a robust RL problem in the setting where the perturbation affects only the reward function. This result will then be used to prove the robustness theorem under joint perturbations of both the dynamics and the reward functions. Since the perturbations ˜θ ∈ Θ only affect the reward, we have Pπ 

> ˜θ

= Pπθ .

Theorem 3.1. Let π ∈ Π, τ > 0 and ε ≥ 0. Define the reward-only robust set by 

(3.1) bCπτ,ε (θ) := 

n˜θ ∈ Θ τ log 

 X

> s∈S

¯dπρ,θ (s) X

> a∈A s

μ(a|s) exp 

 Rθ (s,a )−R˜θ (s,a )

> τ

 

≤ ε

o

.

Let Jτ (π; θ) be the entropy-regularized objective (2.1) under the baseline model θ. Then 

(3.2) inf  

> ˜θ∈bCπτ,ε (θ)

EPπθ

> ρ

 Z ∞

> 0

e−rt X

> a∈A St

π(a|St) R˜θ(St, a ) d t



≥ Jτ (π; θ) − εr .

where an equality is achieved if there exists ˜θ∗ ∈ Θ such that R˜θ∗ (s, a ) = Rθ(s, a ) − τ log π(a|s) 

> μ(a|s)

− ε . 

In this case, ˜θ∗ is a worst-case model. 

Theorem 3.1 shows that the optimal policy for the entropy-regularized objective (2.1) is also optimal in the worst-case scenario with reward uncertainty given in (3.1) , which extends similar ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 5

results for discrete-time maximum-entropy MDPs in [ 15 , 2] to the continuous-time setting. The proof is given in Appendix B.2. By Theorem 3.1, the policy obtained from an entropy-regularized objective is robust to reward perturbations, as long as the new reward does not fall too far below the original one, quantified by smoothed maximum over the state–action space. It is easy to show bCπτ,ε (θ) expands as τ

increases, indicating that entropy regularization improves policy robustness. The parameter ε

represents the adversary’s budget for allowable perturbations: as ε increases, the set of admissible perturbations grows, but the corresponding worst-case lower bound decreases. 3.2. Robustness under Joint Perturbations of Dynamics and Rewards. We now turn to robustness guarantees for entropy-regularized CTMDPs when the perturbation also affects the state dynamics. As shown in [ 15 , Theorem 4.1], the exact equivalence to a robust RL formulation, as in Theorem 3.1, does not hold in this setting. Instead, we will show that the entropy-regularized objective provides a lower bound on the worst-case value over a family of environments. 

Robustness Guarantee with State Occupancy Measure. The first theorem quantifies robustness in terms of perturbations to the induced state-occupancy distribution. 

Theorem 3.2. For all π ∈ Π, τ > 0, and ε ≥ 0,

(3.3) 

inf  

> ˜θ∈eCπτ,ε (θ)

EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt X

> a∈A St

π(a|St) R˜θ (St, a ) d t



≥ exp 



r EPπθ

> ρ

 Z ∞

> 0

e−rt  X

> a∈A St

π(a|St) log Rθ (St, a ) − τ KL( π∥μ)( St)



dt



− ε − log r



,

where the robust set eCπτ,ε (θ) is defined as 

(3.4) eCπτ,ε (θ) := 

n˜θ ∈ Θ τ log 

 X

> s∈S

¯dπρ,θ (s) X

> a∈A s

μ(a|s) exp 

 fθ, ˜θ (s, a )

τ

 

≤ ε

o

,

where fθ, ˜θ(s, a ) := log ¯dπρ,θ (s)¯dπρ, ˜θ (s) − log R˜θ (s,a )  

> Rθ(s,a )

.

Theorem 3.2 provides the first theoretical robustness guarantee for entropy regularization in continuous-time RL, whose proof is given in Appendix B.3. It shows that a policy that performs well across a range of dynamics can be obtained by solving an entropy-regularized problem with a pessimistic log-transformed reward, and, conversely, that solving an entropy-regularized problem is implicitly solving a robust RL problem under a modified reward function. 

Remark 3.3 ( Pessimistic reward without state entropy ). The key feature is that a pessimistic reward (3.3) is a simple log-transformation of the original reward. This is achieved by constructing the robust set eCπτ,ε (θ) using a smoothed maximum under the baseline occupancy measure ¯dπρ,θ over the state space and the reference policy μ over the action space. This not only yields a nonuniform perturbation budget over the spaces, allowing larger perturbations at less frequently visited states and actions, but also allows for showing a robust bound with the simple log-transformed reward. In contrast, existing robustness results for discrete-time MDPs [ 15 , 2] construct robust sets using a uniform distribution over the state space, which introduces an additional entropy term of the state dynamics in the pessimistic objective (see Appendix B.4 for an analogous result in the present setting). This entropy term is difficult to compute for complex or unknown state dynamics and thus limits the applicability of those results in general RL settings. The following proposition shows that the robust set eCπτ,ε (θ) expands as the entropy regularization parameter τ increases, confirming that stronger entropy regularization leads to more robust policies. The proof is provided in Appendix B.5. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 6

Proposition 3.4. Let π ∈ Π and ε ≥ 0. The set-valued map τ 7 → eCπτ,ε (θ) is nondecreasing, i.e., 

eCπτ1,ε (θ) ⊆ eCπτ2,ε (θ) for all 0 < τ 1 ≤ τ2. Moreover, 

eCπ

> ∞,ε

(θ) := lim  

> τ→∞

eCπτ,ε (θ) = [

> τ > 0

eCπτ,ε (θ) = 

n˜θ ∈ Θ X

> s∈S

¯dπρ,θ (s) X

> a∈A s

μ(a|s) fθ, ˜θ(s, a ) ≤ ε

o

,

and 

eCπ

> 0,ε

(θ) := lim  

> τ→0

eCπτ,ε (θ) = \

> τ > 0

eCπτ,ε (θ) = 

n˜θ ∈ Θ fθ, ˜θ(s, a ) ≤ ε , ∀a ∈ A s, ¯dπρ,θ -a.s. s ∈ S 

o

,

where fθ, ˜θ(s, a ) is given in Theorem 3.2. 

Robustness Guarantee with State Transition Intensity. A limitation of Theorem 3.2 is that the robust set eCπε,τ (θ) in (3.4) is defined directly in terms of ratios of occupancy measures, making it difficult to determine whether a given perturbed model belongs to this set. To improve the interpretability, we provide a robustness result in which the set is characterized in terms of the discrepancy between the transition rates of the baseline and perturbed state dynamics. To this end, for any perturbed model ˜θ ∈ Θ, we define the following the local relative entropy rate of the two CTMCs: (3.5) ℓ(λaθ , λ a 

> ˜θ

)( s) := X 

> s′̸=s

"

λass ′,θ log λass ′,θ 

λass ′,˜θ

− (λass ′,θ − λass ′,˜θ)

#

≥ 0 , ∀s ∈ S , a ∈ A s ,

which measures the instantaneous relative entropy per unit time between the path measures of the baseline model Pπθ and the perturbed model Pπ 

> ˜θ

(see also [ 43 ]). It is the continuous-time analogue of the KL divergence between discrete-time one-step transition kernels. We further define the local state-reward perturbation cost rate of the model ˜θ by (3.6) cθ, ˜θ(s, a ) := ℓ(λaθ , λ a 

> ˜θ

)( s) − r log R˜θ(s, a )

Rθ(s, a ) , ∀s ∈ S , a ∈ A s ,

which quantifies the magnitude of perturbations that jointly affect the state dynamics and instantaneous rewards. The cost cθ, ˜θ increases as the perturbed intensity λass ′,˜θ deviates from the baseline λass ′,θ , or as the perturbed model assigns a lower reward to a state–action pair than the baseline reward in the training environment. We assume that the perturbed dynamics and the baseline model share the same jump structure: they admit the same possible transitions, but may assign different rates to those transitions. 

Assumption 3.5. For all ˜θ ∈ Θ, s, s ′ ∈ S and a ∈ A s, λass ′,θ > 0 if and only if λass ′,˜θ > 0.We now state a robustness result in which the robust set is explicitly characterized in terms of the perturbation cost cθ, ˜θ.

Theorem 3.6. Suppose that Assumption 3.5 holds. For all τ > 0, π ∈ Π, and ε ≥ 0,

(3.7) 

inf 

> ˜θ∈C πτ,ε (θ)

EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt X

> a∈A St

π(a|St) R˜θ (St, a ) d t



≥ exp 



EPπθ

> ρ

 Z ∞

> 0

e−rt 

 X

> a∈A St

π(a|St)  r log Rθ (St, a ) − τ KL( π ∥ μ)( St)



dt



− εr − log r



,

where the robust set Cπτ,ε (θ) is given by 

(3.8) Cπτ,ε (θ) := 

(

˜θ ∈ Θ X

> s∈S

¯dπρ,θ (s)τ log 

" X

> a∈A s

μ(a|s) exp 

 cθ, ˜θ (s,a )

> τ

#

≤ ε

)

,

with the perturbation cost cθ, ˜θ (s, a ) defined in (3.6) .

Compared with Theorem 3.2, Theorem 3.6 constructs the robust set more explicitly by directly aggregating the local perturbation cost cθ, ˜θ. The proof starts by lower bounding the performance of π in a perturbed model by its performance in the baseline model and the likelihood-ratio ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 7

ξt = log dPπθ  

> dPπ
> ˜θFt

at all time t ≥ 0, where Ft is the observation σ-algebra up to time t. We then establish a semi-martingale decomposition for (ξt)t≥0 showing dξt = ℓ(λπθ , λ π 

> ˜θ

)( St)dt + dMt,

where Mt is a zero-mean martingale under Pπθ , and ℓ(λπθ , λ π 

> ˜θ

) is a π-aggregated relative entropy rate between Pπθ and Pπ 

> ˜θ

. We further control this aggregated rate function with the (pointwise) local relative entropy rate ℓ(λaθ , λ a 

> ˜θ

), and construct the robust set using the duality of the KL divergence at each state. The details are given in Appendix B.6. We emphasize that constructing Cπτ,ε (θ) using the local relative entropy rate ℓ(λaθ , λ a 

> ˜θ

) is crucial for continuous-time models. In contrast, the robust set derived for discrete-time MDPs in [ 15 ,Theorem 4.2] degenerates to an empty set as the time step size tends to zero. We illustrate this phenomenon in the following simple example. 

Example 3.7 (Degeneracy of discrete-time robust sets ). Consider for simplifying an uncontrolled CTMDP, an unperturbed reward, and a finite-horizon objective over the interval 

[0 , 1] . For any given time grid {ti}Ni=0 with ti = i∆t and ∆t = 1 /N , [ 15 ] constructs the robust set of the corresponding discrete-time MDP as: 

C∆t(θ) = 

˜θ ∈ Θ Epθ



> N

X

> i=0

log X

> st+1 ∈S

pθ(st+1 |st)

p˜θ(st+1 |st)

 ≤ ε

 ,

where pθ and p˜θ are the one-step transition probabilities for the baseline and perturbed (uncon-trolled) MDPs, respectively. As ∆t → 0, using the definition of the transition intensities, 

log X

> s′∈S

pθ(s′|s)

p˜θ(s′|s) = log 

1 + X 

> s′̸=s

λss ′,θ 

λss ′,˜θ

+ O(∆ t)

 = log 

1 + X 

> s′̸=s

λss ′,θ 

λss ′,˜θ

 + O(∆ t).

This implies that the left-hand-side of the constraint in C∆t(θ) is of the order O(N ) even when 

˜θ = θ. Consequently, for a fixed ε > 0, C∆t(θ) degenerates to an empty set as ∆t → 0.Stronger entropy regularization yields a larger robust set τ 7 → C πτ,ε (θ), analogous to Proposition 3.4. 

Proposition 3.8. Suppose that Assumption 3.5 holds, and let π ∈ Π and ε ≥ 0. Then the set-valued map τ 7 → C πτ,ε (θ) is nondecreasing, i.e., Cπτ1,ε (θ) ⊆ C πτ2,ε (θ) for all 0 < τ 1 ≤ τ2.Moreover, 

Cπ

> ∞,ε

(θ) := lim   

> τ→∞

Cπτ,ε (θ) = [

> τ > 0

Cπτ,ε (θ) = 

n˜θ ∈ Θ X

> s∈S

¯dπρ,θ (s) X

> a∈A s

μ(a|s) cθ, ˜θ(s, a ) ≤ ε

o

,

and 

Cπ

> 0,ε

(θ) := lim   

> τ→0

Cπτ,ε (θ) = \

> τ > 0

Cπτ,ε (θ) = 

n˜θ ∈ Θ X

> s∈S

¯dπρ,θ (s) max 

> a∈A s

cθ, ˜θ(s, a ) ≤ ε

o

,

where cθ, ˜θ(s, a ) is defined in (3.6) .

Note that in the limit approaching the unregularized case τ → 0, Cπ

> 0,ε

(θ) in Proposition 3.8 measures model perturbations in an averaged sense over the state space, whereas eCπ

> 0,ε

(θ) in Proposition 3.4 measures the model perturbation in a stronger pointwise manner over the state space. The proof of Proposition 3.8 is given in Appendix B.7. 3.3. Illustration of Robust Sets for Market Making. Theorems 3.2 and 3.6 provide two complementary ways to quantify the robustness of entropy-regularized objectives under state perturbations: one via a global perturbation of the state-occupancy measures, which is more intrinsic to the overall optimization objective, and the other via a local perturbation of the state transition intensities, which is a more interpretable model-level perturbation. In this section, we illustrate these two robust sets through a CTMDP example arising from dynamic market making problems in finance [7]. In this problem, an agent (market maker) continuously posts bid and ask quotes over time to provide liquidity of a risky asset, while optimally balancing expected trading profits against ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 8

their own inventory risk. The state q ∈ S = {− 10 , −9, . . . , 10 } denotes the agent’s inventory level, and the action δ = ( δ−, δ +) ∈ A = ∆ 20 × ∆20 denotes the bid and ask quotes (relative to the mid-price), chosen from a 20-point uniform discretization of [0 .06 , 0.24] . The state dynamics follows a controlled CTMC in which, given a state–action pair (q, δ ), transitions occur only to adjacent inventory levels q ± 1 with upward rate λδq,q +1 ,θ = 1{q< 10 }Λθ(δ−) and downward rate 

λδq,q −1,θ = 1{q> −10 }Λθ(δ+), where θ = ( αΛ, β Λ) and Λθ(δ) = λR

1+exp( αΛ+βΛδ) for a fixed λR > 0.The instantaneous reward is Rθ(q, δ ) = λδq,q +1 ,θ δ− + λδq,q −1,θ δ+ − γ

2 σ2q2, which combines the expected profit from the earned spread with a quadratic penalty on the inventory level. We take the reference policy μ to be the uniform distribution over A, the discount factor r = 1 , and the initial state distribution ρ = δ{q=0 }.Note that perturbations in the parameter θ affect both the state transition dynamics and the reward function. To illustrate the robustness guarantees, we fix a behavior policy π designed to mimic the inventory-stabilizing structure of the optimal market-making policy, and evaluate the robust sets eCπτ,ε (θ) in (3.4) and Cπτ,ε (θ) in (3.8) for a fixed budget parameter ε = 0 .01 and baseline parameters θ = ( −1, 10) , which are calibrated from real data in [ 3]. Further details are in Appendix C. 100 75 50 25 0 25 50 75 100                                      

> relative change in (%)
> 40
> 20
> 0
> 20
> 40
> relative change in  (%)
> Local: 5.03%
> Global: 0.44%
> = 0.0001
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 9.61%
> Global: 1.24%
> = 0.01
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 11.08%
> Global: 26.77%
> = 1.0
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 11.10%
> Global: 36.55%
> = 10.0
> Robust sets under a heuristic policy, =q= 0 ,r= 1.0, local =global = 0.01

Figure 1. Robust regions for the market making example with r = 1 and 

ε = 0 .01 . Each panel shows simultaneous perturbations of αΛ and βΛ for a fixed 

τ . The green region is the local intensity-based robust set Cπτ,ε (θ), and the purple region is the global occupancy-based robust set eCπτ,ε (θ); the black cross marks the baseline parameter. Figure 1 plots certified robust regions under relative perturbations of θ = ( αΛ, β Λ), with axes showing percentage changes from the baseline θ. The green region is the local intensity-based certificate, the purple region is the global occupancy-based certificate. Both regions expand with 

τ , as predicted by Propositions 3.4 and 3.8. Moreover, as shown in the propositions, when τ ↓ 0,the log-sum-exp criteria become worst-case constraints. The global set is controlled by the largest state–action occupancy/reward distortion, while the local set uses an occupancy-weighted sum of statewise worst action perturbations, making the global certificate more restrictive here. As τ increases, the constraints become averaged perturbation budgets. The global set can then exceed the local set because it measures the realized effect after the policy and dynamics interact. Here, the policy is designed to stabilize inventory near zero, so sizable changes in (αΛ, β Λ) may still leave the discounted occupancy concentrated around central states. Such perturbations can have small global occupancy cost but large local relative-entropy cost. Thus the two certificates are complementary: the local set gives an interpretable model-level condition and is relatively stable across policy and initial-distribution choices, whereas the global set measures the realized change in the controlled process and can be less conservative, but is more sensitive to the policy and initial distribution. Appendix C provides additional numerical comparisons of these robust sets. Appendix E presents an empirical analysis of certificate tightness, demonstrating that the certificate value acts as a continuous predictor of performance degradation and quantifying the conservatism of the certified region. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 9

4. Experimental Results 

We validate the robustness theory on two event-driven control problems: a criss-cross queueing network (presented here) and single-asset market making (Appendix D). Both are implemented as continuous-time Markov jump processes matching the theoretical setting. Appendix F illustrates the benefits of this event-driven implementation over a fixed–time-grid implementation. We evaluate on the criss-cross queueing network described in [ 10 ] with three job classes and two servers. We use the heavy-traffic instance: λ = [1 .5, 1.5, 1.5] , μ = [2 .0, 3.0, 2.0] , h = [3 .0, 1.0, 2.0] ,

Qmax = 50 , T = 50 . The environment is simulated as a continuous-time Markov jump process. At each step, the next event time is sampled from the exponential clock with rate equal to the total arrival and effective service intensity, and the reward accumulated over the inter-event interval is computed exactly. Thus the implementation is event-driven , unlike most contemporary works in the Deep RL literature which utilize a fixed time discretization (see Appendix F for a comparison). We train three classes of neural network policies: (i) πstd trained via policy gradient without entropy regularization, (ii) πτ policies trained using various τ > 0, and (iii) 

πnoisy ϵ-greedy policies derived from the standard policy. Full details on experimental setup are provided in Appendix D. 

Robustness to Dynamics Perturbations. To test robustness, we evaluate each learned policy under perturbed arrival and service intensities λ′ 

> i

= αλλi and μ′ 

> i

= αμμi, where (αλ, α μ) ranges over a grid containing the baseline model (1 , 1) . We report the gain of entropy-regularized policy over the standard deterministic policy. Figure 2 (left panel) shows that the selected policy with temperature τ = 0 .01 improves over πstd on a large part of the perturbation grid. The gains are especially visible away from the baseline model, indicating that the entropy-regularized policy is less sensitive to misspecification of the continuous-time jump intensities. 0.6 0.8 1.0 1.2 1.4 1.6 1.8 2.0              

> Arrival rate scale
> 0.6
> 0.8
> 1.0
> 1.2
> 1.4
> 1.6
> 1.8
> 2.0
> Service rate scale
> Gain: = 0.01 vs std
> 150
> 100
> 50
> 0
> 50
> 100
> 150
> J0.6 0.8 1.0 1.2 1.4 1.6 1.8 2.0
> Arrival rate scale
> 0.6
> 0.8
> 1.0
> 1.2
> 1.4
> 1.6
> 1.8
> 2.0
> Service rate scale
> Gain: = 0.01 vs noise
> 300
> 200
> 100
> 0
> 100
> 200
> 300
> J

Figure 2. Robustness to arrival and service intensity perturbations in the criss-cross queueing network. Left: gain of the entropy-regularized policy over the standard deterministic policy. Right: gain over a noisy standard policy ( ϵ-greedy policy with ϵ = 0 .1). Black lines denote sign changes in ∆J.A natural question is whether the improved robustness is simply due to added randomness. To test this, we compare against an uninformed noisy baseline πnoise = 0 .9πstd + 0 .1 Unif (A) that injects action noise. The right panel of Figure 2 shows that the entropy-regularized policy πτ

outperforms across a broad region of the perturbation grid. This indicates that the gains arise from strategic value-aware randomization induced by entropy regularization, rather than from mere noise injection. Figure 3 depicts a sweep over the regularization temperature. The left panel shows the nominal and worst-case performance of the learned policies over the perturbation grid; small positive values of τ improve worst-case performance relative to πstd , while large values of τ reduce both nominal and worst-case performance. This agrees with the theory: moderate entropy-regularization enlarges the range of perturbations against which the policy is protected, whereas excessive regularization makes the policy too diffuse to control congestion effectively. The right panel of Figure 3 shows the same experiments in terms of gains relative to the standard policy. We observe that the worst-case gain relative to πstd can exceed the corresponding nominal gain. This ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 10 13450 13500 13550 13600 13650 13700 13750 13800 13850                  

> Nominal Performance
> 3460
> 3480
> 3500
> 3520
> 3540
> 3560
> 3580
> 3600
> 3620
> Worst-Case Performance
> =1e-05
> =0.0001
> =0.0005
> =0.001
> =0.005
> =0.01
> =0.1
> =1.0
> =10.0
> Nominal vs Worst-Case ( sweep, queueing)
> sweep
> std
> noise 10 510 410 310 210 110 010 1
> Temperature
> 350
> 300
> 250
> 200
> 150
> 100
> 50
> 0
> 50
> J( ) J( std )
> Gain vs std (sweep, queueing)
> Nominal gain
> Worst-case gain
> noise nominal
> noise worst-case

Figure 3. Effect of the regularization temperature in the criss-cross queueing network. Left: nominal and worst-case performance over the perturbation grid. Right: corresponding gains relative to πstd .indicates that, under adverse intensity perturbations, πstd deteriorates more severely than the entropy-regularized policy. Analogous results for market making experiments are reported in Appendix D, where formal statistical tests confirm significant worst-case improvements for both domains. 5. Conclusion 

We rigorously showed that entropy-regularized control provides robustness guarantees for continuous-time MDPs. For reward perturbations, the entropy-regularized objective admits an exact robust-control interpretation. For joint perturbations of rewards and transition intensities, it optimizes certified lower bounds on worst-case performance over policy-dependent uncertainty sets. We characterized these sets in two complementary ways: globally through discounted occupancy measures and locally through relative entropy rates of transition intensities. Unlike discrete-time formulations, the resulting continuous-time certificates avoid the additional unknown state-distribution entropy term and remain non-degenerate as the action frequency increases. Our experiments support these findings. Both of the queueing-control and market making experiments show that moderate entropy regularization improves robustness to intensity mis-specification over deterministic and noisy baselines. Entropy regularization is not necessarily the ideal robust RL method: it still requires choosing a temperature parameter, and the certified sets are policy-dependent. Nonetheless, our results suggest that entropy regularization is a simple and principled mechanism for obtaining robustness guarantees in continuous-time RL, without introducing an explicit adversary or a separate robust optimization procedure. 

Limitations and Future Work. Several limitations remain in our work. First, our experiments validate the theory, but broader empirical studies on larger-scale benchmarks could study how the robustness gains scale with state and action complexity. Second, our characterized robust sets provide worst-case certificates rather than exact descriptions of all perturbations under which entropy regularization improves performance, and may therefore be conservative. Developing tighter certificates is an important direction for future work. Finally, our analysis focuses on finite spaces; extending the framework to continuous state and action spaces would broaden its applicability. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 11 

Acknowledgments 

JC was supported by the EPSRC Centre for Doctoral Training in Mathematical Modelling, Analysis and Computation (MAC-MIGS) funded by the UK Engineering and Physical Sciences Research Council (grant EP/S023291/1). YZ was funded in part by JPMorgan Chase & Co. Any views or opinions expressed herein are solely those of the authors listed, and may differ from the views and opinions expressed by JPMorgan Chase & Co. or its affiliates. This material is not a product of the Research Department of J.P. Morgan Securities LLC. This material does not constitute a solicitation or offer in any jurisdiction. 

Disclaimer 

This paper was prepared for informational purposes in part by the Quantitative Trading & Research Group of JPMorganChase & Co. This paper is not a product of the Research Department of JPMorganChase & Co. or its affiliates. Neither JPMorganChase & Co. nor any of its affiliates makes any explicit or implied representation or warranty and none of them accept any liability in connection with this paper, including, without limitation, with respect to the completeness, accuracy, or reliability of the information contained herein and the potential legal, compliance, tax, or accounting effects thereof. This document is not intended as investment research or investment advice, or as a recommendation, offer, or solicitation for the purchase or sale of any security, financial instrument, financial product or service, or to be used in any way for evaluating the merits of participating in any transaction, and shall not constitute a solicitation under any jurisdiction or to any person, if such solicitation under such jurisdiction or to such person would be unlawful. 

References 

[1] Elie Aljalbout, Jiaxu Xing, Angel Romero, Iretiayo Akinola, Caelan Reed Garrett, Eric Heiden, Abhishek Gupta, Tucker Hermans, Yashraj Narang, Dieter Fox, Davide Scaramuzza, and Fabio Ramos. The reality gap in robotics: Challenges, solutions, and best practices. Annual Review of Control, Robotics, and Autonomous Systems , 9:403–432, 2026. [2] Yonatan Ashlag, Uri Koren, Mirco Mutti, Esther Derman, Pierre-Luc Bacon, and Shie Mannor. State entropy regularization for robust reinforcement learning. In Advances in Neural Information Processing Systems ,volume 38, 2025. [3] Alexander Barzykin, Philippe Bergault, and Olivier Guéant. Algorithmic market making in dealer markets with hedging and market impact. Mathematical Finance , 33(1):41–79, 2023. [4] Lijun Bo, Yijie Huang, Xiang Yu, and Tingting Zhang. Continuous-time q-learning in jump-diffusion models under Tsallis entropy, 2024. [5] Rob Brekelmans, Tim Genewein, Jordi Grau-Moya, Grégoire Delétang, Markus Kunesch, Shane Legg, and Pedro A. Ortega. Your policy regularizer is secretly an adversary. Transactions on Machine Learning Research ,2022. [6] Alexander Bukharin, Yan Li, Yue Yu, Qingru Zhang, Zhehui Chen, Simiao Zuo, Chao Zhang, Songan Zhang, and Tuo Zhao. Robust multi-agent reinforcement learning via adversarial regularization: Theoretical foundation and stable algorithms. In Advances in Neural Information Processing Systems , volume 36, pages 68121–68133, 2023. [7] Álvaro Cartea, Sebastian Jaimungal, and José Penalva. Algorithmic and High-Frequency Trading . Cambridge University Press, 2015. [8] Ziyi Chen, Yan Wen, Zhengmian Hu, and Heng Huang. Robust reinforcement learning with general utility. In 

Advances in Neural Information Processing Systems , volume 37, pages 11290–11344, 2024. [9] Ziheng Cheng, Xin Guo, and Yufei Zhang. Deterministic policy gradient for reinforcement learning with continuous time and state, 2026. [10] Jim G. Dai and Mark Gluzman. Queueing network controls via deep reinforcement learning. Stochastic Systems , 12(1):30–67, 2022. [11] Esther Derman, Matthieu Geist, and Shie Mannor. Twice regularized MDPs and the equivalence between robustness and regularization. Advances in Neural Information Processing Systems , 34:22274–22287, 2021. [12] Esther Derman, Yevgeniy Men, Matthieu Geist, and Shie Mannor. Robustness and regularization in rein-forcement learning. In NeurIPS 2023 Workshop on Generalization in Planning , 2023. [13] Jodi Dianetti, Roxana Dumitrescu, Giorgio Ferrari, and Renyuan Xu. Entropy regularization in mean-field games of optimal stopping. arXiv preprint arXiv:2509.18821 , 2025. [14] Monroe D. Donsker and S. R. Srinivasa Varadhan. Asymptotic evaluation of certain Markov process expecta-tions for large time—iii. Communications on Pure and Applied Mathematics , 29(4):389–461, 1976. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 12 

[15] Benjamin Eysenbach and Sergey Levine. Maximum entropy RL (provably) solves some robust RL problems. In International Conference on Learning Representations , 2022. [16] Noufel Frikha, Maximilien Germain, Mathieu Laurière, Huyên Pham, and Xuanye Song. Actor-critic learning for mean-field control in continuous time. Journal of Machine Learning Research , 26(127):1–42, 2025. [17] Xuefeng Gao, Lingfei Li, and Xun Yu Zhou. Reinforcement learning for jump-diffusions, with financial applications. Mathematical Finance , 2026. Early View. [18] Matthieu Geist, Bruno Scherrer, and Olivier Pietquin. A theory of regularized Markov decision processes. In 

Proceedings of the 36th International Conference on Machine Learning , volume 97 of Proceedings of Machine Learning Research , pages 2160–2169, 2019. [19] Michael Giegrich, Christoph Reisinger, and Yufei Zhang. Convergence of policy gradient methods for finite-horizon exploratory linear-quadratic control problems. SIAM Journal on Control and Optimization ,62(2):1060–1092, 2024. [20] Julien Grand-Clément and Christian Kroer. Scalable first-order methods for robust MDPs. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 35, pages 12086–12094, 2021. [21] Xianping Guo and Onésimo Hernández-Lerma. Continuous-time Markov decision processes. In Continuous-Time Markov Decision Processes: Theory and Applications , pages 9–18. Springer, 2009. [22] Xin Guo, Renyuan Xu, and Thaleia Zariphopoulou. Entropy regularization for mean field games with learning. 

Mathematics of Operations Research , 47(4):3239–3260, 2022. [23] Tuomas Haarnoja, Aurick Zhou, Kristian Hartikainen, George Tucker, Sehoon Ha, Jie Tan, Vikash Kumar, Henry Zhu, Abhishek Gupta, Pieter Abbeel, and Sergey Levine. Soft actor-critic algorithms and applications. 

arXiv preprint arXiv:1812.05905 , abs/1812.05905, 2018. [24] Yijie Huang, Mengge Li, Xiang Yu, and Zhou Zhou. Continuous-time reinforcement learning for optimal switching over multiple regimes, 2025. [25] Hisham Husain, Kamil Ciosek, and Ryota Tomioka. Regularized policies are reward robust. In International Conference on Artificial Intelligence and Statistics , pages 64–72. PMLR, 2021. [26] Garud N. Iyengar. Robust dynamic programming. Mathematics of Operations Research , 30(2):257–280, 2005. [27] Yanwei Jia. Continuous-time risk-sensitive reinforcement learning via quadratic variation penalty. Applied Mathematics & Optimization , 93(2):58, 2026. [28] Yanwei Jia, Du Ouyang, and Yufei Zhang. Accuracy of discretely sampled stochastic policies in continuous-time reinforcement learning. arXiv preprint arXiv:2503.09981 , 2025. [29] Yanwei Jia and Xun Yu Zhou. Policy evaluation and temporal-difference learning in continuous time and space: A martingale approach. Journal of Machine Learning Research , 23(154):1–55, 2022. [30] Yanwei Jia and Xun Yu Zhou. Policy gradient and actor-critic learning in continuous time and space: Theory and algorithms. Journal of Machine Learning Research , 23(275):1–50, 2022. [31] Yanwei Jia and Xun Yu Zhou. q-learning in continuous time. Journal of Machine Learning Research ,24(161):1–61, 2023. [32] Bekzhan Kerimkulov, James-Michael Leahy, David Siska, Lukasz Szpruch, and Yufei Zhang. A Fisher–Rao gradient flow for entropy-regularised Markov decision processes in Polish spaces. Foundations of Computational Mathematics , pages 1–75, 2025. [33] Navdeep Kumar, Esther Derman, Matthieu Geist, Kfir Levy, and Shie Mannor. Policy gradient for rectangular robust Markov decision processes. In Advances in Neural Information Processing Systems , volume 36, 2023. [34] Guanghui Lan. Policy mirror descent for reinforcement learning: Linear convergence, new sampling complexity, and generalized problem classes. Mathematical Programming , 198:1059–1106, 2023. [35] Mengmeng Li, Daniel Kuhn, and Tobias Sutter. Policy gradient algorithms for robust MDPs with nonrectan-gular uncertainty sets. SIAM Journal on Optimization , 36(1):120–151, 2026. [36] Yongyuan Liang, Yanchao Sun, Ruijie Zheng, and Furong Huang. Efficient adversarial training without attacking: Worst-case-aware robust reinforcement learning. Advances in Neural Information Processing Systems , 35:22547–22561, 2022. [37] Shiau Hong Lim, Huan Xu, and Shie Mannor. Reinforcement learning in robust Markov decision processes. In Advances in Neural Information Processing Systems , volume 26, 2013. [38] Michael Lutter, Shie Mannor, Jan Peters, Dieter Fox, and Animesh Garg. Robust value iteration for continuous control tasks. In Robotics: Science and Systems , 2021. [39] Huiling Meng, Ningyuan Chen, and Xuefeng Gao. Reinforcement learning for intensity control: An application to choice-based network revenue management. arXiv preprint arXiv:2406.05358 , 2024. [40] Jun Morimoto and Kenji Doya. Robust reinforcement learning. Neural Computation , 17(2):335–359, 2005. [41] Arnab Nilim and Laurent El Ghaoui. Robust control of Markov decision processes with uncertain transition matrices. Operations Research , 53(5):780–798, 2005. [42] Bernt Øksendal and Agnès Sulem. Applied Stochastic Control of Jump Diffusions , volume 3. Springer, 2007. [43] Manfred Opper and Guido Sanguinetti. Variational inference for Markov jump processes. Advances in Neural Information Processing Systems , 20, 2007. [44] Takayuki Osogami. Robustness and risk-sensitivity in Markov decision processes. In Advances in Neural Information Processing Systems , volume 25, 2012. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 13 

[45] Xue Bin Peng, Marcin Andrychowicz, Wojciech Zaremba, and Pieter Abbeel. Sim-to-real transfer of robotic control with dynamics randomization. In 2018 IEEE International Conference on Robotics and Automation (ICRA) , pages 3803–3810. IEEE, 2018. [46] Adolfo Perrusquía and Wen Yu. Continuous-time reinforcement learning for robust control under worst-case uncertainty. International Journal of Systems Science , 52(4):770–784, 2021. [47] Lerrel Pinto, James Davidson, Rahul Sukthankar, and Abhinav Gupta. Robust adversarial reinforcement learning. In International Conference on Machine Learning , 2017. [48] Martin L. Puterman. Markov Decision Processes: Discrete Stochastic Dynamic Programming . Wiley, 1994. [49] Konrad Rawlik, Marc Toussaint, and Sethu Vijayakumar. On stochastic optimal control and reinforcement learning by approximate inference. In Proceedings of the Twenty-Third International Joint Conference on Artificial Intelligence , pages 3052–3056, 2013. [50] Christoph Reisinger and Yufei Zhang. Regularity and stability of feedback relaxed controls. SIAM Journal on Control and Optimization , 59(5):3118–3151, 2021. [51] Zhenjie Ren, Xiaoli Wei, Xiang Yu, and Xun Yu Zhou. Continuous-time q-learning for mean-field control with common noise, part-I: Theoretical foundations, 2026. [52] Zhenjie Ren, Xiaoli Wei, Xiang Yu, and Xun Yu Zhou. Continuous-time q-learning for mean-field control with common noise, part-II: q-learning algorithms, 2026. [53] John Schulman, Sergey Levine, Pieter Abbeel, Michael Jordan, and Philipp Moritz. Trust region policy optimization. In International Conference on Machine Learning , pages 1889–1897. PMLR, 2015. [54] John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov. Proximal policy optimization algorithms. arXiv preprint arXiv:1707.06347 , 2017. [55] Lukasz Szpruch, Tanut Treetanthiploet, and Yufei Zhang. Optimal scheduling of entropy regularizer for continuous-time linear-quadratic reinforcement learning. SIAM Journal on Control and Optimization ,62(1):135–166, 2024. [56] Chen Tessler, Yonathan Efroni, and Shie Mannor. Action robust reinforcement learning and applications in continuous control. In International Conference on Machine Learning , pages 6215–6224. PMLR, 2019. [57] Josh Tobin, Rachel Fong, Alex Ray, Jonas Schneider, Wojciech Zaremba, and Pieter Abbeel. Domain randomization for transferring deep neural networks from simulation to the real world. In 2017 IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) , pages 23–30. IEEE, 2017. [58] Emanuel Todorov. Linearly-solvable Markov decision problems. In Advances in Neural Information Processing Systems , volume 19, pages 1369–1376, 2006. [59] Qiuhao Wang, Chin Pang Ho, and Marek Petrik. Policy gradient in robust MDPs with global convergence guarantee. In Proceedings of the 40th International Conference on Machine Learning , volume 202 of Proceedings of Machine Learning Research , pages 35763–35797. PMLR, 2023. [60] Xiaoli Wei and Xiang Yu. Continuous time q-learning for mean-field control problems. Applied Mathematics & Optimization , 91(1):10, 2025. [61] Wolfram Wiesemann, Daniel Kuhn, and Berç Rustem. Robust Markov decision processes. Mathematics of Operations Research , 38(1):153–183, 2013. [62] Minghui Zhang, Xun Li, and Xin Zhang. Continuous-time q-learning for Markov regime switching system under Tsallis entropy, 2026. [63] Hanyang Zhao, Wenpin Tang, and David Yao. Policy optimization for continuous reinforcement learning. 

Advances in Neural Information Processing Systems , 36:13637–13663, 2023. [64] Ruida Zhou, Tao Liu, Min Cheng, Dileep Kalathil, Panganamala R. Kumar, and Chao Tian. Natural actor-critic for robust reinforcement learning with function approximation. In Advances in Neural Information Processing Systems , volume 36, 2023. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 14 

Appendix A. Extended Related Work 

Continuous-time RL with entropy regularization. Entropy regularization is widely used in continuous-time RL to encourage exploration, smooth policy optimization, and mitigate instabilities from time discretization. It underpins algorithm designs for controlled diffusions, including policy evaluation and TD [ 29 ], policy -gradient and actor–critic methods [ 30 , 19 ], Q-learning [ 31 ], and TRPO/PPO [ 63 ]. The framework has recently been extended beyond pure diffusions to jump -diffusions[ 17 , 4], point processes[ 39 ], and regime -switching systems[ 24 , 62 ], and to risk-aware objective [ 27 ]. Parallel efforts integrate entropy regularization into mean -field control and games [22, 16, 60, 51, 52, 13]. Our work focuses on continuous-time pure jump processes and develops efficient event-driven algorithms tailored to this setting. Indeed, when the state contains a diffusion component, the system evolves continuously, and existing algorithms typically require extremely fine time discretizations to sample actions accurately, resulting in substantial computational costs [ 55 , 17 ,28 ]. In contrast, our continuous-time MDP framework evolves at random Poisson event times, which provide an intrinsic event clock and eliminate the need for either a fixed sampling grid or an artificially imposed action frequency. This feature constitutes a key advantage of our approach. As demonstrated empirically in Appendix F, the event-based formulation removes the sensitive time-step hyperparameter, avoids the fundamental bias–variance trade-off inherent in grid-based methods, and ensures that computational costs scale naturally with the underlying event rate. Similar observations have been made for specific intensity-control models [39]. Moreover, despite the rapid development of RL algorithms, there is currently no theoretical work establishing that entropy regularization itself provides robustness guarantees in continuous-time RL. The closest related works are [ 50 , 27 ]. In particular, [ 50 ] shows that optimal entropy-regularized policies depend continuously on model perturbations, while [ 27 ] introduces a risk-sensitive formulation to enforce robustness explicitly. In contrast, our paper provides the first theoretical robustness guarantees arising directly from entropy regularization in continuous-time RL. We show that robustness emerges naturally by incorporating policy entropy into the objective function, as is commonly done in the RL literature, and we explicitly characterize the corresponding sets of admissible model perturbations. 

Appendix B. Proofs 

This appendix contains the full proofs of all theoretical results. B.1. Equivalence of the arrival-driven model and CTMDP. We show that the state process generated by the arrival-driven sampling model has the same law as the aggregated CTMDP under the following structural assumption: for each state s ∈ S , there exists a state-dependent decision rate λs > 0 such that, for every a ∈ A s and s′̸ = s, λass ′ = λsp(s′ | s, a ), where 

p(· | s, a ) ∈ P (S). Accordingly, λass = − P  

> s′̸=s

λass ′ = −λs

 1 − p(s | s, a ).Fix π ∈ P (A | S ). In the arrival-driven sampling model, at a decision epoch Tn with current state STn = s, an action An ∼ π(· | s) is sampled and then held fixed on [Tn, T n+1 ). Since the event rate at state s is λs and does not depend on the chosen action, the holding time 

ζn := Tn+1 − Tn satisfies ζn | STn = s ∼ Exp (λs). Moreover, conditional on (STn , A n) = ( s, a ),the post-event state satisfies 

P(STn+1 = s′ | STn = s, A n = a) = p(s′ | s, a ), s′ ∈ S .

Averaging over An ∼ π(· | s) yields 

P(STn+1 = s′ | STn = s) = X

> a∈A s

π(a|s) p(s′ | s, a ) =: P π(s, s ′) .

Therefore, conditional on the current state s, the next event time is exponential with rate 

λs, and the post-event state is drawn from the kernel P π(s, ·). Hence the state process (St)t≥0ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 15 

generated by the arrival-driven sampling model is a CTMC with off-diagonal transition rates 

λsP π(s, s ′) = λs

X

> a∈A s

π(a|s) p(s′ | s, a ) = X

> a∈A s

π(a|s)λass ′ = λπss ′ , s′̸ = s . 

Its diagonal entries are therefore also given by λπss = − P  

> s′̸=s

λπss ′ . Thus the state process has generator λπ = ( λπss ′ )s,s ′∈S , which is exactly the generator of the aggregated CTMDP under policy π. Since both processes also start from the same initial distribution ρ, they induce the same law on the state process. In particular, they yield the same objective function. B.2. Proof of Theorem 3.1. 

Proof. For brevity, write R := Rθ, eR := R˜θ and ¯dπρ (s) := ¯dπρ,θ (s). We assume ˜θ ∈ Θ( θ) only perturbs reward and leaves the transition dynamics unchanged, thus Pπ 

> ˜θ

= Pπθ . Then, for every 

˜θ ∈ Θ( θ),

J(π; ˜θ) := EPπθ

> ρ

 Z ∞

> 0

e−rt X

> a∈A St

π(a|St) eR(St, a ) d t



= 1

r

X

> s∈S

¯dπρ (s) X

> a∈A s

π(a|s) eR(s, a ).

Define the reward-only robust set by (B.1) bRπτ,ε (θ) := 

n bR : S × A → R τ log 

h X 

> s∈S

¯dπρ (s) X

> a∈A s

μ(a|s) exp 

 R(s, a ) − bR(s, a )

τ

i 

≤ ε

o

,

where ε ∈ R, and consider the auxiliary optimization problem (B.2) Jrew := inf  

> bR∈bRπτ,ε (θ)

1

r

X

> s∈S

¯dπρ (s) X

> a∈A s

π(a|s) bR(s, a ).

The constraint is equivalent to 

X

> s∈S

¯dπρ (s) X

> a∈A s

μ(a|s) exp 

 R(s, a ) − bR(s, a )

τ



≤ exp 

 ετ



.

We compute Jrew by Lagrange duality. Let λ ≥ 0 be the Lagrange multiplier. The Lagrangian is 

L( bR, λ ) = 1

r

X

> s∈S

¯dπρ (s) X

> a∈A s

π(a|s) bR(s, a ) + λ

 X

> s∈S

¯dπρ (s) X

> a∈A s

μ(a|s) exp 

 R(s, a ) − bR(s, a )

τ



− exp 

 ετ



.

For each fixed λ > 0, the map bR 7 → L( bR, λ ) is strictly convex. Moreover, Slater’s condition holds: taking bR = R + c with c > 0 sufficiently large gives 

X

> s∈S

¯dπρ (s) X

> a∈A s

μ(a|s) exp 

 R(s, a ) − bR(s, a )

τ



= e−c/τ < exp 

 ετ



.

Hence strong duality applies. Moreover, any dual maximizer must satisfy λ∗ > 0, since for λ = 0 

we have inf bR L( bR, 0) = −∞ . Fix λ > 0. Differentiating L with respect to bR(s, a ) gives 

∂L ∂ bR(s, a ) = 1

r ¯dπρ (s) π(a|s) − λτ ¯dπρ (s) μ(a|s) exp 

 R(s, a ) − bR(s, a )

τ



.

Solving this derivative to zero yields 

exp 

 R(s, a ) − bR∗(s, a )

τ



= τrλ π(a|s)

μ(a|s) .

Therefore 

bR∗(s, a ) = R(s, a ) − τ log π(a|s)

μ(a|s) − τ log τrλ .

Since λ∗ > 0, complementary slackness implies that the constraint is active at the optimum: 

X

> s∈S

¯dπρ (s) X

> a∈A s

μ(a|s) exp 

 R(s, a ) − bR∗(s, a )

τ



= exp 

 ετ



.ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 16 

Substituting the expression for bR∗ gives 

exp 

 ετ



= τrλ 

X

> s∈S

¯dπρ (s) X

> a∈A s

π(a|s) = τrλ ,

where we used P 

> a∈A s

π(a|s) = 1 and P 

> s∈S

¯dπρ (s) = 1 . Hence 

λ∗ = τr exp 



− ετ



.

Consequently, (B.3) bR∗(s, a ) = R(s, a ) − τ log π(a|s)

μ(a|s) − ε. 

Substituting (B.3) into (B.2) gives 

Jrew = 1

r

X

> s∈S

¯dπρ (s)

 X

> a∈A s

π(a|s) R(s, a ) − τ KL( π∥μ)( s) − ε



= Jτ (π; θ) − εr .

Now define the parameterized robust set by 

bCπτ,ε (θ) := 

n˜θ R˜θ ∈ bRπτ,ε (θ)

o

.

Then 

n

R˜θ ˜θ ∈ bCπτ,ε (θ)

o

⊆ bRπτ,ε (θ). Therefore, 

inf  

> ˜θ∈bCπτ,ε (θ)

J(π; ˜θ) = inf  

> ˜θ∈bCπτ,ε (θ)

1

r

X

> s∈S

¯dπρ (s) X

> a∈A s

π(a|s) R˜θ(s, a )

≥ inf  

> bR∈bRπτ,ε (θ)

1

r

X

> s∈S

¯dπρ (s) X

> a∈A s

π(a|s) bR(s, a ) = Jτ (π; θ) − εr .

This proves the reward-robust lower bound. Moreover, suppose there exists ˜θ∗ ∈ Θ( θ) such that 

R˜θ∗ (s, a ) = Rθ(s, a ) − τ log π(a|s)

μ(a|s) − ε, s ∈ S , a ∈ A s.

Then, by (B.3), 

τ log 

" X

> s∈S

¯dπρ (s) X

> a∈A s

μ(a|s) exp 

 R(s, a ) − R˜θ∗ (s, a )

τ

#

= τ log 

"

exp 

 ετ

 X 

> s∈S

¯dπρ (s) X

> a∈A s

π(a|s)

#

= ε. 

Hence ˜θ∗ ∈ bCπτ,ε (θ) and 

inf  

> ˜θ∈bCπτ,ε (θ)

J(π; ˜θ) = Jτ (π; θ) − εr ,

with ˜θ∗ a minimizer. □

B.3. Proof of Theorem 3.2. 

Proof. Fix π ∈ Π. For ϑ ∈ { θ, ˜θ}, recall the discounted state occupancy measure and define the corresponding state–action occupancy measure by 

dπρ,ϑ (s) := EPπϑ

> ρ

 Z ∞

> 0

e−rt 1{St=s} dt



, νπρ,ϑ (s, a ) := EPπϑ

> ρ

 Z ∞

> 0

e−rt 1{St=s,A t=a} dt



,

and the normalized occupancies by ¯dπρ,ϑ (s) := r dπρ,ϑ (s) and ¯νπρ,ϑ (s, a ) := r ν πρ,ϑ (s, a ). We have 

¯νπρ,ϑ (s, a ) = ¯dπρ,ϑ (s)π(a | s), and the objective can be rewritten as 

Jπ(ϑ) := EPπϑ

> ρ

 Z ∞

> 0

e−rt Rϑ(St, A t) d t



= X

> s,a

νπρ,ϑ (s, a )Rϑ(s, a ) = 1

r

X

> s,a

¯νπρ,ϑ (s, a )Rϑ(s, a ) .ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 17 

To evaluate the objective under a perturbed parameter ˜θ ∈ Θ, we restrict the summation to the support of the baseline occupancy measure. Let Xθ := {(s, a ) : ¯νπρ,θ (s, a ) > 0}. Since the occupancies are non-negative and R˜θ(s, a ) > 0, hence 

Jπ(˜θ) ≥ 1

r

X

> (s,a )∈X θ

¯νπρ,θ (s, a )¯νπρ, ˜θ(s, a )¯νπρ,θ (s, a ) R˜θ(s, a ) .

Because ¯νπρ,θ (s, a ) = 0 for all (s, a ) /∈ X θ, we have P 

> (s,a )∈X θ

¯νπρ,θ (s, a ) = 1 . Thus, applying Jensen’s inequality to the concave logarithm yields (B.4) log Jπ(˜θ) ≥ − log r + X

> (s,a )∈X θ

¯νπρ,θ (s, a ) log 

 ¯νπρ, ˜θ (s,a )¯νπρ,θ (s,a ) R˜θ(s, a )



.

We think of R(s, a ) := log  Rθ(s, a ) as the nominal reward. For (s, a ) ∈ X θ, we define the adversarial reward as bR(s, a ) := log   ¯νπρ, ˜θ (s,a )¯νπρ,θ (s,a ) R˜θ(s, a ). If ¯νπρ, ˜θ(s, a ) = 0 , we take bR(s, a ) = −∞ ,in which case the subsequent lower bounds hold trivially. For (s, a ) ∈ X θ, using ¯νπρ,θ (s, a ) = ¯dπρ,θ (s)π(a | s) we obtain 

R(s, a ) − bR(s, a ) = log  Rθ(s, a ) − log 

 ¯dπρ, ˜θ (s)¯dπρ,θ (s) R˜θ(s, a )



= log dπρ,θ (s)dπρ, ˜θ (s) − log R˜θ (s,a )  

> Rθ(s,a )

.

Therefore, by the definition of the robust set eCπτ,ε (3.4) , ˜θ ∈ eCπτ,ε (θ) if and only if the perturbed reward bR(s, a ) belongs to the reward-only robust set bRπτ,ε (B.1) centered at R = log Rθ. Adopting the standard convention that 0 · f (x) = 0 for undefined or infinite f (x), we can evaluate the summation over the entire state–action space S × A s. Applying Theorem 3.1 to R and bR gives: 

inf  

> ˜θ∈eCπτ,ε (θ)

X

> s∈S

X

> a∈A s

¯νπρ,θ (s, a ) bR(s, a ) ≥ X

> s∈S

X

> a∈A s

¯νπρ,θ (s, a )



log Rθ(s, a ) − τ log π(a | s)

μ(a | s)



− ε. (B.5) Combining (B.4) and (B.5) yields 

inf  

> ˜θ∈eCπτ,ε (θ)

log Jπ(˜θ) ≥ − log r + X

> s∈S

X

> a∈A s

¯νπρ,θ (s, a )



log Rθ(s, a ) − τ log π(a | s)

μ(a | s)



− ε

= − log r + X

> s∈S

¯dπρ,θ (s)

 X

> a∈A s

π(a | s) log Rθ(s, a ) − τ KL( π∥μ)( s)



− ε. 

Since the logarithm is increasing, and using ¯dπρ,θ (s) = rdπρ,θ (s), we obtain 

inf  

> ˜θ∈eCπτ,ε (θ)

Jπ(˜θ) ≥ exp 



r X

> s∈S

dπρ,θ (s)

 X

> a∈A s

π(a | s) log Rθ(s, a ) − τ KL( π∥μ)( s)



− log r − ε



,

which completes the proof. □

B.4. Robustness Guarantees under Uniform Distributions. Proposition B.1. Fix π ∈ Π, τ > 0 and ε ∈ R. Assume that As = A for all s and μ(· | s) is uniform on A, then 

inf   

> ˜θ∈eCπ,sa τ,ε (θ)

EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt R˜θ (St, A t) d t



≥ exp 



r EPπθ

> ρ

 Z ∞

> 0

e−rt 

log Rθ (St, A t) − τ KL( ¯dπρ,θ ∥ϑS ) − τ KL( π∥μ)( St)



dt



− ε − log r



,

where ϑS is the uniform distribution on S, and the robust set eCπ,sa τ,ε (θ) is defined as 

(B.6) eCπ, sa  

> τ,ε

(θ) := 

(

˜θ ∈ Θ τ log 

 1

|S||A| 

X

> (s,a )∈X

exp 

 log ¯dπρ,θ (s)¯dπρ, ˜θ (s) − log R˜θ (s,a ) 

> Rθ(s,a )

τ

 

≤ ε

)

.ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 18 

Proof. Since As = A for all s, write X := S × A . Recall that the normalized discounted state–action occupancy measure is given by ¯νπρ,θ (s, a ) = ¯dπρ,θ (s)π(a|s). For later use, we denote this measure by ¯νπ for simplicity. We define ¯νμ(s, a ) := ¯dπρ,θ (s)μ(a|s), and let 

u(s, a ) := 1

|S||A| = ϑS (s)μ(a|s), (s, a ) ∈ X ,

where μ(· | s) is uniform on A and ϑS is uniform on S. For each ˜θ, define 

X˜θ(s, a ) := log ¯dπρ,θ (s)¯dπρ, ˜θ(s) − log R˜θ(s, a )

Rθ(s, a ) .

The robust set in Theorem 3.2 uses the reference measure ¯νμ. Changing the reference measure to any probability measure ¯νr with compatible support amounts to 

τ log 

 X

> s,a

¯νr(s, a ) exp 

 X˜θ(s, a )

τ

 

= τ log 

 X

> s,a

¯νμ(s, a ) exp 

 X˜θ(s, a ) + τ log ¯νr (s,a )¯νμ(s,a )

τ

 

.

Thus Theorem 3.2 applies with the reference measure ¯νr by replacing the entropy penalty 

KL(¯ νπ∥¯νμ) by KL(¯ νπ∥¯νr). Taking ¯νr = u gives the robust set 

eCπ, sa  

> τ,ε

(θ) = 

(

˜θ ∈ Θ τ log 

 1

|S||A| 

X

> s∈S

X

> a∈A

exp 

 X˜θ (s,a )

> τ

 

≤ ε

)

.

Therefore, by Theorem 3.2 with the uniform state–action reference measure u,

inf   

> ˜θ∈eCπ, sa
> τ,ε (θ)

EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt X

> a

π(a|St)R˜θ(St, a ) d t



≥ exp 



r EPπθ

> ρ

 Z ∞

> 0

e−rt X

> a

π(a|St) log Rθ(St, a ) d t



− τ KL( νπ∥u) − ε − log r



.

(B.7) Now we decompose KL ( ¯νπ∥u). Since ¯νπ(s, a ) = ¯dπρ,θ (s)π(a|s) and u(s, a ) = ϑS (s)μ(a|s), the chain rule for relative entropy gives 

KL(¯ νπ∥u) = KL  ¯dπρ,θ ϑS

 + X

> s∈S

¯dπρ,θ (s)KL( π∥μ)( s)= KL  ¯dπρ,θ ϑS

 + r EPπθ

> ρ

 Z ∞

> 0

e−rt KL( π∥μ)( St) d t



.

(B.8) Substituting (B.8) into (B.7) proves the claim. □

B.5. Proof of Proposition 3.4. 

Proof. Fix π ∈ Π and ˜θ ∈ Θ. Recall that ¯νμ(s, a ) := ¯dπρ,θ (s)μ(a|s) for any s ∈ S , a ∈ A s. For brevity we write f (s, a ) := fθ, ˜θ(s, a ) and Φτ ( ˜θ) := τ log P

> s∈S

P 

> a∈A s

¯νμ(s, a ) exp 

 f (s,a )

> τ



. Then, by definition (3.4), eCπτ,ε (θ) = {˜θ ∈ Θ : Φ τ (˜θ) ≤ ε}.We first prove monotonicity. Let Zτ := P

> s∈S

P 

> a∈A s

¯νμ(s, a )ef (s,a )/τ and define the normalized exponential weights qτ (s, a ) := ¯νμ(s,a )ef (s,a )/τ  

> Zτ

. Differentiating Φτ (˜θ) = τ log Zτ gives 

ddτ Φτ (˜θ) = log Zτ − 1

τ

X

> s∈S

X

> a∈A s

qτ (s, a )f (s, a ).

Moreover, 

KL( qτ ∥¯νμ) = X

> s∈S

X

> a∈A s

qτ (s, a ) log qτ (s, a )¯νμ(s, a ) = 1

τ

X

> s∈S

X

> a∈A s

qτ (s, a )f (s, a ) − log Zτ .

Hence ddτ Φτ ( ˜θ) = −KL (qτ ∥¯νμ) ≤ 0, implying that Φτ ( ˜θ) is nonincreasing in τ . Therefore, for 

0 < τ 1 ≤ τ2, we have Φτ2 (˜θ) ≤ Φτ1 (˜θ), which proves eCπτ1,ε (θ) ⊆ eCπτ2,ε (θ).ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 19 

We next identify the limiting sets. Since S and As are finite, the log-sum-exp limit gives 

lim   

> τ→0

Φτ (˜θ) = max 

> s∈S , a ∈A s
> ¯νμ(s,a )>0

fθ, ˜θ(s, a ).

Since a ∈ A s = supp μ(· | s), this is equivalent to the condition 

fθ, ˜θ(s, a ) ≤ ε, for all a ∈ A s and ¯dπρ,θ -a.s. s ∈ S .Therefore, \

> τ > 0

eCπτ,ε (θ) = 

n˜θ ∈ Θ fθ, ˜θ(s, a ) ≤ ε, ∀a ∈ A s, ¯dπρ,θ -a.s. s ∈ S 

o

.

Similarly, as τ → ∞ , ef (s,a )/τ = 1 + f (s,a ) 

> τ

+ o  1

> τ

, uniformly over (s, a ). Hence 

X

> s,a

¯νμ(s, a )ef (s,a )/τ = 1 + 1

τ

X

> s,a

¯νμ(s, a )f (s, a ) + o  1

> τ

.

Using log(1 + x) = x + o(x) gives 

lim   

> τ→∞

Φτ (˜θ) = X

> s∈S

¯dπρ,θ (s) X

> a∈A s

μ(a|s)fθ, ˜θ(s, a ).

Thus the limiting robust set as τ → ∞ is 

eCπ

> ∞,ε

(θ) = 

n˜θ ∈ Θ X

> s∈S

¯dπρ,θ (s) X

> a∈A s

μ(a|s)fθ, ˜θ(s, a ) ≤ ε

o

.

Since the family { eCπτ,ε (θ)}τ > 0 is nondecreasing in τ , its lower endpoint is the intersection over 

τ > 0 and its upper endpoint is the limiting union as τ → ∞ . This completes the proof. 

□

B.6. Proof of Theorem 3.6. Given a policy π ∈ Π and state s ∈ S , similarly to (3.5) , we define the policy-wise local relative entropy by 

ℓ(λπθ , λ π 

> ˜θ

)( s) := X 

> s′̸=s

h

λπss ′,θ log λπss ′,θ 

λπss ′,˜θ

− λπss ′,θ + λπss ′,˜θ

i

,(B.9) The following lemma shows that the policy-wise local relative entropy is bounded above by the 

π-weighted average of the corresponding action-wise local relative entropy. 

Lemma B.2. Fix a state s ∈ S and a stationary Markov policy π(·| s) ∈ P (A). Assume that for every s′̸ = s and every action a ∈ A , λass ′,θ > 0 implies λass ′,˜θ > 0. Then 

ℓ(λπθ , λ π 

> ˜θ

)( s) ≤ X

> a∈A

π(a|s) ℓ λaθ , λ a

> ˜θ

(s) .

Proof. Define f (x, y ) := x log (x/y ) − x + y with x, y > 0 having the same support. Notice that the Hessian matrix of f is 

Hf =

" 1 

> x

− 1

> y

− 1

> yxy2

#

.

The Hessian matrix is positive semidefinite on the domain, implying that (x, y ) 7 → f (x, y ) is jointly convex. Hence given s′̸ = s and a, let x = λass ′,θ and y = λass ′,˜θ. Apply Jensen’s inequality under π(·| s):

f

 X 

> a

π(a|s)λass ′,θ , X

> a

π(a|s)λass ′,˜θ



≤ X

> a

π(a|s) f  λass ′,θ , λ ass ′,˜θ

 .

Summing over s′̸ = s yields the claim. □

Another lemma we need is the so-called Donsker–Varadhan variational formula, see [ 14 ]. We provide a proof for reader’s convenience. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 20 

Lemma B.3. Let A be a finite action set, μ ∈ P (A) be a fixed reference probability measure and 

τ > 0. Define 

φ(f ) := τ log 

 X 

> a

μ(a)e f (a)

> τ



, f : A → R .

For any π ∈ P (A) with π ≪ μ,

−τ KL( π∥μ) = inf 

> f

n

− X

> a

f (a)π(a) + φ(f )

o

.

Proof. Fix τ > 0, μ ∈ P (A), and π ∈ P (A) with π ≪ μ. For any function f : A → R, define the normalization constant Zf := P 

> a∈A

μ(a) exp   f (a)

> τ

, and μf ∈ P (A) by (B.10) μf (a) := μ(a) exp 

  f (a)

> τ

 

> Zf

, a ∈ A .

Notice that whenever μ(a) = 0 , μf (a) = 0 for all f , hence μf and μ have the same support. We first rewrite the objective inside the infimum. Taking logarithm of (B.10) and rearranging gives 

f (a) = τ log μf (a)

μ(a) + τ log Zf , a ∈ supp( μ) .

Multiplying by π and summing over a ∈ supp( μ) yields 

− X

> a

f (a)π(a) = −τ X

> a

π(a) log μf (a)

μ(a) − τ log Zf .

Notice that 

−τ X

> a

π(a) log μf (a)

μ(a) = −τ X

> a

π(a) log π(a)

μ(a) − τ X

> a

π(a) log μf (a)

π(a)= −τ KL( π∥μ) + τ KL( π∥μf ),

where we used KL (π∥μf ) = P 

> a

π(a) log π(a)  

> μf(a)

and this is finite since μf and μ have the same support and π ≪ μ. Therefore, for every f ,

− X

> a

f (a)π(a) + τ log   X 

> a

μ(a)e f (a) 

> τ

 = −τ KL( π∥μ) + τ KL( π∥μf ) ≥ − τ KL( π∥μ),

since KL( π∥μf ) ≥ 0. Taking the infimum over f yields 

inf 

> f

n

− X

> a

f (a)π(a) + τ log   X 

> a

μ(a)e f (a) 

> τ

o

≥ − τ KL( π∥μ) .

It remains to show that there exists f ∗ s.t. KL (π∥μf ∗ ) = 0 , hence we attain the equality. Choose any constant c ∈ R and define, for a ∈ supp (μ), f ∗(a) := τ log π(a) 

> μ(a)

+ c, and define f ∗(a)

arbitrarily for a / ∈ supp( μ). Then 

μ(a)ef ∗(a)/τ = μ(a) π(a)

μ(a) ec/τ = π(a)ec/τ , Zf ∗ = X

> a

π(a)ec/τ = ec/τ ,

implies that μf ∗ (a) = π(a) for all a and hence KL( π∥μf ∗ ) = 0 , which proves the lemma. □

Now we are ready to prove Theorem 3.6. 

Proof. To simplify notations, we introduce the following shorthand. For any s ∈ S , denote 

eRπ(s) := P 

> a∈A

π(a|s)R˜θ(s, a ) and write eR = R˜θ and R = Rθ. Also we write Pπ := Pπθ for the law of the process under the baseline parameter θ. For the transition intensities, denote 

λss ′ := λss ′,θ and eλss ′ := λss ′,˜θ. Moreover, we define the likelihood-ratio process Zt := dPπ 

> ˜θ
> dPπθFt

and Z′ 

> t

:= 1 

> Zt

.ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 21 

We start by taking a log-transform of the robust objective 

log inf 

> ˜θ∈Cπτ



EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt eRπ(St) d t

 

,

since R˜θ(s, a ) > 0 for all (s, a ), the objective is strictly positive, hence the log-transform is well-defined. Moreover, since x 7 → log x is strictly increasing on the domain, it can be moved inside the infimum. Applying the change of measure and Jensen’s inequality to the concave function log and using that re −rt dt is a probability measure on [0 , ∞), we have 

log inf 

> ˜θ∈Cπτ



EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt eRπ(St) d t

 

≥ inf 

> ˜θ∈Cπτ

EPπ

> ρ



log 

 Z ∞

> 0

re −rt Zt  

> eRπ(St)
> r

dt

 

≥ inf 

> ˜θ∈Cπτ

EPπ

> ρ

 Z ∞

> 0

re −rt log 



Zt 

> eRπ(St)
> r



dt



= inf 

> ˜θ∈Cπτ



− EPπ

> ρ

 Z ∞

> 0

re −rt log Z′ 

> t

dt



+ EPπ

> ρ

 Z ∞

> 0

re −rt log 

 eRπ (St)

> r



dt

 

.

(B.11) Applying Girsanov theorem [42, Thm. 1.35] for CTMCs, we have 

log Z′ 

> t

=

Z t

> 0

X 

> s′̸=Su−

log λπSu−s′

eλπSu−s′

dN Su−,s ′ 

> u

−

Z t

> 0

X 

> s′̸=Su−

 λπSu−s′ − eλπSu−s′

 du , 

where 

N s,s ′ 

> t

:= X

> 0<u ≤t

1{Su−=s, S u=s′} .

Taking expectation under Pπ and using the compensator of the counting processes yields 

EPπ  log Z′

> t

 = EPπ

 Z t

> 0

X 

> s′̸=Su−



λπSu−s′ log λπSu−s′

eλπSu−s′

−  λπSu−s′ − eλπSu−s′



du



= EPπ

 Z t

> 0

ℓ(λπ, eλπ)( Su) d u



.

since the set of jump times has Lebesgue measure 0, we have R t 

> 0

f (Su−)d u = R t 

> 0

f (Su)d u a.s. for any Borel measurable function f .Therefore, by the expression for EPπ  log Z′

> t

, the first term in the last line of equation (B.11) is 

inf 

> ˜θ∈Cπτ

−EPπ

> ρ

 Z ∞

> 0

re −rt log Z′ 

> t

dt



= inf 

> ˜θ∈Cπτ

−EPπ

> ρ

 Z ∞

> 0

re −rt  Z t

> 0

ℓ(λπ, eλπ)( Su) d u



dt



.

For any s ∈ S , define f (s) := ℓ(λπ, eλπ)( s). Applying Fubini’s theorem path-wise, we have 

Z ∞

> 0

re −rt  Z t

> 0

f (Su) d u



dt =

Z ∞

> 0

f (Su)

 Z ∞

> u

re −rt dt



du =

Z ∞

> 0

e−ru f (Su) d u . 

Therefore, 

inf 

> ˜θ∈Cπτ

−EPπ

> ρ

 Z ∞

> 0

re −rt log Z′ 

> t

dt



= inf 

> ˜θ∈Cπτ

−EPπ

> ρ

 Z ∞

> 0

e−rt ℓ(λπ, eλπ)( St) d t



.

Hence the last line of equation (B.11) becomes (B.12) inf 

> ˜θ∈Cπτ



− EPπ

> ρ

 Z ∞

> 0

e−rt ℓ(λπ, eλπ)( St) d t



+ EPπ

> ρ

 Z ∞

> 0

re −rt log 

 eRπ (St)

> r



dt

 

.

Applying Lemma B.2, for any s ∈ S ,

−ℓ(λπ, eλπ)( s) ≥ − X

> a∈A

π(a|s) ℓ(λa, eλa)( s) .ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 22 

Moreover, by Jensen’s inequality, for any s ∈ S ,

log 

 eRπ (s)

> r



= log 

 X 

> a

π(a|s) eRπ (s,a )

> r



≥ X

> a

π(a|s) log 

 eR(s,a )

> r



.

Substituting these into (B.12) yields 

log inf 

> ˜θ∈Cπτ



EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt eRπ(St) d t

 

≥ inf 

> ˜θ∈Cπτ



− EPπ

> ρ

 Z ∞

> 0

e−rt X

> a

π(a|St) ℓ(λa, eλa)( St) d t



+ EPπ

> ρ

 Z ∞

> 0

re −rt X

> a

π(a|St) log 

 eR(St,a )

> r



dt

 

.

(B.13) For the second term in the right hand side of equation (B.13) , by adding and subtracting 

log 

 R(St,a )

> r



, we have 

inf 

> ˜θ∈Cπτ

EPπ

> ρ

 Z ∞

> 0

re −rt X

> a

π(a|St) log 

 eR(St,a )

> r



dt



= inf 

> ˜θ∈Cπτ

EPπ

> ρ

 Z ∞

> 0

re −rt X

> a

π(a|St) log 

 eR(St,a )

> R(St,a )



dt



+ EPπ

> ρ

 Z ∞

> 0

re −rt X

> a

π(a|St) log 

 R(St,a )

> r



dt



.

Therefore, 

log inf 

> ˜θ∈Cπτ



EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt eRπ(St) d t

 

≥ inf 

> ˜θ∈Cπτ

EPπ

> ρ

 Z ∞

> 0

e−rt X

> a

π(a|St)



− ℓ(λa, eλa)( St) + r log eR(St,a )

> R(St,a )



dt



+ EPπ

> ρ

 Z ∞

> 0

e−rt X

> a

π(a|St)



r log 

 R(St,a )

> r

 

dt



.

By Lemma B.3, the following inequality holds, for any f : A → R,(B.14) X

> a

π(a)f (a) ≤ τ KL( π∥μ) + φ(f ) ,

where φ(f ) = τ log 

 P 

> a

μ(a)e f (a)

> τ



. Applying (B.14) to cθ, ˜θ(s, a ) (3.6) state-wise yields 

− X

> a

π(a|s)cθ, ˜θ(s, a ) ≥ − τ KL( π(·| s)∥μ(·| s)) − φ(cθ, ˜θ(s, ·)) .ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 23 

Therefore, 

log inf 

> ˜θ∈Cπτ



EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt eRπ(St) d t

 

≥ inf 

> ˜θ∈Cπτ

EPπ

> ρ

 Z ∞

> 0

e−rt 

− τ KL( π∥μ)( St) − φ(cθ, ˜θ(St, ·)) 



dt



+ EPπ

> ρ

 Z ∞

> 0

e−rt X

> a

π(a|St)



r log 

 R(St,a )

> r

 

dt



= EPπ

> ρ

 Z ∞

> 0

e−rt  X 

> a

π(a|St) r log R(St, a ) − τ KL( π∥μ)( St)



dt



+ inf 

> ˜θ∈Cπτ

EPπ

> ρ

 Z ∞

> 0

−e−rt φ(cθ, ˜θ(St, ·)) d t



− log r . 

By the definition of Cπτ (3.8), for any ˜θ ∈ Cπτ (θ),

EPπ

> ρ

 Z ∞

> 0

− e−rt φ(cθ, ˜θ(St, ·)) d t



= − τr

X

> s

¯dπρ,θ (s) log 

 X 

> a

μ(a|s) exp 

 cθ, ˜θ (s,a )

> τ

 

≥ − εr .

Consequently, 

log inf 

> ˜θ∈Cπτ



EPπ

> ˜θ
> ρ

 Z ∞

> 0

e−rt eRπ(St) d t

 

≥ EPπ

> ρ

 Z ∞

> 0

e−rt  X 

> a

π(a|St) r log R(St, a ) − τ KL( π∥μ)( St)



dt



− εr − log r , 

which completes the proof. □

B.7. Proof of Proposition 3.8. 

Proof. Fix π ∈ Π and ˜θ ∈ Θ. For each s ∈ S , define gs(τ ) := τ log   P  

> a∈A s

μ(a|s)ecθ, ˜θ (s,a )/τ 

with τ > 0. Then the defining criterion of Cπτ,ε (θ) is Ψτ (˜θ) := P 

> s∈S

¯dπρ,θ (s)gs(τ ) ≤ ε.The same log-sum-exp argument used in the proof of Proposition 3.4, applied statewise with base measure μ(· | s) and function cθ, ˜θ(s, ·), gives 

ddτ gs(τ ) = −KL( qτ (· | s)∥μ(· | s)) ≤ 0,

where 

qτ (a|s) := μ(a|s)ecθ, ˜θ (s,a )/τ 

P 

> b∈A s

μ(b | s)ecθ, ˜θ (s,b )/τ .

Thus each gs(τ ) is nonincreasing in τ , and therefore Ψτ ( ˜θ) is also nonincreasing. Hence, for 

0 < τ 1 ≤ τ2, we have Ψτ2 (˜θ) ≤ Ψτ1 (˜θ), which proves Cπτ1,ε (θ) ⊆ C πτ2,ε (θ).It remains to identify the limiting sets. By the standard log-sum-exp limits as discussed in the proof of Proposition 3.4, for each s ∈ S ,

lim   

> τ→0

gs(τ ) = max 

> a∈A s

cθ, ˜θ(s, a ), lim   

> τ→∞

gs(τ ) = X

> a∈A s

μ(a|s)cθ, ˜θ(s, a ).

Since S is finite, these limits pass through the finite weighted sum defining Ψτ . Therefore, 

Cπ

> 0,ε

(θ) = 

n˜θ ∈ Θ X

> s∈S

¯dπρ,θ (s) max 

> a∈A s

cθ, ˜θ(s, a ) ≤ ε

o

,

and 

Cπ

> ∞,ε

(θ) = 

n˜θ ∈ Θ X

> s∈S

¯dπρ,θ (s) X

> a∈A s

μ(a|s)cθ, ˜θ(s, a ) ≤ ε

o

.ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 24 

The monotonicity proved above then implies 

Cπ

> 0,ε

(θ) ⊆ C πτ,ε (θ) ⊆ C π

> ∞,ε

(θ), ∀τ > 0.

□

Appendix C. Detailed Setup for the Market Making Example 

We use a market-making problem as the worked example. In market making, a dealer continuously posts bid and ask quotes, earns the spread when market orders are filled, and controls inventory risk through the quoted distances. Since the inventory changes are only triggered by order arrivals, this is naturally an event-driven continuous-time control problem. We now consider a simplified single-asset market making model. The state q is the dealer’s inventory taking values in S = {− 10 , −9, . . . , 9, 10 } and an action δ = ( δ−, δ +) ∈ A specifies the bid and ask quote distances. Motivated by [ 3], where optimal quotes remain in a narrow range near zero inventory, we discretise the quote interval [0 .06 , 0.24] bps into 20 evenly spaced points, 

∆20 = {¯δ(1) , . . . , ¯δ(20) }, ¯δ(i) = δmin + ( i − 1) δmax −δmin  

> 19

, i = 1 , . . . , 20 ,

with δmin = 0 .06 and δmax = 0 .24 . The action space is thus A = ∆ 20 × ∆20 with |A| = 400 , and we take the reference policy to be uniform, μ(δ|q) = 1 /|A| .For a given state–action pair (q, δ ), inventory can only move to q + 1 or q − 1, subject to the boundary constraints. These jumps occur when the dealer’s bid or ask quote is filled by incoming market orders. The fill intensity is the instantaneous arrival rate of such fills: economically, a quote placed further from the mid-price is less attractive and hence is filled less often. Following [ 3], we use the logistic specification 

Λθ(δ±) = λR

1 + exp( αΛ + βΛδ±) , θ = ( αΛ, β Λ),

with fixed scale parameter λR. Since βΛ > 0, the fill intensity is decreasing in the quote distance. The controlled transition rates are therefore 

λδq,q +1 ,θ = 1{q< 10 }Λθ(δ−), λδq,q −1,θ = 1{q> −10 }Λθ(δ+).

Here a bid fill increases inventory from q to q + 1 , while an ask fill decreases inventory from q to 

q − 1. The running reward is the expected spread capture minus a quadratic inventory penalty: 

Rθ(q, δ ) = 1{q< 10 }Λθ(δ−)δ− + 1{q> −10 }Λθ(δ+)δ+ − γ

2 σ2q2.

We use the calibrated parameters from [ 3], rescaled to minutes for time-dependent quantities. The baseline values are thus λR = 1000 8·60 min −1, σ = 50 √8·60 bps · min −1/2, αΛ = −1 and βΛ =10 bps −1. The quote-space parameters αΛ and βΛ are left unchanged under this rescaling because 

αΛ +βΛδ is dimensionless. We set γ = 0 .05 bps −1 ·M$ −1. Since our robustness certificate contains the term log (R˜θ/R θ), rewards must be strictly positive. We therefore use the shifted reward 

R+ 

> θ

(q, δ ) := Rθ(q, δ ) + C, where C := − min ˜θ min q,δ R˜θ(q, δ ) + 10 −5 so that R+ 

> θ

(q, δ ) ≥ 10 −5

uniformly over all (q, δ ).Finally, to compare the two policy-dependent robust sets eCπτ,ε (θ) in (3.4) and Cπτ,ε (θ) in (3.8) ,we fix a common behavior policy π. The policy is chosen to mimic the inventory-stabilizing structure of the optimal market-making policy: when the inventory is positive, the policy favors actions with relatively tighter ask quotes and wider bid quotes, thereby encouraging sell fills and pushing inventory back towards zero; when the inventory is negative, it favors tighter bid quotes and wider ask quotes, encouraging buy fills. Hence the policy induces mean-reverting inventory dynamics and reflects the economic trade-off between spread capture and inventory-risk control. We fix the robustness budget at ε = 0 .01 . Moreover, we set r = 1 . With this normalization, the lower-bound constants in the two certificates coincide; see Theorems 3.2 and 3.6. Therefore, the numerical comparison mainly reflects the geometry and relative size of the robust sets, rather than differences caused by the scale of the corresponding robust lower bounds. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 25 

Under this setting, the local relative entropy between the nominal and perturbed jump rates is explicit, 

ℓ(λδθ, λ δ

˜θ)( q) = 1{q< 10 }



Λθ(δ−) log Λθ(δ−)Λ˜θ(δ−) − Λθ(δ−) + Λ ˜θ(δ−)



+ 1{q> −10 }



Λθ(δ+) log Λθ(δ+)Λ˜θ(δ+) − Λθ(δ+) + Λ ˜θ(δ+)



.

Thus, for each candidate perturbation ˜θ, the local intensity-based robust constraint is evaluated as 

Φloc (˜θ; τ ) := X

q∈S 

¯dπρ,θ (q) τ log 

 1

|A| 

X

δ∈A 

exp 

 ℓ(λδθ ,λ δ 

> ˜θ

)( q)−r log R+˜θ (q,δ )

R+ 

> θ

(q,δ )

τ



,

and we include ˜θ in Cπτ,ε (θ) whenever Φloc ( ˜θ; τ ) ≤ ε. To compute the normalized state occupancy measure, we use the formula ¯dπρ, ˜θ = rρ (rI − Qπθ )−1, which follows from the standard resolvent representation of CTMDPs. Here Qπθ is the generator induced by the fixed policy π under the parameter ˜θ, and ρ is the initial distribution. For the global occupancy-based robust set, the perturbation is measured through the change in discounted state occupancy. For each ˜θ, we first compute ¯dπρ, ˜θ = rρ (rI − Qπ

˜θ )−1, using the same fixed policy π and initial distribution ρ. The global constraint is then evaluated as 

Φglob (˜θ; τ ) := τ log 

 X

q∈S 

¯dπρ,θ (q) 1

|A| 

X

δ∈A 

exp 

 log ¯dπρ,θ (q)¯dπρ, ˜θ (q) −log R+˜θ (q,δ )

R+ 

> θ

(q,δ )

τ



.

We include ˜θ in eCπτ,ε (θ) whenever Φglob (˜θ; τ ) ≤ ε.100 75 50 25 0 25 50 75 100                                      

> relative change in (%)
> 40
> 20
> 0
> 20
> 40
> relative change in  (%)
> Local: 5.03%
> Global: 0.66%
> = 0.0001
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 9.61%
> Global: 3.35%
> = 0.01
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 11.08%
> Global: 39.08%
> = 1.0
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 11.10%
> Global: 53.13%
> = 10.0
> Robust sets under a uniform policy, =q= 0 ,r= 1.0, local =global = 0.01

Figure 4. Robust regions for the market making example under the same setting as Figure 1 but with a uniform policy. Compared with Figure 1, where the policy has an inventory-stabilizing structure motivated by the optimal market-making control, Figure 4 repeats the experiment under a less informative benchmark policy, i.e. a uniform policy on A. The local intensity-based certificate is almost unchanged, whereas the global occupancy-based certificate becomes larger. This reflects the different nature of the two constraints. The local certificate penalizes pointwise jump-rate perturbations averaged under the baseline occupancy, and is little affected here since the initial law is concentrated near zero and the baseline occupancy profile remains similar. By contrast, the global certificate depends on the realized occupancy distortion between the baseline and perturbed controlled processes, and is therefore more sensitive to the interaction between the policy and the perturbed dynamics. Next, to isolate the effect of the initial distribution, we replace the Dirac distribution at zero (ρ = δ{q=0 }) with a uniform distribution ( ρ = Unif (S)). We maintain the inventory-stabilizing benchmark policy from Figure 1. Because this policy mirrors the structure of the optimal control, keeping it fixed allows us to focus on robustness within a near-optimal regime. As shown in Figure 5, the local certificate changes only mildly: changing ρ only changes the baseline occupancy weights, while the pointwise intensity are unchanged. Hence, under the stabilizing ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 26 100 75 50 25 0 25 50 75 100                                     

> relative change in (%)
> 40
> 20
> 0
> 20
> 40
> relative change in  (%)
> Local: 4.54%
> Global: 0.90%
> = 0.0001
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 8.03%
> Global: 7.33%
> = 0.01
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 14.34%
> Global: 54.84%
> = 1.0
> 100 75 50 25 025 50 75 100
> relative change in (%)
> Local: 14.69%
> Global: 56.79%
> = 10.0
> Robust sets under a heuristic policy, = Unif( ), r= 1.0, local =global = 0.01

Figure 5. Robust regions for the market making example under the heuristic policy as Figure 1 but with a uniform initial state distribution. policy, the geometry of the local set is largely preserved. By contrast, the global certificate changes substantially because its constraint depends on the occupancy ratio. For small τ , the log-sum-exp is close to a worst-case constraint, so a large occupancy ratio at a few states can dominate the certificate. Compared with ρ = δ{q=0 }, the uniform initial law spreads mass across states and makes these worst-case ratios less extreme. For larger τ , the constraint averages more across states, and the occupancy ratios are closer to one on the states with significant mass. This makes the global constraint less restrictive and gives a larger certified region. Hence the global certificate, although sometimes much larger, is more sensitive to the policy and initial distribution, while the local certificate is comparatively stable. 

Appendix D. Robustness of Entropy-Regularised Policies: Extended Experiments 

This appendix presents empirical evidence that entropy-regularised policies trained with the arrival-driven framework exhibit improved worst-case performance under model perturbation. We evaluate on two domains—single-asset market making and criss-cross queueing scheduling—using a common methodology: train at nominal parameters, then evaluate across a two-dimensional perturbation grid and compare the worst-case (minimum over the grid) discounted reward of 

πstd (τ = 0 ) against πτ for several values of the entropy temperature τ .D.1. Experimental Setup. 

General methodology. For each domain, a standard policy πstd is trained with τ = 0 (no entropy bonus), and a family of entropy-regularised policies πτ is trained for each τ in a logarithmically spaced grid. All policies are trained at nominal environment parameters. At evaluation time, each policy is deployed without retraining across a 21 × 21 grid of perturbed parameters. For each grid cell, the mean discounted reward is estimated over 10 evaluation episodes. We also evaluate a noise baseline πnoise , which uses the πstd policy but replaces the chosen action with a uniform random action with probability ϵ = 0 .1.We report: 

• Nominal performance : mean reward at the unperturbed (nominal) grid cell, averaged over seeds. 

• Worst-case performance : minimum over all grid cells of the seed-averaged reward. 

• Brittleness : relative drop from nominal to worst-case, (1 − WC /Nom) × 100% .D.1.1. Market-Making Environment. We use a single-asset market-making environment similar to that discussed in the main body of this work (arithmetic Brownian motion midprice, constant Poisson arrivals) with a logistic fill probability model. To ensure the entropy bonus is meaningful relative to the action space, we reduce the action space to 3 × 3 = 9 discrete actions (bid and ask spreads each drawn from {0.05 , 0.25 , 0.45 }), yielding Hmax = ln 9 ≈ 2.20 .ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 27 

Table 1. Market-making robustness experiment: environment parameters. 

Parameter Value Description Arrival rate λR 2.0833 Order arrival rate Fill intercept α −1.0 Logistic fill parameter Fill slope β 10 .0 Logistic fill parameter Inventory penalty γ 0.05 Running inventory penalty Volatility σ 2.2822 Midprice diffusion coefficient Drift μ 0.0 Midprice drift Initial price S0 100 .0 Starting midprice Max inventory qmax 10 Inventory bound Terminal time T 10 .0 Episode length Trajectories/epoch 200 Batch size Bid/ask spread grid {0.05 , 0.25 , 0.45 } 3 × 3 = 9 actions Training epochs 2,000 Per policy Seeds 0–9 10 independent seeds Network MLP [64 , 64] Hidden layer sizes Learning rate 0.001 Adam optimiser 

Perturbation axes. We perturb the volatility σ and arrival rate λR jointly via scale factors: 

σ′ = σ · sσ, λ′ 

> R

= λR · sλ, with (sσ, s λ) ∈ [0 .25 , 4.0] 2 on a 21 × 21 grid. This range is chosen because volatility enters the optimal spread quadratically due to the inventory penalty term (δ∗ ∝ γσ 2(T − t)), so a 4× increase in σ produces a 16 × increase in the inventory penalty contribution, qualitatively changing which spreads are optimal. Entropy temperature values. τ ∈ { 10 −4, 5 × 10 −4, 10 −3, 5 × 10 −3, 10 −2, 10 −1, 1.0}.D.1.2. Queueing Scheduling Environment. We use the criss-cross queueing network from §4, with two servers and three job classes. Server 1 can serve classes 1 and 3; server 2 can serve classes 2 and 3. The four discrete actions correspond to the joint priority assignments for both servers: (S1 →c1, S2 →c2), (S1 →c1, S2 →c3), (S1 →c3, S2 →c2), (S1 →c3, S2 →c3). 

Table 2. Queueing robustness experiment: environment parameters. 

Class 1 Class 2 Class 3 Arrival rate λi 0.5 0.5 0.5 Service rate μi 2.0 3.0 2.0 Holding cost hi 3.0 1.0 2.0 Queue capacity Qmax 50 Terminal time T 50.0 Trajectories/epoch 100 Training epochs 2,000 Seeds 0–4 (5 seeds) Network MLP [64 , 64] 

Learning rate 0.001 

Perturbation axes. We perturb the arrival rates of classes 1 and 3 independently via scale factors: 

λ′ 

> 1

= λ1 · s1, λ′ 

> 3

= λ3 · s3, with (s1, s 3) ∈ [0 .5, 3.0] 2 on a 21 × 21 grid, while λ2, all service rates, and all holding costs remain fixed. This asymmetric perturbation changes which class creates the bottleneck for server 1 (which must split capacity between classes 1 and 3), thereby changing the optimal scheduling prior-ity. Crucially, the base arrival rates are set low enough ( λi = 0 .5, μi ≥ 2.0) that even at 

(s1, s 3) = (3 .0, 3.0) , the system remains within its stability region. This ensures that any perfor-mance degradation under perturbation arises from policy mismatch —the trained policy choosing ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 28 

suboptimal priorities—rather than from environmental overload where no scheduling policy can prevent queue growth. Entropy temperature values. τ ∈ { 10 −4, 5 × 10 −4, 10 −3, 5 × 10 −3, 10 −2, 10 −1, 1.0}.D.2. Statistical Methodology. Each seed produces an independent policy via a different random initialisation and training trajectory batch. The trained policy is then evaluated on independent trajectory batches at each perturbation grid cell. For a given policy type (e.g., πτ at a specific τ ), the mean reward at each grid cell is first averaged over the evaluation episodes, then averaged across seeds to obtain the seed-averaged reward surface. The worst-case performance is the minimum of this surface over the perturbation grid. Hypothesis test. For each τ > 0, we test whether entropy regularisation improves worst-case performance: 

H0 : WC( πτ ) ≤ WC( πstd ) vs. H1 : WC( πτ ) > WC( πstd ).

Let (i∗ 

> τ

, j ∗ 

> τ

) = arg min i,j ¯Rτ (i, j ) and (i∗

> 0

, j ∗ 

> 0

) = arg min i,j ¯R0(i, j ) denote the worst-case grid cells for πτ and πstd respectively, where ¯R is the seed-averaged reward. The test statistic is 

t = WC( πτ ) − WC( πstd )

q

SE 2 

> τ

+ SE 20

,

where SE τ and SE 0 are the standard errors (across seeds) at the respective worst-case grid cells. Under H0, this follows an approximate t-distribution (Welch’s t-test with unequal variances). We report one-sided p-values. Justification. Welch’s t-test is appropriate here because: (i) each seed produces a fully independent policy and evaluation, so seeds are the natural unit of replication; (ii) Welch’s variant does not assume equal variance across the πτ and πstd conditions; and (iii) with 5–10 seeds, the t-distribution (rather than a z-test) correctly accounts for small-sample uncertainty in the variance estimate. We do not apply multiple comparison corrections across τ values, as the goal is to identify the best τ rather than to claim all values improve robustness. The systematic inverted-U pattern across τ (visible in Figures 7 and 9) provides additional evidence against cherry-picking. D.3. Market-Making Results. Table 3 reports the full results for the market-making experi-ment. The standard policy exhibits 7.1% brittleness under volatility and arrival rate perturbation. 

Table 3. Market-making robustness results (10 seeds, 2,000 epochs). Nominal and worst-case (WC) are seed-averaged discounted rewards. Gain is relative to πstd worst-case. Statistically significant improvements ( p < 0.05 , one-sided Welch’s t-test) are shown in bold .                                                             

> τNominal (SE) Worst-Case (SE) WC Gain t-stat p
> 0(πstd )231 .11 (0 .06) 214 .60 (0 .18) ———noise ( ϵ=0 .1)231 .07 214 .31 −0.13% ——
> 10 −4231 .14 (0 .06) 214 .80 (0 .30) +0 .10% 0.60 0.28
> 5×10 −4231 .22 (0.06 )215 .27 (0.21 )+0.32 %2.48 <0.01 10 −3231 .25 (0.09 )215 .62 (0.43 )+0.48 %2.19 <0.025
> 5×10 −3231 .07 (0 .05) 214 .24 (0 .32) −0.17% −0.96 —
> 10 −2231 .09 (0 .07) 214 .69 (0 .29) +0 .04% 0.27 0.40 10 −1230 .89 (0 .07) 213 .77 (0 .23) −0.39% −2.81 —
> 1.0230 .65 (0 .05) 211 .98 (0 .10) −1.22% −12 .81 —

Two entropy temperatures yield statistically significant worst-case improvements: τ = 5 × 10 −4

(+0 .32% , t = 2 .48 , p < 0.01 ) and τ = 10 −3 (+0 .48% , t = 2 .19 , p < 0.025 ). Both also slightly improve nominal performance, indicating no robustness–performance trade-off. Larger 

τ values degrade both nominal and worst-case performance, consistent with excessive entropy overwhelming the reward signal. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 29 0.5 1.0 1.5 2.0 2.5 3.0 3.5 4.0 

> Volatility scale
> 0.5
> 1.0
> 1.5
> 2.0
> 2.5
> 3.0
> 3.5
> 4.0
> Arrival rate scale

Gain: = 0.001 vs std 

> 1.00
> 0.75
> 0.50
> 0.25
> 0.00
> 0.25
> 0.50
> 0.75
> 1.00
> J

(a) Gain of πτ =5 ×10 −4 over πstd .0.5 1.0 1.5 2.0 2.5 3.0 3.5 4.0 

> Volatility scale
> 0.5
> 1.0
> 1.5
> 2.0
> 2.5
> 3.0
> 3.5
> 4.0
> Arrival rate scale

Gain: = 0.001 vs noise  

> 1.0
> 0.5
> 0.0
> 0.5
> 1.0
> J

(b) Gain of πτ =5 ×10 −4 over πnoise .

Figure 6. Market-making: reward gain heatmaps across the volatility scale ×

arrival rate scale perturbation grid. Positive (blue) regions indicate where the entropy-regularised policy outperforms the baseline. The entropy-regularised policy gains most in the high-volatility region (top of grid), where the optimal spread changes qualitatively from the nominal setting. 10 4 10 3 10 2 10 1 10 0

Temperature 

2

1

0

1

> J( ) J( std )

Gain vs std ( sweep, MM)   

> Nominal gain
> Worst-case gain
> noise nominal
> noise worst-case

Figure 7. Market-making: worst-case and nominal gain vs. πstd as a function of 

τ (10 seeds, mean ± SE). The inverted-U shape confirms that moderate entropy regularisation improves worst-case robustness, while excessive regularisation de-grades performance. D.4. Queueing Scheduling Results. Table 4 reports the full results for the queueing schedul-ing experiment. The standard policy exhibits 8.9% brittleness under asymmetric arrival rate perturbation. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 30 

Table 4. Queueing robustness results (5 seeds, 2,000 epochs). Format matches Table 3. 

τ Nominal (SE) Worst-Case (SE) WC Gain t-stat p

0 (πstd ) 14 ,912 (1 .0) 13 ,589 (105) — — —noise ( ε=0 .1) 14 ,912 13 ,615 +0 .19% — —

10 −4 14 ,914 (0.1) 13 ,871 (52 ) +2.08 % 2.41 < 0.01 

5 × 10 −4 14 ,913 (1 .2) 13 ,744 (113) +1 .14% 1.00 0.16 10 −3 14 ,913 (1 .4) 13 ,771 (128) +1 .34% 1.09 0.14 5 × 10 −3 14 ,912 (1 .1) 13 ,593 (112) +0 .03% 0.02 0.49 10 −2 14 ,913 (1 .1) 13 ,666 (114) +0 .56% 0.49 0.31 10 −1 14 ,911 (1 .1) 13 ,528 (87) −0.45% −0.45 —

1.0 14 ,914 (0 .4) 13 ,799 (32) +1 .55% 1.91 0.03 

The best entropy temperature is τ = 10 −4, which achieves a statistically significant +2 .08% 

worst-case improvement ( t = 2 .41 , p < 0.01 ). At τ = 1 .0, the improvement is +1 .55% (t = 1 .91 ,

p = 0 .03 ). As in market making, nominal performance is virtually unchanged across all τ values, confirming that the robustness benefit comes without a performance trade-off. 0.5 1.0 1.5 2.0 2.5 3.0     

> 1scale
> 0.5
> 1.0
> 1.5
> 2.0
> 2.5
> 3.0
> 3 scale
> Gain: = 0.0001 vs std
> 200
> 100
> 0
> 100
> 200
> J

(a) Gain of πτ =10 −4 over πstd .0.5 1.0 1.5 2.0 2.5 3.0      

> 1scale
> 0.5
> 1.0
> 1.5
> 2.0
> 2.5
> 3.0
> 3 scale
> Gain: = 0.0001 vs noise
> 200
> 100
> 0
> 100
> 200
> J

(b) Gain of πτ =10 −4 over πnoise .

Figure 8. Queueing: reward gain heatmaps across the λ1 scale × λ3 scale perturbation grid. The entropy-regularised policy gains most in the high-arrival corner ( s1 = s3 = 3 .0), where the load balance between classes 1 and 3 for server 1 shifts from the nominal setting. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 31 10 5 10 4 10 3 10 2 10 1 10 0 10 1     

> Temperature
> 350
> 300
> 250
> 200
> 150
> 100
> 50
> 0
> 50
> J( ) J( std )
> Gain vs std (sweep, queueing)
> Nominal gain
> Worst-case gain
> noise nominal
> noise worst-case

Figure 9. Queueing: worst-case and nominal gain vs. πstd as a function of τ (5 seeds, mean ± SE). Small τ values ( 10 −4) improve worst-case performance, while 

τ = 10 −1 and greater slightly degrades it. D.5. Discussion. Across both domains, entropy regularisation with appropriately chosen τ

significantly improves worst-case performance under model perturbation without degrading nominal performance. The optimal τ is small in both cases ( 5 × 10 −4 for market making, 10 −4

for queueing), indicating that light regularisation suffices to hedge against parameter uncertainty. The mechanism is consistent across domains: entropy regularisation prevents the policy from concentrating all probability mass on a single action that is optimal only under the nominal parameterisation. By maintaining probability mass on alternative actions, the entropy-regularised policy remains competitive when the optimal action shifts under perturbation. In market making, higher volatility makes wider spreads optimal; in queueing, higher arrival rates for class 3 make prioritising class 3 on server 1 optimal. In both cases, πstd commits too strongly to the nominally optimal action, while πτ hedges. 

Appendix E. Certificate Tightness Analysis 

This section analyses the tightness of the theoretical robustness certificate from Theorem 3.6. We use the same market-making parameterization and perturbation axes as in Section 3.3, with θ = ( αΛ, β Λ). Recall that the robust set Cπτ,ε (θ) in (3.8) is defined by the constraint 

P 

> s

¯dπρ,θ (s) τ log  P  

> a

μ(a|s) exp (cθ, ˜θ(s, a )/τ ) ≤ ε. For the market-making parameterization, note that the state-action pair is (q, δ ), we define a pointwise certificate function by 

F (q; ˜θ) := τ log  X 

> δ

μ(δ|q) exp( cθ, ˜θ(q, δ )/τ ),

and the scalar 

G(˜θ) := max  

> q

F (q; ˜θ) − r log(1 /r ).

Since max q F ≥ P 

> q

¯dπ(q)F (q), the condition G( ˜θ) ≤ 0 implies ˜θ ∈ C πτ,ε (θ) (with ε = r log (1 /r )). Thus the certificate guarantees that the entropy-regularized policy πτ maintains performance whenever G( ˜θ) ≤ 0. We evaluate this on the single-asset market-making environment with logistic fill probabilities, perturbing the fill parameters (αΛ, β Λ) and (λR, β Λ) on 21 × 21 grids. The analytical softmax policy πτ (δ|q) = softmax (Rθ(q, δ )/τ ) is used throughout, matching the theoretical assumptions exactly, along with learned PG and A2C policies. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 32 

E.1. G as a Continuous Robustness Predictor. Figure 10 plots the certificate value G( ˜θ)

against the empirical performance ratio J(π, ˜θ)/J (π, θ ) for all 441 grid cells on the αΛ-βΛ

perturbation plane, evaluated under three policy types: the analytical softmax policy πτ (δ|q) = softmax (Rθ(q, δ )/τ ) that the certificate theorem assumes, and two learned policies (Soft-PG and Soft-AC) trained with entropy regularization. All three policy types cluster tightly together, confirming that the learned policies converge to essentially the same behavior as the theoretical optimum in this environment. The certificate is valid across all policy types: every certified point ( G ≤ 0, black squares) achieves J/J nom ≥ 0.95 , with zero violations. Moreover, G is monotonically predictive of degradation across the entire grid—not just within the certified region. Table 5 quantifies this: no cell with J/J nom < 0.90 has G < 1.03 , and no cell with J/J nom < 0.50 

has G < 36 .4.0 20 40 60 80 100 120 140                     

> G()
> 0.25
> 0.50
> 0.75
> 1.00
> 1.25
> 1.50
> 1.75
> 2.00
> J( , ) /  J( , )
> = 0.05
> 020 40 60 80 100 120 140
> G()
> = 1.0
> 020 40 60 80 100 120 140
> G()
> = 50.0
> Analytical
> Soft-PG
> Soft-AC
> Certified ( G0)
> Certificate Value Gvs Empirical Performance Ratio

Figure 10. Certificate value G( ˜θ) vs empirical performance ratio for three entropy temperatures and three policy types (analytical softmax, Soft-PG, Soft-AC). All three cluster tightly, confirming that learned policies match the theoretical optimum. Certified points ( G ≤ 0, black squares) all lie above the 0.9 threshold (dashed grey). 

Table 5. Tightness of the certificate ( τ = 1 .0, αΛ-βΛ plane). For each G

threshold, the table reports the number of included grid cells and the minimum empirical performance ratio within that set. 

G threshold Cells included Min J/J nom 

G ≤ 0 (certificate) 24 / 441 0.953 

G ≤ 1 69 0.903 

G ≤ 5 144 0.806 

G ≤ 10 202 0.726 

G ≤ 50 367 0.449 

The certificate boundary G = 0 is conservative: the empirical 90% -robustness region contains 

257 cells while the certificate guarantees only 24 . However, the conservatism is bounded—the “true” G threshold for 90% robustness is approximately G ≈ 1, so the certificate is off by roughly one G-unit rather than by orders of magnitude. E.2. Certified Region Size vs τ . Figure 11 plots the size of the certified region |{ ˜θ : G( ˜θ) ≤ 0}| 

as a function of the entropy temperature τ , computed on a dense logarithmic grid from τ = 10 −3

to τ = 10 2. The theoretical prediction is confirmed: the certified region grows monotonically with τ on both the αΛ-βΛ and λR-βΛ perturbation planes. The growth saturates around τ ≈ 1,reaching 24 cells ( αΛ-βΛ) and 26 cells ( λR-βΛ). This plateau reflects the geometric structure of the certificate: as τ → ∞ , the soft-max in the G formula converges to a hard maximum, and the certified set approaches its τ -independent upper bound. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 33 

For comparison, the empirical robust region (defined as J/J nom ≥ 0.9) contains ∼ 260 cells and is approximately constant across τ . The ∼10 × gap between certified and empirical region sizes reflects the conservatism identified in §E.1. 10 3 10 2 10 1 10 0 10 1 10 2

> Temperature
> 0
> 50
> 100
> 150
> 200
> 250
> 300
> Number of robust cells (out of 441)

Certified Region Size vs       

> Certified ( -plane)
> Certified ( R-plane)
> Empirical robust ( -)

Figure 11. Certified region size (number of grid cells with G ≤ 0) as a function of τ . Both perturbation planes show monotone growth, confirming the theoretical prediction. Green diamonds show the empirical robust region size for reference. E.3. Transition vs Reward Decomposition. The certificate function decomposes as Xδ(q) = 

ℓδ(q) − r · log (R˜θ(q, δ )/R θ(q, δ )) , where ℓa is the local relative entropy (KL divergence) between baseline and perturbed fill intensities ( transition perturbation ), and the log-ratio term captures the 

reward perturbation . Figure 12 decomposes these two contributions at representative perturbation points. = 1.0,  = 8.75    

> = 1.0,  = 11.25
> = 1.25,  = 10
> = 0.75,  = 10
> = 1.25,  = 8.75
> = 0.75,  = 11.25
> = 1.5,  = 7.5
> = 0.5,  = 12.5
> 10 1
> 10 0
> 10 1
> 10 2
> Mean contribution to  Xa
> Decomposition of Certificate Bound
> Transition KL ( a)
> Reward | rlog( R/R0)|

Figure 12. Decomposition of the certificate bound into transition KL ( ℓa) and reward perturbation ( |r · log (R˜θ/R θ)|) contributions, shown on a log scale. The transition KL dominates by 1.5–3 orders of magnitude at all perturbation points. The transition KL term dominates overwhelmingly: it exceeds the reward term by a factor of 

150 –800 × across all grid points. This has two implications. First, the policy is inherently robust to reward misspecification—a reward-only certificate (setting ℓδ = 0 ) would cover nearly the entire perturbation grid. Second, the conservatism of the joint certificate is driven entirely by the transition KL term. Since the local relative entropy of logistic fill intensities grows quadratically with parameter perturbation, even moderate changes in (αΛ, β Λ) produce large KL values that ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 34 

push G above zero. Tightening the transition bound (e.g., via action-dependent KL budgets or state-dependent certificates) would directly enlarge the certified region. E.4. Worst-Case Adversary Within the Certified Set. For each τ , we identify the worst-case perturbation within the certified set: 

˜θ∗ = arg min {G(˜θ)≤0} J(πτ , ˜θ).

Table 6. Worst-case adversary within the certified set ( αΛ-βΛ plane) for all three policy types. The certificate guarantees robustness at these points; the empirical performance ratio confirms it. 

τ Policy Worst-case (αΛ, β Λ) G(˜θ∗) J J/J nom 

0.05 

Analytical (−1.00 , 10 .25) −0.088 58 .66 0.946 

Soft-PG (−1.00 , 10 .25) −0.088 58 .16 0.969 

Soft-AC (−1.00 , 10 .25) −0.088 57 .37 0.961 1.0

Analytical (−0.80 , 9.00) −0.019 58 .16 0.953 

Soft-PG (−1.00 , 10 .25) −0.114 58 .49 0.967 

Soft-AC (−1.00 , 10 .25) −0.114 57 .77 0.960 50 .0

Analytical (−1.00 , 10 .25) −0.116 57 .63 0.946 

Soft-PG (−1.00 , 10 .25) −0.116 58 .17 0.960 

Soft-AC (−1.00 , 10 .25) −0.116 57 .78 0.959 

Across all policy types and all τ values, the worst-case certified point achieves J/J nom ≥ 0.946 ,well above the 0.9 robustness threshold. The learned policies (Soft-PG, Soft-AC) achieve slightly 

higher worst-case ratios than the analytical softmax ( 0.96 –0.97 vs 0.95 ), likely because the narrow action space makes all policies nearly equivalent. The worst-case perturbations are consistently at (αΛ, β Λ) = ( −1.00 , 10 .25) —a βΛ shift of only 0.25 from nominal—with G values close to zero (boundary of the certified set). This confirms that the certificate boundary coincides with the “hardest” perturbations it can guarantee, and that the guarantee holds equally for learned and analytical policies. E.5. Perturbation Budget Curves. Figure 13 provides one-dimensional cross-sections through the certificate surface and empirical performance, fixing one parameter at its nominal value and sweeping the other. These curves directly visualise the safety margin between the certificate boundary ( G = 0 , blue dashed line) and the onset of significant performance degradation (J/J nom = 0 .9, red dashed line). ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 35 8 9 10 11 12          

> (fill slope)
> 0
> 5
> 10
> 15
> 20
> 25
> 30
> G( )
> Cross-section: =1.0 (nominal), sweep
> G()
> J/Jnom
> 1.4 1.2 1.0 0.8 0.6
> (fill intercept)
> 0
> 5
> 10
> 15
> 20
> 25
> 30
> 35
> G( )
> Cross-section: = 10.0 (nominal), sweep
> G()
> J/Jnom
> 0.6
> 0.8
> 1.0
> 1.2
> 1.4
> J/Jnom
> 0.6
> 0.8
> 1.0
> 1.2
> 1.4
> 1.6
> J/Jnom

Perturbation Budget Curves ( = 1.0) 

Figure 13. One-dimensional cross-sections at τ = 1 .0. Left: sweep βΛ with 

αΛ = −1.0 fixed. Right: sweep αΛ with βΛ = 10 .0 fixed. The certificate boundary ( G = 0 , blue) is crossed well before the empirical 90% performance boundary (red), quantifying the conservatism along each perturbation direction. Along the βΛ-axis (left panel), the certificate allows perturbations of roughly ±0.5 around the nominal βΛ = 10 , while the empirical 90% robustness extends to approximately βΛ ∈ [8 .5, 12 .0] .Along the αΛ-axis (right panel), the certificate permits αΛ ∈ [−1.1, −0.9] while the empirical region extends to αΛ ∈ [−1.3, −0.7] . In both directions, the certificate is conservative by a factor of roughly 2–3× in perturbation magnitude. Importantly, performance degrades gradually 

beyond the certificate boundary (there is no cliff edge), suggesting that the certificate provides a useful (if conservative) early warning even beyond its formal guarantee. E.6. Reward-Only Certificate. Theorem 3.1 provides an exact equivalence for reward-only perturbations (setting ℓδ = 0 ). Since §E.3 established that the transition KL term ℓδ dominates the certificate by 150 –800 × over the reward term, we expect that dropping ℓδ should certify nearly the entire perturbation grid. Figure 14 confirms this prediction. The reward-only certificate Grw certifies 401 –409 out of 

441 grid cells across all three τ values, compared to 18 –24 for the joint certificate G.0 1 2 3 4 5                

> Greward ()
> 0.25
> 0.50
> 0.75
> 1.00
> 1.25
> 1.50
> 1.75
> 2.00
> J( , )/ J( , )
> = 0.05
> Not certified
> Certified (401/441)
> 0.5 0.0 0.5 1.0 1.5 2.0 2.5
> Greward ()
> = 1.0
> Not certified
> Certified (409/441)
> 0.4 0.2 0.0 0.2 0.4 0.6
> Greward ()
> = 50.0
> Not certified
> Certified (409/441)
> Reward-Only Certificate (Theorem 3.1): a= 0

Figure 14. Reward-only certificate (Theorem 3.1, ℓδ = 0 ) vs empirical perfor-mance ratio. The reward-only certificate covers ∼93% of the perturbation grid with zero violations, confirming that the policy is inherently robust to reward misspecification. Figure 15 shows the reward-only certified region size across the full τ sweep. While the joint certificate G covers only 5–24 cells, the reward-only certificate covers ∼400 cells across the entire ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 36 

temperature range, with the gap to the empirical robust region ( ∼255 cells) closing to a factor of 

∼1.1.10 3 10 2 10 1 10 0 10 1 10 2        

> Temperature
> 0
> 100
> 200
> 300
> 400
> Number of robust cells (out of 441)
> Reward-Only vs Joint Certificate Region
> Reward-only cert ( -)
> Joint cert G(-)
> Empirical robust ( -)

Figure 15. Certified region size vs τ : reward-only certificate (green) vs joint G

(blue). The large gap confirms that certificate conservatism is entirely driven by the transition KL term. This analysis directly identifies the source of conservatism: the local relative entropy of logistic fill intensities grows quadratically with parameter deviation, producing large KL values even for moderate changes in (αΛ, β Λ). Tightening the transition bound—e.g., via action-dependent KL budgets—would directly enlarge the certified region. E.7. Global Certificate Tightness. Theorem 3.2 defines a global occupancy-based certificate 

Φglob that weights by the occupancy measure under perturbed dynamics, potentially yielding a less conservative bound than the local G which takes a maximum over states. We also consider the occupancy-weighted local certificate Gw = P 

> q

μ0(q)F (q) − r log (1 /r ), which uses the nominal 

occupancy as weights (so that Gw ≤ G by construction). Table 7 compares all certificate variants. The occupancy-weighted certificates certify modestly more cells than the state-max G: at τ = 1 .0, Gw certifies 25 cells and Φglob certifies 25 cells, compared to 24 for G.

Table 7. Certified region sizes (number of grid cells) on the αΛ-βΛ plane for different certificate variants. The empirical robust region (defined as J/J nom ≥ 0.9)is shown for reference. 

Certificate τ = 0 .05 τ = 1 .0 τ = 50 .0

G (state-max, Thm 3.6) 18 24 24 

Gw (occupancy-weighted) 19 25 26 

Φglob (Thm 3.2) 18 25 26 

Grw (reward-only, Thm 3.1) 401 409 409 Empirical ( J/J nom ≥ 0.9) 253 257 256 ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 37 0 20 40 60 80 100 120 140                   

> glob ()
> 0.25
> 0.50
> 0.75
> 1.00
> 1.25
> 1.50
> 1.75
> 2.00
> J/Jnom
> = 0.05
> Not certified
> Certified (18/441)
> 020 40 60 80 100 120 140
> glob ()
> = 1.0
> Not certified
> Certified (25/441)
> 020 40 60 80 100 120
> glob ()
> = 50.0
> Not certified
> Certified (26/441)
> Global Certificate glob (Proposition 3.4) vs Performance

Figure 16. Global certificate Φglob vs empirical performance ratio. The vertical line marks the certificate threshold r log (1 /r ) ≈ 0.37 . All certified points ( Φglob ≤

r log(1 /r ), red squares) achieve J/J nom > 0.94 .10 2 10 1 10 0 10 1

Temperature 

0

100 

200 

300 

400 

> Number of robust cells (out of 441)

Certificate Comparison: G vs loc vs glob    

> Glocal max (Thm 3.6)
> loc occ-weighted
> glob (Prop 3.4)
> Empirical robust

Figure 17. Certified region size vs τ for three certificate variants: G (state-max, blue), Gw (occupancy-weighted, orange), and Φglob (global, green). Green diamonds show the empirical robust region. All three certificates show monotone growth, with the occupancy-weighted variants certifying slightly more cells. The modest improvement from occupancy weighting reflects the structure of the inventory-stabilising policy: because the softmax policy penalises extreme inventories, the occupancy measure concentrates near q = 0 , where the F -values are similar to their maximum. In environments where occupancy is more dispersed (e.g., with weaker inventory penalties), the global certificate would provide a larger improvement. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 38 1.4 1.2 1.0 0.8 0.6      

> 8
> 9
> 10
> 11
> 12
> Certificate Coverage at = 1.0
> Both certify (24)
> Only glob (1)
> Only G(0)
> Neither (416)

Figure 18. Certificate coverage heatmap at τ = 1 .0. Green cells are certified by both G and Φglob ; blue cells are certified only by Φglob . The black star marks the nominal operating point. Figure 19 further confirms that Gw ≤ G pointwise across all 441 grid cells, with most points clustering near the diagonal—indicating that the worst-case state dominates the average. That the improvement from occupancy-weighting is small (only 1–2 additional cells) confirms that the conservatism of G is not driven by the max-over-states aggregation. Rather, the bottleneck lies in the per-state bound F (q), which aggregates the transition KL uniformly over all actions. 0 20 40 60 80 100 120     

> G(state-max)
> 0
> 20
> 40
> 60
> 80
> 100
> 120
> Gw (occupancy-weighted)
> State-Max vs Occupancy-Weighted Certificate ( = 1.0)
> Gw=G

Figure 19. G (state-max) vs Gw (occupancy-weighted) for all 441 grid cells at 

τ = 1 .0. All points lie below the diagonal ( Gw ≤ G), confirming the theoretical ordering. Most points cluster near the diagonal, indicating that the worst-case state dominates the average. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 39 = 0.05 = 1.0 = 50.0       

> 0
> 50
> 100
> 150
> 200
> 250
> Certified region size
> 18 24 24
> 19 25 26
> 253 257 256

Certified Region: State-Max vs Occupancy-Weighted vs Empirical     

> G0 (state-max)
> Gw0 (occ-weighted)
> Empirical J/Jnom 0.9

Figure 20. Certified region sizes for G (state-max), Gw (occupancy-weighted), and the empirical robust region. Occupancy-weighting provides a small improve-ment ( 1–2 additional cells), but the ∼10 × gap to the empirical region remains. E.8. Policy-Weighted KL Decomposition. The certificate computes an effective per-state KL contribution via the soft-max aggregation ℓcert (q) = τ  lse (ℓ/τ ) − log |A| , which aggregates over all actions. The actual policy πτ (· | q) = softmax (Rθ(q, ·)/τ ) concentrates mass on high-reward (often low-KL) actions, so the effective KL experienced by the policy is ℓπ(q) = P

δ π(δ | q) ℓδ(q).= 1.0,  = 8.75 

> = 1.0,  = 11.25
> = 1.25,  = 10
> = 0.75,  = 10
> = 1.25,  = 8.75
> = 0.75,  = 11.25
> = 1.5,  = 7.5
> = 0.5,  = 12.5
> 10 1
> 10 2
> Transition KL contribution

Policy-Weighted vs Certificate KL at q = 0, = 1.0   

> cert (logsumexp)
> (policy-weighted)
> unif (uniform avg)

Figure 21. Comparison of three KL aggregation methods at q = 0 (τ =1.0) across 8 representative perturbation points. The certificate’s logsumexp aggregation (blue) exceeds the policy-weighted KL (orange) and the uniform average (green), but the amplification is modest ( 1–2.5×). ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 40 1.4 1.2 1.0 0.8 0.6    

> 8
> 9
> 10
> 11
> 12
> KL Amplification Factor at q= 0, = 1.0
> 1.0
> 1.2
> 1.4
> 1.6
> 1.8
> 2.0
> 2.2
> 2.4
> cert /

Figure 22. KL amplification ratio ℓcert /ℓ π across the αΛ-βΛ grid at q = 0 ,

τ = 1 .0. The ratio ranges from 1.0 (at small perturbations) to 2.5 (at grid extremes). The black star marks the nominal point. The amplification factor is modest ( 1.0–2.5×), indicating that at τ = 1 .0 the softmax policy distributes weight broadly enough that the logsumexp aggregation closely approximates the policy-weighted KL. At smaller τ (more concentrated policies), the amplification would be larger. Combined with the finding that state-max and occupancy-weighted certificates differ minimally (§E.7), this localises the certificate’s conservatism to the inherent gap between the local relative entropy of logistic intensities and the actual performance degradation—a gap that could be closed with distribution-dependent KL bounds. E.9. Action Space Convergence. Example 3.7 shows that for discrete-time robust sets, the certificate degenerates as the action space grows: the discrete-time KL penalty scales with |A| ,eventually making the robust set empty. In continuous time, the normalisation log |A| in the certificate formula compensates for the action space size. We verify this numerically by computing the certified region for action grids ranging from |A| = 25 (5 × 5) to |A| = 10 ,000 (100 × 100 ). 10 2 10 3 10 4       

> || (number of actions)
> 21.0
> 21.5
> 22.0
> 22.5
> 23.0
> 23.5
> 24.0
> Certified region size
> Action Space Convergence ( = 1.0)
> Recomputed Cshift
> Fixed Cshift (from N= 20)

Figure 23. Certified region size vs action space cardinality at τ = 1 .0. Both the recomputed Cshift (blue) and fixed Cshift (red) variants converge by |A| ≈ 225 ,confirming that the continuous-time certificate remains non-degenerate. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 41 10 2 10 3 10 4  

> || (number of actions)
> 17
> 18
> 19
> 20
> 21
> 22
> 23
> 24
> Certified region size
> Action Space Convergence (Multiple )
> = 0.05
> = 1.0
> = 50.0

Figure 24. Action space convergence for three entropy temperatures. All three converge rapidly and plateau at their respective certified region sizes ( 18 –24 cells). The certificate is non-degenerate across 400 × variation in |A| .The reward shift Cshift is constant at 38 .66 across all action grid sizes because the minimum reward always occurs at the grid boundary δ = δmin = 0 .12 with extreme inventory q = ±10 .20 40 60 80 100    

> N(ticks per side)
> 37.0
> 37.5
> 38.0
> 38.5
> 39.0
> 39.5
> 40.0
> 40.5
> Cshift
> Reward Shift Cshift vs Action Grid Resolution

Figure 25. Reward shift Cshift vs action grid resolution Nδ. The shift is constant because the minimum reward is determined by the grid boundary, not the interior resolution. The convergence is rapid: the certified region stabilises by Nδ = 15 (|A| = 225 ) for all τ values. This confirms the theoretical advantage of the continuous-time certificate formulation over its discrete-time counterpart: the log |A| normalisation ensures that refining the action discretisation does not degrade the certificate, in stark contrast to the degeneration described in Example 3.7. 

Appendix F. Event-Based vs Fixed-Grid Time discretization 

This section presents empirical evidence comparing two approaches for implementing continuous-time environments as RL training simulators: fixed-grid discretization, which evaluates the policy at uniformly spaced time steps tk = k∆t, and event-based (arrival-driven) discretization, which advances the simulation to the next stochastic event (e.g., order arrival, queue transition) and evaluates the policy only at event times. We argue that event-based discretization is preferable on three grounds: (i) it eliminates a sensitive hyperparameter ( ∆t), (ii) it avoids a fundamental bias–variance trade-off that makes fixed-grid performance non-monotone in ∆t, and (iii) its computational cost scales with the event rate of the underlying process rather than an artificial grid resolution. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 42 

F.1. The discretization Trade-Off. In a fixed-grid implementation with step size ∆t, the agent observes the state and selects an action at each time tk = k∆t, k = 0 , 1, . . . , ⌊T / ∆t⌋. The choice of ∆t introduces a trade-off: 

• Large ∆t (coarse grid): The agent makes fewer decisions per episode, reducing the action space temporally but introducing discretization error : the policy cannot react to events that occur between grid points. 

• Small ∆t (fine grid): discretization error decreases, but the number of steps per episode grows as T / ∆t, increasing the trajectory length. This exacerbates the credit assignment problem : each individual reward signal becomes smaller (scaling as ∆t), and the agent must propagate information over more steps to learn long-horizon behaviour. Variance in policy gradient estimates also increases with trajectory length. This trade-off implies that there exists an intermediate ∆t at which fixed-grid RL performance peaks—and this optimum is a priori unknown to the practitioner. Event-based discretization sidesteps this trade-off entirely by operating at the natural time scale of the environment: the agent acts only when something happens. F.2. Market-Making Experiment. We train policy gradient (PG) and advantage actor-critic (A2C) agents on the single-asset market-making environment (cf. §D.1.1) under both event-based and fixed-grid discretizations. For fixed-grid, we vary the number of steps per episode: 

n ∈ { 50 , 100 , 200 , 500 , 1000 }, corresponding to ∆t = T /n . All agents use the same network architecture (MLP [64 , 64] ), learning rate ( 10 −3), and are trained for 4,000 epochs with 5 seeds each. The evaluation metric is the per-episode total reward (undiscounted), which is comparable across discretizations. Table 8 reports converged per-episode rewards. The Asmussen–Scanlan (AS) analytical optimal provides a reference upper bound ( J∗ = 68 .13 ). 

Table 8. Market-making: per-episode total reward (5 seeds, last 200 training epochs). %J∗ is the fraction of the AS optimal achieved. Event-driven results are from separate policy evaluation since per-step training rewards require knowledge of the (stochastic) number of events per episode.                                                                                 

> discretization Algorithm J(mean ±SE) %J∗nsteps Fixed-grid ( n=200 )PG 66 .41 ±0.37 97 .5% 200 Event-driven PG 64 .88 ±0.36 95 .2% variable Fixed-grid ( n=500 )PG 62 .30 ±0.62 91 .5% 500 Fixed-grid ( n=1000 )PG 52 .54 ±0.38 77 .1% 1000 Fixed-grid ( n=100 )PG 47 .48 ±0.31 69 .7% 100 Fixed-grid ( n=50 )PG 24 .04 ±0.06 35 .3% 50 Fixed-grid ( n=200 )A2C 60 .53 ±0.06 88 .9% 200 Event-driven A2C 57 .04 ±0.22 83 .7% variable Fixed-grid ( n=500 )A2C 54 .62 ±0.14 80 .2% 500 Fixed-grid ( n=1000 )A2C 50 .42 ±0.07 74 .0% 1000 Fixed-grid ( n=100 )A2C 44 .86 ±0.09 65 .8% 100 Fixed-grid ( n=50 )A2C 22 .47 ±0.07 33 .0% 50

Several observations are immediate. First, performance is strongly non-monotone in n for fixed-grid agents: both PG and A2C peak at n = 200 (∆t = 0 .05 ), with substantial degradation for both coarser and finer grids. At n = 1000 (∆t = 0 .01 ), PG achieves only 77 .1% of J∗, a 20 -percentage-point drop from the peak—despite having five times finer temporal resolution. Second, event-driven agents achieve competitive performance without requiring any grid resolution tuning: event-driven PG attains 95 .2% of J∗, which places it between the best ( n = 200 ) and second-best ( n = 500 ) fixed-grid configurations. An RL practitioner who does not know the optimal 

n in advance risks choosing n = 500 or n = 1000 , both of which significantly underperform event-driven PG. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 43 

F.3. discretization Sweep: Wall-Clock Cost. To quantify the computational trade-off, we conduct a dedicated sweep of A2C agents across six ∆t values with identical training budgets (2,000 epochs, 5 seeds). We record wall-clock training time on the same hardware. 

Table 9. A2C discretization sweep: per-episode total reward and wall-clock training time (5 seeds, 2,000 epochs). 

Method ∆t J (mean ± SE) %J∗ Time (s) FG A2C 0.02 22 .46 ± 0.06 33 .0% 132 

FG A2C 0.01 44 .68 ± 0.11 65 .6% 239 

FG A2C 0.005 60 .46 ± 0.14 88 .7% 527 

FG A2C 0.002 54 .52 ± 0.10 80 .0% 1,296 

FG A2C 0.001 50 .07 ± 0.06 73 .5% 2,763 

FG A2C 0.0005 46 .51 ± 0.05 68 .3% 6,039 

Arrival-driven A2C — 57 .04 ± 0.22 83 .7% 876 

The non-monotone pattern is stark: performance peaks at ∆t = 0 .005 (88 .7% of J∗) and declines for both coarser and finer grids. The finest grid ( ∆t = 0 .0005 ) achieves only 68 .3% 

of J∗ while requiring 6,039 seconds of training— 11 .5× slower than the peak and 6.9× slower than arrival-driven. The arrival-driven agent achieves 83 .7% of J∗ at 876 seconds, placing it favourably on the performance–cost Pareto frontier (see Figure 27). 10 3  

> 10 2
> t
> 30
> 40
> 50
> 60
> 70
> Per-Episode Reward
> Discretisation Sensitivity: Reward vs t
> Fixed-grid A2C
> Arrival-driven A2C (57.0 ± 0.22)
> AS optimal (68.1)

(a) Per-episode reward vs ∆t. Performance is non-monotone, peaking at an intermediate ∆t that is unknown a priori .10 3  

> 10 2
> t
> 10 3
> Wall-Clock Time (seconds)
> Training Time vs t
> Fixed-grid A2C
> Arrival-driven A2C (876s)

(b) Wall-clock training time increases monotoni-cally as ∆t decreases. Finer grids are both slower and worse. 

Figure 26. Fixed-grid A2C discretization sweep on the market-making environ-ment (5 seeds, 2,000 epochs). The dashed red line shows the arrival-driven A2C baseline. Finer temporal resolution ( ∆t → 0) does not improve RL performance; instead, the credit assignment problem dominates, and the agent fails to learn effective policies despite much higher computational cost. ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 44 0 1000 2000 3000 4000 5000 6000   

> Wall-Clock Time (seconds)
> 30
> 40
> 50
> 60
> 70
> Per-Episode Reward
> t=0.02
> t=0.01
> t=0.005
> t=0.002
> t=0.001
> t=0.0005
> Performance vs Training Cost
> Arrival-driven A2C
> J*= 68.1

Figure 27. Performance vs training cost for fixed-grid A2C at various ∆t

(blue circles) and arrival-driven A2C (red square). The arrival-driven agent achieves strong performance without requiring grid resolution tuning. No fixed-grid configuration simultaneously dominates the arrival-driven agent in both reward and training time by a large margin. F.4. Queueing Scheduling Experiment. We also compare discretizations on the criss-cross queueing network (cf. §D.1.2), with arrival rates λ = (1 .5, 1.5, 1.5) and service rates μ =(2 .0, 3.0, 2.0) . The holding costs are h = (3 .0, 1.0, 2.0) . Two analytical baselines provide reference points: the cμ -rule ( Jcμ = −1163 .3) and the MaxPressure policy ( JMP = −1276 .4), where less negative is better. 

Table 10. Queueing scheduling: per-episode total reward (5 seeds, 2,000 epochs). Analytical baselines are shown for reference. 

Method J (mean ± SE) FG A2C ( ∆t = 0 .1) −1,105 ± 3

Event-driven PG −1,139 ± 5

cμ -rule (analytical) −1,163 

Event-driven A2C −1,251 ± 10 

MaxPressure (analytical) −1,276 

FG A2C ( ∆t = 0 .01 ) −1,337 ± 4

The same non-monotone pattern appears: a 10 × refinement of the grid from ∆t = 0 .1 to 

∆t = 0 .01 degrades performance by 21% (from −1,105 to −1,337 ). The event-driven PG agent achieves −1,139 , outperforming the cμ -rule analytical baseline and falling between the two fixed-grid A2C configurations. F.5. Discussion. The experiments above support three conclusions about time discretization for continuous-time RL environments: 1. Fixed-grid performance is non-monotone in ∆t. In both market making and queueing, performance peaks at an intermediate ∆t and degrades for both coarser and finer grids. This is not a training budget artefact: all configurations receive the same number of epochs. The root cause is a trade-off between discretization bias (large ∆t, too few decisions) and credit assignment difficulty (small ∆t, too many low-signal steps per episode). The optimal ∆t depends on the environment dynamics and is unknown a priori, making it a difficult hyperparameter to tune. 2. Event-based discretization eliminates ∆t. The arrival-driven implementation has no grid resolution hyperparameter. It automatically operates at the natural time scale of the stochastic process, making decisions only when events occur. This yields competitive performance without tuning: event-driven PG achieves 95 .2% of J∗ in market making (vs. 97 .5% for the best-tuned ENTROPY REGULARIZATION IMPROVES ROBUSTNESS IN CONTINUOUS-TIME RL 45 

fixed grid) and outperforms the cμ -rule in queueing. An RL practitioner using event-driven discretization avoids the risk of accidentally choosing a suboptimal ∆t (e.g., n = 1000 in market making achieves only 77% of J∗). 3. Computational cost scales with physics. In the event-based approach, the number of steps per episode is determined by the event rate of the environment (e.g., order arrival intensity in market making, transition rates in queueing), not by an artificial grid. This avoids wasting computation on time intervals where no events occur. In the market-making discretization sweep, the finest grid ( ∆t = 0 .0005 ) requires 6,039 s of training and achieves only 68 .3% of J∗, while arrival-driven A2C uses 876 s and achieves 83 .7% . More generally, event-based cost grows linearly with the total event rate, providing a natural computational scaling.