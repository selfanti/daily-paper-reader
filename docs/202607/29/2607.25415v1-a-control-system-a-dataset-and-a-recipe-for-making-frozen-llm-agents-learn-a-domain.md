---
title: "A Control System, a Dataset, and a Recipe for Making Frozen LLM Agents Learn a Domain"
title_zh: 一个控制系统、一个数据集以及一份让冻结的LLM智能体学习领域的配方
authors: Debjyoti Paul
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25415v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 基于强化学习对冻结LLM智能体外套进行策略学习
tldr: 冻结LLM agent的性能受限于固定harness（提示模板、工具集等），现有优化方法不可审计或依赖黑盒API。本文提出将harness转为小规模可读动作空间，用ε-贪心上下文bandit和REINFORCE在线学习策略，以多目标奖励评分。在三个任务（工具使用、代码生成HumanEval、多跳检索HotpotQA）和两个模型提供商上评估，效果显著。发布代码、数据集和部署方案，助力领域适配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25415v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 998, \"height\": 819, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25415v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1537, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25415v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1561, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25415v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1177, \"height\": 266, \"label\": \"Table\"}]"
motivation: 现有LLM agent harness优化方法成本高、不可审计且无法用于黑盒API，需更受约束的可控方案。
method: 将harness视为固定动作空间，使用ε-贪心上下文bandit和REINFORCE在线学习策略，以多目标奖励评分。
result: 在工具使用、HumanEval和HotpotQA任务上，本地Ollama和AWS Bedrock模型均获得性能提升。
conclusion: 该控制系统能高效适配新领域，发布可复现代码与配方，适用于黑盒模型API。
---

## 摘要
生产级LLM智能体越来越多地由一个冻结模型组装而成，该模型包裹在一个框架中：包括提示模板、工具集、记忆/检索层、规划策略和验证策略。2026年的两个系统——Meta-Harness (Lee et al., 2026) 和 HyperAgents (Meta AI, 2026) ——表明，这个框架本身可以由一个智能体提议者进行优化甚至自我重写，但代价是昂贵的代码搜索循环或不受约束的自我修改代码，这两者都无法审计，也无法在完全黑盒的模型API中使用。我们采取了一个更狭窄、更受约束的立场：将框架视为一个小的、固定的、人类可读的动作空间，并通过经典的样本高效强化学习（一个ε-贪婪的上下文赌博机和REINFORCE）在线学习其上的策略，根据多目标奖励（任务成功、验证器得分、策略合规性、成本、延迟以及不支持声明的惩罚）进行评分。我们使用DSPy (Khattab et al., 2024) 作为上下文组装器和最强非自适应基线（DSPy BootstrapFewShot静态提示）的来源来实例化这个控制系统，并在三个可验证任务领域——工具使用工作流、代码生成（HumanEval）和多跳检索问答（HotpotQA）——以及两个模型提供商（本地Ollama模型和AWS Bedrock）上进行评估。我们发布了框架控制系统代码、跨领域可验证任务套件、训练中的完整轨迹/奖励分解日志，以及一个与提供商无关的部署配方，用于将其应用于新组织的领域和验证设置。

## Abstract
Production LLM agents are increasingly assembled from a frozen model wrapped in a harness: a prompt template, a tool set, a memory/retrieval layer, a planning strategy, and a verification policy. Two 2026 systems, Meta-Harness (Lee et al., 2026) and HyperAgents (Meta AI, 2026), show that this harness can itself be optimized or even self-rewritten by an agentic proposer -- at the cost of either an expensive code-search loop or unconstrained self-modifying code, neither of which is auditable or usable with a fully black-box model API. We take a narrower, more constrained position: treat the harness as a small, fixed, human-legible action space and learn a policy over it online with classic sample-efficient reinforcement learning (an $ε$-greedy contextual bandit and REINFORCE), scored against a multi-objective reward (task success, verifier score, policy compliance, cost, latency, and an unsupported-claim penalty). We instantiate this control system with DSPy (Khattab et al., 2024) as both the context assembler and the source of the strongest non-adaptive baseline (a DSPy BootstrapFewShot static prompt), and evaluate it across three verifiable task domains -- tool-use workflows, code generation (HumanEval), and multi-hop retrieval QA (HotpotQA) -- and two model providers (a local Ollama model and AWS Bedrock). We release the harness-control-system code, the cross-domain verifiable task suite, the full trajectory/reward-decomposition logs from training, and a provider-agnostic deployment recipe for applying this to a new organization's domain and verification setup.

---

## 论文详细总结（自动生成）

# 1. 论文的核心问题与整体含义（研究动机和背景）
- **生产级 LLM 智能体的核心瓶颈**：实际部署中，冻结的大语言模型（LLM）性能不仅取决于模型本身，更取决于其“框架”（harness）——即提示模板、工具集、记忆/检索层、规划策略和验证策略的组合。2026 年的两个前沿工作（Meta-Harness、HyperAgents）尝试自动优化框架，但它们依赖 LLM 生成或重写任意代码，代价高昂、不可审计，且无法用于黑盒 API（如仅支持 chat-completions 的模型）。
- **本文立场**：提出一种更受约束、更实用的替代方案——将框架定义为**小规模、固定、可枚举的动作空间**，并通过经典的样本高效强化学习（ε-贪心上下文 Bandit 和 REINFORCE）在线学习策略，以多目标奖励（任务成功率、验证器得分、策略合规性、成本、延迟、伪声明惩罚）优化。这样既保证了每个决策可控、可追溯，又无需代码执行能力，适用于任何黑盒 API。

# 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- 将框架分解为 6 个离散杠杆（共 729 种配置）：
  - **提示风格**：直接/结构化/反思
  - **工具策略**：从不/按需/总是（领域内重新解释，如检索强度）
  - **记忆策略**：无/仅成功轨迹/成功与失败轨迹
  - **规划策略**：无/简要计划/计划并修订
  - **验证策略**：无/最终检查/逐步检查
  - **最大步数**：2/4/6
- 使用 **DSPy** 作为上下文组装器：根据选定配置构建 DSPy Signature 和模块（Predict、ChainOfThought 或 draft-then-revise），并将记忆策略的过去轨迹作为少量示例附加。
- **奖励函数**：多目标加权奖励：  
  `R = w_s · success + w_v · verifier_score + w_c · compliance - w_u · unsupported_claims - w_$ · tokens/1000 - w_l · latency/1000`  
  其中 success 由领域特定的确定性验证器判定（代码：单元测试；工具：状态差分；检索：精确匹配 + token-F1）。
- **控制器**：
  - **随机搜索**（下限）
  - **ε-贪心上下文 Bandit**：使用任务特征向量的线性价值估计，采用乐观初始化以避免冷启动陷阱（见实验部分）
  - **REINFORCE**：对 729 个动作的 softmax 策略，线性特征对数，熵正则化，移动平均基线
  - **DSPy-static 基线**：通过 DSPy BootstrapFewShot 对每个 (模型, 领域) 运行一次得到的静态提示，作为最强的非自适应对比基线。

## 算法流程（文字说明）
1. 定义任务分布和特征向量。
2. 定义确定性验证器（在接触模型之前）。
3. 定义小规模、可枚举的框架动作空间（6 个维度）。
4. 使用 DSPy 将候选配置组装为具体上下文。
5. 在线训练阶段：智能体每次选择动作（框架配置），执行任务，收集轨迹（动作、奖励分解、原始输出）。
6. 控制器（Bandit 或 REINFORCE）更新策略参数，逐步优化。
7. 部署时，可结合低置信度时交给人处理的升级规则（Agentic-RPA 学习循环）。

# 3. 实验设计：数据集 / 场景、benchmark、对比方法
### 数据集与场景
- **三个可验证任务领域**（共 120 个任务，80/20 训练/测试分割）：
  1. **工具使用**（40 个合成多工具 CRM 工作流：创建/更新/删除/合并，状态差分判定）
  2. **代码生成**（40 个 HumanEval 问题，通过官方单元测试判定）
  3. **多跳检索 QA**（40 个 HotpotQA 问题，精确匹配判定，使用自带黄金+干扰段落作为本地检索语料）
- 额外发布完整轨迹日志（4,620 个 episode）及奖励分解。

### Benchmark 与对比方法
- **模型提供商**：
  - Ollama（本地 qwen2.5:7b）
  - AWS Bedrock（Haiku 作为主流模型，Sonnet 作为更强模型 spot check）
- **对比方法**（共 4 种优化器）：
  - 随机搜索（下限）
  - DSPy-static（DSPy BootstrapFewShot 优化后的静态提示，最强非自适应基线）
  - ε-贪心上下文 Bandit
  - REINFORCE

# 4. 资源与算力
- **总花费**：7.62 美元（包括主矩阵、spot check 和样本效率运行）。
- **吞吐量**：Ollama 本地模型约 2.7 episode/分钟（串行），Bedrock Haiku 可扩展并发。
- **未说明**：具体的 GPU 型号、数量、训练时长。仅提及主矩阵中 Ollama 25 episode/种子，Haiku 60 episode/种子，Sonnet 40 episode/种子。样本效率实验使用 300 episode/run。
- **说明**：作者强调所有实验均为真实测量，无模拟。

# 5. 实验数量与充分性
### 主要实验矩阵
- 4 个优化器 × 3 个领域 × 2 个模型（Ollama + Bedrock Haiku）= 2,460 episode
- Ollama：1 种子 × 25 episode/任务；Bedrock Haiku：3 种子 × 60 episode/任务
- 额外 spot check：Bedrock Sonnet，2 种子 × 40 episode（960 episode）
- 样本效率实验：工具使用 / Bedrock Haiku，2 种子 × 300 episode（1,200 episode）
- **总计**：4,620 个 logged episode。

### 实验充分性分析
- **正面**：覆盖三个差异显著的任务领域、两种类型（本地 / 云）的模型、多轮种子；提供了详细的可复现轨迹；报告了冷启动陷阱、崩溃隔离、缓存测量错误等修复前后的对比，增强公平性。
- **局限性**：
  - 主矩阵中在线控制器的 episode 预算（25-60）远小于动作空间大小（729），导致样本效率不足，无法达到其潜在上限（作者坦诚指出）。
  - 种子数偏少（1-3），统计显著性有限。
  - OpenAI API 密钥不可用，未能扩展至另一个主流模型。
  - 工具使用领域为模拟 CRM，非真实生产系统。
  - 不支持声明度量仅针对检索领域，非通用幻觉检测器。

# 6. 论文的主要结论与发现
- **出乎意料的 headline 结果**：在所有领域和模型上（包括更强的 Bedrock Sonnet），**DSPy 优化的静态基线始终匹配或击败在线自适应控制器（Bandit 和 REINFORCE）**，且 token 成本显著更低（如工具使用/Haiku：DSPy-static 96% 成功率，285 tokens/episode vs REINFORCE 62%，680 tokens/episode）。
- **根本原因**：并非方法本身失效，而是**样本效率差距**。在线控制器从均匀先验开始，需要接近动作空间大小（729）的样本数才可能有效，而主矩阵预算仅 25-60 episode。后续 300 episode 的长运行中，Bandit 和 REINFORCE 仍远低于静态基线水平（0.6-0.74 vs 0.96），说明差距真实存在。
- **实践启示**：在线框架控制应当**先通过静态优化引导**（如 DSPy BootstrapFewShot），再部署在线控制器，而非冷启动。该结论直接指导了部署配方（RECIPE.md）的设计。
- **工程陷阱与修复**：
  - **冷启动陷阱**：Bandit 默认 argmax 选择动作 0（工具策略=never），导致工具使用任务 0% 成功率。修复：乐观初始化 + 随机打破平局。
  - **崩溃隔离**：Sonnet 模型上的 plan-and-revise 配置导致 DSQy 结构化输出适配器崩溃，一个 episode 失败拖垮整个作业。修复：episode 级 try/except 捕获解析失败，作为正常失败处理。
  - **缓存测量错误**：DSPy 磁盘缓存返回空 usage dict，导致 token 计数被低估。修复：字符长度回退估计。

# 7. 优点：方法或实验设计上的亮点
- **高度受约束、可审计**：将框架限制为 729 个已知配置，每个决策可溯源，适合企业级合规需求。
- **黑盒 API 友好**：控制器无需代码执行权限，仅需标准 chat-completions 端点，适用于防火墙后的部署。
- **多目标奖励**：首次将策略合规性、成本、延迟、伪声明惩罚纳入优化目标，而非仅关注准确率。
- **真实可复现**：发布完整代码、数据集、轨迹日志（4,620 episode）以及详细的部署配方（RECIPE.md），包括低置信度时移交人类处理的安全机制。
- **诚实报告负面结果**：在线控制器未超越静态基线时，没有试图美化，而是深入分析原因（样本效率差距），并提出可操作的改进方向（先引导再部署）。
- **发现并公开常见工程陷阱**：冷启动、崩溃隔离、缓存测量等细节分享，对实际部署极具价值。

# 8. 不足与局限
- **实验覆盖有限**：
  - 主矩阵 seed 数少（1-3），episode 预算过低，未能充分展现在线控制器的潜力。
  - 仅覆盖两个模型提供商，未纳入 OpenAI（API 不可用）等主流模型。
  - 工具使用领域为合成模拟，未在真实生产系统上验证。
- **方法本身限制**：
  - 动作空间固定为 729 种组合，可能无法覆盖所有有用变化（如连续型参数）。
  - 不支持声明惩罚仅基于检索领域的词法重叠，不是通用幻觉检测。
- **资源使用未细化**：未报告 GPU 型号、训练时长等，重复实验可能需自行推算。
- **总结**：作者坦诚这些局限是配置或后续实验的扩展选择，而非架构缺陷，并指出更大的 episode 预算和初始化策略（从静态基线初始化在线策略）是自然的未来工作方向。

（完）
