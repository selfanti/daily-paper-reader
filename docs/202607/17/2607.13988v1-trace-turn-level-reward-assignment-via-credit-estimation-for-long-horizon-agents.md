---
title: "TRACE: Turn-level Reward Assignment via Credit Estimation for Long-Horizon Agents"
title_zh: TRACE：通过信用估计实现长程智能体的回合级奖励分配
authors: "Leitian Tao, Baolin Peng, Wenlin Yao, Tao Ge, Hao Cheng, Mike Hang Wang, Jianfeng Gao, Sharon Li"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13988v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向智能体强化学习的密集信用分配方法
tldr: 长程多轮智能体在大量工具调用后产生最终答案，导致奖励稀疏且方差高。TRACE通过冻结参考模型获取黄金答案的对数概率，将其转化为状态值，并利用时序差分推导每步奖励，实现密集信用分配。在BrowseComp-Plus基准上，Qwen3-4B从7.2提升至35.6，Qwen3-30B-A3B从8.4提升至42.6。该方法无需额外评论模型或过程标签，RL训练收敛更快且效果显著。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13988v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1313, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13988v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13988v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1430, \"height\": 295, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13988v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1445, \"height\": 361, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13988v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 723, \"height\": 390, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13988v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1477, \"height\": 826, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13988v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 420, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13988v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1405, \"height\": 1255, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13988v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1404, \"height\": 869, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13988v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1445, \"height\": 507, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13988v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1032, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13988v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1445, \"height\": 603, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13988v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1395, \"height\": 426, \"label\": \"Table\"}]"
motivation: 长程多轮智能体的最终奖励稀疏且高方差，无法有效区分有益动作与最终错误，导致信用分配困难。
method: 将卷展表示为状态转换，利用冻结参考模型的对数概率得到状态值，通过时序差分计算每步奖励，实现密集信用分配。
result: 在BrowseComp-Plus上，Qwen3-4B提升至35.6，Qwen3-30B-A3B提升至42.6，且无需SFT或网页数据。
conclusion: TRACE提供密集奖励，显著提升长程工具使用能力，RL训练效率更高且泛化性强。
---

## 摘要
多轮智能体通过一系列工具交互来解决复杂任务，最终给出答案，这使得信用分配成为后训练中的基本挑战。结果奖励为短程推理提供了可靠的监督，但随着轨迹增长到数十或数百次工具调用，结果奖励变得稀疏且方差大。它们也可能具有误导性：一次失败的探索可能包含许多使智能体更接近目标的有用动作，但仅基于结果的训练会将这些动作与最终错误赋予相同的负优势。我们提出TRACE（通过信用估计实现回合级奖励分配），一种用于智能体强化学习的密集信用分配方法。TRACE将探索轨迹表示为工具调用边界处的状态转换，从冻结的参考模型获取黄金答案的对数概率，将其转换为对数比率状态值，并推导每个动作的奖励作为这些值的时间差分变化。这不需要额外的评判器或过程标签训练，其一步对数比率时间差分组件可在冗余工具调用间进行 telescoping。在长程复杂搜索任务中，TRACE仅使用纯强化学习显著提升了基础模型的工具使用能力，无需冷启动监督微调、智能体中训练阶段或在实时网络数据上训练。在封闭网络的BrowseComp-Plus基准上，它将Qwen3-4B从7.2提升到35.6，将Qwen3-30B-A3B从8.4提升到42.6。学习到的搜索行为也迁移到开放网络基准，学习曲线显示强化学习训练期间更早的改进和更快的收敛。

## Abstract
Multi-turn agents solve complex tasks through extended sequences of tool interactions before producing a final answer, making credit assignment a fundamental challenge during post-training. Outcome rewards provide reliable supervision for short-horizon reasoning, but become sparse and high-variance as trajectories grow to tens or hundreds of tool calls. They can also be misleading: a failed rollout may contain many useful actions that move the agent closer to the goal, yet outcome-only training assigns them the same negative advantage as the eventual mistake. We propose TRACE (Turn-level Reward Assignment via Credit Estimation), a dense credit-assignment method for agentic reinforcement learning. TRACE represents rollouts as state transitions at tool-call boundaries, obtains gold-answer log-probabilities from a frozen reference model, transforms them into log-ratio state values, and derives per-action rewards as Temporal-Difference changes in those values. This requires no additional critic or process-label training, and its one-step log-ratio TD component telescopes across redundant tool calls. On long-horizon complex search, TRACE substantially improves base-model tool-use ability using pure RL, without a cold-start supervised fine-tuning stage, an agentic mid-training stage, or training on live-web data. On the closed-web BrowseComp-Plus benchmark, it raises Qwen3-4B from $7.2$ to $35.6$ and Qwen3-30B-A3B from $8.4$ to $42.6$. The learned search behavior also transfers to open-web benchmarks, and the learning curves show earlier improvement and faster convergence during RL training.

---

## 论文详细总结（自动生成）

## 详细论文总结：TRACE：通过信用估计实现长程智能体的回合级奖励分配

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长程多轮智能体在执行复杂任务（如网页导航、工具调用）时，需要经过数十乃至上百次交互才产生最终答案。现有强化学习（RL）方法通常只使用最终结果奖励（outcome reward），但该奖励稀疏、方差高，且无法区分轨迹中有用的中间动作与冗余或有害的动作。这导致信用分配困难：有用探索与最终错误被赋予相同的负优势，阻碍有效学习。
- **研究动机**：希望在不依赖过程标签、强判别模型或训练额外奖励模型的前提下，为长程智能体提供密集且可靠的回合级（turn-level）信用信号，以提升纯RL训练的效果。
- **整体含义**：TRACE通过将卷展分解为工具调用边界处的状态转换，利用冻结参考模型估计每个前缀对黄金答案的预测能力，从而导出每步动作的时差（TD）信用。该方法无需冷启动监督微调、中训练阶段或实时网络数据，显著提升了基模型在长程搜索任务上的工具使用能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：用冻结的参考模型（policy初始化副本）作为稳定探针，评估每个轨迹前缀（包括前k次工具调用和观察）使黄金答案更可预测的程度。将黄金答案的对数概率转换为对数比率状态值，然后利用相邻状态值的时间差分（TD）变化作为每步动作的奖励。
- **关键技术细节**：
  - **工具边界状态**：将完整卷展τ = (x, a1, o1, ..., aT, oT, ŷ) 按工具调用边界划分为前缀S0, S1, ..., ST（S0含问题，Sk含前k次交互）。每次过渡由 (a_{k+1}, o_{k+1}) 引起。
  - **状态值通过对数比率答案概率**：
    - 对于前缀Sk，计算黄金答案y*的平均对数概率：ℓ̄_k = (1/|y*|) Σ_t log πref(y*_t | Sk, y*_{<t})。
    - 定义剩余间隙 d_k = -ℓ̄_k + ε，状态值 V(Sk) = log(d_0 / d_k)。V(S0)=0，V越大表示更接近黄金答案。
  - **回合级奖励通过时差信用**：
    - 一步TD信用：δ_k = V(Sk+1) - V(Sk) = log(d_k / d_{k+1})。正值为进步，负值为退步。该信用可 telescoping（累积仅依赖始末状态）。
    - K步TD信用：由于工具效应可能延迟，使用截断K步TD备份：c^{(K)}_{g,k} = (1/Z_{g,k}) Σ_{u=k}^{h_{g,k}} γ_{td}^{u-k} δ_{g,u}，其中h_{g,k} = min(k+K-1, T_g-1)，Z为归一化因子。
    - 终端锚定：当备份窗口到达轨迹末尾时，额外加入终端结果优势：r_turn_{g,k} = c^{(K)}_{g,k} + 1[h_{g,k}=T_g-1] λ_term γ_{td}^{T_g-k} A_out_g。
  - **联合优化与结果奖励**：
    - 结果优势A_out_g采用GRPO的组归一化：A_out_g = (R_g - R̄)/σ_R（若σ_R=0则取0）。
    - 每令牌混合优势：Â_{g,t} = α_out A_out_g + α_turn r_turn_{g, turn(t)}。
    - 优化目标为带裁剪的GRPO目标：J_TRACE(θ) = (1/G) Σ_g (1/|I_g|) Σ_t min(ρ_{g,t}(θ)Â_{g,t}, clip(ρ_{g,t}(θ), 1-c-, 1+c+)Â_{g,t})。
- **算法流程**（文字说明）：
  1. 从当前策略采样G条轨迹，计算终端奖励R_g和组归一化优势A_out_g。
  2. 对每条轨迹，遍历工具边界状态，用冻结参考模型计算每个前缀下黄金答案的平均对数概率，转换为剩余间隙d和状态值V。
  3. 对每个工具调用步k，计算一步TD信用δ_k = V(S_{k+1}) - V(S_k)，再计算K步归一化信用c^{(K)}。若窗口到达末尾则加入终端锚定，得到回合奖励r_turn。
  4. 混合优势：工具令牌使用Â = α_out * A_out + α_turn * r_turn；答案令牌仅使用结果优势（带权重缩放）。
  5. 计算重要性权重ρ，执行带裁剪的GRPO更新。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

- **数据集与场景**：
  - **训练数据**：基于OpenResearcher离线语料合成的多文档搜索任务，要求链式检索多个不可替换的证据文档，迫使长时间交互。
  - **评估场景**：
    - 封闭网页：BrowseComp-Plus（使用官方离线语料库构建FAISS索引，匹配训练接口）。
    - 开放网页：BrowseComp、GAIA、xbench-DeepSearch（通过Serper API提供实时网页搜索）。
- **Benchmark**：四个长期深度研究基准：BrowseComp-Plus（封闭）、BrowseComp（开放）、GAIA（开放）、xbench-DeepSearch（开放中文QA）。
- **对比方法**：
  - 外部深度搜索代理：ASearcher-QwQ-32B、WebDancer-32B、CutBill-30B-A3B、TongyiDS-30B-A3B（作为参考，非严格控制）。
  - 内部控制RL基线：均使用相同Qwen3骨干、浏览器接口、训练数据、终端奖励和评估协议：
    - Base（未训练搜索策略）。
    - GRPO（仅终端结果奖励）。
    - GSPO（序列级比率替代令牌级）。
    - GiGRPO（组内组策略优化）。
  - TRACE（本文方法）。
- **评估指标**：准确率（Accuracy），并报告四个基准的平均值。

### 4. 资源与算力

- 文中附录A.1给出了详细超参数表，但**未明确说明使用的GPU型号、具体数量和训练时长**。
- 可知的算力相关配置：
  - Rollout引擎使用SGLang，每个引擎配1 GPU，运行请求64，rollout批量大小64，每个提示采样8个轨迹。
  - 全局批量大小128，优化器Adam，学习率1e-6，常数调度。
  - 最大轨迹长度48000 tokens，最大工具调用60次，观测token上限2048。
  - 训练步数约200步（从图3学习曲线可见）。
- 需要指出：论文未披露整体训练所需的GPU时数、集群规模等细节，限制了可复现性评估。

### 5. 实验数量与充分性

- **实验数量**：
  - 主要结果（表1）：两个模型（4B和30B-A3B）在四个基准上的性能，每个模型对比Base、GRPO、GSPO、GiGRPO、TRACE共5种方法，共40个数据点。
  - 学习动态对比（图3、4）：4B和30B的训练/评估曲线，轨迹长度趋势。
  - 消融实验（表2、图5）：信用分配格式对比（原始delta、线性剩余gap、对数比例）、系数α_turn（1,3,5,7）、TD视野K（0,1,3,5）、参考模型选择（step 0 vs step 200）— 均在4B模型上进行。
  - 定性分析（附录A.5）：5个成功轨迹和5个失败轨迹的信用分布详细解读。
- **充分性与公平性**：
  - 内部控制基线使用了相同骨干、数据、工具、协议，确保了公正比较。
  - 消融实验覆盖了方法的关键超参数，验证了设计选择。
  - 局限性：消融和主要结果均为单次运行，未报告标准差或多次重复，可能受随机性影响；外部代理因使用不同数据/训练流水线，仅作参考。此外，泛化实验仅在搜索场景，对编码或开放任务未验证。

### 6. 论文的主要结论与发现

- TRACE在封闭网页BrowseComp-Plus上将Qwen3-4B从7.2提升至35.6，Qwen3-30B-A3B从8.4提升至42.6，远超Base和GRPO等基线。
- 学习行为迁移到开放网页基准：30B-A3B达到BrowseComp 12.9、GAIA 52.0、xbench-DeepSearch 45.0。
- 与GRPO相比，TRACE学习曲线更早上升、收敛更快，且导致更长的交互轨迹（图4）。
- 对数比例TD信用优于原始delta或线性剩余gap（表2），因其更好地归一化了不同置信水平下的信用，并具备telescoping性质。
- 超参数选择：系数α_turn=3最佳，TD视野K=3最佳，参考模型使用初始检查点即可。
- 定性分析显示TRACE能准确识别关键时刻（信息检索或发现关键事实的步骤），而失败轨迹往往在一次误导性`browser.find`后崩塌，且终端奖励无法区分这些内部差异。

### 7. 优点

- **算法规整，无需额外资源**：无需过程标签、强判别模型、训练评论家或过程奖励模型，仅利用冻结的参考模型计算对数概率即可导出密集信用。
- **可信赖的锚定**：最终结果奖励仍作为训练锚点，回合级信用仅作为辅助信号，保证了正确性目标不被稀释。
- **可解释性**：TD信用具有明确的意义（正表示提高答案可预测性），且可telescoping防止冗余步骤膨胀信用。
- **实验设计控制严谨**：所有内部对比使用完全相同骨干、数据、环境，隔离了信用分配方法的影响。
- **强泛化表现**：在封闭语料训练后直接迁移到开放网页和跨语言（中文）场景，展示了所学策略的通用性。
- **学习效率高**：比GRPO更早开始提升，收敛更快，且最终策略更好。

### 8. 不足与局限

- **应用场景有限**：仅针对具有短黄金答案的搜索任务。对于长输出（如代码补丁）、结构化答案（如多文件修改）或开放式任务（满足用户偏好），参考模型进行对数概率评估的适用性不明确。
- **需要已知黄金答案**：方法依赖于训练时已知黄金答案，这限制了其在答案不可得或模糊反馈场景下的使用。
- **参考模型固定**：参考模型需冻结且与初始策略相同，若策略经历大幅漂移，值估计可能不匹配；但消融表明使用更新后的检查点（step 200）结果类似，问题不严重。
- **缺乏统计显著性报告**：主要结果和消融均基于单次运行，未报告多次重复的平均值和方差，无法评估随机噪声对结论的影响。
- **计算资源描述不完整**：未给出GPU型号、数量、训练总时数，降低了可复现性和成本评估。
- **仅验证了搜索代理**：尽管方法通用，但实验仅在浏览器搜索场景进行，未覆盖其他长程智能体任务（如软件开发、通用计算机控制），需进一步验证。
- **可能忽略的多重比较**：消融中超参数在不同值下表现不同，但未进行基于多次运行的统计检验，部分差异可能由随机性引起。

（完）
