---
title: On the Comparison of Reinforcement Learning and Adaptive Control for Linear Systems under Packet Loss and Uncertainty
title_zh: 关于丢包和不确定性下线性系统的强化学习与自适应控制的比较
authors: Moh Kamalul Wafi
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.32003v1"
tags: ["query:agentic-rl"]
score: 8.0
evidence: DDPG策略梯度方法比较
tldr: 本文针对不确定线性系统在输入量化、通信丢包及动态切换（从标称不稳定到更不稳定）场景下的控制问题，对比了自适应量化控制(AQC)与深度确定性策略梯度(DDPG)强化学习。AQC利用确认消息补偿丢包，并基于Lyapunov理论提供稳定性保证；DDPG则基于标称模型训练，无确认消息。数值结果显示，DDPG在其训练环境下瞬态响应更快、阻尼更优，但在模型不确定性、丢包和动态切换下，AQC凭借严格稳定性保证表现出更强的鲁棒性。该研究揭示了数据驱动性能与基于模型鲁棒性的内在权衡，为网络化不确定系统选用强化学习或自适应控制提供了洞见。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.32003v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.32003v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1122, \"height\": 602, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.32003v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 867, \"height\": 451, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.32003v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1610, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.32003v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1620, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.32003v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1717, \"height\": 1072, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.32003v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 811, \"height\": 167, \"label\": \"Table\"}]"
motivation: 研究不确定线性系统在量化丢包信道及动态切换下，强化学习与自适应控制的性能对比与适用性。
method: 对比自适应量化控制(AQC)与深度确定性策略梯度(DDPG)，AQC利用确认消息补偿丢包，DDPG基于标称模型无确认。
result: DDPG在训练环境下瞬态响应快、阻尼好；AQC在模型不确定、丢包和动态切换下鲁棒性更优。
conclusion: 揭示了数据驱动性能与模型鲁棒性的权衡，为网络化不确定系统的控制器选型提供参考。
---

## 摘要
本文对自适应量化控制（AQC）和深度确定性策略梯度（DDPG）强化学习在具有输入量化且通信信道存在丢包的不确定线性系统中进行了比较研究。所考虑的场景还包括在运行过程中从标称不稳定系统动态切换为更不稳定的系统。AQC针对未知系统动态设计，利用确认消息补偿丢包，而DDPG控制器使用标称系统模型进行训练，不依赖确认消息。数值结果表明，DDPG控制器在其训练环境中能实现更快的瞬态响应和更好的阻尼。然而，在模型不确定性、丢包和动态切换的情况下，AQC由于其严格的Lyapunov稳定性保证，始终表现出更强的鲁棒性。这些结果突显了数据驱动性能与基于模型鲁棒性之间的权衡，并为强化学习和自适应控制在网络化不确定系统中的应用提供了见解。

## Abstract
This paper presents a comparative study between Adaptive Quantized Control (AQC) and Deep Deterministic Policy Gradient (DDPG) reinforcement learning for uncertain linear systems with input quantization over communication channels subject to packet loss. The considered setting also includes dynamic switching from a nominal unstable system to a more unstable one during operation. The AQC is designed for unknown system dynamics using acknowledgment messages to compensate for packet losses, whereas the DDPG controller is trained using the nominal system model without acknowledgment messages. Numerical results show that the DDPG controller achieves faster transient responses and improved damping within its training environment. However, under model uncertainty, packet loss, and dynamic switching, the AQC consistently demonstrates superior robustness owing to its rigorous Lyapunov stability guarantees. These results highlight the trade-off between data-driven performance and model-based robustness, and provide insight into the applicability of reinforcement learning and adaptive control for networked uncertain systems.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：对比两种控制方法——**自适应量化控制（AQC）** 与**深度确定性策略梯度（DDPG）强化学习**——在面对**通信丢包、输入量化、模型不确定性以及动态系统切换**（从标称不稳定切换到更不稳定）时的性能差异。  
- **背景动机**：现代网络化系统常受通信带宽和丢包限制，同时系统参数可能未知或变化。经典自适应控制具有严谨稳定性保证，而数据驱动的深度强化学习擅长在复杂环境中学习最优策略。论文旨在揭示两类方法的**互补优势**与**适用边界**，为不确定性网络化系统的控制器选型提供依据。

## 2. 论文提出的方法论

### 2.1 自适应量化控制（AQC）

- **系统模型**：离散线性系统 `x(k+1) = A x(k) + η(k) B v(k)`，其中 `η(k)∈{0,1}` 表示丢包，`v(k)` 是对数量化后的控制输入。  
- **量化器**：组件级对数量化 `q_i(k, u_i)`，等价表示为**时变扇区界非线性**，满足 `M1(k) ≤ q(u)/u ≤ M2(k)`，通过参数 `Δ(k)` 调节粗糙度。  
- **控制器设计**：基于 Lyapunov 稳定性，求解 Riccati 方程得到稳定增益 `K_g`，并设计**时变反馈增益 `K(k)` 更新律**（利用确认消息感知丢包）：  
  `K(k+1) = K(k) - η(k)/(1+x^T P x) · Q B^† [x(k+1)-A_s x(k)-B q_s(k,u)] x^T(k)`。  
- **稳定性条件**：丢包率上界需满足 `¯p/(1-¯p) (λ_max(P)¯σ_A^2 I - P) < γR`，并在每个时步强制扇区界条件 `R - 2K^T Δ B^T P B Δ K ≥ γR > 0`。  
- **关键特性**：利用确认消息，即使在丢包时也更新量化器参数和增益，保证**Lyapunov 意义下的概率稳定性**。

### 2.2 深度确定性策略梯度（DDPG）

- **算法框架**：基于 Actor-Critic 的 off-policy 强化学习。Actor 网络输出确定性策略 `u = μ_θ(x)`，Critic 网络估计 Q 值。  
- **训练环境**：使用标称系统矩阵 `A`，不依赖确认消息。奖励函数设计为 `r(k) = -100 (|x|>Th) 或 1/(|e|+ψ) (otherwise)`。  
- **网络结构**：Actor 和 Critic 均为前馈神经网络，具体层数和神经元数如 25 个 tanh+softmax / ReLU 等。  
- **量化处理**：DDPG 控制输出经与 AQC 相同的时变对数量化器后作用于系统，以公平比较。

## 3. 实验设计

- **系统**：二阶离散不稳定系统 `z(k+2)+βζ z(k+1)+αζ z(k)=η(k)bζ v(k)`，状态 `x=[z(k), z(k+1)]^T`。标称 `α1=1.5`，切换后 `α2=2`，β=-0.5，b=0.4。  
- **场景设置**：
  - 控制器激活时间 `k_on=1s`，切换时间 `t_d ∈ {10,15,20,25,30}s`。  
  - 丢包概率 `¯p∈{0, 0.15, 0.30, >0.5}`。  
  - 初始条件：多个不同初始状态。  
- **Benchmark**：理想无丢包情况作为基线。  
- **对比方法**：  
  - AQC（有确认消息）  
  - DDPG（无确认消息，标称模型训练）  
  - 自适应控制（AC）作为额外对比（表1）。  
- **评价指标**：系统状态响应曲线、稳定性开关情况。

## 4. 资源与算力

- **文中未明确说明**所使用的 GPU 型号、数量、训练时长等硬件资源。仅提及 DDPG 训练参数：`γ=0.9, ts=1s, E=1000 episodes, T=750`，未报告训练耗时。  
- AQC 算法无需离线训练，在线自适应更新。

## 5. 实验数量与充分性

- **实验数量**：  
  - 图5：AQC 与 DDPG 在不同切换时间（5种）和不同初始条件下的响应对比（约10个子图）。  
  - 图6：两种方法在三种丢包率（含极高温高压）下的表现对比。  
  - 表1：三种控制方法在 α2 从1到20变化下的稳定性状态汇总。  
- **充分性评价**：实验覆盖了**切换时间、丢包概率、初始条件、模型参数扰动**等多个关键维度，变化范围较广。但**缺少统计性检验（如多次蒙特卡洛均值/方差）** 和严格的消融实验（如丢包率连续变化曲线、量化精度的影响等）。**公平性**方面，DDPG 训练环境与测试环境存在“分布偏移”（标称 vs. 切换），而 AQC 允许在线自适应，属于方法本身特性所致，并非不公平。

## 6. 论文的主要结论与发现

1. **DDPG 优势**：在训练环境（标称模型、无丢包）中，瞬态响应更快、阻尼更好。  
2. **AQC 优势**：在模型漂移、丢包、动态切换下，鲁棒性显著优于 DDPG，这得益于严格的 Lyapunov 稳定性保障和确认消息反馈。  
3. **量化对比**：表1显示 DDPG 仅能在 α2=1 附近维持稳定（训练范围），AQC 稳定范围远大于 DDPG（至 α2=19），而自适应控制（AC）甚至对 α2=20 仍稳定。  
4. **核心启示**：数据驱动方法擅长在熟悉环境中优化性能，而基于模型的方法在面临未知变化时更具鲁棒性；实际应用中应结合两者优势。

## 7. 优点

- **问题设定新颖**：同时考虑了量化、丢包、动态切换、模型不确定性四个实际挑战，贴近工程场景。  
- **对比框架清晰**：将具有严格理论保证的自适应控制与流行深度强化学习方法置于相同量化环境下进行公平对比。  
- **AQC 设计严谨**：提供了完整的 Lyapunov 稳定性定理，并给出可实现的时变量化器选点算法（Algorithm 1）。  
- **实验参数多样**：切换时间、丢包概率、初始条件均有系统变化，且表1提供大范围参数扫描，结论有说服力。

## 8. 不足与局限

- **实验覆盖不足**：  
  - 仅测试了一个具体二阶线性系统，非线性、更高阶或MIMO系统未考虑。  
  - 未提供丢包率连续变化的性能曲线或统计置信区间。  
  - 未做消融实验（如DDPG在训练中加入丢包或域随机化能否改善泛化）。  
- **计算资源未报告**：使得可复现性降低。  
- **DDPG 训练环境片面**：仅用标称模型训练，未尝试迁移学习或鲁棒训练策略，可能低估强化学习潜力。  
- **稳定性保证缺失**：DDPG 缺乏任何形式的形式化稳定性证明，仅凭经验验证；论文未讨论如何在强化学习中嵌入稳定性约束。  
- **未考虑时延、噪声等常见干扰**：仅聚焦丢包和切换，实际网络环境更复杂。

（完）
