---
title: Comparative Study of Multi-Agent Actor-Critic Algorithms in Parameterized Action Reinforcement Learning
title_zh: 参数化动作强化学习中多智能体演员-评论家算法的比较研究
authors: "Ubayd Ali Bapoo, Clement N Nyirenda"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19117v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 多智能体actor-critic算法用于合作任务
tldr: 参数化动作强化学习需离散动作和连续参数化，单智能体算法（GAC、SAC、TQC）有效。本文扩展至多智能体共享经验框架（MAGAC、MASAC、MATQC），不使用CTDE。实验显示多智能体框架显著提升GAC性能，但对SAC和TQC提升有限；超过五智能体后增益递减，计算成本骤增。结果揭示了学习性能与可扩展性间的权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19117v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 1096, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19117v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 906, \"height\": 710, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19117v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 876, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19117v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 875, \"height\": 457, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19117v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 925, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19117v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 132, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19117v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 921, \"height\": 135, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19117v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 920, \"height\": 122, \"label\": \"Table\"}]"
motivation: 单智能体参数化动作RL算法在多智能体场景的扩展未探索，需评估共享经验多智能体框架的可扩展性。
method: 提出独立actor-critic但共享回放缓冲区的多智能体扩展（MAGAC、MASAC、MATQC），在Platform-v0和Goal-v0测试3/5/10个智能体。
result: MAGAC显著提升GAC性能，MASAC和MATQC提升较小；超五智能体后性能收益递减，计算时间显著增加。
conclusion: 共享经验多智能体方法对贪婪actor-critic有效，但软演员评论家和截断分位数评论家增益有限，需权衡性能与计算效率。
---

## 摘要
参数化动作强化学习在需要离散动作选择和连续参数化的环境中表现出强大的性能。先前的研究证明了单智能体演员-评论家算法——贪婪演员-评论家（GAC）、软演员-评论家（SAC）和截断分位数评论家（TQC）——在基准参数化动作任务上的有效性，但它们向多智能体设置的扩展仍未得到充分探索。本文对这些算法的共享经验多智能体扩展进行了比较研究：多智能体贪婪演员-评论家（MAGAC）、多智能体软演员-评论家（MASAC）和多智能体截断分位数评论家（MATQC）。所提出的框架没有遵循集中训练、分散执行（CTDE）范式，而是使用多个独立的演员-评论家智能体，它们共享一个回放缓冲区，同时保持独立的策略和价值网络。我们在Platform-v0和Goal-v0基准测试上，使用三智能体、五智能体和十智能体配置评估这些算法与单智能体对应版本的性能，以评估可扩展性。性能通过十次独立运行的平均评估回报和训练时间进行衡量，并采用单因素方差分析和Tukey HSD事后检验来评估统计显著性。结果表明，多智能体框架持续改善了贪婪演员-评论家的性能，而MASAC和MATQC相对于其单智能体版本表现出相对较小的提升。当智能体数量超过五个时，性能提升有限，而计算成本显著增加，尤其是对MAGAC而言。这些结果突出了学习性能与计算效率之间的权衡，为共享经验多智能体演员-评论家方法在参数化动作强化学习中的可扩展性提供了见解。

## Abstract
Parameterized action reinforcement learning has shown strong performance in environments requiring both discrete action selection and continuous parameterization. Prior work established the effectiveness of single-agent actor-critic algorithms - Greedy Actor-Critic (GAC), Soft Actor-Critic (SAC), and Truncated Quantile Critics (TQC) - on benchmark parameterized action tasks, but their extension to multi-agent settings remains largely unexplored. This paper presents a comparative study of shared-experience multi-agent extensions of these algorithms: Multi-Agent Greedy Actor-Critic (MAGAC), Multi-Agent Soft Actor-Critic (MASAC), and Multi-Agent Truncated Quantile Critics (MATQC). Rather than following the centralized training, decentralized execution (CTDE) paradigm, the proposed framework uses multiple independent actor-critic agents that share a replay buffer while maintaining separate policy and value networks. We evaluate the algorithms on the Platform-v0 and Goal-v0 benchmarks against their single-agent counterparts, using three-, five-, and ten-agent configurations to assess scalability. Performance is measured by average evaluation return and training time across ten independent runs, with one-way ANOVA and Tukey HSD post-hoc tests used to assess statistical significance. Results show that the multi-agent framework consistently improves Greedy Actor-Critic performance, while MASAC and MATQC show comparatively modest gains over their single-agent versions. Increasing the number of agents beyond five yields limited additional performance while substantially raising computational cost, particularly for MAGAC. These results highlight a trade-off between learning performance and computational efficiency, offering insight into the scalability of shared-experience multi-agent actor-critic methods for parameterized action reinforcement learning.

---

## 论文详细总结（自动生成）

# 论文总结：参数化动作强化学习中多智能体演员-评论家算法的比较研究

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：参数化动作强化学习（Parameterized Action RL）适用于同时需要离散动作选择和连续参数化的环境（如搜索引擎匹配计划生成）。先前工作已验证了单智能体演员-评论家算法（GAC、SAC、TQC）在基准任务上的有效性，但**多智能体场景下的扩展尚未被探索**。因此，需要研究共享经验的多智能体框架能否提升学习性能，并评估其可扩展性。
- **核心问题**：将单智能体参数化动作RL算法（GAC、SAC、TQC）扩展到多智能体共享经验框架后，相比单智能体版本是否有显著性能提升？智能体数量增加到多少时出现性能饱和与计算效率下降？
- **整体含义**：探究在参数化动作空间下，多智能体共享经验（非CTDE范式）对三种代表性actor-critic算法的影响，揭示学习性能与计算开销之间的权衡，为搜索引擎等大规模决策系统的算法设计提供指导。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：不采用传统的集中训练-分散执行（CTDE）架构，而是提出**轻量级共享经验多智能体框架**。框架内多个独立的actor-critic智能体与同一环境交互，各自维护独立的策略网络和价值网络，但**共享一个回放缓冲区（replay buffer）**，每个智能体从该缓冲区采样经验进行更新。智能体独立生成参数化动作后，通过**动作融合策略（action fusion）** 合并为单一可执行动作，以适配只接受单个动作的环境。
- **关键技术细节**：
  - 智能体数量可配置（3、5、10）。
  - 扩展的算法：MAGAC（基于GAC）、MASAC（基于SAC）、MATQC（基于TQC），保持原算法更新机制。
  - **不包含集中式评论家**，因此不是完整CTDE实现。
  - 超参数优化使用Microsoft NNI进行，确保公平性。
  - 性能评估指标：平均评估回报（Average Evaluation Return）、平均训练时间（Average Training Time）、单因素方差分析（One-Way ANOVA）和Tukey HSD事后检验。
- **算法流程**（文字说明）：
  1. 初始化环境和智能体（actor和critic网络）。
  2. 每个时间步：所有智能体观察当前状态，独立选择参数化动作（离散动作+连续参数）。
  3. 通过动作融合策略将多个动作合并为一个动作，执行于环境。
  4. 将经验（状态、动作、奖励、下一状态）存入共享回放缓冲区。
  5. 每个智能体从缓冲区采样小批量经验，按各自算法（GAC/SAC/TQC）更新actor和critic。
  6. 重复直至训练结束。

## 3. 实验设计：数据集/场景、基准、对比方法

- **环境/场景**：
  - 两个标准参数化动作RL基准：**Platform-v0** 和 **Goal-v0**（来自Bester等人的参数化动作RL框架）。两者均要求智能体同时选择离散动作及其连续参数。
- **对比方法**：
  - 基线（单智能体）：GAC、SAC、TQC （来自先前工作[11]）。
  - 多智能体变体：MAGAC、MASAC、MATQC，分别使用**3、5、10个智能体**配置。
- **对比维度**：
  - 相同超参数设置（仅修改算法更新机制和智能体数量）。
  - 每个配置运行10次独立训练（不同随机种子），每次5000个episode。
  - 统计显著性检验：单因素ANOVA + Tukey HSD。

## 4. 资源与算力

- 论文**未明确说明**所使用的GPU型号、数量或具体硬件配置。训练时长以表格形式给出（平均训练时间，单位为hh:mm:ss），但未提及GPU规格或是否使用并行计算。
- 例如：MAGAC 10智能体在Platform环境训练时间约6小时，但未说明是单GPU还是多GPU。
- **指出**：文中只提供了训练时间数据作为计算成本度量，缺乏详细的算力资源描述。

## 5. 实验数量与充分性

- **实验数量**：
  - 每个算法×智能体配置组合在2个环境上各进行10次独立运行（共约：3个基线 + 3个算法×3种规模 = 12种配置 × 2环境 × 10 runs = 240次训练，加上ANOVA所需数据）。
  - 进行了ANOVA和Tukey HSD统计检验，提供了F统计量、p值、效应量（η²）等。
- **充分性与公平性**：
  - 优点：所有算法使用相同训练流程和超参数（经NNI优化），仅改变智能体数量和算法更新机制，保证了对比公平。
  - 覆盖范围：测试了3/5/10三种规模，分析了不同规模的影响；但缺失更多规模（如2、15等）或不同任务难度的实验。
  - 局限性：只在两个基准环境上测试，泛化性有限；未在更复杂的合作多智能体任务（如SMAC、MaMuJoCo）上验证。未进行消融实验（如去掉共享回放或不同融合策略的效果）。

## 6. 论文的主要结论与发现

1. **MAGAC（基于GAC）在多智能体框架中收益最大**：相比单智能体GAC，MAGAC在Goal和Platform环境上平均回报显著提升（Goal: -8.551 → -7.914 ~ -7.885；Platform: 0.170 → 0.183~0.185）。
2. **MASAC和MATQC提升有限**：多智能体扩展对SAC和TQC的性能提升较小，甚至在某些配置下略低于基线（如MASAC 3/5 agent在Goal上回报略低于SAC基线）。
3. **智能体数量边际效益递减**：从3个智能体增加到5个有微弱提升，但增至10个后性能几乎不再改善，计算时间却大幅增加（MAGAC 10 agent在Platform训练时间达6小时，为5 agent的两倍多）。
4. **统计显著性**：ANOVA和Tukey HSD表明MAGAC与MASAC、MATQC之间存在显著差异（p值极小），而MASAC与MATQC之间无显著差异。
5. **算法选择比单纯增加代理数更重要**：多智能体框架的有效性高度依赖底层算法特性（贪婪策略 vs 熵正则 vs 分布化）。

## 7. 优点

- **方法创新点明确**：将单智能体参数化动作RL算法扩展到共享经验多智能体框架，并进行了系统的比较研究，填补了该方向空白。
- **控制变量严谨**：所有实验采用相同代码库、超参数、训练流程，仅改变算法和智能体数量，确保对比公平。
- **统计方法合理**：使用ANOVA和Tukey HSD检验显著性，并报告效应量（η²），增强结果可信度。
- **考虑了计算成本**：不仅报告回报，还详细记录了训练时间，分析了性能与效率的权衡，对实际部署有指导意义。

## 8. 不足与局限

- **实验覆盖不足**：
  - 只在两个简单基准环境（Platform、Goal）上测试，缺乏更复杂、更具挑战性的合作多智能体场景（如SMAC、MaMuJoCo、多智能体包围等）。
  - 未测试非参数化动作空间或竞争性环境，泛化性有限。
  - 动作融合策略为简单合并，未对比其他融合方式（如投票、平均、教师强制）的影响。
- **框架设计局限**：
  - 未实现完整的CTDE（无集中式评论家），因此无法评估更强大的协调机制（如QMIX、MAPPO）在参数化动作空间下的表现。
  - 未采用参数共享（parameter sharing），每个智能体独立网络导致计算量线性增长，可能忽略更高效的设计。
- **消融研究缺失**：未进行消融实验（例如：去掉共享回放缓冲区、改变回放采样比例、不同融合策略），对每个组件贡献的归因不清楚。
- **超参数敏感性未讨论**：虽然用了NNI优化，但未报告超参数搜索空间或最终设置，不利于复现。
- **算力描述缺失**：未提供GPU型号、数量、显存等信息，训练时间无法直接与其他工作比较。
- **应用风险**：论文动机来自搜索匹配计划，但实验未在真实搜索引擎环境中验证，实际效果未知。

（完）
