---
title: Scalable Policy Optimization for Networked Multi-Agent Reinforcement Learning with Continuous State-Action Spaces
title_zh: 面向连续状态-动作空间的网络化多智能体强化学习的可扩展策略优化
authors: "Dongming Wang, Pengcheng Dai, Wenwu Yu, Wei Ren"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18554v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向网络化多智能体强化学习的可扩展策略梯度
tldr: 针对连续状态动作空间的网络化多智能体强化学习，提出CDCPG算法。每个智能体基于局部邻域维护actor，并通过谱随机特征表示构建截断动作值函数的局部化评论家。理论贡献包括：条件期望构造解决截断不匹配问题；证明归一化随机特征在持续激励条件下无条件稳定；在指数空间衰减下，算法达到ε近似最优解需Õ(ε⁻²)样本，计算通信复杂度仅与邻域大小相关。实验验证了局部性和特征维度的预测。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18554v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18554v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 874, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18554v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 786, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18554v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 775, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18554v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 785, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18554v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1871, \"height\": 619, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18554v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1884, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18554v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 715, \"height\": 140, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18554v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1104, \"height\": 253, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18554v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 612, \"height\": 364, \"label\": \"Table\"}]"
motivation: 现有方法难以处理连续状态动作空间且网络规模大的多智能体协同学习，需要可扩展的策略梯度算法。
method: 提出CDCPG，每个智能体维护局部actor和基于谱随机特征的局部化最小二乘时序差分评论家，利用条件期望构建截断动作值函数。
result: 在指数衰减和持续激励条件下，算法以Õ(ε⁻²)样本达到ε近似最优，计算通信复杂度仅取决于邻域大小而非网络规模。
conclusion: CDCPG为连续空间网络化多智能体强化学习提供了理论可扩展的解，且自适应局部性规则平衡精度与复杂度。
---

## 摘要
我们针对具有连续状态和动作空间的网络化马尔可夫决策过程中的协作强化学习，提出了连续分布式耦合策略梯度（CDCPG）算法。每个智能体在有界图邻域上维护一个局部执行器，并通过局部转移核的光谱随机特征表示，由局部最小二乘时间差分评价器评估截断的动作价值函数。本分析做出了四项贡献。首先，截断的动作价值函数被构造为关于邻域的条件期望，从而得到一个良定义的局部贝尔曼理论，消除了朴素截断论证中的延续核不匹配问题。其次，我们揭露了归一化随机特征在时间差分稳定性上的维度障碍，并证明了一个无条件的激励界，将稳定性简化为一个对称的持续激励条件，该条件可通过在线矩阵集中度证书进行监控。第三，在智能体交互呈指数级空间衰减、激励条件以及目标光滑性的前提下，CDCPG使用~O(ε^{-2})个共享样本将平均每个智能体的驻点测度驱动至显式表征的近似下限的任意超额ε之内，且超额依赖性匹配光滑非凸一阶收敛率；每个智能体的计算与通信受邻域大小而非网络大小支配。第四，一个自适应邻域规则选择半径以平衡截断误差和图衰减残差与目标精度。在网络化线性-二次基准上的实验验证了邻域性和特征维度的预测。

## Abstract
We develop the Continuous Distributed Coupled Policy Gradient (CDCPG) algorithm for cooperative reinforcement learning in networked Markov decision processes with continuous state and action spaces. Each agent maintains a local actor over a bounded graph neighborhood, and a localized least-squares temporal-difference critic evaluates a truncated action-value function through a spectral random-feature representation of the local transition kernel. The analysis makes four contributions. First, the truncated action-value function is constructed as a conditional expectation over the neighborhood, yielding a well-posed localized Bellman theory that removes the continuation-kernel mismatch of naive truncation arguments. Second, we expose a dimensional obstruction to temporal-difference stability for normalized random features and prove an unconditional excitation bound that reduces stability to a symmetric persistence-of-excitation condition, monitorable through an online matrix-concentration certificate. Third, under exponential spatial decay of agent interactions, the excitation condition, and smoothness of the objective, CDCPG drives an averaged per-agent stationarity measure to within any excess $ε$ of an explicitly characterized approximation floor using $\widetilde{\mathcal{O}}(ε^{-2})$ shared-oracle samples, and the excess dependence matches the smooth nonconvex first-order rate; per-agent computation and communication are governed by the neighborhood size rather than the network size. Fourth, an adaptive-locality rule selects the radius that balances truncation and graph-decay residuals against the target accuracy. Experiments on a networked linear-quadratic benchmark corroborate the locality and feature-dimension predictions.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：网络化多智能体强化学习（MARL）面临的核心挑战是可扩展性：联合状态-动作空间随智能体数量指数增长，集中化方法不可行；纯分散化方法又难以协调。网络化系统中每个智能体的动态、奖励和观测仅受附近邻居影响，这为设计局部化方法提供了机会。
- **背景**：现有工作主要在离散状态-动作空间下利用指数衰减性质获得可扩展性（如Qu et al. 2020, Lin et al. 2021）。在连续状态-动作空间中，由于需要函数近似、核近似误差以及策略评估的适定性问题，直接扩展存在根本困难。论文旨在将局部化框架扩展到连续空间，并解决随之而来的一系列技术挑战。

## 2. 方法论

### 核心思想
- 提出**连续分布式耦合策略梯度（CDCPG）** 算法。每个智能体维护：
  - **局部执行器（actor）**：策略仅依赖其κ<sub>π</sub>跳邻域内的局部状态。
  - **局部化评论家（critic）**：基于谱随机特征（Random Fourier Features, RFF）的LSTD估计器，评估截断的动作价值函数。
- 利用**条件期望截断**构建良定义的局部贝尔曼算子，消除朴素截断的核不匹配问题。
- 在指数空间衰减、持续激励条件和目标光滑性假设下，给出严格的样本复杂度分析。

### 关键技术细节
1. **条件期望截断**：
   - 定义截断Q函数为原始Q函数关于局部邻域X<sub>κ</sub><sup>i</sup>的条件期望：  
     \(\bar{Q}_i^\pi(x) = \mathbb{E}_{\nu^\pi}[Q_i^\pi(S,A) \mid X_\kappa^i = x]\)。
   - 构造辅助局部贝尔曼算子，并证明其不动点残差被指数衰减量控制。
2. **谱随机特征表示**：
   - 利用高斯转移核的平移不变性，通过RFF近似局部转移核。
   - 推导归一化RFF的L<sup>1</sup>逼近误差界（Theorem 1）。
3. **TD稳定性假设**：
   - 提出特征维度相关的TD稳定性假设（Assumption C）：原始特征TD矩阵的最小奇异值被μ<sub>M</sub>(m) > 0下界。
   - 证明维度障碍：不存在与m无关的稳定性常数（Proposition 2）。
   - 利用折扣占优关系推导无条件激励界：σ<sub>min</sub>(M) ≥ (1-√γ)λ<sub>min</sub>(H) （Lemma 3），将TD稳定性归结为对称协方差矩阵的持续激励条件。
   - 提供矩阵Bernstein在线诊断（Proposition 3）以事后验证轨迹条件（Assumption C'）。
4. **样本复杂度**：
   - 在固定邻域半径κ和随机特征维度m下，CDCPG以共享生成模型，使用 \(\tilde{\mathcal{O}}(\epsilon^{-2})\) 次共享采样，将平均每智能体驻点测度驱动到超额ε（超过显式结构下限）。
   - 结构下限包含四项：bin内波动、截断误差、RFF逼近误差和图衰减误差。
5. **自适应邻域规则（Proposition 4）**：选择半径κ使得截断和图中衰减残差小于目标精度，给出上界估计。

### 算法流程（文字说明）
1. 预处理：为每个智能体独立采样随机特征对（频率和相位）。
2. 每轮迭代k：
   - 从当前联合策略π<sub>θ(k)</sub>下共享采样（LSTD数据集和梯度数据集）。
   - 每个智能体i并行：
     - 计算局部LSTD矩阵M<sub>i</sub><sup>(k)</sup>和向量b<sub>i</sub><sup>(k)</sup>（基于κ+1邻域的状态-动作及后继）。
     - 求解正则化LSTD获得原始权值，若矩阵奇异则置零。
     - 投影权值到半径为W<sup>*</sup>的球内（W<sup>*</sup>由先验知识确定）。
     - 利用权值和原始特征计算局部Q估计。
     - 收集κ<sub>c</sub>邻域内所有智能体的权值，构造聚合梯度估计 \(\hat{g}_i^{(k)}\)。
   - 执行投影梯度上升：θ<sub>i</sub><sup>(k+1)</sup> = Π<sub>Θ<sub>0,i</sub></sub>(θ<sub>i</sub><sup>(k)</sup> + η \(\hat{g}_i^{(k)}\))。
3. 输出：均匀随机选择某一轮迭代的参数作为最终策略。

## 3. 实验设计

- **数据集/场景**：**线性耦合二次（LCQ）基准**。n=9个智能体在路径图上，每个智能体有标量状态s<sub>i</sub>∈ℝ、动作a<sub>i</sub>∈[-A,A]。动力学为线性加高斯噪声，奖励为邻域耦合的二次型。状态和动作被投影到有界区间作为数值安全阀。参考控制器为无约束LQR增益。
- **基准方法**：
  - **IPG**：独立策略梯度（无共享信息）。
  - **Networked AC**：一跳共识的Actor-Critic（Zhang et al. 2018）。
  - **MAAC**：多智能体注意力Actor-Critic（Iqbal & Sha 2019）。
  - **Centralized**：全局线性高斯策略的集中式策略梯度。
- **评价指标**：每智能体折扣回报平均值、策略增益（对角线元素）与LQR参考的差距、达到90%改善的迭代次数k<sub>0.9</sub>。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量、训练时长等硬件信息。
- 仅提到每次实验使用5个随机种子（部分消融实验使用3个种子），外迭代K=200，每轮Ns=2000个LSTD样本、Ng=1024个梯度样本。
- 未给出运行时间或能耗数据。

## 5. 实验数量与充分性

- **实验组**：
  1. **主对比实验**：CDCPG vs. 四种baseline，5种子，K=200，记录回报曲线、最终回报、增益轨迹、k<sub>0.9</sub>（见表III、图1-3）。
  2. **RFF特征维度扫描（m）**：50, 100, 250, 500, 1000, 2000，5种子，测量相对Q-error，拟合斜率为-0.46（R²=0.98）（图4）。
  3. **衰减代理分析**：绘制|Cov(r<sub>i</sub>,r<sub>j</sub>)|与图距离的关系，拟合ρ̂=0.86（图5）。
  4. **消融实验**：κ=0,1,2,3（固定λ=1e-4，3种子）和λ=1e-6,1e-4,1e-2,1（固定κ=3，3种子），给出最终回报及差距（表IV、图6）。
- **充分性评价**：
  - **优点**：覆盖了核心要素（局部性、特征维度、消融），对比方法具代表性，使用随机种子反映统计稳定性。
  - **不足**：
    - **场景单一**：仅LCQ一个基准，且是线性二次结构，未在更复杂非线性任务或更大网络上验证。
    - **分析方法不一致**：主实验CDCPG使用的critic是“结构化二次基”而非理论分析的RFF-LSTD critic（RFF-LSTD单独在特征维度扫描中验证），因此主实验结果并未直接验证理论算法。
    - **假设验证缺失**：指数衰减、持续激励等关键假设未在实验中显式验证（仅通过协方差衰减做代理）。
    - **样本量有限**：K=200，种子数5或3，统计显著性未明确报告。
  - 整体而言，实验作为理论一致性检查合理，但不足以全面验证算法在广泛连续网络化MARL问题中的有效性。

## 6. 论文的主要结论与发现

- **理论贡献**：
  - 提出了条件期望截断Q函数，解决了连续空间局部化贝尔曼理论的基础问题。
  - 建立了特征维度感知的TD稳定性假设，并给出无条件激励界，将稳定性条件简化为可监听的对称协方差条件。
  - 在指数衰减、激励条件和光滑性下，CDCPG以\(\tilde{\mathcal{O}}(\epsilon^{-2})\)共享采样达到超额ε（超过结构下限），匹配光滑非凸随机优化的一阶下界。
  - 提供了矩阵Bernstein在线诊断证书和自适应邻域规则。
- **实验发现**：
  - CDCPG（使用结构化二次基评论家）在LCQ上恢复集中式性能，超过IPG、Networked AC、MAAC等局部基线，最终回报距参考仅0.55%。
  - RFF特征维度m增大时，Q-error以约1/√m速率下降（斜率-0.46），与理论L<sup>1</sup>界相容。
  - 奖励协方差随图距离几何衰减（ρ̂=0.86），提示指数衰减假设的可能合理性。
  - 截断半径κ增大可持续减小性能差距（跳跃比≈0.5），规律性λ在适当范围内保持平坦，过大则恶化。

## 7. 优点

- **理论深度高**：系统解决了连续空间网络化MARL从局部贝尔曼算子、随机特征逼近到有限样本收敛的完整链条，包含显式误差分解和结构化下限。
- **可监控性**：提供了矩阵Bernstein在线诊断工具（Proposition 3），允许实践者事后验证轨迹条件，而非仅依赖不可验证的假设。
- **自适应机制**：提出自适应邻域规则（Proposition 4），在给定目标精度下平衡截断误差和图衰减误差，具有实用指导意义。
- **清晰的问题意识**：明确指出现有离散空间方法向连续空间扩展的四项核心挑战（C1-C4），并逐一解决。

## 8. 不足与局限

- **假设强度大**：指数衰减（B2）、持续激励（C/C'）、微分正则性（D3）、图尾假设等严格条件在真实问题中难以验证，限制了理论的实际适用性。
- **实验覆盖有限**：仅使用一个简单线性二次基准，未涉及非线性、大规模或真实机器人场景；未与其他近期可扩展MARL方法（如MAPPO、QMIX等）对比。
- **理论与实验脱节**：主实验使用的critic并非理论分析中的RFF-LSTD评论家，而是针对问题结构设计的二次基，因此实验结果无法直接佐证理论保真度。
- **未提供算力资源**：缺少硬件和运行时间信息，无法评估算法实际效率。
- **维度障碍影响**：理论分析揭示随机特征TD稳定性具有维度障碍（Proposition 2），导致结构下限中的bin项随m增长，这在实际中可能造成较大常数。
- **仅考虑共享生成模型**：采样模型假设每个样本对所有智能体可见（共享oracle），未扩展到独立轨迹或分布式采样场景。

（完）
