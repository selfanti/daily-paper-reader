---
title: Learning the Supports for Categorical Critic in Reinforcement Learning
title_zh: 在强化学习中学习分类评论家的支撑集
authors: "Jen-Yen Chang, Takayuki Osa, Tatsuya Harada"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01880v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 强化学习中分类评论家学习
tldr: 传统分布强化学习中的直方图损失要求预定义固定支持区间，而强化学习中目标值非平稳随机，导致应用困难。本文提出动态学习支持上下界的方法，联合优化边界与类别表示，并证明该目标构成均方贝尔曼误差的上界且比固定支持更紧。在连续控制任务上，该方法稳定适配区间，性能匹配甚至超越需预设支持的HL-Gauss算法。这一工作无需预设支持区间，提升了直方图损失在强化学习中的实用性和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01880v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 730, \"height\": 499, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01880v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 785, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01880v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1438, \"height\": 597, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01880v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1452, \"height\": 406, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01880v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1417, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01880v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1450, \"height\": 822, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01880v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1454, \"height\": 404, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01880v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1218, \"height\": 290, \"label\": \"Table\"}]"
motivation: 现有直方图损失要求预定义固定支持区间，难以适应强化学习中非平稳随机的目标值。
method: 动态学习支持区间的上下界，联合优化边界与类别表示，理论证明构成均值贝尔曼误差的更紧上界。
result: 在连续控制任务上稳定适应支持区间，性能匹配或超越需预设支持的HL-Gauss算法。
conclusion: 无需预设支持区间，提升了直方图损失在强化学习中的实用性与鲁棒性。
---

## 摘要
值函数是基于Actor-Critic的深度强化学习（RL）中的核心组成部分。传统上，这些函数通过最小化与自举目标值相关的均方误差（MSE）作为回归任务进行训练。与此同时，在分布式RL中，基于分布式贝尔曼算子对回报的分布进行建模。本研究探讨了高斯直方图损失（HL-Gauss），这是一种较新的方法，通过将每个标量贝尔曼目标编码为高斯平滑的分类目标，将值估计重新构建为分类任务。尽管具有潜力，将基于直方图的损失应用于RL仍面临固有挑战，最显著的是需要预先定义固定的支撑区间，而这在RL任务中通常因目标值的非平稳性和随机性而变得复杂。在本工作中，我们提出了一种方法，动态学习支撑的上下界，而非事先指定。我们推导出一个目标函数，在联合学习这些边界的同时学习标量值的分类表示，并证明该目标函数构成均方贝尔曼误差的上界。我们的理论分析进一步表明，该边界比HL-Gauss中非学习支撑的边界更紧。实验上，所提出的目标函数能够稳定地调整支撑区间，并且在大多数连续控制任务上与基于HL-Gauss的Actor-Critic算法性能相当，同时在部分任务上有所提升，且无需预先指定支撑区间。

## Abstract
Value functions are an essential component in actor-critic based deep reinforcement learning (RL). Conventionally, these functions are trained as a regression task by minimising the mean squared error (MSE) relative to bootstrapped target values. Meanwhile, in distributional RL, a distribution of returns is modelled based on the distributional Bellman operator. This work investigates the Gaussian Histogram Loss (HL-Gauss), a recent approach that reframes value estimation as classification by encoding each scalar Bellman target as a Gaussian-smoothed categorical target. Despite its potential, applying histogram-based losses to RL presents inherent challenges, most notably the requirement to pre-define a fixed support interval, which is often complicated by the non-stationary and stochastic nature of target values typically found in RL tasks. In this work, we propose an approach that dynamically learns the lower and upper bounds of the support instead of assigning them beforehand. We derive an objective that jointly learns these bounds whilst learning the categorical representation of the scalar values, and we show that this objective forms an upper bound on the mean-squared Bellman error. Our theoretical analysis further shows that this bound is tighter than that of non-learned supports of HL-Gauss. Empirically, the proposed objective enables stable adaptation of the support interval and matches HL-Gauss-based actor-critic algorithms on most continuous-control tasks whilst improving on a subset, without requiring a pre-specified support interval.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在基于Actor-Critic的深度强化学习中，值函数通常通过回归（MSE）训练。近年来，分布强化学习中的“高斯直方图损失（HL-Gauss）”通过将标量贝尔曼目标编码为高斯平滑的分类目标，将值估计重构为分类任务，具有梯度平滑、可扩展等优点。然而，HL-Gauss要求**预先定义固定的支撑区间**[ν_min, ν_max]，这在强化学习中严重受限：目标值随策略演化而非平稳随机，固定区间过窄会导致截断偏差（漏掉重要尾部信息），过宽则会稀释每个bin的分辨率（量化偏差增大），且手工调优成本高。
- **研究动机**：消除对预设区间的依赖，**动态学习支撑区间**，在保持高分辨率的同时避免截断。
- **整体含义**：提出DySEL（Dynamic Support Endpoint Learning）算法，将支撑区间学习建模为约束优化问题，理论证明其目标构成均方贝尔曼误差的更紧上界，实验表明在DM Control连续控制任务上性能匹配或超越需要预设区间的HL-Gauss。

## 2. 论文提出的方法论

### 2.1 核心思想
- 将支撑区间学习视为一个**约束优化**问题：最小化支撑区间宽度（以收紧上界），同时满足概率质量覆盖约束（避免截断）。
- 通过**极小极大博弈（min-max game）**求解：主目标压缩区间，拉格朗日乘子λ强制覆盖约束，形成对抗平衡。

### 2.2 关键技术细节
- **公式推导**：从均方贝尔曼误差出发，证明其被HL-Gauss损失上界约束（式(11)），上界包含三项：支撑绝对宽度、分布匹配项（KL散度）、截断偏差。固定区间下前两项常被忽略；动态学习中宽度项可优化，截断偏差需控制。
- **转化为约束优化**：利用AM-GM不等式将乘积项转化为求和；将截断偏差简化为控制泄漏质量(1-Z)（Z为覆盖质量）。最终目标函数（式(16)）：
  \[
  L(\theta,\phi,\lambda) = \alpha \max(|\nu_{\min}|,|\nu_{\max}|) + \frac{1}{\alpha}(-\sum_i c_i \log h_i(x)) + \lambda((1-Z)-\epsilon)
  \]
  其中 \(\theta\) 为评论家参数，\(\phi\) 为支撑网络参数，\(\lambda\) 为可训练拉格朗日乘子。
- **学习流程**：支撑网络（MLP）输出上下界；交叉熵损失使用高斯平滑目标；λ通过梯度上升最大化违背约束的惩罚，自动扩张支撑覆盖泄漏质量。
- **超参数**：α控制宽度惩罚与交叉熵的权衡（网格搜索[0.1,0.5]）；初始支撑设为[-10,10]（可灵活选更窄区间）；bin数k=128；sigma-to-width比例0.75；ϵ=0.005（不调）。

## 3. 实验设计

- **基准环境**：DeepMind Control Suite（DM Control）的11个连续控制任务，包括cheetah-run、finger-turn_hard、fish-swim、hopper-hop/stand、humanoid-run/stand/walk、quadruped-run/walk、walker-run。最大回报1000。
- **对比方法**：
  - TD3（base，原始回归）
  - TD3+HLG（固定支撑[-100,100]，bin数128）
  - TD3+DySEL（本文方法）
- **评估指标**：Inter-Quantile Mean (IQM)，带95% bootstrap置信区间（遵循Agarwal et al. 2021最佳实践）。
- **训练设置**：每个任务10个随机种子（0-9），训练3M时间步，每10K步评估20集。
- **消融实验**：
  - 组件消融：去除宽度惩罚 vs. 去除质量约束，观察稳定性。
  - 初始支撑区间敏感性：[-1,1]、[-5,5]、[-10,10]对比。
  - α敏感性：在部分任务上扫描0.1-0.5。
  - 使用学习到的最终区间作为固定区间给HL-Gauss，对比动态适配的优势。

## 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量及训练总时长**。仅在附录提到实现基于JAX 0.6.2、MuJoCo 3.3.7、DM Control 1.0.34、gym 0.23.1。按常理推测，11个任务×10种子×3M步的RL实验（含TD3 baseline）通常需要多块GPU数天至一周，但原文未给出精确算力报告。

## 5. 实验数量与充分性

- **数量充分**：覆盖11个不同难度的连续控制任务（含人型机器人等复杂任务），每个10种子，共110次独立运行。消融实验覆盖了组件、初始条件、超参数敏感性，以及用学习区间作为参考的对比。
- **公平性**：所有方法共享相同网络结构、batch size、学习率、目标网络更新率等；HL-Gauss基线使用业界常用区间[-100,100]（理论全覆盖）；DySEL的α经网格搜索后用于最终报告（注：论文承认α是在相同种子上选择的，因此IQM应视为乐观估计，但附有α敏感性图供阅者判断）。
- **充分性评价**：实验设计较为严谨，符合RL社区最佳实践（IQM、置信区间、多种子）。**主要局限在于评估仅限于DM Control，未在Atari、机器人真实任务或其他标准RL基准上验证**。

## 6. 论文的主要结论与发现

1. **理论贡献**：证明了均方贝尔曼误差被HL-Gauss损失上界约束，且界紧度与支撑绝对值直接相关，从而理论支撑了学习最窄可行区间的好处。
2. **方法有效性**：DySEL在大多数任务上与TD3+HLG竞争力相当，在**人型机器人任务（humanoid-run/stand/walk）上显著优于**基线，证明动态支撑在高维复杂任务中的优势。
3. **自适应能力**：学习到的支撑区间会随训练演变：部分任务迅速收敛至稳定值，部分任务逐渐扩张，DySEL能自动适应两种状态，而固定区间无法同时适应早期和晚期策略。
4. **组件必要性**：去除宽度惩罚导致区间发散（交叉熵利用膨胀Z作弊）；去除质量约束导致区间过窄、截断偏差增大，均造成性能下降。
5. **初始支撑鲁棒性**：不同初始区间（[-1,1]，[-5,5]，[-10,10]）最终收敛到相似的支持演化曲线，仅在人型任务上轻微不稳定。
6. **超参数α的重要性**：α决定了宽度惩罚与交叉熵的权衡：大α促使区间窄、分辨率高；小α促使区间稳定。需根据任务特点调整，但仅需搜索一个标量，取代了原HL-Gauss对手调区间上下界的需要。

## 7. 优点

- **理论创新**：首次将动态支撑学习建模为约束优化问题，并给出上界分析，解释为何窄区间能导致更紧的MSE上界。
- **方法简洁实用**：仅需训练一个额外的支撑网络（MLP）和一个拉格朗日乘子，无需改变原有Actor-Critic架构，易于集成。
- **消除人工调参**：避免了手工设定固定区间这一困难步骤，用α的单参数网格搜索代替，且α的敏感性有详细分析。
- **稳定且可解释**：学习到的区间演化符合直觉（先快速调整，后随策略进步平滑扩张或稳定）；消融实验清楚展示了各组分的贡献。
- **实验规范**：使用IQM、多种子、置信区间，遵循RL统计最佳实践；开源代码承诺提供。

## 8. 不足与局限

- **理论保证较弱**：极小极大拉格朗日博弈的稳定性依赖λ的学习率（论文设为1e-3），未使用PID控制等方法，可能引起早期振荡。
- **对称支持局限性**：学习到的支撑几乎对称，未能处理非对称回报分布（例如大部分正回报）。作者建议未来分别惩罚上下界。
- **评估范围有限**：仅在一个基准（DM Control）上验证，未在Atari、MuJoCo其他版本或真实机器人任务上测试，泛化性存疑。
- **α仍需调优**：虽然减少了参数，但α仍需要任务级网格搜索（虽然范围不大）。论文明确指出ID=报告结果为乐观上限，未提供无调优的泛化结果。
- **σ固定假设**：高斯标准差由sigma-to-width比例固定，未尝试自适应σ。
- **计算资源未声明**：缺少GPU型号、数量、训练时长的具体信息，不利于复现和效率评估。

（完）
