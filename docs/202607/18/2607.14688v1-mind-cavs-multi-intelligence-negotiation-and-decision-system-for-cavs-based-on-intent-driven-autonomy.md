---
title: "MIND-CAVs: Multi-Intelligence Negotiation and Decision System for CAVs based on Intent-Driven Autonomy"
title_zh: MIND-CAVs：基于意图驱动的CAVs多智能体协商与决策系统
authors: "Mainak Mondal, Yihang Feng, Yangchao Luo, Han Song"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14688v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 多智能体协商与决策框架用于自动驾驶车辆
tldr: 现有自动驾驶车辆孤立运行，仅广播低级运动状态，缺乏意图通信和全局协调。MIND-CAVs框架通过将感知转化为结构化意图表示，利用V2X交换并由边缘服务器仲裁冲突，云端记录以持续训练。在CARLA仿真多车道高速场景中，该方法相比孤立驾驶、先到先得和多智能体强化学习基线，减少了不安全距离和无效制动，提升了换道完成时间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14688v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1745, \"height\": 770, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1828, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 904, \"height\": 366, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14688v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 906, \"height\": 369, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14688v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1506, \"height\": 585, \"label\": \"Table\"}]"
motivation: 现有自动驾驶缺乏意图通信与全局协调机制，导致决策不一致和效率低下。
method: 车辆将感知抽象为意图表示，通过V2X交互；边缘服务器结合学习与规则仲裁冲突，云端记录并更新。
result: 在多车道高速场景中，换道完成时间缩短，不安全接近和无效制动显著减少。
conclusion: MIND-CAVs通过意图驱动协商实现了全局一致且安全的协同驾驶。
---

## 摘要
现代自动驾驶车辆大多作为孤立代理运行：它们依赖车载感知和决策模块，并广播仅暴露低层运动学状态的基本安全消息（BSM）。虽然现有的协作驾驶框架支持有限的传感器共享，但它们很少传达高层机动意图，边缘计算主要用于内容分发而非决策仲裁。因此，当前的网联自主性缺乏一种原则性机制，用于在车辆之间做出全局一致、意图感知的协调决策。为填补这一空白，我们提出MIND-CAVs，一种基于意图驱动的网联自动驾驶车辆（CAV）多智能体协商与决策框架。每辆车将原始传感器观测抽象为结构化意图表示，通过V2X链路交换，并从路边边缘服务器接收全局一致的协调计划。边缘代理结合学习型和基于规则的仲裁机制来协商车辆间的冲突意图，而云平台记录决策以供审计和持续再训练。我们在基于CARLA的AI-in-the-loop平台上实现MIND-CAVs，并在涉及冲突机动和路线约束出口的多车道高速公路场景中进行评估。实验结果表明，与孤立自主、先到先得仲裁和多智能体强化学习基线相比，该方法改善了机动完成时间，减少了不安全接近和不必要的制动。

## Abstract
Modern autonomous vehicles largely operate as isolated agents: they rely on on-board perception and decision modules and broadcast Basic Safety Messages (BSMs) that expose only low-level kinematic state. While existing cooperative driving frameworks enable limited sensor sharing, they rarely communicate high-level maneuver intentions, and edge computing is primarily used for content delivery rather than decision arbitration. As a result, current connected autonomy lacks a principled mechanism for making globally consistent, intent-aware coordination decisions across vehicles. To address this gap, we propose MIND-CAVs, a Multi-Intelligence Negotiation and Decision framework for connected autonomous vehicles (CAVs) based on intent-driven autonomy. Each vehicle abstracts raw sensor observations into structured intent representations, exchanges them over V2X links, and receives globally consistent coordination plans from roadside edge servers. Edge agents combine learned and rule-based arbitration mechanisms to negotiate conflicting intents among vehicles, while a cloud platform records decisions for auditing and continual retraining. We implement MIND-CAVs in a CARLA-based AI-in-the-loop platform and evaluate it in multi-lane highway scenarios involving conflicting maneuvers and route-constrained exits. Experimental results show improved maneuver completion time and reduced unsafe proximity and unnecessary braking compared with isolated autonomy, first-come-first-served arbitration, and multi-agent reinforcement learning baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：当前自动驾驶车辆主要作为孤立代理运行，仅通过V2X广播低层运动学状态（如BSM/CAM），不传达高层机动意图。这导致车辆只能从短时观测推断周围行为，在密集多车环境中产生保守驾驶、振荡行为、不必要的制动和安全裕度脆弱。
- **背景**：现有协作驾驶框架（如协同感知、集中式调度）要么依赖难以集成到部署栈的集中式规划器，要么交换意图但缺乏结构化协商和仲裁机制，要么侧重于感知和通信效率而非多车决策治理。多接入边缘计算（MEC）主要用于内容分发而非决策仲裁。此外，缺乏可审计的决策记录，使得系统不透明，难以通过监管验证和事故调查。
- **整体意义**：本文提出MIND-CAVs，一个基于意图驱动的多智能体协商与决策框架，旨在实现全局一致、意图感知的协调决策，同时满足安全性、低延迟和可问责性。

## 2. 方法论：核心思想、关键技术细节、算法流程

### 2.1 核心思想
- **意图驱动协商**：每辆车将原始传感器观测抽象为结构化意图表示（目标、机动类、执行时域、安全约束），通过V2X发送至MEC边缘服务器。边缘服务器结合学习与规则机制仲裁冲突意图，返回批准（ACK）、修订（PLAN）或拒绝（NACK）决策。
- **分层架构**：车辆智能（VI）→ 边缘智能（EI）→ 云智能（CI）。VI生成意图-计划对；EI进行实时仲裁和安全门控；CI记录结构化审计日志用于离线分析和持续训练。
- **可问责性设计**：每个仲裁结果生成结构化审计记录 `R_t^i = ⟨I_t^i, P_t^i, ˆP_t^i, o_t^i, d_t^i⟩`，支持确定性重放和监管检查。

### 2.2 关键技术细节
- **意图与计划定义**：
  - 意图 `I_t^i = ⟨g_t^i, a_t^i, τ_t^i, C_t^i⟩`，其中 `g_t^i` 为目标（如车道、出口），`a_t^i` 为机动类（如换道、合流），`τ_t^i` 为执行时域，`C_t^i` 为安全约束。
  - 计划 `P_t^i` 为有限的高层机动原语序列（如保持车道、加速、开始换道）。
- **车辆侧意图生成**：基于本地观测 `o_t^i`（含运动学、车道上下文、压缩俯视图）和全局路线目标，通过VLM辅助模块生成 `(I_t^i, P_t^i)`。车辆执行“先批准后执行”循环，超时则回退保守策略。
- **MEC仲裁（算法1）**：
  1. 接收请求 `(I_t^i, P_t^i, o_t^i)` 和当前活跃计划数据库 `D_t^j`。
  2. 构造结构化提示，输入给约束VLM，输出决策和修订计划。
  3. 若决策为ACK或PLAN，则通过确定性时空验证器检查冲突（距离 < d_safe 时冲突）。
  4. 若验证失败，覆盖为NACK。
  5. 优先级规则：紧急车辆 > 较早提交时间戳 > 车辆ID。
  6. 修订仅允许一次，以保持低延迟。
  7. 更新数据库并写审计日志。
- **延迟分析**：
  - 车辆端（Jetson AGX）：意图生成约270ms。
  - 车辆→MEC传输（5G V2X）：<10ms（200KB payload）。
  - MEC仲裁（A100）：ACK响应约45ms，PLAN修订约400ms。
  - 返回传输：<10ms。
  - 端到端：ACK约335ms，PLAN约690ms，均低于700ms。

## 3. 实验设计

- **仿真平台**：CARLA 0.9.15 + 自定义MIND-CAVs Simulation Conductor（AI-in-the-loop协调层）。
- **场景**（三个，每次10次运行，独立随机VLM采样）：
  - **S1（单车辆多车道变换）**：两辆车并排，左车道车辆需换到最右车道，右车道保持车道。
  - **S2（冲突车道变换）**：与S1初始相同，但左车意图向右，右车意图向左，直接冲突。
  - **S3（路线约束出口）**：左车道车辆需在60秒内从右侧出口驶出，进行多车道序列规划。
- **对比基线**：
  - **IA（孤立自主）**：无意图交换，仅本地感知和运动学推理。
  - **FCFS（先到先得仲裁）**：确定性集中控制器，基于请求到达顺序解决冲突，无协商或修订。
  - **MARL（多智能体强化学习）**：共享状态，学习式协调，无显式意图协商。
- **评估指标**：
  - 机动完成时间。
  - 间隙违规（车辆间欧式距离 < 5m）。
  - 不必要制动（速度下降 > 5 km/h 且无其他车辆在50m内）。

## 4. 资源与算力

- **仿真平台硬件**：Intel i7-10700 CPU（2.90 GHz）、64GB RAM、NVIDIA RTX 3070 GPU、Ubuntu 24.04 LTS、CARLA 0.9.15。
- **MEC硬件假设**：车辆端使用Jetson AGX-class（4-bit量化）；MEC端使用A100-class（约22 tokens/s）。
- **训练时长**：论文未明确说明LLM/VLM的训练时间或算力消耗，也未提及MARL的训练时长。VLM是预训练模型，仅提及推理延迟。因此，训练资源与时长信息不完整。

## 5. 实验数量与充分性

- **数量**：每个场景执行10次运行，共3个场景，总计30次实验。此外，对比了3个基线（+1个提出的方法），总实验组数约4×3×10=120次运行。但未提及消融实验（如无VLM、无安全验证等变体）。
- **充分性与公平性**：
  - **充分性**：三个场景覆盖了单车规划、冲突机动和长时域约束，但仅限高速公路、两辆车场景。场景复杂度有限，未考虑密集交通、行人、信号灯等。
  - **公平性**：所有方法使用相同初始状态和动力学模型，随机性来源明确（VLM采样）。指标统计了均值、标准差和95%置信区间，结果清晰。
  - **不足**：未进行统计显著性检验（如t检验）；未报告运行时间方差；未评估不同参数（如安全阈值、数量车辆）的敏感性。因此，实验覆盖不够全面，但针对该场景对比是客观的。

## 6. 主要结论与发现

- MIND-CAVs在所有三个场景中均实现了最低或最低的完成时间（S1: 29.10s vs 基线38-41s；S2: 39.46s vs 44-46s；S3: 39.56s vs 40-42s），降低约24-29%。
- 间隙违规减少：S1中MIND-CAVs为零违规，其他基线有0.2-0.4次；S3中0.2次 vs 1.0-1.6次。
- 不必要制动显著减少：S1中MIND-CAVs为零，其他基线1.1-1.5次；S2中0.4次 vs 1.5-1.7次；S3中0.6次 vs 0.9-1.6次。
- 方差更小，表明协调更稳定。
- 结论：意图驱动的协商结合集中式安全门控可提升效率和安全性，且不引入协调导致的延迟。

## 7. 方法或实验设计的亮点（优点）

- **架构创新**：分层Vehicle-MEC-Cloud设计，将协调、实时仲裁和可问责性作为架构原语整合进可部署V2X框架。
- **可问责性内建**：每个决策生成结构化审计记录，支持确定性重放和监管审核，提升透明度和信任。
- **AI-in-the-loop仿真平台**：将VLM集成到闭环控制中，实现实时意图生成、仲裁和安全验证，支持多种协调模式的公平对比。
- **延迟分析**：给出了详细的端到端延迟估算（ACK~335ms，PLAN~690ms），论证了在实际5G V2X下的可行性。
- **安全门控**：确定性时空验证器确保即使VLM输出有误，执行也是安全的，增强了可靠性。

## 8. 不足与局限

- **场景局限于结构化的高速公路**：仅涉及两辆车的简单交互，未评估密集多车（>2）、城市交叉口、行人、非机动车、感知噪声等复杂场景。
- **固定速度上限**：实验设置中MEC强制50 km/h上限以控制推理频率（防止API限流），未反映真实高速公路速度变化，可能影响泛化性。
- **VLM非形式化验证**：仲裁使用大语言模型作为语义规划器，未提供形式化安全保证。尽管有安全门控，但决策逻辑可能不可预测或存在偏见。
- **缺乏消融实验**：未单独验证意图表示、优先级规则、VLM设计、安全验证等组件的贡献。
- **统计显著性未检验**：仅提供了置信区间，未进行假设检验（如p值），无法确认差异是否显著。
- **实现细节不完整**：未公开VLM模型选型（如具体哪个LLM/VLM）、训练数据和微调策略，影响可复现性。
- **硬件资源假设较理想**：MEC使用A100-class GPU在真实部署中可能成本高昂，且未测试嵌入式MEC性能。

（完）
