---
title: Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning
title_zh: 面向智能体强化学习的单次展开异步优化
authors: "Zhenyu Hou, Yujiang Li, Jie Tang, Yuxiao Dong"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07508v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 单轨迹异步优化用于智能体强化学习
tldr: 现有异步强化学习在长周期智能体任务中面临稳定性和策略偏离问题。本文提出单幕异步优化(SAO)，用单幕采样替代组采样，结合双面token级裁剪和值模型训练。在SWE-Bench等基准上持续优于GRPO，成功用于训练750B参数的GLM-5.2模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07508v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1342, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1011, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 594, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1420, \"height\": 414, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1422, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07508v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 644, \"height\": 426, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07508v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1396, \"height\": 655, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 575, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1420, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07508v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 761, \"height\": 218, \"label\": \"Table\"}]"
motivation: 解决异步RL在智能体任务中因组采样导致的策略偏离和训练不稳定问题。
method: 提出单幕采样策略，每个提示仅用一个轨迹更新，配合严格双面token级裁剪和值模型训练。
result: 在SWE-Bench Verified、BeyondAIME等基准上一致超越GRPO，稳定训练千步，并用于750B参数模型训练。
conclusion: 单幕异步优化有效提升智能体RL的稳定性和效果，适用于在线学习场景。
---

## 摘要
强化学习（RL）在大语言模型（LLM）的后训练中变得越来越重要。先前的LLM强化学习流程大多是同步的和批量交错的，这对于长周期智能体任务效率低下。最近，异步强化学习通过随着展开的到达更新模型，成为一种更高效的替代方案。然而，现有的异步强化学习系统通常强调吞吐量，而训练稳定性和任务有效性在很大程度上未被充分探索。例如，一个关键挑战是广泛使用的GRPO框架中的分组采样并不自然适用于异步智能体训练。在本文中，我们提出了单次展开异步优化（SAO）来解决异步强化学习中的稳定性和离策略挑战。为了减少离策略效应并提高泛化能力，我们用单次展开采样替代分组采样，即每个提示使用一次展开。我们通过实际的价值模型训练设计进一步改进了这种单次展开策略。为了提高优化稳定性，我们引入了一种严格的双边令牌级裁剪策略。SAO能够稳定训练一千步，并在智能体编码和推理基准（如SWE-Bench Verified、BeyondAIME和IMOAnswerBench）上持续优于GRPO及其变体。我们还证明了单次展开强化学习在模拟在线学习环境中特别有效，其中模型必须适应不断变化的环境。为此，SAO已成功部署在用于训练开源GLM-5.2模型（750B-A40B）的智能体强化学习流程中。

## Abstract
Reinforcement learning (RL) is becoming increasingly important for post-training large language models (LLMs). Previous RL pipelines for LLMs were mostly synchronous and batch-interleaved, which is inefficient for long-horizon agentic tasks. Recently, asynchronous RL has emerged as a more efficient alternative by updating the model as rollouts arrive. However, existing asynchronous RL systems often emphasize throughput, while leaving training stability and task effectiveness largely underexplored. For example, a key challenge is that group-wise sampling in the widely-used GRPO framework does not naturally fit asynchronous agentic training. In this paper, we present Single-rollout Asynchronous Optimization (SAO) to address the stability and off-policy challenges in asynchronous RL. To reduce off-policy effects and improve generalization, we replace group-wise sampling with single-rollout sampling, that is, using one rollout per prompt. We further improve this single-rollout strategy with practical value-model training designs. To improve optimization stability, we introduce a strict double-side token-level clipping strategy. SAO is able to train stably for one thousand steps and consistently outperform GRPO and its variants on agentic coding and reasoning benchmarks, such as SWE-Bench Verified, BeyondAIME, and IMOAnswerBench. We also demonstrate that single-rollout RL is particularly effective in a simulated online learning setting, where the model must adapt to changing evolving environments. To this end, SAO is successfully deployed in the agentic RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）

- **背景**：强化学习（RL）在大语言模型（LLM）后训练中日益重要，但传统流程多为同步和批量交错式（batch-interleaved），对于长周期智能体任务（如代码生成、多步推理）效率低下——长轨迹成为“掉队者”，导致GPU空闲等待。
- **核心问题**：异步RL能通过连续消耗已完成rollout来提高利用率，但引入了两重挑战：
    1. **训练不稳定**：异步性导致策略滞后（policy lag），轨迹由多个旧策略版本生成，造成严重的离策略（off-policy）效应，损害稳定性。
    2. **不兼容组采样**：主流GRPO要求对一个提示采样一组响应，利用组内均值估计优势，这既引入了等待延迟（组内最慢样本），也不兼容仅提供单条轨迹反馈的在线/复杂智能体环境。
- **论文目标**：提出一种兼顾稳定性和有效性的异步RL方法，使得异步训练能稳定进行千步以上，并在智能体编码和推理任务上超越现有同期方法。

### 2. 方法论：核心思想、关键技术细节

**SAO（Single-rollout Asynchronous Optimization）** 包含三个核心组件：

#### 2.1 双面重要性采样裁剪（Double-sided Importance Sampling, DIS）稳定训练
- **直接使用rollout概率**：放弃传统的π_old追踪（因异步中旧策略版本众多），直接以π_rollout作为行为代理，用当前策略π_θ与π_rollout的log概率比计算重要性权重。
- **严格双面token级裁剪**：将概率比r_t(θ)限制在区间[1-ε_l, 1+ε_h]内，超出范围的token完全掩码（mask），不参与梯度计算，防止极端离策略更新。
- 目标函数：L(θ) = E_t[ f(r_t(θ), ε_l, ε_h) · A_t · log π_θ(a_t|s_t) ]，其中f(·)为裁剪函数。

#### 2.2 单rollout采样降低离策略效应
- **替换组采样**：每个提示只产生一条轨迹，一旦完成立即送入训练，避免组采样带来的等待离策略。
- **增强价值模型训练**以降低单样本方差：
    - **价值更新快于策略**：每步策略更新前，对价值网络执行K次更新（实验K=2），使价值更快适应当前策略。
    - **冻结注意力参数**：发现价值模型训练中注意力层梯度较大，固定注意力层的参数只优化MoE层，提高稳定性。
    - **跳观察token级GAE**：针对智能体任务轨迹（动作-观察交叠），设计跳过环境反馈token的优势计算，仅依赖纯模型输出，避免噪声传播。公式：Â(a_i,N) = δ + γλ Â(a_{i+1,0})，其中δ = r_t + γ V(a_{i+1,0}) - V(a_i,N)。
    - **价值预训练数据规模化**：增大价值预训练语料以解决“冷启动”问题。

#### 2.3 算法流程概述
- 异步引擎：rollout模型持续生成，每条轨迹完成后立即送入训练队列。
- 训练时：从队列获取单条轨迹，计算log概率与rollout log概率的比率，用双面裁剪函数过滤，结合跳观察GAE优势估计，按设定更新策略和价值模型（价值更新频率更高）。

### 3. 实验设计：数据集/场景、benchmark、对比方法

#### 数据集与评估场景
- **数学推理基准**（带Python工具调用，允许50轮交互）：AIME2025、BeyondAIME、HMMT Nov 2025、IMOAnswerBench。
- **编码基准**：SWE-Bench Verified（使用OpenHands脚手架，最多300轮交互，128K token上下文）。
- **模拟在线学习场景**：设计三阶段变化写作风格偏好任务（可爱、中二病、古典风格），奖励由LLM法官GLM-4.7评分。

#### 对比方法
- 基线：Qwen3-30B-A3B的SFT模型（无RL）。
- 主要对比：标准GRPO、GRPO（w/ DIS，即应用双面裁剪的GRPO）、SAO（文中方法）。
- 消融实验：还对比了Vanilla VAPO、Running Mean baseline、单次价值更新版本、全参数价值训练版本等。

### 4. 资源与算力（文中说明）

- 论文未明确说明使用的GPU型号、数量和训练时长，仅在训练细节提到批量大小128、最大长度128K tokens等超参数。训练模型为Qwen3-30B-A3B（约30B激活参数，MoE架构）。SAO成功部署于GLM-5.2模型（750B-A40B）的RL pipeline。具体算力不详。

### 5. 实验数量与充分性

- **主实验**：4个数学推理基准 + 1个编码基准，与SFT、GRPO、GRPO(w/ DIS)对比，结果以表格和训练曲线展示。
- **消融实验**（表3、4）：包括更快的价值更新（Critic更新频率）、冻结注意力vs全参数、Vanilla VAPO、Running Mean基线等，在AIME2025和BeyondAIME上报告准确率。
- **训练动态分析**：图4展示了解释方差、批评梯度范数、token裁剪比率等指标。
- **在线学习模拟**：图5展示了风格迁移过程中的准确率变化和奖励曲线，并与Running Mean基线对比。
- **附录实验**：还探讨了step-level vs token-level价值设计（表5），结果显示token-level更优。此外对比了SPO和Running Mean单轨迹策略（附录A.2）。
- **客观性与公平性**：使用了公开可复现的基准，采用Pass@1准确率，报告多次运行均值减少方差。与GRPO及优化变体在同一backbone上比较，消融设计合理。

### 6. 主要结论与发现

- SAO在所有5个基准上一致优于SFT和GRPO（及其DIS变体），尤其在AIME2025上达到97.3%（SFT 80.4%，GRPO 84.2%）。
- 标准GRPO约在160步后性能崩溃；GRPO+DIS可稳定训练，但SAO在400步后表现明显更优，并可持续千步以上稳定提升。
- 单rollout策略在模拟在线学习中能快速适应偏好变化，且奖励收敛水平高于Running Mean基线。
- 关键设计有效性：双面裁剪、冻结注意力、更快的价值更新、跳观察GAE均经消融验证不可或缺。

### 7. 优点

- **方法创新**：巧妙结合单rollout采样与严格裁剪，在保留异步效率的同时解决了稳定性和离策略问题，无需追踪多版本旧策略。
- **解决实际痛点**：直指组采样GRPO在异步和智能体场景下的根本缺陷（等待延迟、不兼容单轨迹反馈），提供实用替代。
- **实验覆盖全面**：涵盖数学推理、代码生成、在线学习（非平稳环境），消融实验覆盖每个设计组件，训练动态分析揭示机制。
- **可扩展性**：成功应用于750B参数的GLM-5.2模型训练，表明方法具有大规模实用性。
- **与现有系统互补**：在异步系统（如AReaL、ROLL Flash）基础上提供算法层面的稳定性保障。

### 8. 不足与局限

- **实验范围有限**：仅使用Qwen3-30B-A3B一个backbone进行核心实验，结论可能不直接迁移到更小模型、非智能体RLHF或密集奖励短轨迹场景。
- **依赖基础设施**：需要精确保留异步生成中的token级log概率，对工程部署有一定要求。
- **在线学习模拟过于简化**：实际用户面在线适应需要更强的安全监控和隐私审查，论文未讨论。
- **算力消耗未透明**：未提供GPU小时等具体资源信息，可复现性受限。
- **缺乏长期训练稳定性测试**：虽然提到千步稳定，但更长时间的极端训练行为未知。
- **未探讨负社会影响**：方法可能被误用于优化有害目标，论文仅在“Broader Impact”中简短提及需负责任发布。

（完）
