---
title: "Harness-RL: Black-Box Reinforcement Learning with Action-Args Decoupling for Central-Agent Multi-Agent Harnesses"
title_zh: Harness-RL：面向中心智能体多智能体控制台的黑盒强化学习与动作参数解耦
authors: "Xinke Jiang, Zhixin Zhang, Zhibang Yang, Jiaran Gao, Rihong Qiu, Shijin Chen, Xu Chu, Junfeng Zhao, Yasha Wang"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29641v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 用于多智能体中央策略训练的强化学习框架
tldr: 多代理harness中中心代理训练面临动作与参数梯度冲突及动态会话结构难以建模的挑战。本文提出Harness-RL，通过接口级黑盒轨迹构建捕获真实调用关系，并采用CAPO将策略梯度按前向激活路由至动作或参数子空间。在七个基准上取得领先F1，消融表明中心仅优化更有效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29641v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1327, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29641v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1490, \"height\": 857, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29641v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1322, \"height\": 722, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.29641v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1641, \"height\": 1502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29641v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1646, \"height\": 287, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29641v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1645, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29641v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1561, \"height\": 923, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29641v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1274, \"height\": 464, \"label\": \"Table\"}]"
motivation: 中心代理协调子代理时，动作与参数的梯度冲突，且动态会话无法简化为扁平序列。
method: 提出CAPO与黑盒轨迹构建，用前缀树对齐奖励，按激活分区路由梯度。
result: 在七个多跳问答基准上，1.5B和3B模型平均F1达42.93和47.79。
conclusion: 验证CAPO有效性，表明中心仅优化优于联合训练。
---

## 摘要
大型语言模型智能体越来越多地通过多智能体控制台来解决长时程任务，其中中心智能体协调专门的子智能体、工具和环境。在此类控制台中训练中心策略面临两个挑战。首先，动作标签是低基数的决策，而其参数构成高维条件序列；使用共享的序列级信号优化两者可能产生冲突梯度。其次，动态调度创建了具有分支、并行调用和重写上下文的相互依赖的会话，这无法忠实地简化为一个扁平的令牌序列。我们提出了Harness-RL，一种结构化的强化学习框架，将冲突感知策略优化（CAPO）与接口级黑盒轨迹构建相结合。黑盒组件捕获接口调用记录，构建每会话前缀树，并将结果与过程奖励与可训练令牌对齐。CAPO利用前向激活来识别与动作和参数令牌关联的参数分区，然后将策略梯度路由到相应的子空间。Harness-RL支持仅中心智能体以及联合多智能体训练。在七个多跳问答和智能体检索基准上，使用Qwen2.5-1.5B和Qwen2.5-3B分别达到平均F1分数42.93和47.79，而消融实验验证了CAPO的贡献，并在评估设置中偏向于仅中心智能体优化。我们的代码可在https://github.com/jiangxinke/Harness-RL获取。

## Abstract
Large language model agents increasingly solve long-horizon tasks through multi-agent harnesses in which a central agent coordinates specialized sub-agents, tools, and environments. Training the central policy in such a harness raises two challenges. First, an action label is a low-cardinality decision, whereas its args form a high-dimensional conditional sequence; optimizing both with a shared sequence-level signal can produce conflicting gradients. Second, dynamic scheduling creates interdependent sessions with branches, parallel calls, and rewritten contexts, which cannot be faithfully reduced to one flat token sequence. We introduce Harness-RL, a structured reinforcement learning framework that combines Conflict-Aware Policy Optimization (CAPO) with interface-level black-box trajectory construction. The black-box component captures Interface Call Records, builds per-session prefix trees, and aligns outcome and process rewards with trainable tokens. CAPO uses forward activations to identify parameter partitions associated with action and args tokens, then routes their policy gradients to the corresponding subspaces. Harness-RL supports both central-only and joint multi-agent training. Across seven multi-hop question answering and agentic retrieval benchmarks, it reaches average F1 scores of 42.93 and 47.79 with Qwen2.5-1.5B and Qwen2.5-3B, respectively, while ablations validate the contribution of CAPO and favor central-only optimization in the evaluated setting. Our code is available at https://github.com/jiangxinke/Harness-RL.

---

## 论文详细总结（自动生成）

# Harness-RL论文总结

## 一、核心问题与整体含义（研究动机和背景）

本文关注**中心智能体驱动的多智能体控制台（Central-Agent Multi-Agent Harness）中的策略训练问题**。在复杂的多智能体系统中，一个中心智能体需要协调多个专门的子智能体、工具和外部环境来完成长时程任务。作者指出，训练该中心策略面临两个关键挑战：

- **挑战1（优化冲突）**：智能体的决策由"动作-参数"（Action–Args）对构成。Action是低基数离散决策，Args是高维条件序列生成。标准GRPO/DAPO等算法对整个序列统一计算策略梯度，导致一个样本的低质量Args可能对正确的Action产生负向更新干扰，造成两类异质决策之间的梯度冲突（⟨∇θJaction, ∇θJargs⟩ < 0）。
- **挑战2（轨迹建模困难）**：中心智能体动态调度多个子智能体，会产生分支、循环、并发、回溯、上下文压缩等复杂结构，形成多个相互关联的交互树。这些交互无法被简化为单一的线性token序列，为rollout构建、轨迹拼接和注意力掩码带来了巨大开销。

**论文整体含义**：作者提出Harness-RL框架，通过"动作-参数梯度解耦"和"接口级黑盒轨迹构建"两个核心机制，系统性地解决了多智能体控制台中中心策略难以用强化学习有效训练的问题，填补了该领域在中心智能体策略优化方面的空白。

## 二、论文提出的方法论

### 2.1 核心思想

Harness-RL将强化学习目标分解为Action项与Args项之和（JHARNESS-RL = Jaction + Jargs），构建功能参数分区，将不同token类型产生的策略梯度路由至相应子空间，从而避免异质决策间的梯度干扰。同时，将整个多智能体控制台视为黑盒，通过接口调用记录来重建结构化的RL训练轨迹。

### 2.2 关键技术细节

**（1）黑盒轨迹构建（Interface-Level Black-Box RL）**，包含三个步骤：

- **接口调用记录（Interface Call Record）捕获**：记录每个会话中每次调用的Token-In（输入）、Token-Out（输出）和对应的采样log-probabilities，形成记录ξe,h,k = ⟨Xe,h,k, Ye,h,k, ℓe,h,kroll⟩。
- **前缀匹配与树构建**：将每个会话的调用按输入输出的token前缀关系组织为前缀树，共享前缀合并，重写上下文在最长共享前缀处挂载，从而编码token可视性而非因果执行。
- **分段奖励归因**：将组归一化的结果奖励（outcome reward，如F1指标）与决策级过程奖励（process reward）对齐到可训练token上（公式22：An,t = mn,t[Ae(n)out + λproc Σd∈Qe(n) I[(n,t)∈Ie(n),d]Ae(n),dproc]）。

**（2）冲突感知策略优化（CAPO）**，包含三个步骤：

- **高质量轨迹构建**：对每个训练查询采样多个rollout，仅保留成功轨迹作为探测集，减少失败行为激活模式的污染。
- **激活统计与功能分区**：利用模型前向传播中的激活信号，分别计算Action Token和Args Token在各层各结构单元的响应强度（公式8/9），选择TopK高响应单元构成各自的参数分区Uaction和Uargs。
- **Token特定梯度路由**：对Action和Args两个分区分别计算策略梯度（∇θbJb = Σ(n,t)∈Ib An,t ∇θbℓn,t(θ)），只更新各自的参数子空间。

### 2.3 训练目标

最终使用rollout级平衡的截断目标（公式24）：
JHARNESS-RL = (1/K) Σe (1/max(|Itraine|,1)) Σ(n,t)∈Itraine min(ρn,tAn,t, clip(ρn,t, 1−ϵ, 1+ϵ)·An,t)

## 三、实验设计

### 3.1 数据集与基准

- **训练数据**：从2WikiMultiHopQA和HotpotQA的训练集中筛选需要多步推理和子智能体协调的查询。
- **评估基准**（7个）：2WikiMultiHopQA、HotpotQA（域内）；Bamboogle、FRAMES、MuSiQue、Natural Questions、TriviaQA（域外）。覆盖多跳推理、事实检索和分布外迁移场景。
- **评价指标**：token-level F1（%）。

### 3.2 模型与智能体配置

- **策略骨干**：Qwen2.5-1.5B-Instruct和Qwen2.5-3B-Instruct。
- **控制台结构**：中心智能体 + Search Agent（带维基百科和知识图谱检索工具）+ Conclusion Agent（答案合成工具）。默认设置下子智能体为冻结的环境组件，仅优化中心智能体。

### 3.3 对比方法（4个家族，14个基线）

- **无检索**：Base、CoT
- **朴素RAG**：FS-RAG、FL-RAG
- **智能体RAG**：ReAct、IRCoT、TC-RAG
- **基于RL的智能体**：ReSearch、Search-R1、AEPO、ARPO、MEM1、AgenticRAG-R1

## 四、资源与算力

- **硬件**：NVIDIA A100-SXM4-80GB GPU（Qwen2.5-1.5B用4张，Qwen2.5-3B用8张）；CPU为Intel Xeon Platinum 8488C（96物理核心）；系统内存503GB。
- **软件环境**：Ubuntu 22.04.5 LTS、Python 3.11.11、PyTorch 2.6.0、Transformers 4.51.3。
- **训练配置**：每查询4个rollout（K=4）、学习率2×10⁻⁷、100次优化器更新、最大harness步数12、输入/输出长度16,384/1,024 tokens、温度0.8、裁剪系数ϵ=0.2。
- **训练时长**：**论文未明确给出具体训练时长**，仅说明在A100上进行的更新次数。

## 五、实验数量与充分性

论文包含以下实验：

1. **主实验**：在7个基准 × 2个模型规模上对比了14种基线方法，共14组对比场景。
2. **CAPO消融**：去掉CAPO后，在7个基准上和2个模型规模下的对比（表2）。
3. **训练范围消融**：仅中心智能体训练 vs. 联合训练所有智能体（中心+子智能体），在准确率和F1双指标下评估（表3）。
4. **功能分区分析**：可视化了Qwen2.5-1.5B（28层）和Qwen2.5-3B（36层）的层间Action/Args功能分区差异（图3）。
5. **案例研究**：展示了一个歧义多跳问题上中心智能体如何在保持正确DISPATCH动作的同时细化Args的完整交互轨迹。

**充分性评估**：实验覆盖了域内和域外多个数据集、两个不同规模的模型、多种类型的基线方法以及关键组件的消融，整体较为全面。但训练数据主要来自2WikiMultiHopQA，而评估涉及更加多样的域外数据集，存在一定的分布偏移因素。由于各基线通常在相同条件下运行并报告标准化F1指标，公平性总体可接受，但论文未提供方差/显著性检验的详细数据。

## 六、主要结论与发现

1. **Harness-RL整体性能显著领先**：在Qwen2.5-1.5B上平均F1达42.93，超过最强基线AgenticRAG-R1达15.28个点；在Qwen2.5-3B上平均F1达47.79，超过最强基线14.24个点。在全部7个基准上均取得最优结果。
2. **CAPO有效**：Qwen2.5-1.5B上去掉CAPO导致平均F1下降1.89点（42.93→41.04）；Qwen2.5-3B下降1.57点（47.79→46.22）。CAPO在FRAMES、NQ、TriviaQA上的提升最明显。
3. **功能分区确实存在**：图3显示Action Tokens和Args Tokens在不同层上的激活重要性分布不同，支持基于激活的功能分区假设。
4. **仅中心智能体训练优于联合训练**：在Qwen2.5-3B上，仅中心训练的平均准确率（42.88 vs 40.49）和平均F1（47.79 vs 45.36）均高于联合训练所有智能体，说明在中心协调任务中，保持全局规划目标的一致性更为重要。
5. **中心智能体的行为可解释地改善**：案例研究显示，模型能够保留正确的DISPATCH动作，同时基于子智能体反馈迭代优化Args，最终解决歧义问题。

## 七、优点

1. **问题识别精准**：清晰地指出了现有agentic RL方法的两个隐性假设（序列级统一优化、线性轨迹建模）在真实多智能体控制台中的失效，并针对性地提出解决方案。
2. **方法设计有创新性**：CAPO利用模型内部分工（功能分区）来解耦异质决策的优化，思想新颖；黑盒机制将复杂的控制台交互统一建模为前缀树，提高了系统的泛用性。
3. **系统设计完整**：框架同时覆盖了轨迹构建、奖励归因、参数优化、训练目标设计等多个层面，形成了从环境交互到策略更新的闭环。
4. **实验验证充分**：在7个基准、2个模型规模上进行了系统评估，覆盖面广；消融实验明确验证了各组件贡献。
5. **实现可复现**：公开代码地址，提供了详细的超参数配置和计算环境信息。
6. **支持多种训练模式**：同时支持仅中心智能体训练和联合多智能体训练，提供了训练范围的灵活性。

## 八、不足与局限

1. **训练数据来源单一**：RL训练数据主要来自2WikiMultiHopQA和HotpotQA两个数据集，与评估集之间存在分布差异，可能会限制在更加多样化任务上的泛化能力。
2. **CAPO效果不稳定**：在部分数据集上（如1.5B的Bamboogle、3B的2Wiki和MuSiQue），完整的Harness-RL反而不如去掉CAPO的版本，说明解耦优化在不同数据分布上的收益存在波动，其适用边界有待进一步明确。
3. **联合训练效果不及单智能体训练**：论文中联合训练未能超过仅中心训练，实验评估的harness拓扑可能不足以体现联合训练的优势。该结论可能不适用于更复杂的多智能体协作场景。
4. **训练时长不可知**：论文没有报告具体的训练时长，这对于评估方法的计算成本与实际可用性造成一定障碍。
5. **子智能体冻结假设可能限制扩展**：在默认设定下搜索和结论智能体是冻结的，无法利用训练数据来共同优化检索-推理协同，在更加复杂或专精的领域任务上可能受限。
6. **缺少统计显著性报告**：论文未提供多次运行的标准差或显著性检验结果，无法严格判断观测到的性能差异是否统计显著。
7. **未来工作范围有限**：作者未在更大规模模型（如7B+）、更多样化的控制台拓扑或多模态动作上进行验证，论文结论的普适性仍有待检验。

**（完）**
