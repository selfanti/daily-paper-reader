---
title: "Dreaming Of Others: Latent Teammate Modeling In World Models For Multi-Agent Reinforcement Learning"
title_zh: 想象他人：多智能体强化学习中世界模型的潜在队友建模
authors: Tomas Leroy-Stone
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31361v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 多智能体强化学习中的世界模型潜在队友建模
tldr: 多智能体强化学习中，队友策略不可直接观测导致协调困难。现有World Model（如Dreamer）无法处理队友不确定性。本文在Dreamer的RSSM中将潜状态分解为环境与队友两部分，并学习心智理论头从局部轨迹推断队友潜嵌入（角色、意图、动作）。该方法使智能体能想象并适应多样化合作者，在部分可观测场景支持零样本和少样本协调，将世界模型扩展为社会行为模拟器。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31361v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.31361v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1097, \"height\": 714, \"label\": \"Figure\"}]"
motivation: 现有World Model无法处理多智能体中的队友不确定性，限制了其在MARL中的应用。
method: 将Dreamer风格RSSM的潜状态分解为环境与队友组件，并添加ToM头推断队友潜嵌入。
result: 支持零样本和少样本协调，在部分可观测场景中适应多样化合作者。
conclusion: 世界模型不仅是环境预测器，更可成为社交行为模拟器，推动通用化、人机兼容AI。
---

## 摘要
在协作多智能体强化学习（MARL）中，智能体必须与内部策略和意图不可直接观测的伙伴进行协调。尽管像Dreamer这样的世界模型在单智能体设置中展现了强大的泛化能力和样本效率，但它们在MARL中的应用因无法处理队友引发的不确定性而受到限制。我们提出一种新视角：将队友视为智能体世界模型中结构化的、可学习的组件。我们引入了一种架构，将Dreamer风格的递归状态空间模型（RSSM）的潜在状态分解为环境和队友组件，并学习一个辅助的心智理论（ToM）头部，以从部分轨迹中推断伙伴行为的潜在嵌入，如角色、意图和预测动作。这些队友潜在变量约束了演员和评论家，使智能体能够想象并适应多样的合作者。我们概述了该方法如何在部分可观测设置中支持零样本和少样本协作，并提出了一套基准测试和评估协议来评估其影响。这项工作将世界模型定位为不仅是环境动态的预测器，而且是社会行为的模拟器，为可泛化、与人类兼容的人工智能开辟了新的方向。

## Abstract
In cooperative multi-agent reinforcement learning (MARL), agents must coordinate with partners whose internal policies and intentions are not directly observable. While world models such as Dreamer have demonstrated strong generalization and sample efficiency in single-agent settings, their application to MARL remains limited by an inability to handle teammate-induced uncertainty. We propose a new perspective: treat teammates as structured, learnable components within the agent's world model. We introduce an architecture that factorizes the latent state of a Dreamer-style recurrent state-space model (RSSM) into environment and teammate components, and learns an auxiliary Theory-of-Mind (ToM) head to infer latent embeddings of partner behavior such as character, intent, and predicted actions from partial trajectories. These teammate latents condition the actor and critic, enabling the agent to imagine and adapt to diverse collaborators. We outline how this approach can support zero-shot and few-shot coordination in partially observable settings and propose a set of benchmarks and evaluation protocols to assess its impact. This work positions world models as not only predictors of environmental dynamics, but as simulators of social behavior, opening new directions for generalizable, human-compatible AI.

---

## 论文详细总结（自动生成）

# 结构化总结：Dreaming Of Others

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在协作多智能体强化学习（MARL）中，智能体必须与策略和意图不可直接观测的队友协调。现有的世界模型（如Dreamer）在单智能体任务中表现优异，但在MARL中无法处理队友行为带来的非平稳性和部分可观测性，从而限制了其在社交场景中的应用。
- **研究背景**：
  - 世界模型（Dreamer系列）通过学习压缩的潜在动力学，在单智能体领域实现高样本效率和泛化。
  - 协作MARL面临非平稳性挑战（队友更新策略导致环境动态变化）和部分可观测性。
  - 已有将世界模型扩展到MARL的工作（如MA-Dreamer、CoDreamer、GAWM），但通常假设共享或集中式想象，未解决对队友的个体化建模问题。
  - Theory of Mind（ToM）和代理建模方法（如ToMnet）能推断他人策略，但尚未与基于世界模型的决策框架深度融合。
- **整体含义**：本文提出将世界模型扩展为“社会行为模拟器”，通过将队友视为结构化、可学习的潜在过程，而非噪声，从而提升零样本/少样本协调能力，推动人机兼容AI发展。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 在Dreamer风格的递归状态空间模型（RSSM）中，将潜在状态分解为环境分量（\(z^{\text{env}}_t\)）和队友分量（\(z^{\text{team}}_t\)）。
- 增加一个心智理论（Theory-of-Mind, ToM）头部，用于从部分观测历史中推断队友行为的潜在嵌入（如性格、意图、动作预测）。
- 这些队友潜在变量用于条件化actor和critic，使智能体在想象中模拟不同合作伙伴的行为，从而适应未见过的协作者。

### 关键技术细节
1. **架构（图1）**：
   - 编码器从观测\(x_t\)和智能体自身动作\(a_t^0\)生成确定性隐状态\(h_t\)。
   - 潜在状态分解：\(z_t = [z^{\text{env}}_t, z^{\text{team}}_t]\)。
   - 观测解码器：从\(z^{\text{env}}_t\)重建\(\hat{x}_t\)。
   - 队友策略解码器：从\(z^{\text{team}}_t\)预测队友下一动作的分布\(\hat{\pi}^j_t(\cdot)\)。
   - 所有智能体动作（\(a_t^0, a_t^j\)）共同驱动过渡到\(h_{t+1}\)。
   - 演员-评论家训练时，隐状态和队友潜在变量条件化策略和价值头，在想象中采样\(z^{\text{team}}_t\)模拟不同队友。

2. **队友建模目标（公式1）**：
   \[
   \mathcal{L}_{\text{ToM}} = \mathbb{E}_t\left[ -\sum_a \pi^j_t(a) \log \hat{\pi}^j_t(a) \right] + \alpha \text{KL}\left( q(z^{\text{team}}_t | h_t) \parallel p(z^{\text{team}}_t | h_{t-1}, a_{t-1}) \right)
   \]
   - 第一项：交叉熵，使预测分布逼近队友的真实行为分布（实践中可用one-hot或平滑标签）。
   - 第二项：KL散度，正则化队友潜在变量的时序一致性。
   - 总目标函数在标准Dreamer损失上增加\(\lambda_{\text{ToM}} \mathcal{L}_{\text{ToM}}\)。

3. **部署与适应**：
   - 测试时，模型在线推断\(z^{\text{team}}_t\)，用其条件化actor和critic。
   - 想象滚动时采样合理的队友轨迹，支持零样本协调；随着更多交互数据积累可实现少样本改进。

## 3. 实验设计

- **本文未报告任何实验结果**，仅提出评估协议和计划使用的基准。
- **提议的基准环境**：
  - 多智能体粒子环境（MPE）：轻量级，用于诊断模型是否学习到可区分的队友潜在变量以及分离社交与物理动态。
  - Overcooked-AI：主要零样本协调测试床，使用标准伙伴划分评估即时协调能力。
  - Melting Pot：大规模测试，评估在不同社交语境和伙伴群体下的鲁棒性及对规范/惯例分布偏移的处理。
- **对比方法**：文中未列出具体对比基线，但提及了现有MARL方法（MAPPO, QMIX）和世界模型变体（MA-Dreamer, CoDreamer, GAWM）的相关工作，暗示将与之比较。
- **评估指标**：计划报告回合回报、样本效率、零样本协调得分、少样本改进幅度、跨角色鲁棒性。

## 4. 资源与算力

- **未明确说明**。论文为概念提案，没有实际训练运行，因此未提及GPU型号、数量或训练时长。

## 5. 实验数量与充分性

- **实验数量**：零。该论文被描述为“proposal”和“call for discussion”，旨在报告架构设计和评估协议，而非实证结果。
- **充分性**：由于缺乏实验，无法判断方法的实际有效性。评估协议虽详细（包含多个环境和指标），但尚未执行。实验的客观性和公平性无法评估。

## 6. 论文的主要结论与发现

- 本文没有实证发现，主要贡献是提出了一个概念性架构和论证：
  1. 队友应被建模为结构化潜在过程而非噪声，以减少非平稳性。
  2. 因式分解的潜在空间和ToM头部使世界模型能够适应部分可观测的多智能体协作。
  3. 该方法为开发可泛化的、能与人类协作的AI提供了新方向（“世界模型不仅是环境预测器，也是社会行为模拟器”）。

## 7. 优点

- **创新性**：首次将队友建模明确整合到Dreamer风格世界模型的潜在状态分解中，不同于现有工作中共享想象或中心化假设。
- **理论动机清晰**：明确指出了队友引发的非平稳性问题，并通过结构化潜变量（而非噪声）来缓解。
- **架构设计简洁**：利用已有的RSSM架构，仅增加一个额外的潜在分量和一个解码器头，保持了Dreamer的端到端学习范式。
- **泛化目标明确**：支持零样本和少样本协调，面向人机协作，符合实际应用需求。
- **评估协议系统**：提出了从环境诊断到大规模鲁棒性测试的分级评估策略，涵盖了多个标准化工具（JaxMARL, BenchMARL）。

## 8. 不足与局限

- **缺乏实证验证**：该工作仅为提案，无任何实验结果，方法的实际性能未知，可信度不足。
- **潜在实现复杂性**：
  - 分解的潜在状态是否真正能隔离环境和队友动态？可能存在纠缠问题。
  - ToM目标中的KL正则化系数\(\alpha\)需要调参，对训练稳定性有影响。
  - 队友动作分布的预测需要真实队友策略，而实际中队友策略可能不断变化或存在对抗性。
- **评估偏差风险**：提议的基准（MPE, Overcooked-AI, Melting Pot）虽广泛使用，但可能无法覆盖更复杂的社交场景（如谈判、角色切换）。
- **假设局限**：
  - 假设队友行为可以通过历史观察部分建模，但未考虑队友可能具有分层策略或长期规划。
  - 未处理多个队友的联合建模（论文中仅提及一个队友\(j\)），扩展到多队友时潜在空间维度可能过大。
- **对比基线不明确**：未列出将在实验中对比的具体算法，难以评估该方法相对于现有方法的预期增益。
- **应用限制**：该方法需要观测队友的动作（作为输入），在完全不可观测队友动作的环境中（如隐藏动作）无法直接使用。

（完）
