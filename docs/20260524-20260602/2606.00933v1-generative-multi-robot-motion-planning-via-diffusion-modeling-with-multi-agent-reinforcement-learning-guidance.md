---
title: Generative Multi-Robot Motion Planning via Diffusion Modeling with Multi-Agent Reinforcement Learning Guidance
title_zh: 基于扩散建模与多智能体强化学习引导的生成式多机器人运动规划
authors: "Suk Ki Lee, Venkata Sai Deepak Mutta, Hyunwoong Ko"
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00933v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 结合多智能体强化学习的多机器人协调运动规划
tldr: "多机器人协调运动规划面临可扩展性与交互感知的权衡。本文提出去中心化扩散轨迹生成与多智能体强化学习(MARL)价值引导相结合的框架，各机器人独立用扩散模型生成可行轨迹，并通过集中值函数梯度引导去噪过程实现交互感知。在四机器人迷宫环境中，该方法将智能体间干扰率从55.4%降至41.8%，表明MARL引导能有效协调去中心化规划器，无需联合建模。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00933v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1949, \"height\": 1663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1575, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 948, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 712, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 901, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00933v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1830, \"height\": 833, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00933v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 947, \"height\": 944, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.00933v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 775, \"height\": 117, \"label\": \"Table\"}]"
motivation: 集中式多机器人规划难以扩展，而去中心化规划忽略智能体间交互，导致冲突频繁。
method: 各机器人用扩散模型独立生成轨迹；集中值函数经MARL训练，在反向扩散时通过梯度引导轨迹偏向高期望回报区域。
result: "在四机器人迷宫任务中，该方法将干扰率从55.4%降低至41.8%，显著提升协调性。"
conclusion: MARL价值引导可在保持去中心化可扩展性的同时有效引入多机器人协调，无需联合规划或重训练生成模型。
---

## 摘要
在共享环境中协调多个机器人需要为每个智能体生成可行轨迹，同时考虑智能体间的交互。集中式规划方法随着机器人数量增加而难以扩展，而允许每个智能体独立规划的分散式方法则本质上无法考虑智能体间的交互。本文提出一种结合分散式生成轨迹规划与基于多智能体强化学习（MARL）协调的协同多机器人运动规划框架。每个机器人在单智能体运动数据上训练的扩散模型独立生成候选轨迹，利用生成模型产生可行且多样轨迹的能力。为了减少智能体间的冲突，通过基于梯度的引导，使用MARL训练的集中式价值函数指导逆向扩散过程，从而在无需集中式联合规划或重新训练生成模型的情况下实现交互感知的轨迹生成。这种引导遵循指数倾斜公式，其中价值函数将去噪分布偏向于具有更高期望多智能体回报的轨迹。该框架在包含四个移动机器人的模拟迷宫环境中进行了评估。实验结果表明，所提出的价值引导扩散规划将智能体间干扰率从55.4%降低到41.8%，证明在保持分散式轨迹生成可扩展性的同时能够有效实现协调。这些结果表明，基于MARL的价值引导可以在不需要完全联合多机器人模型的情况下，有效地将协调引入分散式生成规划器。

## Abstract
Coordinating multiple robots in shared environments requires generating feasible trajectories for each agent while accounting for interactions among agents. Centralized planning approaches become difficult to scale as the number of robots increases, while decentralized approaches that allow each agent to plan independently do not inherently account for inter-agent interactions. This paper presents a framework for coordinated multi-robot motion planning that combines decentralized generative trajectory planning with multi-agent reinforcement learning (MARL)-based coordination. Each robot independently generates candidate trajectories using a diffusion model trained on single-agent motion data, leveraging the generative model's ability to produce feasible and diverse trajectories. To reduce conflicts between agents, a centralized value function trained via MARL guides the reverse diffusion process through gradient-based steering, enabling interaction-aware trajectory generation without centralized joint planning or retraining of the generative model. This guidance follows an exponential tilting formulation, in which the value function biases the denoising distribution toward trajectories with higher expected multi-agent return. The framework is evaluated in a simulated maze environment with four mobile robots. Experimental results show that the proposed value-guided diffusion planning reduces the inter-agent interference rate from 55.4% to 41.8%, demonstrating that coordination can be effectively achieved while preserving the scalability of decentralized trajectory generation. These results suggest that MARL-based value guidance can effectively introduce coordination into decentralized generative planners without requiring a fully joint multi-robot model.

---

## 论文详细总结（自动生成）

# 基于扩散建模与多智能体强化学习引导的生成式多机器人运动规划——详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多机器人共享环境下的运动规划面临两难——集中式规划可显式建模智能体间交互，但随机器人数量增加计算复杂度急剧上升，难以扩展；分散式规划让各机器人独立生成轨迹，具有良好可扩展性，但无法天然考虑智能体间的交互，容易导致轨迹冲突、空间竞争和运动干扰。
- **研究动机**：需要一种框架，既保留分散式规划的可扩展性和独立性，又能有效引入协调机制，降低智能体间的干扰，且无需重新训练生成模型或进行联合联合优化。
- **整体含义**：本文提出将单智能体扩散模型训练的轨迹生成与多智能体强化学习（MARL）训练的集中价值函数相结合，通过价值引导（value guidance）在推理时调整去噪过程，使每个机器人独立生成的轨迹偏向更高协调回报的区域，从而在不牺牲可扩展性的前提下实现协调运动规划。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 整体架构

框架包含四个阶段：
1. **轨迹分布构建**：通过单智能体强化学习（PPO）训练导航策略，收集多样、可行、目标导向的轨迹数据集。
2. **扩散模型学习单智能体轨迹分布**：使用条件扩散模型（Temporal U-Net）学习轨迹分布 \(p_\theta(\tau|c)\)，条件为起点和目标。
3. **分散式多智能体轨迹生成**：每个机器人共享相同的扩散模型，独立采样轨迹，联合分布 \(p_\theta(X)=\prod p_\theta(\tau^i|c^i)\)，实现线性可扩展。
4. **价值引导的多智能体规划**：利用 MARL 训练的集中价值函数 \(V_\phi(X)\) 对联合轨迹评分，在反向去噪过程中加入梯度项，使采样偏向高协调价值的区域。

### 2.2 关键技术细节

- **价值引导公式**：引导分布 \(\tilde{p}_\theta(X) \propto p_\theta(X) \exp(\lambda V_\phi(X))\)，梯度为 \(\nabla_X \log \tilde{p}_\theta(X) = \nabla_X \log p_\theta(X) + \lambda \nabla_X V_\phi(X)\)。
- **反向去噪修改**：对每个智能体 \(i\)，去噪均值 \(\mu^i_t\) 加上 \(\lambda \Sigma_t \nabla^i\)，其中 \(\nabla^i\) 是联合梯度中对应智能体 \(i\) 的分量。
- **边界条件保持**：在每个去噪步通过 inpainting 强制满足起点和目标约束。
- **算法流程**（Algorithm 1）：
  1. 各智能体从噪声初始化轨迹并施加边界条件。
  2. 对每个去噪步 \(t=T \to 1\)：
     - 分别计算每个智能体的去噪均值 \(\mu^i_t\)。
     - 构造联合轨迹 \(X_t\)，计算联合梯度 \(\nabla_X V_\phi(X_t)\)。
     - 对每个智能体按修正的均值采样 \(\tau^i_{t-1}\)，并再次施加边界条件。
  3. 返回最终干净轨迹 \(X_0\)。

## 3. 实验设计

- **环境与场景**：
  - 单智能体轨迹收集：30m×30m 自定义迷宫（含内部墙壁），使用 Leatherback 机器人（四轮转向，半径0.254m）。训练单机器人导航策略（PPO），收集2800条轨迹，用于训练扩散模型。
  - 多智能体协调价值训练：在 **无障碍物的开放空间** 中，四个 Leatherback 机器人朝向共同目标点形成编队（各自有相对目标偏移），使用 MAPPO 训练集中价值函数（critic）以评估协调质量。
  - **推理评估场景**：在 **同一迷宫** 中，四个机器从左下区域出发，分别向各自偏移的右上目标导航，轨迹长度176步。
- **对比方法**：无引导的扩散规划（Baseline） vs. 带 MARL 价值引导的扩散规划（Guided）。
- **评估指标**：
  - 主要指标：智能体间干扰率 \(R_{\text{agent}}\) = 某智能体与另一智能体距离 < 0.508m 的时间步占比。
  - 辅助指标：障碍物干扰率 \(R_{\text{obs}}\) = 距离墙壁 < 0.254m 的时间步占比。
- **无外部 benchmark 对比**：本文仅进行自身消融对比（有/无引导），未与其他多机器人规划方法（如集中式MAPF、其他分散式方法）对比。

## 4. 资源与算力

- **GPU**：NVIDIA RTX-4090。
- **软件框架**：NVIDIA Isaac Sim 5.1.0、Isaac Lab、PyTorch、skrl 库。
- **训练时长**：论文 **未明确给出** 单智能体PPO训练、扩散模型训练和MARL训练的具体时长或轮数。仅指出滚动成功率（图3）和扩散损失（图4）曲线收敛，MARL示例中距离收敛（图5），但未提供训练时间。
- **推理成本**：每次生成需256次去噪步，每步计算联合梯度，但未给出具体推理时间。

## 5. 实验数量与充分性

- **实验重复次数**：每组条件（baseline和guided）各重复 **10次**，结果取平均。这是合理的统计重复。
- **场景多样性**：仅 **一个迷宫场景**、**四个机器人**、**一种起点-目标配置**（每组偏移1.5m）。缺乏对不同地图拓扑、不同机器人数量、不同障碍密度、不同任务（如交叉流、编队变化等）的检验。
- **消融实验**：无。未分析不同引导尺度\(\lambda\)的影响，未单独分析扩散模型质量或价值函数质量。
- **对比方法**：仅对比自身无引导版本，未与现有的多机器人规划方法（如集中式Conflict-Based Search、分散式ORCA、基于学习的集值规划等）比较，公平性和先进性说服力有限。
- **定性分析**：展示了单次示例轨迹（图6），有助于理解行为变化，但不足以统计泛化。
- **客观性**：指标定义清晰，但干扰率阈值选择（0.508m）未提供理论依据，可能偏向某种结果。此外，障碍干扰率增加（4.6%→7.6%）表明存在trade-off，但论文未进一步评估对实际执行成功率的影响。

## 6. 论文的主要结论与发现

- **定量结果**：MARL价值引导将智能体间干扰率从55.4%显著降低至41.8%，减少了13.6个百分点。
- **定性行为**：引导后的轨迹在全局路径相似的前提下，智能体间空间间隔更大，但出现局部振荡和更靠近墙壁的现象。
- **结论**：MARL价值引导可在保持分散式生成可扩展性的同时有效协调多机器人运动，无需联合建模或重训练扩散模型。
- **辅助发现**：障碍干扰率有所上升（4.6%→7.6%），原因在于价值函数训练于开放空间，缺乏障碍信息，导致引导可能将轨迹推向墙壁边界，体现了环境不匹配带来的 trade-off。

## 7. 优点

- **方法创新性**：巧妙结合了生成式轨迹模型（扩散模型）的多样性与MARL集中值函数的协调信号，通过推理时梯度引导实现零样本协调，无需修改扩散模型参数。
- **可扩展性**：分散式轨迹生成的计算复杂度随智能体数量线性增长，适合扩展到更多机器人。
- **模块化**：单智能体扩散模型和MARL价值函数可分别独立训练，且价值函数可替换为其他协调信号。
- **轻量推理**：不需要在线联合规划或通信，仅在去噪过程中共享一次梯度计算（集中但无联合规划）。
- **实证有效性**：在仿真环境上验证了干扰率的显著下降，展示了方法潜力。

## 8. 不足与局限

- **实验覆盖不足**：
  - 仅测试4个机器人，未验证更大规模（如>10）的可扩展性。
  - 仅单一迷宫地图，未测试不同障碍密度、狭窄通道、非结构化环境。
  - 未与现有主流多机器人规划方法（如CBS、ORCA、MAPPO等）对比，无法评估绝对性能优势。
- **环境不匹配问题**：
  - 价值函数在无障碍开放空间训练，导致在迷宫内引导时可能产生靠近墙壁的不良行为，trade-off未得到解决。
  - 障碍干扰率上升可能在实际任务中导致碰撞风险，论文未讨论安全裕度。
- **缺乏消融研究**：
  - 未分析不同引导尺度\(\lambda\)对干扰率和障碍率的影响。
  - 未分析扩散模型质量（如训练数据量、去噪步数）对最终结果的影响。
  - 未分析价值函数质量（如训练回报收敛情况）对引导效果的影响。
- **指标局限性**：
  - 干扰率基于固定阈值，未能反映近距离程度和碰撞风险连续性。
  - 仅以时间步比例度量，未考虑任务完成时间、成功率、路径效率等更全面的指标。
- **未验证物理实现**：仅仿真验证，未在真实机器人系统上部署，实际传感器噪声、动态不确定性等未考虑。
- **计算资源信息不透明**：未给出训练和推理的具体耗时、显存需求，难以复现或对比效率。

（完）
