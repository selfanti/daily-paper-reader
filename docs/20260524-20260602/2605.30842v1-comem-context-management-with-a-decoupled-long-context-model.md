---
title: "CoMem: Context Management with A Decoupled Long-Context Model"
title_zh: CoMem：基于解耦长上下文模型的上下文管理
authors: "Yuwei Zhang, Chengyu Dong, Shuowei Jin, Changlong Yu, Hejie Cui, Hongye Jin, Xinyang Zhang, Hamed Bonab, Colin Lockard, Jianshu Chen, Zhenyu Shi, Jingbo Shang, Xian Li, Bing Yin"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30842v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 面向智能体模型的解耦记忆管理框架与异步流水线
tldr: 长上下文代理模型需通过迭代总结管理历史交互，但额外解码开销导致高延迟。CoMem将记忆管理解耦并与代理工作流异步并行执行，采用k步偏移流水线掩盖延迟，通过奖励驱动训练确保记忆模型捕获足够统计信息。在SWE-Bench-Verified上，CoMem实现1.4倍延迟提升且性能几乎不变，延迟优势随吞吐量增加而扩大，为代理推理和记忆压缩的独立优化提供了模块化框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1595, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30842v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 822, \"height\": 491, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30842v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1751, \"height\": 433, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30842v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1648, \"height\": 837, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30842v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 803, \"height\": 599, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30842v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1595, \"height\": 674, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30842v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 847, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30842v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30842v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 783, \"height\": 386, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30842v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1267, \"height\": 478, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30842v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 574, \"height\": 234, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30842v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 897, \"height\": 348, \"label\": \"Table\"}]"
motivation: 现有上下文管理产生大量额外解码开销，显著增加端到端响应延迟，亟需高效解耦方案。
method: 提出CoMem框架，解耦记忆管理并异步并行，采用k步偏移流水线重叠总结与推理，用奖励驱动训练对齐记忆模型。
result: 在SWE-Bench-Verified上获得1.4倍延迟提升，性能几乎不变，延迟优势随吞吐量增加而扩大。
conclusion: CoMem实现效率与效果更优权衡，为独立优化代理推理和记忆压缩提供模块化路径。
---

## 摘要
上下文管理通过迭代总结先前的交互历史，使智能体模型能够解决长视界任务。然而，这一过程通常会因额外的总结令牌而产生大量的解码开销，显著影响部署时的端到端响应延迟。在本文中，我们提出 CoMem，一种新颖的框架，它将记忆管理与主智能体工作流解耦，使这些过程能够并行执行。我们提出了一种 $k$ 步偏移的异步流水线，将记忆模型的总结与智能体的推理重叠，从而有效隐藏上下文处理的延迟。为确保在这种异步设置下的鲁棒性，我们引入了一种奖励驱动的训练策略，使记忆模型能够捕捉到足够用于智能体决策的统计信息。理论分析证实，与耦合架构相比，CoMem 提供了更优的效率-效果权衡。我们在 SWE-Bench-Verified 上的大量实验结果表明，CoMem 在保持大部分性能的同时，将原始长上下文解决方案的延迟提升了 1.4 倍。此外，我们证明这些延迟收益随着系统吞吐量的增加而显著扩大，为智能体推理和记忆压缩的独立优化提供了一条模块化的前进路径。

## Abstract
Context management enables agentic models to solve long-horizon tasks through iterative summarization of previous interaction histories. However, this process typically incurs substantial decoding overhead for the extra summarization tokens, which significantly affect the end-to-end response latency at deployment. In this paper, we introduce CoMem, a novel framework that decouples memory management from the primary agent workflow, enabling these processes to execute in parallel. We propose a $k$-step-off asynchronous pipeline that overlaps the memory model's summarization with the agent's inference, effectively masking the latency of context processing. To ensure robustness under this asynchronous setting, we introduce a reward-driven training strategy that aligns the memory model to capture sufficient statistics for the agent's decision-making. Theoretical analysis confirms that CoMem offers a superior efficiency-effectiveness trade-off compared to coupled architectures. Our extensive experimental results on SWE-Bench-Verified show that CoMem provides 1.4x latency improvements upon vanilla long-context solutions while preserving most of the performance. Furthermore, we demonstrate that these latency gains scale favorably with increased system throughput, offering a modular path forward for the independent optimization of agent reasoning and memory compression.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：大型语言模型（LLM）智能体在解决长视界任务（如软件工程、科学发现）时，需要维持对完整交互历史的连贯理解。然而，随着交互历史增长，解码阶段的 KV 缓存线性膨胀，注意力计算成本升高，导致严重的内存带宽瓶颈和端到端响应延迟。现有解决方案（如滑动窗口注意力、RAG、系统级优化）或牺牲远距离依赖，或依赖语义相似性导致推理碎片化，或无法消除解码阶段对大量静态历史的重编码冗余。
- **核心目标**：提出一种在保持任务性能（如解决率）的同时，显著降低长上下文智能体推理延迟的框架。

### 2. 论文提出的方法论

- **核心思想**：将记忆管理（长上下文压缩）与智能体推理解耦，采用一个轻量级专用记忆模型异步地在后台压缩历史，主智能体使用压缩后的摘要和最近的原始缓冲进行解码，从而将计算负担移出关键解码路径，实现并行执行。
- **关键技术细节**：
  - **解耦架构**：记忆模型（`f`，轻量级长上下文模型）将长历史 `τ_{<t}` 压缩为紧凑状态 `s_t`，满足 `|s_t| < |τ_t|`；智能体模型（`π`，更大容量模型）基于 `s_t` 和短期缓冲 `C_t` 生成动作。
  - **k-step-off 异步流水线**：以 `k` 步为一个周期。在每个周期开始时异步启动记忆模型压缩所有历史；智能体继续使用最新的摘要和原始缓冲执行 `k` 步；摘要完成后在下个周期开始时被消费，此时智能体 KV 缓存失效，需进行一次完整非缓存预填充（开销被 `k` 步分摊）。公式为：  
    `a_t ~ π(a_t | f(τ_{≤ t-k}), τ_{t-k+1:t})`。
  - **奖励驱动的训练策略**：使用 GRPO 算法，以动作一致性（function equivalence）为奖励：给定候选摘要 `s = f(τ_{< t-k})`，冻结智能体产生动作 `â_t = π(·|s, C_t)`，奖励 `R(s) = sim(â_t, a*_t)`，其中 `a*_t` 是全上下文智能体的地面真值动作。通过最大化该奖励强制摘要保留决策所需信息。
- **理论分析**：推导了摘要长度上界公式 `L_sum / L_full < 1/(1 + WY/(S·P))`，通过实例计算得出对于 32K 前缀长度，摘要需小于 7.36K 令牌，证明压缩可行性。

### 3. 实验设计

- **数据集/场景**：主要使用 **SWE-Bench-Verified**（500 个 GitHub 问题），训练数据来自 R2E-Gym-Lite 生成的轨迹。另在 **BrowseComp-EN**（多步网页检索深度研究基准）上进行了泛化性实验。
- **Benchmark**：解决率（%Resolved）、工具调用次数、推理延迟（端到端时间、每一步时间等）。消融实验包括不同 `k` 值、批处理大小、并发度等。
- **对比方法**：
  - **Full-Context**：标准全上下文推理。
  - **No Summary**：移除摘要，仅使用原始缓冲。
  - **Qwen3-4B (base)**：使用开箱即用的 Qwen3-4B 作为记忆模型。
  - **Qwen3-4B (SFT)**：经过监督微调的记忆模型。
  - **GRPO AC**：本文提出的基于 GRPO 的动作一致性训练记忆模型。
  - 额外外部基线：**MemAgent** (Yu et al., 2025) 通用记忆模型。
  - 智能体骨干：DeepSWE (32B)、Qwen3-Coder-Max (480B)、GLM-4.7 (355B)。

### 4. 资源与算力

- 文中明确说明：
  - **训练**：使用 Qwen3-4B 作为记忆模型，在 R2E-Gym-Lite 轨迹上训练 3 个 epoch，batch size 128，每组 16 个 rollouts。训练使用 GRPO，学习率 1e-6，余弦调度。具体 GPU 型号未明确，但推断使用 A100 或 H200 系列。
  - **推理**：DeepSWE 运行于 A100 (80GB)；Qwen3-Coder-Max 和 GLM-4.7 运行于 H200。使用 vLLM 0.14.0，启用前缀缓存和分块预填充。记忆模型服务器在并发测试中使用单台服务器，Qwen3-4B 支持约 300 个并发智能体实例。
- **资源细节**：未明确披露训练使用的 GPU 数量及总时长，仅提供了超参数（epoch 数、学习率等）。

### 5. 实验数量与充分性

- **充分性**：实验覆盖全面且客观。
  - 在三个不同规模的智能体骨干（32B、480B、355B）上评估。
  - 进行了延迟与批处理大小的可扩展性实验（32~256 批处理）。
  - 进行了并发度交叉分析（16、32、64 并发），包括执行时间和 KV 缓存占用。
  - 消融 `k` 值（1、2、4、8、16）。
  - 与外部基线 MemAgent 对比。
  - 泛化性实验：在 BrowseComp-EN 上验证；跨脚手架（R2E-Gym → OpenHands）鲁棒性测试。
  - 跨骨干可迁移性测试（Qwen3-Coder-Max 训练的记忆模型迁移到 GLM-4.7）。
  - 记忆服务器调度开销分析（不同并发下 TTFT 和 TPOT）。
- **公平性**：所有实验设置保持一致，使用相同种子、最大步数、提示模板等。训练使用 GRPO，与 SFT 和 base 对比，消融充分。

### 6. 论文的主要结论与发现

- CoMem 在三大智能体骨干上均实现显著延迟降低：端到端加速最高达 **2.08×**（GLM-4.7，批处理 128），峰值逐步骤加速达 **4.95×**（64 并发）。
- 解决率与全上下文基线相当甚至略优：在 DeepSWE 上 41.0% vs 40.4%；在 GLM-4.7 上 62.7% vs 69.0%，差距较小。
- 延迟优势随吞吐量增加而放大（批处理 256 时加速 2.52×），证明框架在高负载下更有效。
- 训练策略（GRPO AC）显著优于未训练（base）和 SFT 基线，且可在不同骨干之间迁移（仅 loss 0.8%）。
- 对脚手架变化鲁棒（交叉脚手架仅降0.3%），泛化到 BrowseComp-EN 甚至提升准确率（28.1% → 32.0%）。
- 一个 4B 记忆模型可支持约 300 个并发 GLM-4.7 智能体实例。

### 7. 优点

- **创新的解耦架构**：将记忆管理与推理分离，允许独立优化，降低系统耦合。
- **异步流水线设计**（k-step-off）有效掩盖记忆模型延迟，无需智能体等待。
- **功能等价奖励**训练：直接优化摘要对下游决策的充分性，而非表面文本相似度，训练高效（无需在线环境交互）。
- **理论分析**：为摘要长度提供定量上限指导。
- **大量实验覆盖**：多种骨干、场景、消融、可扩展性，证明了方法的通用性和鲁棒性。

### 8. 不足与局限

- **实验覆盖**：主要聚焦于软件工程（SWE-Bench），仅在一个网页检索场景（BrowseComp）上验证，其他长视界任务（如对话、数学推理）未涉及。
- **对 k 敏感性**：极端值（k=1 或 k=16）导致性能或延迟退化，需在合理区间内选择。
- **记忆模型规模**：目前仅实验了 4B 模型，更大或更小的记忆模型未系统比较。
- **潜在偏差**：训练数据来自 R2E-Gym-Lite，可能不完全覆盖真实 GitHub issue 分布；智能体动作一致性奖励基于全上下文基线，若基线存在偏差会被放大。
- **应用限制**：依赖异步流水线，要求记忆模型服务器持续可用，且需额外管理 KV 缓存失效时机；对实时性要求极高的场景，k-step-off 可能引入轻微状态滞后。
- **未讨论**：记忆模型的安全性与偏见问题，以及在高并发下记忆服务器队列延迟虽被掩盖但仍存在资源消耗。

（完）
