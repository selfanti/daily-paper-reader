---
title: "AI4AI-Bench: Benchmarking LLM Agents in Algorithmic Design for Recursive Self-Improvement"
title_zh: AI4AI-Bench：面向递归自我改进的算法设计中LLM智能体基准测试
authors: "Yizhe Chi, Wenyi Li, Deyao Hong, Xiaoqiu Wang, Mingju Gao, Kaisen Yang, Bingxiang He, Youjie Zheng, Calvin Xiao, Qinhuai Na"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.20318v1"
tags: ["query:agentic-rl"]
score: 7.0
evidence: 面向递归自我改进的LLM智能体基准
tldr: "递归自改进（RSI）要求智能体能改进产生智能体的训练算法，但现有基准无法区分学习方式改变与执行细节变化。AI4AI-Bench设定10个冻结研究仓库、覆盖10种训练算法族，代理限时4小时重写算法，之后从头运行12小时，由隐藏评估器按统一映射打分。实测6个系统29种配置平均分0.166，最优仅0.250；大多数提交根本未改变学习方式，仅少数有效改动者平均0.226 vs 0.126。更多推理投入使改动比例从8%升至64%，平均分提升一倍。该基准公开全套任务与评估器，为RSI能力提供可重复的衡量标准。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.20318v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 1149, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.20318v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1663, \"height\": 747, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.20318v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1708, \"height\": 790, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.20318v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1583, \"height\": 521, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.20318v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 871, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.20318v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1708, \"height\": 836, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.20318v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1110, \"height\": 679, \"label\": \"Table\"}]"
motivation: 现有基准无法隔离“改进训练算法”的核心能力，要么混入数据收集、要么只做超参调优，必须专门设计基准来测量智能体设计算法的能力。
method: 构建10个冻结仓库覆盖10种训练算法族，代理4小时内重写训练算法，再从头重跑12小时，用统一0-1尺度由固定评估器打分。
result: 在6个系统29种配置上平均分0.166，最佳0.250；只有少数提交真正改动学习方式，其平均分0.226远高于未改动的0.126，更多推理投入可进一步提高得分。
conclusion: 该基准揭示当前LLM代理在设计训练算法上能力薄弱，并发布全部任务与评估器，为递归自改进研究提供可重复标准化评测。
---

## 摘要
递归自我改进（RSI）探讨一个AI系统能否改进生成AI系统的过程，从而使下一代系统继承这种改进。该过程即训练算法：更好的目标或更新规则能提高每次后续运行的计算-能力交换率，包括生成下一个智能体的那次运行。因此，RSI是否可行取决于智能体能否设计训练算法。现有基准均未孤立这一能力：现有测试套件通过收集数据或调整超参数即可获胜，且没有哪个基准能将运行方式的变化与模型学习方式的变化区分开来。我们提出AI4AI-Bench，包含跨越10个训练算法家族的10个冻结研究仓库。在每个任务中，智能体有4小时在一台B300上重写训练算法；其代码随后从头重新运行至多12小时，并由一个对智能体隐藏的固定评估器依据与仓库原始算法相同的程序进行评分。由于这10个指标不可通约，每个任务都映射到统一尺度上，其中0表示无信息模型，0.1表示仓库随附的算法，1.0表示任务最优值。在全部10个任务上对6个系统的29种配置进行测试，平均得分为0.166，最佳系统达到0.250：即使最强系统也仅缩小了已有算法与最优值之间差距的五分之一以下。提交结果显示了差距所在：大多数提交从未改变模型的学习方式，而少数改变了的提交平均得分为0.226，其余为0.126。更多的推理努力主要带来了尝试改变的意愿，使这部分提交从占全部提交的8%提升到64%，平均得分从0.094提升到0.196。我们公开了任务套件、评估器和所有评分提交，以便在这些系统变化时能够重复测量。

## Abstract
Recursive self-improvement (RSI) asks whether an AI system can improve the process that produces AI systems, so that the next system inherits the improvement. That process is the training algorithm: a better objective or update rule improves the compute\mbox{-}capability exchange rate for every subsequent run, including the one that produces the next agent. Whether RSI is feasible therefore turns on whether an agent can design training algorithms. No benchmark isolates that ability: existing suites are won by collecting data or by tuning hyperparameters, and none tells a change to how a run is executed apart from a change to how the model learns. We present AI4AI\mbox{-}Bench, 10 frozen research repositories spanning 10 training algorithm families. In each task, an agent has 4 hours on one B300 to rewrite the training algorithm; its code is then rerun from scratch for up to 12 hours and scored by a fixed evaluator hidden from the agent, against the repository's original algorithm under the same procedure. Because the 10 metrics are incommensurable, every task is mapped onto one scale on which $0$ is an uninformative model, $0.1$ is the algorithm the repository ships, and $1.0$ is the task optimum. Across 29 configurations of 6 systems on all 10 tasks the mean score is $0.166$, and the best system reaches $0.250$: even the strongest closes under a fifth of the distance between the algorithm that was already there and the optimum. The submissions show where that distance went: most never change how the model learns at all, and the minority that do average $0.226$ against $0.126$ for the rest. More reasoning effort mostly buys the willingness to go there, taking that minority from $8\%$ of submissions to $64\%$ and the mean score from $0.094$ to $0.196$. We release the task suite, the evaluators and every scored submission, so that the measurement can be repeated as these systems change.