---
title: Progressive Agent Skill Generation via Reinforcement Learning
title_zh: 通过强化学习进行渐进式智能体技能生成
authors: "Junhao Shen, Zhanqiu Zhang, Yiwen Guo, Hong Cheng"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.01678v1"
tags: ["query:agentic-rl"]
score: 9.0
evidence: 基于强化学习的渐进式技能生成
tldr: 现有技能生成方法多依赖启发式或流水线，需针对不同证据源专门设计，而学习式方法因技能缺乏自然监督信号而面临挑战。为此提出Skill-α，将技能生成建模为顺序编辑过程，通过回滚奖励比较原始与编辑后技能在锚定查询上的下游执行效果来评估每个编辑。实验证明，在文档到技能与经验到技能两种设置中，Skill-α均优于现有基线，在GPT-4o作为主要模型时，CL-Bench成功率提升3.3个百分点，tau2-bench提升6.7个百分点。该工作验证了逐步生成与回滚奖励的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01678v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1232, \"height\": 379, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01678v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1324, \"height\": 419, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01678v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1173, \"height\": 559, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01678v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 885, \"height\": 660, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01678v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 826, \"height\": 197, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01678v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 688, \"height\": 350, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01678v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1391, \"height\": 388, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01678v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1175, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01678v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1169, \"height\": 245, \"label\": \"Table\"}]"
motivation: 现有技能生成多靠启发式或流水线，需专门设计，且技能缺乏监督信号，导致学习式生成困难。
method: 提出Skill-α，将技能生成建模为顺序编辑过程，利用回滚奖励比较原始与编辑后技能的下游执行效果来评估编辑。
result: 在文档到技能与经验到技能两种设置中，Skill-α均优于基线，在GPT-4o作为主要模型时，CL-Bench提升3.3点，tau2-bench提升6.7点。
conclusion: 验证了强化学习用于技能生成的有效性，并确认回滚奖励与逐步生成是关键。
---

## 摘要
现有的技能生成方法主要依赖于启发式方法或流水线式整合，这些方法必须针对不同的证据来源进行专门设计。相比之下，基于学习的方法提供了一种更统一的方式来跨异构来源建模技能生成。然而，基于学习的技能生成仍然具有挑战性，因为技能缺乏基于相关性或正确性的自然监督信号；其价值在很大程度上只能通过它们是否改善了智能体在下游任务中的行为来判断。为了应对这一挑战，我们提出了Skill-$\alpha$，一种用于逐步生成高质量智能体技能的强化学习方法。具体来说，我们将技能生成形式化为一个顺序编辑过程，将技能构建分解为可单独评估的编辑，并引入了一种新颖的回滚奖励，通过比较在锚定查询上原始技能和编辑后技能下的下游执行情况来评估每次编辑。大量实验表明，在文档到技能和经验到技能两种设置中，Skill-$\alpha$生成的技能比基于启发式或流水线的方法更有效。在主要的GPT-4o工作器下，Skill-$\alpha$在CL-Bench上将平均下游成功率比最强的技能生成基线提高了3.3个百分点，在tau2-bench上提高了6.7个百分点。进一步的消融研究验证了回滚奖励和渐进式生成的重要性。

## Abstract
Existing skill generation methods largely rely on heuristics or pipeline-style consolidation, which must be specially designed for different evidence sources. In contrast, learning-based approaches offer a more unified way to model skill generation across heterogeneous sources. However, learning-based skill generation remains challenging because skills lack a natural supervision signal based on relevance or correctness; their value can largely be determined only by whether they improve the behavior of the agent on downstream tasks. To address this challenge, we propose Skill-$α$, a reinforcement learning method for progressively generating high-quality agent skills. Specifically, we formulate skill generation as a sequential editing process that decomposes skill construction into individually evaluable edits, and introduce a novel rollback reward that evaluates each edit by comparing downstream execution under the original and edited skills on an anchored query. Extensive experiments show that Skill-$α$ generates more effective skills than methods based on heuristics or pipelines in both document-to-skill and experience-to-skill settings. Under the main GPT-4o worker, Skill-$α$ improves average downstream success rates over the strongest skill-generation baseline by 3.3 points on CL-Bench and 6.7 points on tau2-bench. Further ablations validate the importance of rollback reward and progressive generation.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 核心问题与整体含义（研究动机与背景）
- 大型语言模型智能体常依赖外部技能作为模块化程序单元，以条件化推理并提升复杂任务求解能力。因此，**自动从文档或经验中生成高质量技能**成为关键问题。
- 现有技能生成方法多采用**启发式或流水线式整合**（如 Ctx2Skill、AutoSkill、Trace2Skill 等），需要针对不同证据来源专门设计，且难以解释每条证据应如何改变技能内容。
- 学习式方法虽能统一处理异构证据，但面临核心挑战：**技能缺乏基于相关性或正确性的自然监督信号**，其价值只能通过下游任务中智能体行为是否改善来判断，导致执行层面的信用分配困难。
- 论文提出 **Skill-α**，通过强化学习将技能生成建模为**顺序编辑过程**，配合**回滚奖励**实现执行驱动的编辑级信用分配，从而统一文档到技能与经验到技能两种生成设置。

## 2. 方法论
- **核心思想**：将技能构建分解为一系列局部编辑决策，每一步基于当前技能状态与一条证据执行编辑动作；最终技能由初始草稿逐步演化得到。
- **渐进式生成**：
  - 设当前技能为 \(z_{t-1}\)，证据为 \(x_t\)，编辑策略 \(\pi_\phi\) 采样动作 \(A_t\)，然后应用 `Edit` 更新技能：\(z_t = \text{Edit}(z_{t-1}, A_t)\)。
  - 该过程天然适配长文档或多轨迹体验，避免一次性生成导致的信息遗漏与信用分配困难。
- **动作空间**：\(A = \{\text{CREATE}, \text{UPDATE}, \text{MERGE}, \text{PRUNE}, \text{NOOP}\}\)，分别对应新增规则、修正规则、合并冗余、删除有害/过度具体内容、保持技能不变。
- **回滚奖励**：
  - 每个编辑步骤选取一个**锚定查询** \(q^{anc}_t\) 及其验证器 \(V_t\)。
  - 分别用原始技能 \(z_{t-1}\) 与编辑后技能 \(z_t\) 驱动固定 worker 回答该查询，获得控制分数 \(r^{ctrl}_t\) 与编辑分数 \(r^{edit}_t\)。
  - 奖励公式（Eq.5）：非 NOOP 编辑获得奖励 1 当且仅当 \(r^{edit} > r^{ctrl}\)；NOOP 获得奖励 1 当且仅当组内无其他有效编辑超过控制分数；否则为 0。
  - 这样避免了“正确回答可能来自 worker 自身能力或查询简单”造成的虚假激励，精准归因于本次编辑。
- **训练流程**：
  - 先以 DeepSeek-V4-Pro 合成的监督数据对 Qwen3-8B 进行 SFT 预热，学习 `<think>` + `<action>` 编辑格式。
  - 再用 GRPO 优化策略，固定 GPT-4o 为 worker，rollback 奖励作为组内优势信号。
  - 完整伪代码见 Algorithm 1。

## 3. 实验设计
- **数据集 / 场景**：
  - **文档到技能**：CL-Bench（包含 Rule System Application、Procedural Task Execution、Domain Knowledge Reasoning、Empirical Discovery & Simulation 四类）。
  - **经验到技能**：SpreadsheetBench（电子表格操作）、tau2-bench（Airline、Retail、Telecom 三个领域的工作流复用）。
- **对比基线**：
  - 通用提示基线：No Skill、Anthropic Skill-Creator、Progressive Prompt Skill。
  - 文档到技能专用：AutoSkill、Ctx2Skill。
  - 经验到技能专用：ExpeL、Agent Workflow Memory (AWM)、Trace2Skill、SkillX、SkillPro。
- **评估协议**：
  - 所有方法使用相同源证据与相同 held-out 任务；GPT-4o 作为同 worker 主评估，Claude-Sonnet-4.5 作为跨 worker 迁移评估。
  - CL-Bench 采用 GPT-5.5 按官方任务特定 rubric 评判；SpreadsheetBench 与 tau2-bench 使用基准环境直接反馈。

## 4. 资源与算力
- 论文在训练设置部分提及具体超参数，但**未明确说明 GPU 型号与训练时长**：
  - SFT：8 GPUs，batch size 32，micro-batch 1/GPU，最大序列长度 32,768，学习率 5e-6，3 epochs。
  - GRPO：group size G=8，batch size 8，actor 学习率 5e-7，rollout 温度 1.0，最大 prompt 长度 8,192，最大模型长度 40,960，关闭 KL 正则。
- 由于缺少 GPU 型号（如 A100/H100）及具体 wall-clock 时间，无法精确评估算力成本。

## 5. 实验数量与充分性
- **主要实验**：
  - 文档到技能：CL-Bench 上对比 6 种方法 × 2 个 worker 骨干，报告 4 个类别及平均值。
  - 经验到技能：SpreadsheetBench + tau2-bench 3 个领域，对比 9 种方法 × 2 个 worker 骨干。
- **消融实验**：4 个变体（SFT only、w/o rollback reward、w/o MERGE/PRUNE、w/o NOOP），在 3 个 benchmark 上评估。
- **分析实验**：证据顺序（source/shuffled/reverse）与证据批次大小（1/2/4/8 单位每步）。
- **训练动态**：展示了各变体的奖励曲线与动作分布。
- **充分性与公平性**：
  - 实验覆盖面较广，涵盖文档与经验两种证据源，且统一了证据输入与评估协议。
  - 基线均按各自原生流程实现，并限制在相同训练/测试划分内，避免跨域泄漏。
  - 消融设计有效分离了 RL 信号、动作空间与 NOOP 的作用，证据批次分析补充了生成粒度的影响。
  - 不足之处：tau2-bench 部分列表现方差较大（如 Retail 上多方法并列 80），且未报告多次运行的置信区间，统计显著性有待加强。

## 6. 主要结论与发现
- Skill-α 在**文档到技能**与**经验到技能**两种设置下均优于全部基线：
  - 在 GPT-4o worker 下，CL-Bench 平均成功率比最强基线高 **3.3 个百分点**，tau2-bench 平均高 **6.7 个百分点**。
  - 在 Claude-Sonnet-4.5 迁移 worker 下，各 benchmark 上保持最佳或接近最佳，说明学习到的技能具有跨模型可迁移的结构化知识。
- 消融结果证明：
  - **回滚奖励**是关键训练信号，移除后性能接近仅 SFT。
  - **MERGE/PRUNE 动作**对控制技能膨胀、避免冗余冲突至关重要。
  - **NOOP** 作为校准的保留动作能进一步减少噪声编辑。
- 分析表明渐进生成对**证据顺序较鲁棒**，但对**证据批次大小敏感**：4 单位/步为最佳权衡，过小易过度局部化，过大则信用分配模糊。

## 7. 优点
- **统一框架**：用单一学习策略覆盖文档与经验两类异构证据源，避免了针对不同证据专门设计流水线。
- **细粒度信用分配**：通过将技能构建分解为局部编辑并引入回滚奖励，实现了执行级、编辑级的直接归因，理论上在二元验证器下可保持候选编辑的优劣排序（附录 A 给出理论分析）。
- **可迁移性**：显式验证了跨 worker 迁移能力，证明生成技能是通用任务知识而非特定模型的提示捷径。
- **实验规范**：严格控制证据源与测试划分、统一评估接口，并提供从 SFT 到 RL 的完整训练细节与超参数，便于复现。

## 8. 不足与局限
- **理论保证有限**：附录 A 仅给出局部候选排序的期望性质，单次回滚比较是单条锚定查询上的噪声二元信号，不保证多次局部更新能全局最优地改进最终技能。
- **依赖基准特定验证器**：CL-Bench 需 GPT-5.5 按任务 rubric 评判，SpreadsheetBench 与 tau2-bench 需环境反馈；迁移到新环境需重新设计评估协议。
- **技能表示简单**：目前仅支持文本形式的 SKILL.md 风格程序性指导，未涵盖结构化、多模态或可执行技能格式。
- **计算开销与鲁棒性报告不足**：未给出 GPU 型号与训练时长，也未报告多次运行的标准差；部分 benchmark 上基线之间差距较小，需更严格的统计检验。
- **训练数据依赖强模型**：SFT 数据由 DeepSeek-V4-Pro 合成，若该模型能力或偏差影响数据质量，可能间接限制编辑策略上限。

（完）
